# UI-04: screen review and normal sandbox navigation

Scope: isolated `codex/corgi-interface` worktree, base `20f3084`, exact application source in `ui-04-source-manifest.json`. UI-04 is the presentation/navigation subset of B13, not the integrated trial. The user's latest instructions replace the earlier intrusive preview banner and disabled-form design.

## Changes

- Preserve actual native form DOM in the local demo. The previous `form.card` → `section.card` replacement bypassed CSS and caused the reported cancellation layout failure. GET cancellation previews now navigate with the chosen date. Local non-read HTTP methods are refused before routing; disconnected actions give feedback without executing.
- Restore staff Policies with an authenticated, staff-only read index, broker identity, empty/error states and existing detail links. Existing broker-scoped read helpers are reused; no provider calls or money writes. Per-broker read fan-out is a small-trial tradeoff, not a scalable query design.
- Keep parent breadcrumbs accurate; omit a separate Back control, following the latest user preference. Preserve role while navigating the demo. Redirect already-signed-in staff to their own workspace.
- Replace preview banner/role strip and repeated home disclaimers with a discreet Sandbox details control. Existing product-specific live/simulator explanations remain accurate. Local demo details explicitly identify disconnected sample data.
- Make the fixed sidebar scrollable in short windows. Preserve normal document scrolling. At1000×400, its account block previously ended at462px with overflow visible; after the fix, sidebar scroll64px reveals it at400px.
- Wrap consent wording as one text block; distinguish Reject from Approve; stack approval details on narrow screens; prevent short status badges splitting words. Add meaningful table-region names, query alert/status semantics, relevant autocomplete and KYB field-help associations.
- Add styled loading, error and not-found screens. Clarify already-verified KYB and zero-refund/approval-waiting cancellation copy without changing the underlying decision or amounts.

## Coverage and evidence

The independent reviewer read all existing app TSX screens and shared components. The coordinator inspected every nominal screen below in Chrome at1512px and390px, including full content. Captures are local, synthetic-only, under `/tmp/corgi-ui04`; `audit.json`, `states.json`, `scroll.json` and `http-checks.json` hold actual measurements. Some early captures predate final fixes; the final review distinguishes refreshed captures from reused unchanged geometry. A complete screenshot is not proof of actual scroll reachability.

| Screen | Rendered checks |
|---|---|
| Home | Desktop/mobile; sign-in route; artwork; final simplified home copy |
| Login | Desktop/mobile, invalid-query message; native fields; demo login to Operations; role redirect checked separately with source stubs |
| Operations | Desktop/mobile; workspace cards, sidebar, role explanation |
| Staff Policies | Populated/empty/data-read-error; broker context; detail destination; staff guard independently tested before reads |
| Brokers | Approved/pending/failed rows; empty; table actions and long explanation |
| Claims list | Populated/empty; policy and claim destinations |
| Approvals | Operations read-only; approver decision form; own-request refusal; decided request; empty queue; exact long intent/hash |
| Broker Policies | Populated/empty; policy/new-policy/KYB links |
| Broker KYB | Full unsubmitted form and checkbox; submitted pending/failed/approved views and history; associated help |
| New policy | Full form; pending-KYB warning and query error; native date/select/amount fields |
| Policy detail | Broker/staff contexts; charge, coverage, payment, cancellation GET form, claims/intake, populated journal; draft variant |
| Cancellation preview | Valid date, invalid date refusal, end-of-term zero refund, open-claim explanation, hidden version contract |
| Claim detail | Reserve/history, bank controls, waiting payment and journal; closed variant; staff/approver variants |
| Recovery sections | Refund failed/requested/completed and payment ready/sent/settled/returned sections rendered with synthetic fixtures; no action executed |
| Loading/error/not-found | Dedicated component renders; no raw exception displayed; styled workspace return |

Actual End-key scrolling was separately checked after native scroll animation settled (500ms) for approvals, KYB, new policy, policy detail, cancellation and claim detail at390×844. All reached document end (remaining0±0.5px), with footer bottom842.9–843.3px. The short-height sidebar was separately checked by focus navigation. Browser and native controls are used; no extra keyboard handler or JS scroll lock was introduced.

HTTP checks:18 route/legacy-alias requests returned200;12 discovered non-API internal destinations returned200; no intrusive banner in any response.16 POST/PUT/PATCH/DELETE requests across representative paths returned405 before routing. Unknown paths return a styled404. API/document actions in the disconnected demo produce an explicit no-execution notice. The GET cancellation preview uses the existing pure integer-cent calculation with synthetic inputs; it is not ledger/provider evidence.

Build/typecheck passed on the current application. No lint script exists. Unchanged financial unit evidence is reused from UI-02 (219passed,1optional live Stripe skipped); financial/database/provider integration tests are not rerun. Independent guard/form-contract checks and current verdict are in `../reviews/ui-04.md`. No changes in `lib`, `db`, `scripts` or `app/api`.

## Running the local review

The ignored local harness is not part of the application/submission and contains no credentials. While running, open `http://127.0.0.1:4177/ops/` or `/broker/`. It renders actual page components through an explicit allowlist of synthetic read adapters, binds only127.0.0.1, and rejects unknown service imports. Start it from this worktree with:

```sh
node --import tsx --input-type=module -e 'import React from "react"; globalThis.React=React; await import("./.local/serve-ui-preview.tsx")'
```

This helper replaces the earlier Python static server; do not run both on4177. The normal application setup remains in README. The auxiliary production-mode local Next instance on3108 uses an intentionally unreachable placeholder database, a local session placeholder and a dummy test-prefixed Stripe placeholder for unauthenticated rendering checks only. Initial checks correctly failed on a missing Stripe configuration; no provider calls were made. With placeholders, home/login returned200, unknown path returned the styled404, and all11 protected routes emitted Next streaming redirects to /login (HTTP200 with explicit redirect metadata, not HTTP307 responses).

## Limits and integration

This is an all-screen UI review with enumerated state fixtures, not every possible combination of business events or a live financial end-to-end test. Recovery fixtures exercise visible branches; their sample journal lists are not complete financial histories and cannot establish balance invariants. Loading/error component presentation is inspected separately from actual slow-request/crash recovery. Reference DM Sans remains an OFL approximation of F37 Bolton; earlier reference-color contrast limitations remain, with no full WCAG claim. AF-06 is NOT REVIEWED WITH YOANN.

No main merge, push or Vercel deployment. Integrate this presentation diff with Claude's latest policy/business pages, preserve the latest business conditions and hidden inputs, then rerun integrated checks. The local branch does not include later B4 screens developed in other worktrees.

Final capture refresh is temporarily blocked by Chrome reporting an open extension UI. Independent source, existing image and bottom-scroll checks are favorable; completion remains pending until the affected final renders are refreshed and independently checked. No commit or final PASS is claimed at this checkpoint.

## Integration checkpoint — 2026-09-08T17:52:30.133563+00:00

User explicitly requests committing the pending UI-04 checkpoint before merging main, with final independent UI review delegated to Claude after integration. The prior visual-review gap remains disclosed; this checkpoint is not a final feature PASS. Startup reads: AGENTS.md, AUTOMATIC-FAILS.md, READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, released brief, README, current status and UI-04 handoff. No mandatory kit missing. Next: merge main preserving its business logic; typecheck, build, npm test and render checks. STATUS, PLAN and COMPLIANCE-MATRIX are now owned exclusively by Claude; retain main versions during merge. Keep UI decision entries and handoffs. About 35 hours remain before freeze.
