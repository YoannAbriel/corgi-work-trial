# UI-03: breadcrumb hierarchy and parent navigation

Base 9acbb5b on codex/corgi-interface; exact seven-file delta in [source manifest](ui-03-source-manifest.json). User requested fixing incomplete breadcrumbs and adding back arrows after finding navigation problems in the local preview.

PortalShell derives the root from the rendered role and accepts an optional explicit page trail. PortalFrame receives only labels/links and rendered content. Breadcrumb parents are ordinary Next links; the current item has aria-current=page. A named ArrowLeft link returns to the immediate parent. It works on direct entry without assuming browser history. No history inspection, persistence or new network/provider call is added. Root pages do not show a misleading back target.

Policy detail identifies the policy; New policy identifies the form; cancellation identifies its parent policy and preview; claim detail links through Claims. The broker root is Policies. Staff has Overview and operational sections; the broken staff link to the broker-only policy list is removed, and staff policy detail returns to Operations. No authorization or role-switching behavior was added to the application. Preview role selection is explicitly labeled local inspection tooling.

Reading path: page trail → server PortalShell → PortalFrame breadcrumb/return link → globals.css. Existing financial expressions, server checks, form actions/fields and queries remain unchanged. Mobile places the full breadcrumb below the menu/back controls, with 44 px return targets. No custom keyboard activation handler is used for application links.

## Actual checks

- Typecheck PASS; production build PASS after final mobile CSS (.local/ui03-build.txt), 19 pages generated. No lint script exists.
- No lib/db/scripts/app/api delta against base. No financial unit/provider/database suite rerun for this navigation-only change; prior UI-02 unit results are historical evidence, not a new run.
- CUA local page-derived fixtures: Policies → New policy → back arrow → Policies; Policies → Policy UI-PREVIEW-001 → clickable Policies; cancellation-unavailable view → parent Policy; Claim UI-CLAIM-001 → back to Claims. Current breadcrumb and destination verified at each step.
- At 320 px, cancellation and claim hierarchies stayed within document width 320; back target 44; full breadcrumb/current label visible. Temporary viewport reset.
- Actual Next production login at 3108: Overview / Sign in, Back to Overview returned to /; root breadcrumb Overview and no back arrow. No login submitted and no database connected.
- Local menu pointer/Space toggle checked during the preview correction. Enter-through-CUA limitation from UI-02 remains disclosed; no global keyboard-conformance claim.
- Independent review: [UI-03](../reviews/ui-03.md) PASS; UI02-P01/P02 independently closed after a whole-directory audit. Final staged scan repeated before commit.

## Preview repair and boundaries

The original static fixture had live-looking internal routes that returned 404. The ignored .local/build-navigable-preview.tsx now renders actual page components through explicit synthetic adapters into 14 routes; /ops.html and /approver.html are safe aliases, states.html is a safe New policy alias. No actual service/database module is loaded; unknown imports and database calls throw. All local HTML was scanned: zero form tags and zero external anchors. All 13 internal anchor destinations returned 200. Disabled fieldsets and submit controls are in HTML, not dependent on JavaScript. API/external anchors are replaced in HTML. A direct empty POST to the static server returned 501 in independent review.

The local helper final SHA256 is c34bc590f742a0dae88c250d81fc79c27011764969ecee40280090e61eba997d. Font-study.html is a separate inert font-comparison artifact, not an application page or account record. All application preview pages explicitly label synthetic records and disabled actions. The cancellation fixture honestly states that a computed financial preview requires the connected application. No fabricated provider or ledger evidence is asserted. The local preview is not a deployed trial deliverable.

UI-02's DM Sans approximation and contrast limitations remain unchanged. No main merge, push or Vercel deployment. Full B13, integrated financial testing and candidate walkthrough remain incomplete. Candidate status: NOT REVIEWED WITH YOANN.
