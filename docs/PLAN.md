# Execution plan (Track 1: Policy administration)

Created at 2026-09-08T07:40:00+00:00 from the approved planning discussion. This is the ordered execution backlog. The 16 Linear domain tickets (YOA-593 to YOA-608) and their 162 scenarios remain advisory references, not tasks. Estimates are working hours; each slice includes its tests, an independent review under REVIEWER.md grouped by money-path slice, a STATUS/DECISIONS update and a commit. About 46 hours remained at 10:00 on September 8, including sleep; the backlog totals about 38 working hours, so the buffer is small. If B12 or the Plaid/Increase stretches threaten B13 or B14, they are cut first and recorded.

Status legend: TODO, IN PROGRESS, DONE (tests and independent review PASS for the current code), BLOCKED (with the exact gap).

## Backlog

| ID | Slice | Acceptance criteria (observable) | Depends on | Evidence | Est. | Advisory refs | Status |
|---|---|---|---|---|---|---|---|
| B0 | Bootstrap and deploy | Next.js app deployed on Vercel; `/api/health` reports DB ok from outside the dev machine; Neon DB linked; migration runner; `.env.example`; gitleaks pre-commit hook; history secret scan PASS; Stripe and Sumsub (or Middesk) sandbox accounts created, 15-minute timebox each | none | deployed URL, scan output | 1.5h | YOA-607, YOA-596 | DONE (a8d84eb, review with B1) |
| B1 | Ledger core and pro-rata math | Tables `accounts`, `journal_entries`, `journal_lines` with a balanced-entry constraint; triggers raise on UPDATE/DELETE of protected tables; runtime role without UPDATE/DELETE; negative test proves a mutation fails; pure functions `computeTermDays`, `computeEarnedPremiumCents`, `computeUnearnedPremiumCents`, `computeEndorsementDeltaCents` with hand-worked examples (recited example, decided by Yoann: $1,200 written March 1 2028, 365-day term, cancelled after 100 days: earned 120000 x 100 / 365 = 32876 cents floored, refund 87124 cents; a January 1 2028 policy has 366 days and gives 32786 / 87214, also tested) | B0 | unit tests, negative tests | 2h | YOA-600, YOA-598 | DONE (8f253a7, review PASS) |
| B2 | Issuance and live Stripe collection | Policy stored as events (`policy_events` with `effective_at` and `recorded_at`); premium, tax and fee computed and shown separately; Stripe Checkout Session created with an idempotency key bound to the intent; immutable webhook inbox plus a separate mutable nonfinancial processing table; signature verified on the raw body; duplicate delivery posts once; replaying the event twice is one; journal entries posted atomically with event completion; policy becomes bound only after the webhook | B1 | real test-card payment on the deployed URL, Stripe webhook log, journal rows | 3h | YOA-598, YOA-599 | DONE (b3ba15b, review PASS, re-review PASS) |
| B3 | Broker KYB live | Broker entity submitted to Sumsub (or Middesk); provider statuses mapped to local eligibility (unknown, pending, approved, failed); binding refused server-side unless approved; pending and failed visible in the UI | B0 | provider dashboard screenshots, webhook or poll evidence, refusal test | 2.5h | YOA-597 | DONE (09e71e5, review PASS) |
| B4 | Endorsement with pro-rata delta | Limit change priced from the effective date over the remaining term; delta above the threshold waits for customer approval; approval bound to the quote hash and policy version; delta collected through Stripe (credit noted for the refund path); endorsement schedule and declarations PDFs generated as of any date | B2 | tests with a numeric example, PDF files | 3h | YOA-598, YOA-602 | DONE (26dede3, review FAIL then fixed, re-review PASS 18:07Z; LOW items in B13; live endorsement with Yoann pending) |
| B5 | Cancellation with real refund and clawback | Pro-rata unearned premium and refundable tax computed; refund created through the Stripe Refunds API on the original payment(s); `refund.*` webhooks update the ledger with requested and completed distinguished and failed refunds surfaced; commission clawback entries; short-rate representable | B2, B4 | refund visible in the Stripe dashboard, journal rows | 2.5h | YOA-598, YOA-599 | DONE (109dafb, review PASS, MEDIUM items closed by B7) |
| B6 | T+24h package | Seed-from-zero script; README integration inventory with modes; demo credentials for broker and staff; STATUS and DECISIONS current; deployment verified from outside; T+24h email drafted (Yoann sends) | B2, B3, B5 | README, email draft | 1.5h | YOA-593, YOA-608 | IN PROGRESS (package ready, email to send by Yoann) |
| B7 | Claims, reserves, payout, maker-checker | Claim intake; reserve set and adjusted as append-only events; payment reduces the reserve; incurred = paid + reserve; payment refused past the limit; money-out above $1,000 queued for a distinct human approver (initiator blocked, agent blocked, direct endpoint blocked, changed intent needs fresh approval); simulated rail with delayed settlement and a return event; bank ownership check through a labeled simulator | B1 | tests for self-approval, concurrency, limit | 3.5h | YOA-595, YOA-598 | DONE (65b06be, review FAIL then fixed, re-review PASS 16:16Z; LOW items in B13) |
| B8 | Backdated correction and as-of | Wrong effective date corrected by reversal entries plus re-book; original rows untouched (test); corrected figure on the statement; policy view and PDF as it stood on any date, including between two endorsements; effective-time versus recorded-time semantics documented | B4 | live-fire rehearsal script | 2.5h | YOA-600, YOA-602 | FIXED and merged (2755d11), re-review pending |
| B9 | Broker monthly statement | Statement run stored immutably with a content hash; premium collected, commission earned, clawback, net due; ties to the ledger to the cent (test); rerun of a closed month is identical | B5, B8 | statement page or PDF, equality test | 2h | YOA-600 | DONE (8b5b490, review FAIL then fixed, re-review PASS 17:32Z; LOW items in B13) |
| B10 | Reconciliation | Job pulls Stripe PaymentIntents, Refunds and BalanceTransactions for a window and diffs them against the ledger; classifications matched, local-only, provider-only, amount mismatch, stale; run summary stored; breaks screen with age; planted mismatch found; a failed fetch is never reported clean | B5 | screen with a planted break | 2.5h | YOA-601, YOA-603 | DONE (b1a0bce, review FAIL then fixed, re-review PASS 18:05Z; F-B10-08, 09 and the accepted 07 in B13) |
| B11 | MCP surface | Streamable HTTP MCP endpoint in the app with a per-user API key; read tools `get_policy_as_of`, `get_broker_statement`, `list_reconciliation_breaks`; write tool `request_claim_payment`, plus `run_reconciliation` (moves no money, appends a run; Yoann's suggestion 2026-09-08) that only enqueues an approval request; written list of never-delegated operations | B7, B9, B10 | MCP inspector session log | 2h | YOA-595 | MERGED (e7d7856), independent review pending |
| B12 | Differentiator: impact preview and explanation | Same pure function for preview and execution; preview page before approval (endorsement, cancellation, correction); explanation page after execution listing each amount with its formula line; stale quote rejected when the policy version changed | B4, B5, B8 | tests, screenshots | 2.5h | YOA-602 | TODO |
| B13 | Hardening, screens, integration review | Three key screens (policy detail with as-of, reconciliation breaks, broker statement) with default, loading, empty, error and one edge state; structured redacted logs; independent integration review PASS or disclosed gaps; walkthrough rehearsal with Yoann (one transaction, one correction, one failure) | all | review record | 3h | YOA-602, YOA-606, YOA-608 | IN PROGRESS (ten LOW findings merged b956950; screens, stuck-events view, integration review and walkthrough remain) |
| B14 | Freeze package | Video at most five minutes; evidence pack (Stripe and KYB dashboards, webhook logs); cut list and week-two plan; final email draft with exactly four items; remote SHA verified; history secret scan | B13 | STATUS final handoff section | 2h | YOA-608, YOA-607 | TODO |

## Official requirement coverage

General non-negotiables: deployed URL and two roles (B0, B6); own immutable double-entry ledger (B1); two genuinely live slots (B2 Stripe, B3 KYB); verified webhooks, idempotency, out-of-order tolerance (B2, B5); backdated correction (B8); maker-checker (B7); reconciliation with breaks screen (B10); MCP three read tools and one approval-queued write tool (B11); integer cents and penny rule (B1); running decision log (every slice).

Track 1 gauntlet: written versus earned (B1); one-year term and leap years (B1); endorsement money from the effective date (B4); correction test (B8); cancellation pro-rata with short-rate representable (B5); claims money (B7); commission on collected premium with clawback (B2, B5, B9); broker statement (B9); as-of reconstruction (B8); rounding penny (B1); taxes and fees separated (B2, B5).

Live fire: pay with a test card and watch the journal (B2); backdated fix (B8); replay the webhook twice (B2); cancel with an open claim and explain refund, commission and reserve (B5, B7); policy as it stood between two endorsements (B8); planted payout mismatch (B10).

Integration slots: premium collection LIVE SANDBOX (B2, B5); broker KYB LIVE SANDBOX (B3); bank verification LOCAL SIMULATOR (B7); document generation REAL (B4, B8); claim payout rail LOCAL SIMULATOR (B7).

## Stretch ladder, only after B13

Plaid sandbox live for bank verification (about 1.5h). Increase sandbox live for claim payout (about 2h). Everything else on the brief's stretch ladder stays cut.
