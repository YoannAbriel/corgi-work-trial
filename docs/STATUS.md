# Current status

## 2026-09-10T00:50:00+00:00 | Freeze checks on 756db05: MCP tested from outside, 54 figures re-read, repository and secrets clean

- **MCP tested by an agent holding the demo key**, read tools only, no write tool called. Eight tools with usable descriptions and strict schemas; the two that write are the only ones without `readOnlyHint`. CGP-01707 as of 2026-09-15 answers $1,200.00 and $28.20 with limits $1M/$2M, as of 2026-09-25 $2,400.00 and $56.40 with $2M/$4M: both match the live-fire records. Redwood 2026-09 **revision 6 still answers $437.45 with content hash 5c991578, and revision 7 the same hash**, while the latest is now revision 8 at $142.30 after the cancellation clawback: immutability holds and the month moves forward, which is the story. `list_reconciliation_breaks` returns the four genuine breaks and keeps the 32 probes apart, with the run reporting both. `inspect_reference` answers "Nothing in this database matches that reference." for a recognised shape that matches nothing and for a nonsense string, indistinguishably (decision 53). Refusals conflate "does not exist" with "not visible to you", so the surface is not an existence oracle. Two LOW opened: F-MCP2-01 and F-MCP2-02.
- **The 54 live-fire figures re-read on 756db05**: 54 checks, 0 disagreements, evidence under `docs/evidence/live-fire-day2/re-read-756db05/`. Two of them first reported DISAGREE because the re-read script still held the pre-batch-2 literals of the statements list (decision 64 folded revisions into one row per broker and month); the figures were verified by hand in the saved page text ($437.45 at revisions 6 and 7) and the two literals were corrected, not the expectations.
- **Repository**: working tree clean, main equals origin/main, **gitleaks over the whole history: 725 commits, 15.3 MB, no leaks**. `.env.example` holds placeholders only. No dash in any document of record. Every required file present. `docs/WEEK-TWO-PLAN.md` is deliberately absent until Yoann validates the draft.
- **Deployment**: `/api/health` reports 756db05; **13 of 13 protected paths answer 307 to /login anonymously**; POST /api/mcp without a key answers 401; POST /api/brokers without a session answers 303; the reveal consume route answers 401. Cron is `0 6 * * *`, that is 06:00 UTC on 2026-09-10, **ten minutes after the freeze**, so nothing waits on it.
- **MCP keys on the trial database**: eight rows, **seven revoked including the one whose secret leaked into a tool output at 18:35Z on 2026-09-08**, one live, an agent key with an expiry of 2026-12-09 created by Yoann at 00:44Z. Revocation is append-only (`mcp_key_revocations`), so the revocations are themselves evidence.
- **Left for the freeze**: 82 worktrees carrying 26 `.env.local` copies and 3 `.local` directories, to remove once the video session has finished (they are git-ignored, so nothing of them can reach the submission); the corgi_test runtime password rotation; F-NEWBROKER-02, the one-line ActivityRule union.

## 2026-09-10T00:15:55Z | Voiceover scene 04 corrected to the policy fee; master shortened

Yoann clarified that the recorded click opens the policy fee, not the tax, and requested a shorter explanation rather than padding it to the original timecode. New selected scene 04: `01-ready-clips/04_clickable-amounts_policy-fee.wav` in the Downloads voiceover package, 8.000 seconds, natural provider pace, no added silence. Spoken wording: “You can click an amount to understand where it comes from. Here, I am opening the policy fee, and the explanation shows the rule behind the charge.” The scoped wording avoids saying every number is clickable: source inspection shows the annual-premium tile is plain text while the fee uses AmountExplained. It also avoids the inapplicable 2,820-cent tax example and any rounding claim about the flat fee. Local transcription matches the new text. Prior takes remain archived.

The other 21 selected clips are unchanged. The revised full WAV is `Corgi-voiceover-full-04m36s.wav`: 276.000 seconds, 13,248,000 samples, mono PCM 24-bit at 48 kHz; SHA-256 `4c782ec426f0e47a013348c7dab156631a52db6bce771b79b511f2538e208659`. The new MP3 preview, selected-script table, clip index and manifest use the current continuous 00:00–04:36 assembly; original planning timecodes remain identified separately. The older 4:50 master is superseded and archived, not the current deliverable. Text/provenance secret scan after this update: 185.36 KB, exit 0, no leaks. No product files or deployment changed. Independent asset reviewer returned PASS for technical/source-content readiness of the final replacement and master, reusing the unchanged 21 clips' evidence. It verified exact PCM concatenation of the 22 clips, decoding of the master/preview, provider provenance and current timecode/script consistency. Its record is in the package at `04-production-records/independent-review.md`; this scoped PASS excludes subjective listening and the final video. Candidate voice/camera/screen montage and subjective listening remain the next user steps; no finished video or global trial-completion claim.

## 2026-09-10T00:10:08Z | ElevenLabs voiceover assets generated in Downloads

Yoann now requested synthetic English narration of every row of the manual recording table, a happy/warm delivery, timecode fitting and progressive delivery into Downloads; he explicitly selected ElevenLabs through Higgsfield. The previously reviewed source is `/Users/yoannabriel/Downloads/corgi-video-script-table-en.md`, SHA-256 `7cfba9dafc803bd94f4aed04b772c418e58c908d6e7c2faf353cd5c63c8c5504`; its independent script PASS is in `docs/reviews/manual-video-script-table.md`. The source is no longer in docs/handoffs after concurrent repository work, and was read from Downloads. Main observed at `288246d`, clean and synchronized before this note. The mandatory operating files and source/navigation records were read earlier in this continuous session; AUTOMATIC-FAILS was revisited for the audio work. Higgsfield CLI model/voice schemas and current ElevenLabs TTS guidance were inspected; no product implementation was changed.

Generated package: `/Users/yoannabriel/Downloads/Corgi-Voiceover-ElevenLabs-2026-09-10`. It contains 22 selected WAV clips, their untouched provider originals, prompts, a revised script table, progress/index documents and production records. Provider records confirm `text2speech_v2` with model `elevenlabs` and Benji preset; no cloned voice was used. Scenes 04, 08 and 13 were lightly reformulated to fill their slots and sound more conversational, with prior takes retained separately and unchanged facts. There are 26 generation records including alternatives. The voice direction uses emotion/pause tags; the provider route does not disclose its underlying ElevenLabs version or a native speed parameter. Pitch-preserving tempo adjustment and silence handles are explicitly recorded as local post-processing.

Actual checks: every fitted clip was measured against its slot, transcribed locally using whisper.cpp base.en and checked for factual wording (amounts, dates, approvals and live/simulator limits). This is not a subjective human listening verdict. The full PCM 24-bit mono 48 kHz WAV has exactly 13,920,000 samples, 290.000 seconds; SHA-256 `e51b33efb23414c016bab971160844c184ef629de0adcd2e5398375a2db2fdce`. MP3 preview also generated. Secret scan of 86 text/provenance files (184.29 KB) passed with no leaks; no video OCR/history scan is implied. A timestamp-based trim discrepancy on the first local fitted clip was fixed to sample-count trimming before the final batch; its final duration is verified. Independent final asset review is being completed in the package's `04-production-records/independent-review.md` and will be recorded below when returned.

This session made no financial/provider-sandbox action, approval decision, product commit, push, deployment or publication. Generative-media requests used the user's authorized existing Higgsfield account; no new purchase was made. No completed video, global B14/AF PASS, candidate-understanding confirmation or saved Screen Studio project is claimed. Next executable step: finish the scoped asset review, then Yoann listens to the preview and combines the selected narration with his screen/camera edit.

## 2026-09-09T22:33:18Z | Manual video script rewrite: startup receipt

Yoann stopped automated UI control and now requests one English Markdown table with timecodes, screens, URLs, roles and narration, including the actual UI navigation from Overview. Scope is a B14 preparation document, not product implementation or a finished video. Main observed at 756db05, clean and 11 commits ahead of origin/main; about 7 h 17 remain to the stated freeze. Actual reads: AGENTS, AUTOMATIC-FAILS, WORKFLOW-48H, REVIEWER, READABLE-CODE, BRIEF-REFERENCE, README; current STATUS preparation entries; PLAN B14; relevant COMPLIANCE-MATRIX live-fire rows, DECISIONS sidebar/billing/statements updates and GAP-20/21; original Downloads script, English narration and recording plan. UI source read includes app-shell, section-nav, policy page and view labels, policy list/search, and targeted claims/approvals/MCP/statements/reconciliation sections. Mandatory kit files are present; original kickoff sandbox rules remain unavailable as already disclosed. Local records are the working status source; Linear was not queried.

Planned checks: reconcile every screen and click path with the UI source and the prior deployed-app observations, check continuous timecodes through 4:50, verify spoken word budget and URLs, scan the document for secrets and obtain a scoped independent script review. Recording was actually started after Yoann's earlier "lancé" and app scenes were navigated, superseding the pending-source note below, but this session has not verified a saved project, media duration or cursor metadata. Capture work is no longer the requested next step; Yoann will record manually. No product, provider, approval or financial action is part of this rewrite. AF delivery checks and AF-06 are not established by the script.

## 2026-09-09T22:07:04Z | English recording scripts ready; Screen Studio source selection pending

Downloads artifacts: `corgi-video-script-en.md` (annotated English translation; original unchanged), `corgi-video-narration-en.md` (spoken adaptation), `corgi-video-recording-plan.md` (shot list/cursor holds/editing steps), and independent `corgi-video-script-review.md`. The source was FAIL for verbatim use; corrections cover P1's later cancellation, more than eight entries, proper tax/MCP routes, dated statement revisions, the demo-switcher identity limitation, nonzero/unknown clearing, the probe's missing journal and the later introduction of rule 21. Independent text-readiness PASS for the adapted narration at SHA-256 `143afaace0870aaa0980975eb936aee10c7727b6b439f39a77fffeac6bca10cc`, with actual scene/media checks still required. Reviewer count: 519 spoken words, about 4:19.5 at 120 words/minute before pauses; 4:50 is an editing target, not measured media.

Actual UI work: Screen Studio 3.7.5-4595 opened and configured to Window, No camera, No microphone, No system audio. A separate Chrome window shows the deployed `/api/health`; its response at 21:58:21.407Z was ok=true, database=ok, revision `66fb8636c0a9b3091967d73091cc167626c58d93`. Bookmarks hidden and browser zoom reset for the capture; no credentials entered. The Screen Studio window-selection overlay did not respond to automated selection. Yoann was asked to select the dedicated health window and report recording start. Recorder remains ready for that manual selection. No saved video/project or exported media was produced or verified; no recording, duration, cursor-metadata, playback or completed B14 claim is made.

Checks: the four English artifact files passed `gitleaks stdin --redact --no-banner --no-color` (exit 0; about 47.01 KB text, no leaks), with independent narrative/source review recorded locally. This is not video OCR, repository/history scanning or product testing. Main observed at `facbdd8`, ahead of origin/main by one commit and clean before this note; concurrent product sessions advanced it and committed the startup note. This session made no product changes, provider side effects, approval decisions, commit, push or deployment. Next executable step: after manual Screen Studio source selection, record a short technical take, verify saved cursor/video data, then capture the numbered scenes against the actual deployed revision and validate the finished media. Full B14, final integrated checks and AF-06 remain open outside this text-preparation scope.

## 2026-09-09T22:00:00Z | Video translation and recording preparation requested by Yoann

Scope: translate `/Users/yoannabriel/Downloads/corgi-video-script.md` into English, prepare spoken narration and recording cues, and record read-only deployed-app sequences in Screen Studio for Yoann to add his own voice/camera later. No product code, financial action, approval, credential creation or publication is authorized by this recording task. Initial observed branch main at `8f3cd7c`, clean and ahead of origin/main by 89 commits; concurrent implementation is preserved. About 7 h 50 remain before the September 10 05:50 UTC freeze. Actual reads: AGENTS, AUTOMATIC-FAILS, WORKFLOW-48H, REVIEWER, READABLE-CODE, BRIEF-REFERENCE, the entire source video script, README (integration inventory, MCP, operations, money rules and limitations), PLAN B14, current STATUS through 21:44Z and live-fire summaries, relevant DECISIONS and COMPLIANCE-MATRIX sections, GAP-20/21. No mandatory kit file found absent; original kickoff sandbox rules remain unavailable as disclosed. Local records used; Linear not queried.

Next acceptance evidence: editable local screen recordings and an English narration mapped to actual visible states. Planned checks: deployed health/revision before and after capture; every planned scene visible; duration and recording file integrity; cursor movement and readable amounts; no credential capture; independent scoped content review. Main risks: source script is stale after tonight's policy/claim activity, shared browser cookies invalidate its three-role-tabs advice, and a health response alone does not establish all AF-01 checks. Corrections will be explicit in the recording version; the source file remains unchanged. AF-03 product checks and AF-06 candidate understanding are NOT RUN in this video task; no global delivery PASS is claimed.

Updated at 2026-09-08T07:40:27+00:00. Track: **1: POLICY ADMINISTRATION**, explicitly selected by Yoann. Stack and providers: **SELECTED** in the planning discussion of 2026-09-08 (see DECISIONS.md): Next.js on Vercel, Postgres on Neon, Stripe test mode (live), Sumsub sandbox KYB (live, Middesk fallback), local simulators for bank verification and claim payout, real PDF generation. Repository still contains instructions, planning and preparation only; no product code, deployment or provider account exists yet.

## 2026-09-08T07:40:27+00:00 | Startup receipt and planning session

Files actually read in full this session: CLAUDE.md, AGENTS.md, AUTOMATIC-FAILS.md, READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, HANDOFF-PROMPT.md, README.md, docs/BRIEF-REFERENCE.md, docs/STATUS.md, docs/DECISIONS.md, docs/PROVENANCE.md. Read by targeted search only: GAP-REVIEW.md, docs/reviews/instructions-2026-09-08.md. Not read: READINESS-CHECKLIST.md, STRESS-TEST-PLAN.md, READINESS-BACKLOG.json (advisory, consulted later per scope). Absent files: none of the kit files are missing; docs/PLAN.md and docs/COMPLIANCE-MATRIX.md did not exist at session start (PLAN.md created now, COMPLIANCE-MATRIX.md to be created with B1).

Provider facts verified today against official documentation: Stripe refunds and refund webhooks work in test mode; Vercel Hobby cron runs at most once per day; Plaid sandbox is free and self-serve; Sumsub has an official MCP server, a trial sandbox and a review-state simulation API; Middesk issues a sandbox key at account creation with business-name triggers for verified, in_review and failed; Persona KYB requires contacting their team. Not yet verified: actual signup success for Sumsub or Middesk, Middesk webhook signature scheme, the modeled state's premium tax rate and source.

Linear: authenticated in this session; the 16 domain tickets were listed read-only and left unchanged. Execution issues B0 to B14 created as YOA-609 to YOA-623 with acceptance criteria, blocking relations, evidence and estimates; a project status update was posted. No scenario was marked passed.

Written this session: docs/ATTACK-PLAN.md, docs/checkpoints/t-plus-2h-email.md (draft, Yoann sends), docs/PLAN.md, four decision entries in docs/DECISIONS.md. T+2h email: SENT by Yoann at 09:49 Europe/Zurich (07:49 UTC) on the candidate thread, reported by him and recorded at 2026-09-08T08:07:11+00:00. Checkpoint met; the thread header is authoritative.

## 2026-09-08T08:20:34+00:00 | B0 bootstrap and deploy: in progress, deployment live

Done: Next.js 16 app hand-written at the repository root (no create-next-app boilerplate), TypeScript strict, plain CSS; `db/client.ts` pool on the Neon pooled endpoint; `scripts/migrate.ts` runner applying `db/migrations/*.sql` once each inside a transaction; `GET /api/health`; gitleaks pre-commit hook in `.githooks/` (activated locally with `git config core.hooksPath .githooks`, to be documented in README); `.env.example` with placeholders. Vercel project `corgi-work-trial` linked and connected to the GitHub repository; Neon `corgi-trial-db` provisioned through the Vercel Marketplace and connected to all environments. Production URL: https://corgi-work-trial-iota.vercel.app

Checks actually run: `npm run typecheck` PASS; `npm run build` PASS; `npm run migrate` against Neon PASS (schema_migrations created, no migration yet); `curl https://corgi-work-trial-iota.vercel.app/api/health` from the dev machine returned HTTP 200 with database ok and revision 1e40dc7 (AF-01 evidence for this revision only; the app has no product behaviour yet). Stripe key verified as test mode (`sk_test_` prefix, account US, test balance call); no key value was printed. Assistant choices, minor: npm instead of pnpm (corepack broken locally), no Tailwind, no ORM.

Pending in B0: broker KYB provider access (Sumsub trial verified by signed API call at 2026-09-08T08:28:41+00:00 but KYB is Enterprise-only there, so Middesk signup is being attempted next; see DECISIONS.md), Stripe webhook endpoint (after B2 route exists), history secret scan result below, README setup section. Not started: everything from B1.

## 2026-09-08T08:59:18+00:00 | KYB decision, Stripe proofs, design review

Broker KYB: Stripe Connect Accounts v2 business verification in test mode, decided by Yoann (DECISIONS.md, 08:55Z entry). Verified on the sandbox: three test company accounts created (acct_1UDKmAK6R3nWBqMW verified fixture, acct_1UDKmDK6R39jqMi4 failed fixture showing `verification_failed_tax_id_match`, acct_1UDKmIK6R3fxfHN9 pending fixture), all `livemode: false`, v2 events emitted. Premium collection: test payment pi_3UDKq0K6R3v50tIy1mwQmeWT ($12.61, succeeded) and partial refund re_3UDKq0K6R3v50tIy11aPmuHK ($8.98, succeeded) through the Refunds API; balance transactions show a 67 cent Stripe fee, to be classified explicitly by reconciliation. These are feasibility probes, not product evidence; the probe objects stay in the sandbox with metadata `corgi_probe`. One synthetic probe applicant also exists in the Sumsub sandbox.

Design review: DESIGN FAIL recorded in docs/reviews/architecture.md (21 findings). docs/ARCHITECTURE.md revised at 08:50Z for the material findings; five items stay OPEN for Yoann (worked-example date, negative endorsement delta, closed-month revision rule, commission clawback rounding, modeled state and tax rate). Re-review requested after the open items are decided. Reviewer verdict allows B0 and B1 to proceed.

B0 remaining: the Stripe webhook endpoint on the deployed URL, delivered with slice B1a (protected tables, webhook inbox, route, endpoint registration, replay test) before the pro-rata functions (B1b).

## 2026-09-08T09:12:00+00:00 | B1a done: ledger core, webhook inbox, Stripe endpoint live

Commit 3c9ef3a deployed (health check reports it). Migration 0001 applied on Neon: chart of accounts, append-only journal with deferred balance trigger and server-set recorded_at, immutable webhook_events plus mutable webhook_processing, role app_runtime with SELECT and INSERT only on money tables (password set by script, never committed). The app now connects as app_runtime in production and locally.

Checks executed: `npm run check:ledger-guards` 10/10 PASS on the trial database, every check rolled back (owner UPDATE and DELETE refused by trigger; unbalanced entry and entry without lines refused at commit; recorded_at server-set; app_runtime lacks UPDATE, DELETE, TRUNCATE; app_runtime can insert; global debits = credits). Stripe webhook endpoint we_1UDKzYK6R3v50tIybe5BIytW registered on the production URL (test mode, 9 event types). Against production: invalid signature 400; live-mode event 400; same event delivered twice sequentially and twice concurrently: two events stored for four deliveries, second deliveries answered as duplicate; a real Stripe delivery (evt_3UDL2qK6R3v50tIy0VZVRxVz for test payment pi_3UDL2qK6R3v50tIy0hthuI6E) received by the deployed app, stored once, marked ignored with the reason "no handler yet". Processing lease expires after five minutes so a killed function cannot leave an event stuck (to be demonstrated in B2).

Ledger practice check against Modern Treasury, Square Books and Stripe Ledger write-ups recorded in ARCHITECTURE.md section 1; created_by added to journal headers. Probe objects left in the Stripe sandbox carry metadata corgi_probe. B0 is complete. Independent review of B1a is grouped with the B1b/B2 money-path review. Walkthrough status: NOT REVIEWED WITH YOANN.

## 2026-09-08T09:17:36+00:00 | B1b done: premium arithmetic with tests (commit ea5085d)

`lib/money/dates.ts` (calendar dates, term end on the same date next year or February 28, real day counts) and `lib/money/premium.ts` (earned and unearned premium floored/ceiled, endorsement delta over remaining days from the effective date, state tax floored when charged and ceiled when refunded, commission on collected premium, pro-rata cancellation refund breakdown). All arithmetic in bigint cents. `npm test` (node --test through tsx): 14/14 PASS, including earned + unearned = written on every day of a 366-day term, both example dates (January 1: 32786 / 87214; March 1: 32876 / 87124), backdating changing the delta (43606 vs 48524), the refund breakdown 87214 + 2617 + 0 = 89831. docs/COMPLIANCE-MATRIX.md created with the ledger, webhook, sandbox, secrets and deployment rows.

B1 status: implementation complete (B1a + B1b), tests PASS; independent review requested next; walkthrough NOT REVIEWED WITH YOANN. Open for Yoann: which example date to recite (both are tested), commission clawback rounding (B5), negative endorsement delta handling (B4), closed-month revision rule (B9), modeled state and tax rate source (B2).

## 2026-09-08T09:34:54+00:00 | B1 review FAIL fixed (a94f091), re-review requested; B2 in progress by delegate

Independent B1 review (docs/reviews/b1-ledger-core.md): FAIL on F-B1-01, app_runtime could append balanced lines to a committed entry. Fixed by migration 0003 (entries sealed at commit: lines only in the creating transaction; TRUNCATE triggers; received_at server-set; CHECK livemode = false) and a fail-closed db/client.ts. Proven by `npm run check:ledger-seal` 4/4 on the disposable database corgi_test (same Neon project, created for tests that must commit rows); trial database guards still 10/10. Register of all findings: docs/reviews/FINDINGS.md. Re-review by the B9 reviewer at 17:32Z: PASS at 8b5b490 (docs/reviews/b9-statements.md section 10; production reports 8e6d468, the last code commit, with an empty code diff to 8b5b490). F-B9-09 resolved and verified on production; revision 3 ties on the reviewer's own ledger read; entryRecordedAt kept in the hash on the reviewer's judgement. Open LOW: F-B9-05, F-B9-06 (half), F-B9-08, F-B9-11. Walkthrough: NOT REVIEWED WITH YOANN. Two synthetic replay-test events remain in the production inbox, marked ignored; to be disclosed in README.

Decisions since the last entry: recited example March 1, 2028 (365 days); California premium tax 2.35 percent from official sources (DECISIONS.md). B2 is being built by a delegate in an isolated worktree (branch worktree-agent-*, no push, no deploy); it will be reviewed and merged by the coordinator.

## 2026-09-08T09:39:11+00:00 | B1 independent review PASS (re-review at 8f253a7)

Reviewer confirmed the seal, TRUNCATE and livemode guards and tried bypasses (owner, session settings, pause between header and lines, timezone, trigger disable): all refused or without effect. Residue disclosed, not a finding: the owner can still drop a trigger, and database restores sit outside the guards. .env.example corrected (no owner fallback wording; test database placeholders). B1 is DONE technically; walkthrough NOT REVIEWED WITH YOANN. Note: the shared database already holds migrations 0002 and 0004 applied from the B2 worktree; the repository is behind the schema until B2 merges.

## 2026-09-08T10:19:18+00:00 | B2 merged, deployed and live-proven (commit 0115796)

Slice B2 built by a delegate in a worktree (docs/handoffs/b2-implementation-notes.md), read by the coordinator on the money path, merged with two trivial conflicts, deployed (health reports 0115796). Checks after merge: typecheck, build, `npm test` 56/56, check:ledger-guards 10/10 and check:money-guards 43/43 on the trial database (rolled back), check:payment-replay 15/15 and check:ledger-seal 4/4 on corgi_test.

Live proof at 10:16Z: Yoann paid policy CGP-01062 ($3,450.75 premium, California, effective 2026-10-01, charge 355684 cents) with a Stripe test card on the deployed URL. Stripe delivered payment_intent.succeeded (evt_3UDM4KK6R3v50tIy0S6PIO4D, pi_3UDM4KK6R3v50tIy0F5xaBbu, livemode false) and, after it, checkout.session.completed (evt_1UDM4MK6R3v50tIyurIo0iug): both stored once and processed; four journal entries posted once (cash 355684, unearned premium 345075, tax 8109, fee 2500, commission 51761), policy bound. The operation's lifecycle reads requested, provider_accepted, failed (an earlier simulated failure from the delegate's local run), succeeded, provider_accepted (session completion): every step is an appended event. The T+24h requirement (money moving on a live sandbox rail at the deployed URL) is met at T+4h26; the checkpoint email is drafted in B6 and sent by Yoann.

Delegate findings adopted: webhook payloads were stored as jsonb strings by the B1a code (fixed with sql.json(); the two probe rows keep the old shape and are ignored by queries); Next.js 16 appends a block to AGENTS.md on dev runs (agentRules turned off). Demo data now in the trial database: broker Redwood Commercial Brokers (15%), users broker@, customer@, ops@, approver@example.com sharing DEMO_PASSWORD, policies CGP-01061 (bound through a locally signed webhook) and CGP-01062 (bound through the real Stripe delivery above). Independent review of B2 in progress (docs/reviews/b2-issuance-and-collection.md). Walkthrough NOT REVIEWED WITH YOANN.

## 2026-09-08T11:06:08+00:00 | B2 review FAIL (data), documents and KYB adapter merged (c548ccb), B5 in progress

B2 independent review: FAIL on F-B2-01 (CGP-01061 bound on a locally fabricated payment; Yoann decided reversal plus dated correction event, done as B8a after B5 merges), MEDIUM F-B2-02 (503 while a lease is in flight, stuck-events view), F-B2-03 (Checkout expiry: new intent per attempt, decided), F-B2-04 (a fresh draft must be paid on the deployed URL). Code controls all passed. Register: docs/reviews/FINDINGS.md.

Merged from the documents/KYB delegate: lib/documents (pure fold of policy events as of a date, declarations and endorsement schedule PDFs, text-asserted tests) and lib/kyb (Stripe Connect Accounts v2 adapter, eligibility mapping with captured fixtures, opt-in live test PASS). `npm test` 103 pass, 1 skipped. Findings for Yoann: Stripe's business identity check lands about 45 to 50 seconds after account creation (settling window rule), the published pending EIN fixture yields approved, the ToS attestation and business URL are mandatory. Fourteen test accounts tagged corgi_probe exist in the Stripe sandbox. B5 (cancellation with a real refund) is being built by a delegate, including F-B2-02 and F-B2-03.

Walkthrough status: B1 QUESTIONS OPEN, B2 QUESTIONS OPEN (Yoann asked for shorter explanations; the Code page was rewritten with a flow diagram).

## 2026-09-08T11:33:27+00:00 | B5 merged and live-proven (e120f69), CGP-01061 corrected by reversal (B8a)

B5 (delegate, 9 commits, notes in docs/handoffs/b5-implementation-notes.md) merged: `npm test` 130 (129 pass, 1 skipped), guards 10/10 and 51/51, payment replay 22/22, refund replay 27/27, seal 4/4. Live: CGP-01062 cancelled effective 2026-10-31 through the deployed app; real Stripe refund re_3UDM4KK6R3v50tIy0scSGaps of 324156 cents succeeded on pi_3UDM4KK6R3v50tIy0F5xaBbu; refund.updated received by the deployed app posted refund_completed (324156) and commission_clawback (47506); refund_payable back to 0; ledger balanced. The refund.* events delivered before the deployment are stored as ignored (disclosed); a new refund.updated was made to fire by updating the refund's metadata. Disclosed: this refund crossed the $1,000 money-out threshold with no approver because B7 does not exist yet.

Yoann paid a fresh policy CGP-01274 ($2,312 premium, tax 5433, fee 2500, charge 239133) from a Checkout Session created by the deployed app (closes F-B2-04). The customer email he typed was his real address; replaced by a synthetic example.com address on the nonfinancial customers table (AF-04).

B8a: CGP-01061 (bound on a locally fabricated webhook) corrected by `npm run void:fabricated-binding`: the guard asked Stripe and confirmed the payment intent does not exist, then one transaction wrote a correction_reversal policy event superseding the issuance and four reversal entries mirroring the originals (lib/ledger/reverse.ts, lib/policy/void-fabricated-binding.ts). Originals untouched; the fold skips superseded events; ledger cash_stripe now equals what Stripe holds (270661 cents); the same run on corgi_test showed net zero per account and a refused second attempt. B2 re-review requested. B3 wiring is being built by a delegate; B5 independent review in progress.

## 2026-09-08T12:05:27+00:00 | Walkthrough progress, B2 re-review status, B5 review PASS

Walkthrough with Yoann (four questions on the money path, his own words): B2 outbox order (half right, then explained: crash safety and the stable key), which Stripe event posts (right, with the reason), replay twice is one (inbox by event id right, journal by operation id explained and confirmed back), B5 refund liability (refund_payable as a clearing account explained with his own policy's figures). Status: B2 and B5 EXPLAINED, QUESTIONS OPEN until a second pass tomorrow; B1 QUESTIONS OPEN.

B2 re-review at 109dafb: four fixes verified; new finding F-B2-13 (voided policy could still be paid): first door closed (session expired at Stripe, void fixed at 5b14830), second door (attempt 2 on a voided policy, unique-violation mapped to already_posted) assigned to the B3 delegate with the reviewer's exact spec; re-review after B3 merges. B5 review: PASS with four MEDIUM (F-B5-01 approval on refunds and F-B5-02/03 refund re-issue and recovery assigned to the B7 delegate; F-B5-04 a deployed-app cancellation after B7). B7 (claims, reserves, simulated rail, maker-checker) being built by a delegate.

## 2026-09-08T12:41:45+00:00 | B3 merged and live-proven (b3ba15b, 09e71e5): broker KYB on Stripe Connect

Delegate build (docs/handoffs/b3-implementation-notes.md): migration 0006 (broker_kyb_submissions, EIN last4 only, terms acceptance with date and IP) and 0007 (voided status), broker KYB screen with the decided terms checkbox, submission committed before the provider call, account.updated handling that re-reads the account and appends a status row only on change, settling window of at least 2 minutes, eligibility rechecked at binding time (money arriving for an ineligible broker is not bound, shown in red, staff can bind once approved), staff view /ops/brokers with a re-read action, and the F-B2-13 second-door fixes (voided status, startCheckout refusal, unique violations no longer read as replays, seven replay lines). Checks: 152 tests, money guards 58/58, kyb replay 22/22, payment replay 29/29, refund replay 27/27.

Live: demo broker submitted through the app to Stripe Connect (acct_1UDNobK6R3ohMVag): pending at 12:07:57Z, approved at 12:10:34Z (the settling window held it 2 min 37 s); failed path on corgi_test (acct_1UDNv2K6R3y2nGVW, verification_failed_tax_id_match, Pay disabled, direct POST refused). Webhook leg: the account endpoint cannot receive connected-account events, so a Connect endpoint (we_1UDOHHK6R3v50tIyfbZP2ohD, connect=true, same route, own secret STRIPE_CONNECT_WEBHOOK_SECRET) was registered and the route now verifies against either secret (09e71e5); a contact-email change on the connected account produced account.updated evt_1UDOJoK6R3ohMVaghFAAnpEU, received by the deployed app one second later, processed done, "broker KYB unchanged at approved: nothing appended" (Vercel log: POST /api/webhooks/stripe 200). Stripe refused https://example.com as a business URL (recorded as a failed status row at 12:07:45Z). Fourteen probe accounts plus the demo account exist in the sandbox. Independent review: PASS at 09e71e5, unchanged at faa12a8 (docs/reviews/b3-broker-kyb.md): typecheck, build, 152 tests, money guards 58/58, kyb replay 22/22, payment replay 29/29, fresh database migrated 0001 to 0007, gitleaks on 17 commits clean, trial rows and Stripe read back; three MEDIUM (F-B3-01 suspense account, decided by Yoann at 12:54Z; F-B3-02 misleading retry comments, fixed; F-B3-03 no pending or failed broker visible on the deployed app, fixed at 13:03Z: two more demo brokers seeded, broker2@example.com submitted through the deployed URL with the failing tax-id fixture, pending at 13:02:33Z, then account.updated evt_1UDOgLK6R3FpfF2DhpwJYnqR reached the Connect endpoint at 13:03:26Z and the app appended failed at 13:03:27Z, which is the first webhook-driven KYB transition observed on the deployed app; /ops/brokers now shows approved, failed and never submitted side by side; broker3@example.com is kept unsubmitted so the pending state can be shown live at the debrief) and six LOW in FINDINGS.md. B2 re-review of the F-B2-13 fixes: PASS at b3ba15b (12:44Z). Walkthrough: NOT REVIEWED WITH YOANN. The demo broker's connected account is real product data, not a probe; it carries a corgi_connect_webhook_probe metadata key from the Connect endpoint proof.

## 2026-09-08T13:34:00+00:00 | Rule 14 built: suspense account for money received after eligibility loss

Migration 0010 (account unapplied_customer_cash, status paid_not_bound), lib/ledger/policy-entries.ts (unappliedCashReceivedEntry, collectedFrom on the issuance entries), lib/payments/collection.ts (parking at receipt in the same transaction as the succeeded status; the bind applies the parked cash; the replay guard now reads the premium_collected entry, not any entry), lib/payments/checkout.ts (expireOpenCheckoutSessionsOfBroker) called from the KYB refresh when a broker stops being approved, screens (policy page wording, no Pay button on paid_not_bound, disclosure line on /broker/kyb and /ops/brokers, placeholder label only with a placeholder row). Checks: typecheck, build, 155 tests (154 pass, 1 skipped), kyb replay 23/23 (parking, no double parking on redelivery, bind applies the cash, suspense back to zero, cash_stripe once), payment replay 29/29, money guards 58/58, ledger guards PASS. Migration 0010 applied to corgi_test and to the trial database before the deploy. Not built yet: the refund of parked money for a broker who fails for good (money out, waits for the B7 approval queue). Independent re-review requested from the B3 reviewer. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T14:21:00+00:00 | B7 merged (e77baa6, plus 59f2fcd staff landing): claims, reserves, simulated payout rail, maker-checker

Delegate build (docs/handoffs/b7-implementation-notes.md): migration 0008 (claims, claim_events, approval_requests and decisions, rail records, bank verification records, two money_operations columns), pure claim rules (incurred = paid + reserve, per-occurrence and aggregate limits), claim entries (reserve set and adjusted, payment sent, settled, returned, reserve restored), two local simulators labeled LOCAL SIMULATOR (payout rail with delayed settlement and returns; bank ownership check), approval queue with a hashed intent (a changed destination or amount needs a fresh approval), maker-checker enforced in code and by a database trigger (initiator cannot approve, only staff_approver decides), the refund path of B5 gated above $1,000 (F-B5-01), re-issue of a dead refund under the same key with a Stripe listing first (F-B5-02), recover-stuck-operations job and a resend action (F-B5-03), screens /ops/claims, /ops/claims/[id], /ops/approvals, and the two job endpoints protected by CRON_SECRET. Deviations recorded by the delegate: per-claim lock with pg_advisory_xact_lock (app_runtime has no UPDATE privilege, so SELECT FOR UPDATE is impossible by design); a claim payment counts as paid from the moment it is sent, settled money reported separately; reserve_adjusted carries the new outstanding reserve. Coordinator additions: schema_migrations record renamed on both databases from the old 0007 file name to 0008 (the runner then skips everything), staff landing page /ops with the login redirect for staff roles.

Checks on main after the merge: typecheck, build (31 routes), 220 tests (219 pass, 1 skipped), claims and approvals 55/55, kyb replay 23/23, payment replay 29/29, refund replay 27/27, money guards on corgi_test 110/110 on a solo rerun (a first run under shared load had one Neon CONNECTION_CLOSED, not a guard failure); the delegate's own runs: 110/110 on corgi_test, and on the trial database a catalogue read of every trigger and grant plus an exercised run reported as 108/110 under contention (deadlocks on TRUNCATE probes against the live app), which the delegate was re-running when the session limit stopped it. Not verified: the screens were never rendered by the delegate (type-checked and built only), the job endpoints never called over HTTP. Independent review to be launched once the session limit resets (16:20 local). Decided by Yoann at 14:32Z: a claim payment is paid when sent (DECISIONS.md). Walkthrough: NOT REVIEWED WITH YOANN.

Parallel interface branch (noted 14:29Z): Yoann runs a separate Codex session in the git worktree .worktrees/corgi-interface (branch codex/corgi-interface, cut at 59f2fcd) that adds a portal-style dressing only (navigation, cards, responsive styles, two illustrations generated on his own machine): no change under lib, db, scripts, app/api or the Vercel configuration; existing forms, access checks and business conditions kept; nothing pushed, deployed or merged. Integration into main happens after the B4 screens land; the overlap to watch is app/policies/[policyId]/page.tsx and the global styles. AF-06 is NOT confirmed for that dressing until Yoann has explained the path (server page, PortalShell, CSS, WorkspaceOverview for the ops home) back; the directory is git-ignored at the root so it can never be committed by accident. At 16:25Z Yoann also gave that session the visual pass on the two PDFs (F-B4-04): scope limited to lib/documents/render.tsx plus an optional theme file and bundled font, no change to the snapshot, the fold or the formatting helpers, render tests unchanged, no network at render, a demo issuer name with a test-data label and never a real company's identity; the prompt lives outside the repository.

## 2026-09-08T15:34:03+00:00 | B10 merged (28a361c); B7 review FAIL then fixed (65b06be); decisions 16 and 17

B10 reconciliation, delegate build (docs/handoffs/b10-implementation-notes.md): migration 0011 (reconciliation_runs, reconciliation_items, append-only, guarded), the Stripe source (PaymentIntents, Refunds, BalanceTransactions, paginated) and the claims rail source, the ledger side with the runtime role, a pure diff (matched, local-only, provider-only, amount mismatch, stale; Stripe fee noted only; parked payment matched; reversed operation matched at zero), one run per source and window stored in one transaction after the fetch (a failed fetch stores a failed run with no items and never reads clean), POST /api/jobs/reconcile and GET or POST /api/jobs/daily (recover, settle, reconcile) under CRON_SECRET, vercel.json with one cron at 06:00 UTC, the staff screen /ops/reconciliation (runs, open breaks with age, resolved breaks, Run now), scripts/check-reconciliation.ts 29/29 (planted provider-only PaymentIntent of $42.42 with no metadata found on the real sandbox; a rail transfer with no ledger entry found; a simulated fetch failure produces a failed run). Migration 0011 applied to corgi_test and the trial database. Decisions by Yoann (DECISIONS.md): stale at 24 hours for Stripe and 72 hours for the rail, order recover then settle then reconcile, one cron at 06:00 UTC; "run a reconciliation" goes to the MCP surface in B11. Not yet verified: the run from the deployed app and the cron (after this deploy).

B7 independent review (docs/reviews/b7-claims-and-approvals.md): FAIL on F-B7-01 (HIGH: a refund above $1,000 rejected by the approver could be re-issued by the maker as a new operation with no approval request, and nothing on that path checked maker-checker before Stripe; proven on corgi_test), F-B7-02 (MEDIUM: threshold per payment, so $1,200 out of a claim as two $600 lines with no approver, proven), F-B7-03 (MEDIUM: no cron, no Run now), nine LOW; rule 14: PASS with two LOW. Fixed at 65b06be: the maker-checker gate now sits inside issueRefundsAtStripe so every road to Stripe passes it and a refusal appends nothing; a rejection is an approval-stage failure, and a re-issue above the threshold raises a new approval request and returns to the queue; the claim threshold is cumulative per claim (decision 17); the rejection is worded on the policy page; a Stripe error while expiring a broker's sessions no longer fails the webhook. B10's cron and Run now close F-B7-03. Checks on main at 65b06be: typecheck, build, 276 tests (275 pass, 1 skipped), claims and approvals 63/63 (eight new proof lines), refund replay 27/27, reconciliation 29/29, kyb replay 23/23, payment replay 29/29, money guards on corgi_test 127 PASS lines under shared load (128 claimed by the delegate; rerun pending). Re-review by the B7 reviewer at 16:16Z: PASS for the B7 scope after the fixes, rule 14 stays PASS (docs/reviews/b7-claims-and-approvals.md section 12); F-B7-01 and F-B7-02 re-probed on corgi_test and, for the threshold, live on CLM-00212; F-B7-03 agreed closed by B10; one new LOW F-B7-13. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T15:39:30+00:00 | Production caught up (6e8805e): B10 and the maker-checker fixes live; daily job and reconciliation proven on the deployed app

Deploy incident, disclosed: the pushes carrying the B10 merge (28a361c) and the maker-checker fixes (65b06be) each ended with a docs commit, and the Vercel Ignored Build Step compares only the last commit with its parent, so both builds were canceled and production stayed on 4decb8c from 14:26Z to 15:38Z. Found by reading the deployment list, fixed by pushing a code commit on its own (6e8805e); rule from now on: push code alone, confirm the revision on /api/health, then push docs; Yoann to switch the build step to compare with the last successful deployment (VERCEL_GIT_PREVIOUS_SHA). The B7 reviewer's HTTP checks ran on 4decb8c, which carried all the B7 and rule-14 code it reviewed, so its findings stand.

Deployed evidence at 15:38Z: GET /api/jobs/daily answers 401 with no credential and 401 with a wrong bearer, 200 with CRON_SECRET (so the secret is set in the deployment); POST /api/jobs/reconcile answers 401 with no credential; the daily job ran recover (0 stuck), settle (0 due) and reconcile over the last 7 days: Stripe run 91473119 complete, matched 4, provider-only 8 (the probe PaymentIntents of the checks and of earlier sandbox probes, with no operation id, disclosed in README), amount mismatch 0, stale 0, 11 provider records against 4 ledger records, fees kept by Stripe 18376 cents noted as information; rail run complete, 1 matched (the reviewer's settled payout on CLM-00212), no break. Staff login lands on /ops; /ops/reconciliation renders the runs, the open breaks with their age and meaning, the resolved breaks, the thresholds as assumptions and the LOCAL SIMULATOR label. The cron itself (06:00 UTC) is declared in vercel.json and visible only in the Vercel dashboard; not yet observed firing.

B7 reviewer, trial-database evidence (read-only plus one synthetic claim): all 16 protected tables carry their triggers and app_runtime holds INSERT and SELECT only; claim CLM-00212 on CGP-01274 created through the deployed screens (reserve $5,000, one $1,200 payment approved by approver@example.com, sent and settled on the simulator, reserve left at $3,800 so the cancel-with-open-claim preview stays demonstrable); maker-checker refused the initiator, a broker, a customer and an anonymous caller over HTTP; a changed destination after approval broke the intent hash; the screen's SHA-256 recomputed independently matched; both job endpoints 401 without the secret and 200 with it; a throwaway database migrated 0001 to 0010 in order matched the trial catalogue. Money guards on corgi_test: the solo rerun hit deadlocks on the TRUNCATE probes while the B4 builder and the B10 reviewer used the same database; a quiet rerun is pending.

## 2026-09-08T16:13:06+00:00 | B4 merged (dfdca38) and deployed: endorsements, customer approval, delta collected or refunded, segments, as-of PDFs

Delegate build (docs/handoffs/b4-implementation-notes.md; first builder cut by the session limit at 15:58Z, resumed on opus in the same worktree): migration 0009 (endorsement events, endorsement_collections, unique indexes for applied once and approved once), lib/money/endorsement.ts (the one pure function: price, approval threshold, quote hash, formula lines), lib/policy/endorse.ts (request under a lock, stale hash refused, reduction applied in the same transaction with its refund operation), lib/payments/endorsement-collection.ts (hosted page for a positive delta under endorsement-checkout:<event>[:attempt], four entries and the applied event on payment_intent.succeeded, rule-14 parking when the endorsement cannot be applied), lib/ledger/endorsement-entries.ts, the fold with written premium segments so an endorsed policy can be cancelled (decision 18), customer approval page and /customer list, preview page with formula lines, schedule and explanation on the policy page, declarations and endorsement schedule PDFs as of any date; a negative delta above $1,000 goes through B7's approval queue. Checks on main after the merge: typecheck, build (42 routes), 313 tests (312 pass, 1 skipped), endorsement replay 69/69, payment replay 29/29 (one run failed to start under shared load, rerun clean), refund replay 27/27, kyb replay 23/23, claims and approvals 63/63, reconciliation 29/29, money guards 136/136 PASS at 16:22Z on an ephemeral database created on the same Neon server, migrated 0001 to 0011 in order with the runtime role, then dropped (the shared corgi_test runs kept hitting TRUNCATE-probe deadlocks while three agents used it; those were contention, not guard defects). Migration 0009 applied to corgi_test and to the trial database before the deploy (it alters no CHECK constraint, closing F-B7-12). Deployed: /api/health reports dfdca38; policy CGP-01274 renders the documents block; GET /api/policies/<id>/documents/declarations?asOf=2026-10-01 and .../endorsement-schedule answer application/pdf. Decisions: 18 (segment by segment). Open items tracked: F-B4-01 (refund of parked money, B13), F-B4-02 (suspense account missing from the clearing balances list, B13). Not yet done: a live endorsement and a live delta refund from the deployed app with Yoann's card (planned in the walkthrough session), independent review (launched). Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T16:53:59+00:00 | B9 merged (44188bd) and deployed: broker monthly statement tied to the ledger, with cutoff and revisions

Delegate build (docs/handoffs/b9-implementation-notes.md): migration 0012 (statement_runs and statement_lines, append-only, guarded), lib/statements/compute.ts (lines and totals from journal lines only, net due = the commission_payable movement of the broker in the month, reversals with their sign, parked-then-applied cash counted, canonical text hashed), lib/statements/run.ts (read up to the knowledge cutoff with the runtime role, one transaction, revision N+1 with supersedes_run_id, identical re-runs stored and flagged, changes against the previous revision computed on read), screens /ops/statements and /broker/statements plus the shared detail page with a live "ties to the ledger" line, PDF with revision, cutoff and hash, scripts/check-statements.ts. Merge conflict in scripts/check-money-guards.ts (B4 and B9 both added probes) resolved by keeping both. Checks on main: 328 tests (327 pass, 1 skipped), build, statements 29/29 (the delegate counted 30, asked), endorsement 69/69, payment 29/29, refund 27/27, kyb 23/23, claims 63/63, reconciliation 29/29; money guards 155/155 on an ephemeral database migrated 0001 to 0012 (16:44Z). Migration 0012 applied to corgi_test and the trial database before the deploy; /api/health reports 44188bd. Decisions by Yoann at 16:47Z (decision 19): month by effective date, two money columns, provisional month; the delegate is adding the column and the label on its branch. Independent review launched. Not yet verified: the deployed screens and PDF, a month containing an endorsement, a parked-then-applied payment end to end. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T16:57:31+00:00 | B4 review FAIL (3 MEDIUM) and B10 review FAIL (1 HIGH): fixes dispatched to the original builders

B4 (docs/reviews/b4-endorsements.md, at dfdca38): every recited figure reproduced from independent arithmetic, 312 tests, endorsement replay 69/69, HTTP refusals and both PDFs verified on production, F-B7-12 closed (0009 additive, throwaway database migrated 0001 to 0011). FAIL on F-B4-04 (threshold per endorsement, repeated sub-$1,000 reductions refund without an approver), F-B4-05 (a delta paid after cancellation or void is refused and journaled nowhere; cancellation does not expire open pages), F-B4-06 (no endorsement on the deployed app and no user for the customer of CGP-01274). Six LOW. Actions: cumulative per-policy rule for refunds and the rule-14 parking of a late delta dispatched to the B4 builder; customer2@example.com attached to CGP-01274's customer on the trial database and listed in README; the live endorsement with Yoann's card is planned in the walkthrough session.

B10 (docs/reviews/b10-reconciliation.md, at dfdca38 on production): typecheck, 276 tests, reconciliation 29/29 solo, guards settled at 128 for the B10 revision, gitleaks clean, HTTP matrix on production including one staff Run now (Stripe 14 provider records against 4 ledger: 4 matched, 11 provider-only probes; rail 1 against 2, no break; CRON_SECRET proven configured). FAIL on F-B10-01 (a break outside the compared window leaves the open list and shows as resolved; reproduced, 14 open rail breaks became 0 under a narrower window; the 7-day cron default self-resolves anything older than a week), F-B10-02 (break key includes the classification), F-B10-03 (clearing balances list not built, same as F-B4-02). Six LOW. Actions: resolution only by a covering later run, daily window widened to the oldest open break, key without classification, account-level clearing balances list, dispatched to the B10 builder. Both re-reviews follow the merges. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T16:59:47+00:00 | LOW findings delegate merged (b956950 plus the uuid guard follow-up): ten findings closed

Delegate build (docs/handoffs/low-findings-notes.md, one commit per finding): F-B7-08 pair check between the claim and the operation in the URL; F-B7-10 settleClaimPayment checks its actor (operator or the scheduled job); F-B7-06 a loss dated in the future or before the cover is refused in the pure coverage rule; F-B7-05 claim payouts recovered by the stuck-operations job; F-B7-07 lib/http/path-ids.ts, a malformed id answers 400 on a route and 404 on a page (the coordinator added the same one-line guard to the nine endorsement, document and statement routes and pages created after the delegate's branch point); F-B2-19 partial unique index on the succeeded status per operation (migration 0013) with on conflict do nothing; F-B2-18 replay guard aligned on policyWasVoided so a re-booked policy's replay reads as already posted; F-B3-05 advisory lock per broker and refusal while a submission is in flight; F-B3-04 settling window measured to now; F-B7-09 foreign key journal_entries.claim_id (0013). Delegate checks: 283 tests, claims and approvals 71/71, payment replay 32/32, kyb replay 26/26, refund replay 27/27, reconciliation 29/29; guards 127 PASS plus one contention deadlock. Migration 0013 applied to corgi_test and the trial database before the deploy. Demo consequence of F-B7-06: CGP-01274's cover starts on 2026-09-17, so no new claim can be opened on it until then; the walkthrough session issues a policy effective today for the claim scenarios (CLM-00212 stays). Coordinator checks after the merge: recorded below when they finish.

## 2026-09-08T17:08:33+00:00 | B9 follow-ups merged (9dc1775, renumbered 51883fe): decision 19 implemented

The delegate implemented points 2 and 3 of decision 19: migration 0015_statement_commission_base.sql (renumbered by the coordinator from the delegate's 0013, which collided with 0013_low_findings.sql; the schema_migrations record on corgi_test renamed accordingly, 0015 applied to the trial database, which held zero published runs) adds cash_collected_cents on a run and commission_base_cents on a line, premium_collected_cents now holding the premium alone, read as the unearned_premium movement of the same money operation as the cash entry under the same cutoff, never a rate applied backwards; the canonical text moves to v2 (no published run existed); a month still running at the cutoff is labeled provisional on the lists, the statement page and the PDF, derived from the run's own dates so it stays provisional forever. Also fixed: B4's endorsement_premium_collected and endorsement_commission_earned entries now read as collected premium rather than adjustments. Delegate checks: 333 tests, statements 34/34. The earlier 29 versus 30 count: the summary line "ALL CHECKS PASSED" was counted as a PASS line by the delegate. Coordinator checks on the merged tree: 340 tests (339 pass, 1 skipped), build; the replay sequence after the LOW merge: claims 71/71, payment 32/32, kyb 26/26, refund 27/27, endorsement 69/69, statements and reconciliation pending at the time of writing.

## 2026-09-08T17:15:29+00:00 | B9 review: FAIL at 44188bd (F-B9-01, fixed by the follow-ups) and FAIL at afeba97 (F-B9-09, a format change over existing rows)

docs/reviews/b9-statements.md. Verified good: ties to the ledger to the cent everywhere, including on the deployed app against the trial ledger (Redwood, 2026-09: premium 594817, commission 86441, clawback 47506, net due 38935, matching the reviewer's independent read of the commission_payable movement); a voided binding nets to zero with all four lines visible; a re-run with the same cutoff reproduced the hash as revision 2 flagged identical; a correction after the cutoff is invisible to that cutoff and a fresh run is revision N+1 with the change list; append-only proven; authorisation over HTTP with five identities; month boundary correct (reversals keep the original effective date; clawbacks are dated on the day the cash left Stripe). F-B9-01 (HIGH at 44188bd): endorsement cash dropped from the statement, fixed by the follow-ups at 51883fe and re-verified by the reviewer's probe (cash 169904, premium 163561, commission 24534). F-B9-09 (HIGH at afeba97): migration 0015 changed the meaning of premium_collected_cents while three runs already existed on the trial database, created by the reviewer at 17:03Z after the delegate's zero-runs check: 2026-09 revision 1 and 2 and 2027-09 revision 1 for Redwood, 20 lines, all appends. No money figure is wrong (net due, lines and hashes untouched) but those rows would render "Cash collected $0.00". Fix dispatched to the delegate: a format version column, old rows rendered with their v1 meaning, fresh revisions of those months run on the deployed app after the deploy; the three v1 runs stay and are disclosed here. Also dispatched: F-B9-02 (footer sentence), F-B9-03 (handoff note month), F-B9-04 (form validation). The money guards must be re-proven on an ephemeral database after 0015 and 0016 (columns added to guarded tables): done at the next merge. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T17:26:09+00:00 | B9 format-version fix merged (8b5b490): F-B9-09 closed, plus F-B9-02, 03, 04, 06, 07, 10

Migration 0016_statement_format_version.sql adds canonical_version (old rows default to 1, which is what they are; the migration carries the read-only query to run before applying it anywhere holding runs, its answer for the trial database, three runs all v1-shaped, and the rule: nullable, never NOT NULL DEFAULT). One function, collectedFigures, is the only place that knows what an old column meant: a v1 run renders as the cash it holds with premium "not stored" and a visible note, the PDF branches the same way, and revisions of different formats read "format changed, not comparable by hash". Proved on the disposable database with a hand-inserted v1 row. Also: the page and PDF now name the one case that does not reproduce (entryRecordedAt is in the hashed text and deterministic); the handoff note's parked-cash month corrected; the run form's broker id validated. Checks on main: typecheck, build, 341 tests (340 pass, 1 skipped), statements 38/38. Migration 0016 applied to corgi_test and the trial database before the deploy. Deployed at 17:27Z (/api/health 8b5b490). Fresh revisions run on the deployed app as staff_ops at 17:26Z: Redwood 2026-09 revision 3 (premium collected $5,762.75, cash collected $5,948.17, commission $864.41, clawback -$475.06, net due $389.35, listed as "format changed, not comparable by hash" against revision 2, provisional because the month is running) and 2027-09 revision 2 (zeros, format changed); the three v1 runs (2026-09 revisions 1 and 2, 2027-09 revision 1, created by the B9 reviewer at 17:03Z to 17:05Z) stay and render "format v1: premium not stored". Re-review requested. Money guards re-proven at 17:33Z on an ephemeral database migrated 0001 to 0016 (15 files) with the runtime role: 155/155 PASS, database dropped afterwards.

## 2026-09-08T17:30:32+00:00 | B10 fixes merged (f9fc17f, renumbered 010a6fe): F-B10-01 closed, clearing balances built

The B10 builder fixed the review findings on its branch: migration 0017_reconciliation_item_record_date.sql (renumbered by the coordinator from the builder's 0016, which collided with B9's 0016; the schema_migrations record on corgi_test renamed, 0017 applied to the trial database) stores each item's record date; a break resolves only when a later complete run of the same source whose window contains that date stops reporting it, otherwise it stays open with its age; the daily job widens its window back to the oldest open break, capped at 31 days, and says when it could not reach one (F-B10-01). The break key is source plus reference, our operation id first (F-B10-02). The four clearing accounts (premium_receivable, refund_payable, claims_payable, unapplied_customer_cash) are listed on /ops/reconciliation per policy or claim, read from the journal with no window, with the oldest entry and its age (F-B10-03, closes F-B4-02). A storage failure stores a failed run and does not stop the next source; one banner per source; the run note counts the PaymentIntents not compared (F-B10-04 to 06). Left open on purpose: F-B10-07 (unbounded open-breaks read, so no row is ever dropped), F-B10-08, F-B10-09. Builder checks: 344 tests, reconciliation 39/39, claims 63/63, guards 155/155. Expect a longer board: breaks that used to age out now stay. Coordinator checks after the merge and the deploy: recorded below.

## 2026-09-08T17:38:16+00:00 | B4 fixes merged (dcad358, 26dede3) and B8 merged (f8c904f, e7b5913): corrections by reversal and re-book

B4 fixes (docs/reviews/b4-endorsements.md findings): refundNeedsApproval in lib/approvals/threshold.ts asks whether the refund alone is above $1,000 or takes the policy's refunds already sent plus on their way above it, read at request time by the endorsement and cancellation plans, at send time in assertRefundMaySend excluding the operation being sent, and on re-issue (F-B4-04); expireOpenCheckoutSessionsOfPolicy after a cancellation commits, issuance and endorsement pages alike, failures returned not thrown (F-B4-05); a delta arriving for a cancelled or voided policy, or whose request cannot be read, is parked with the rule-14 entry (F-B4-05); the endorsement standing derives approvalRequired from the events (F-B4-08); the $500 customer threshold counts the additional premium still waiting for that customer (F-B4-09). Builder checks: 335 tests, endorsement replay 80/80, refund 27/27, claims 63/63. F-B4-07, 10, 11, 12 stay open (B13).

B8 (docs/handoffs/b8-implementation-notes.md): lib/money/correction.ts (one pure function: re-prices the endorsement with only the date changed, signed difference, commission on it, both approval questions), lib/ledger/correction-entries.ts, lib/policy/correct-endorsement-date.ts (preview, then one transaction: correction_reversal superseding the wrong endorsed event, reversal entries at the originals' effective date, correction_rebook with fresh premium and tax at the corrected date, then the settlement of the difference through the B4, B5 and B7 machinery: Stripe refund with the approval queue above $1,000, or a hosted Checkout with customer approval above $500; the cash entries are never touched, the difference lands in premium_receivable), lib/payments/correction-collection.ts, lib/policy/correction-read.ts, migration 0014_corrections.sql (correction_collections, guarded), five screens (preview at /policies/{id}/corrections/new, explanation, timeline with effective and recorded dates side by side and superseded events named, as-of view) and three routes, scripts/check-correction-replay.ts 47/47. Deviations: the correction supersedes the wrong endorsed event only (the request is named in the payload); only the most recent endorsement in force can be corrected; a correction is refused while an earlier difference is unsettled; only an endorsement that collected premium can be corrected (one that refunded is refused with a sentence, disclosed); collecting a difference has no broker eligibility gate (it settles a receivable, binds nothing). Builder checks: 328 tests, correction replay 47/47, every other replay unchanged, guards 141 plus contention. Merge conflict in scripts/check-money-guards.ts resolved by keeping both sides. Migration 0014 applied to corgi_test and the trial database before the deploy. Decision points for Yoann: the commission on a correction rounds per piece (same rule as decision 18); corrections limited to endorsements that collected premium. Coordinator checks on the merged tree: typecheck, 370 tests (369 pass, 1 skipped), build, correction replay 47/47, endorsement replay 80/80, reconciliation 39/39, claims and approvals 71/71, statements 38/38, payment replay 32/32, refund replay 27/27, kyb replay 26/26 (all at 17:50Z); money guards 161/161 at 17:45Z on an ephemeral database migrated 0001 to 0017 (17 files) with the runtime role, then dropped.

## 2026-09-08T18:05:00+00:00 | B10 re-review PASS; two follow-up findings fixed (b1a0bce)

The B10 reviewer re-ran its own probes at e7b5913: 32 rail breaks stay open instead of resolving by silence, the scheduled window widens to the oldest open break, caps at 31 days and reports when it cannot reach one, the clearing balances list reads the journal with no window and includes the suspense account (F-B4-02 closed); 60 reconciliation unit tests, check 39/39; deployed run as staff_ops: Stripe 23 provider records against 4 ledger, 20 breaks (sandbox probes), rail 1 against 2, none. Verdict PASS on the six findings. Two new: F-B10-11 (MEDIUM, items written under the old key shape showed a pre-existing break twice: open with a fresh age and resolved under the old key) fixed in the read by excluding from the resolved list any reference still open, no stored row touched; F-B10-12 (LOW, stale 0016 references) corrected. Coordinator checks: 370 tests, reconciliation 39/39. Deploying.

## 2026-09-08T18:07:00+00:00 | B4 re-review PASS

The B4 reviewer re-read the fixes at e7b5913 (docs/reviews/b4-endorsements.md section 11): refundNeedsApproval decisive at send time (a refund written under the threshold cannot leave once another reduction has gone out; failed refunds count for nothing; re-issue uses the same rule); cancellation closes every open hosted page and a delta paid anyway is parked; customer2@example.com signs in on production and sees CGP-01274; approvalRequired recomputed from the events; the $500 threshold cumulative per policy with the deployed preview saying so. 370 tests, endorsement replay 80/80 including the eight new lines, HTTP previews recomputed independently. PASS for the B4 scope. One new LOW passed to the B8 reviewer (F-B4-13, the correction plan's per-operation approval questions). Open LOW unchanged: F-B4-07, 10, 11 (the only remaining case where a delta payment is journaled nowhere: an amount mismatch), 12. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T18:11:14+00:00 | Interface branch merged (codex/corgi-interface 66fb7fe, merge 856e75c): portal-style dressing

Yoann's separate Codex session delivered the interface dressing (docs/handoffs/ui-01.md to ui-04.md and ui-main-merge.md, its own reviews docs/reviews/ui-01.md to ui-04.md): PortalShell and PortalFrame components with breadcrumbs and parent navigation, WorkspaceOverview for the staff home, an ops policies list, error, loading and not-found pages, DM Sans and Inter fonts bundled with their OFL licences, three illustrations generated on Yoann's own machine (webp, no real person), lucide-react icons (the one new dependency). Verified before the merge: no change under lib, db, scripts, app/api or the Vercel configuration; STATUS, PLAN, COMPLIANCE-MATRIX and FINDINGS untouched; the Codex session merged main first (base e110a7c) and kept the business logic, the forms, the access checks and the uuid guards; DECISIONS.md carries six entries written by that session about the UI choices Yoann made (reference style, fonts, navigation, sandbox presentation). Coordinator checks after the merge: typecheck, 370 tests (369 pass, 1 skipped), build. Deploying; an independent UI review (business logic unchanged, access checks, no money computed in the browser, licences, AF-06 readability of the components) and the PDF visual pass (delegate) follow. AF-06 for the dressing: NOT confirmed until Yoann explains the path (server page, PortalShell, PortalFrame, CSS) back. Deployed at 18:12Z (856e75c): every route answered 200 for each role, but the root loading boundary added by ui-04 turned every anonymous or malformed request into a streamed 200 shell (redirect and not-found only inside the payload, no data leaked); the coordinator removed app/loading.tsx and redeployed (F-UI-00).

## 2026-09-08T18:29:20+00:00 | B8 review FAIL (1 HIGH, 2 MEDIUM, 4 LOW): fixes dispatched to the builder

docs/reviews/b8-corrections.md at e7b5913. Sound: the correction transaction (47/47 replay, 36 original rows compared byte for byte before and after, two mirrored reversal entries at the originals' effective date, the re-book at the corrected date, no cash entry touched, the difference in premium_receivable, refusals for a second correction, a cancelled policy, a date outside the term, a broker actor, a short reason), the worked example reproduced from scratch (38630 and 907 as booked, 43561 and 1023 corrected, 5047 to collect, 739 of commission), gitleaks on 63 commits clean, screens and refusals over HTTP on production. FAIL on F-B8-01 (HIGH: the statement drops the cash of a collected correction difference and files its commission as an adjustment without a base, the F-B9-01 class again on live-fire 2), F-B8-02 (MEDIUM: maker-checker per correction and a refused send outcome discarded), F-B8-03 (MEDIUM: the five correction entry points answer 500 on a malformed id). Four LOW. Fixes dispatched to the B8 builder on top of main; re-review after the merge. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T18:30:45+00:00 | Live walkthrough with Yoann, scenario 4: cancel a policy with an open claim, on the deployed app

Yoann drove, the coordinator verified in the trial database and at Stripe. Preview as ops@example.com on CGP-01274 (term 2026-09-17 to 2027-09-17, $2,312.00 annual, open claim CLM-00212 with a $3,800 reserve and $1,200 paid), effective 2026-10-31, pro rata: earned 27870 (44 of 365 days), unearned 203330, tax back 4779, fee 0, total 208109 ($2,081.09), clawback 30499 ($304.99), the open claim and its reserve untouched, every line equal to the coordinator's hand calculation. Confirmed at 18:22:10Z: cancelled event effective 2026-10-31, refund_requested and premium_earned_to_date entries, refund operation ef73b3e2 at 208109 cents queued with approval request 70faaf94. Approved as approver@example.com at 18:25:28Z (intent hash 04cf55bd8856a905045bfc86db859655f8e26ee0e28e3a3f1bc35b5f06faf81b). Sent as ops@example.com: Stripe refund re_3UDN8aK6R3v50tIy0J6CmRy3 created 18:27:57Z, succeeded, 208109 cents, test mode; the deployed app received refund.created (evt_3UDN8aK6R3v50tIy0vhuzzrF), refund.updated (evt_3UDN8aK6R3v50tIy0i0dxINg) and charge.refunded (evt_3UDN8aK6R3v50tIy0LSsG2XU) at 18:27:59Z; the operation reads requested, provider_accepted, succeeded; refund_completed and commission_clawback posted at 18:27:59Z, effective 2026-09-08 (the day the cash left Stripe). Closes F-B5-04. Not done by Yoann: the refusal of the initiator on the approvals queue (the coordinator's instruction came in the wrong order; to be done on the next refund). Yoann's interface findings recorded as F-UI-01 to F-UI-04. Walkthrough status for this scenario: DONE BY YOANN; his explanation of why the reserve stays: pending.

## 2026-09-08T18:32:10+00:00 | PDF visual pass merged (db52e8a, merge c9064d8) and deployed

Delegate work on lib/documents/render.tsx, lib/documents/pdf-theme.ts and lib/statements/pdf.tsx only (docs/handoffs/b12-pdf-notes.md): issuer block "Demo Insurer, work-trial build" with a bordered TEST DATA, SANDBOX label fixed on every page, title band with the portal accent, label and value fact grid, right-aligned figures, section rules, footer on every page with the as-of or cutoff sentence, the generation timestamp and "Page X of Y"; portal tokens only (ink, muted, hairline, panel, accent); US Letter; the statement keeps revision, cutoff, hash and format-v1 note verbatim; F-B4-PDF (one address line) and F-B9-08 (column overprint) closed; two page-break defects found by looking at the pages fixed (rows never split, totals never stranded). No font bundled: fontkit cannot subset the portal's WOFF2 files and an embedded TrueType made react-pdf write glyph indexes that the text extraction used by the tests reads back as garbage; Helvetica's digits are tabular already; zero network calls. Checks: typecheck, 370 tests, build, samples rendered to PNG and inspected, determinism proved with cmp on double renders. One judgement call to confirm: the statement PDF's author metadata now names the demo issuer instead of "Corgi". Deployed at 18:31Z (c9064d8); both policy PDFs answer application/pdf on production.

## 2026-09-08T18:37:52+00:00 | Interface merge review: PASS at 856e75c, one MEDIUM for Yoann

docs/reviews/ui-main-merge.md: business logic untouched (git diff over lib, db, scripts, app/api and the configuration empty; 154 form attributes compared on both revisions, three benign differences: businessUrl gains type=url matching the server rule, the logout form moves into PortalShell with the same action, two non-submitting buttons); every guard line and role check in place and executed before rendering; only two client components (error.tsx, portal-frame.tsx), neither touching money or fetching; LOCAL SIMULATOR and the KYB disclosure verified in the deployed HTML; fonts local under OFL 1.1, no external font or script; illustrations without person or company; gitleaks clean; 22 authenticated pages across four roles at 200, 15 refusals correct, 21 route and role combinations at 375 px without overflow. F-UI-01 (the root loading boundary) reproduced independently and the coordinator's fix confirmed. F-UI-02 MEDIUM open for Yoann: the public home and login lost every visible sandbox sentence; one sentence on each page fixes it; the 16:43Z DECISIONS entry written by the Codex session bundles Yoann's banner removal with that collapsed control and carries no attribution, to be confirmed by Yoann. Yoann's own walkthrough findings renumbered F-YA-01 to 04 to keep the reviewer's numbering. Walkthrough: NOT REVIEWED WITH YOANN for the dressing.

Next: Yoann's confirmation of the change-request shape (YOA-634) then its builder; B12-2 explain-this-amount and B12-3 as-of steps on the rebuilt pages (display decisions to Yoann); B13 LOW batch; walkthrough sessions; B14 freeze package. Live session: step 6 endorsement on CGP-01707.

## 2026-09-08T18:52:54+00:00 | Post-compaction startup receipt, B12 order decided, Linear backlog refreshed

Startup receipt after context compaction: reread in full CLAUDE.md, AUTOMATIC-FAILS.md, AGENTS.md, WORKFLOW-48H.md and REVIEWER.md; read the STATUS and DECISIONS tails, the findings register, git log and the worktree list. Main dba9c43 (origin identical), production c9064d8 (interface and PDF pass). The working tree carried the UI reviewer's own additions to docs/reviews/ui-main-merge.md (F-UI-11 measured on production: anonymous /login shows the two-link shell, never the staff destinations; the fifteen refusal cases re-run on c9064d8), committed here and F-UI-11 registered. Absent files: none. AF gate unchanged since 18:37Z: AF-01 PASS at c9064d8 from outside, AF-02 README inventory and labels consistent, AF-03 guards proven on ephemeral databases (161/161 at the last merge), AF-04 sandbox keys only, AF-05 gitleaks at every commit, AF-06 walkthroughs partly done (B13-4).

Decision recorded (DECISIONS 2026-09-08T18:52:54+00:00): the three B12 items are mandatory in the order MCP agent at the debrief, "explain this amount", "as it stood on" slider. Linear refreshed at 18:47Z: YOA-621 In Progress with YOA-624, 625, 626 as sub-issues; YOA-627 UI polish (In Progress), YOA-628 LOW register, YOA-629 rule 14 refund leg, YOA-630 walkthrough sessions (In Progress), YOA-631 Yoann's own actions (T+24h email, Vercel Ignored Build Step, cron check, Codex entry attribution), YOA-632 demo data notes for the panel; YOA-620 commented.

Running delegates, three: builder-b11 (MCP, committing its notes), builder-b8 (review fixes, typechecking the replay additions), builder-ui-polish (F-YA-01 to 04, F-UI-11, money input mask). All three told at 18:50Z to merge origin/main first, run checks once, skip the guards and report precisely. Next acceptance criterion: B11 merged, deployed and reviewed so that B12-1 runs on production. Planned checks at merge: migration renumbered to 0018, typecheck, tests, build, the builder's MCP check, guards on an ephemeral database, the deployed MCP endpoint called from outside with a staff key (tools/list, one read, the write tool landing in /ops/approvals).

## 2026-09-08T19:05:52+00:00 | B11 MCP surface merged (e7d7856) and deployed; live step 5 by Yoann; F-B2-20 found and fixed (0fa828d)

Delegate build (docs/handoffs/b11-implementation-notes.md, transcript b11-mcp-session.md): POST /api/mcp, streamable HTTP JSON-RPC 2.0 hand-written in lib/mcp/jsonrpc.ts (no new dependency, JSON answers, no session), bearer per-user API key (sha256 stored, secret shown once, public prefix), tools get_policy_as_of, get_broker_statement, list_reconciliation_breaks, run_reconciliation, request_claim_payment (queue only, flagged "raised by an agent" on /ops/approvals), migration 0018 (mcp_api_keys, mcp_key_revocations, mcp_calls, append-only with the usual guards, users.role widened with agent, an agent key refused for a staff_approver by trigger), /ops/mcp-keys, scripts/create-mcp-key.ts, the never-delegated list in code, in tools/list, in initialize and on the screen. Coordinator checks on main after the merge: typecheck, 404 tests (403 pass, 1 skipped), build; the builder's check:mcp 52/52 on corgi_test; money guards on an ephemeral database migrated 0001 to 0018: running at the time of writing, result in the next entry. Migration 0018 applied to the trial database at 18:57Z before the deploy; /api/health reported e7d7856 at 18:59:47Z. Demo key created on the trial database at 19:01Z for ops@example.com, kind agent, prefix cmk_e96f88a4, secret in a git-ignored file only. Deployed endpoint called from outside: no key answers 401 with no detail; initialize answers the server info and 528 characters of instructions; tools/list returns the five tools and the never-delegated policy; list_reconciliation_breaks answers 20 open breaks, the first one the $100.00 sandbox probe. Not yet exercised on production: the write tool (kept for the B12-1 rehearsal), a real MCP client. Independent review: pending Yoann's go for the delegate. README, ARCHITECTURE section 6, PLAN and COMPLIANCE-MATRIX (MCP-01) updated. Builder decisions left to Yoann: run_reconciliation stays agent-callable (his 16:30Z wish) without rate limiting, a key borrows its user's whole visibility, which demo keys exist.

Live step 5 by Yoann at 18:56Z to 18:57Z on the deployed app: policy CGP-01707 (Bay Area Fabrication LLC, customer@example.com, CA, term 2026-09-08 to 2027-09-08, $1,200.00) paid with the test card, bound at 18:57:00Z on payment_intent.succeeded. Verified in the trial database: premium_written 120000, tax_and_fee_billed 5320 (tax 2820, fee 2500), premium_collected 125320, commission_earned 18000, every line equal to the recited example. Found on his page: "last status: provider_accepted" on a bound policy. Cause: checkout.session.completed arrived after payment_intent.succeeded in the same second and appended provider_accepted after succeeded (F-B2-20, MEDIUM, display only; ledger and status right). Fixed at 0fa828d: the session-completed step is appended only while the operation has no final status; payment replay 34/34 with two new lines. Pushed, deploy verification pending.

## 2026-09-08T19:17:40+00:00 | Guards 184/184 on 0018; F-B2-20 deployed; B8 fixes merged (2755d11) and deployed; interface polish merged (c5bcf2a); four delegates running

Money guards proven at 19:11Z on an ephemeral database created on the test server, migrated 0001 to 0018 with the runtime role: 184/184 PASS (the three MCP tables included), database dropped afterwards (the drop had to wait for the check's own connections). F-B2-20 second half deployed: /api/health reported 5f2c841 at 19:07:01Z; the policy page now reads a succeeded payment as succeeded whatever step was appended later (lib/policy/read.ts), so CGP-01707 displays correctly without touching its rows.

B8 review fixes (builder, five commits, docs/handoffs notes in the git log): F-B8-01 HIGH closed by one disposition table in lib/statements/compute.ts that names what the statement does with every entry type, the cash list of journal.ts derived from it, the correction premium base read from the correction's two events, every builder exporting its entry types with satisfies and lib/statements/entry-types.test.ts failing by name on an unknown one; F-B8-02 MEDIUM and F-B4-13 closed (both gates cumulative per policy, verdict and totals written on the correction event and read back by both read paths, a refused send recorded and surfaced); F-B8-03 MEDIUM closed (400 and 404 on malformed ids, shared isUuid); F-B8-04, 05, 07 closed; F-B8-06 stays open in the B13-2 batch. Merged at 2755d11 without conflict; coordinator checks on main: typecheck, 414 tests (413 pass, 1 skipped), build, correction replay 55/55, statements 48/48; pushed alone; /api/health reported 2755d11 at 19:16:49Z. Re-reviewer launched on 2755d11 (append-only section in docs/reviews/b8-corrections.md).

Interface polish (builder, docs/handoffs/ui-polish-notes.md): per-role "what needs you" block and sidebar counts (F-YA-01 and 03) from six server-side count reads in lib/*/read.ts, a failed count shows no badge; two tables with the decision opened per row on /ops/approvals, actions grouped behind native details on the claim, policy, reconciliation and statements screens (F-YA-02); Stripe and rail identifiers behind a "Sandbox references" disclosure, raw values still in the DOM for the evidence (F-YA-03); signed-out frame without sidebar on /login (F-YA-04, F-UI-11 login part); refund banner wording from the refund state (F-YA-04 wording); money inputs with thousands separators formatted in the browser only, the server parser unchanged (Yoann's request). Forms contract identical to main (155 method, action and name lines compared); role and ownership checks untouched; nothing under db, scripts, app/api or the Vercel configuration. Builder checks: typecheck, 403 tests, build, 25 renders at 200 across the five identities on the disposable database. Merged at c5bcf2a (page.tsx auto-merged with the B8 changes); coordinator checks: typecheck, 414 tests, build; pushed alone, deploy in progress. Noted by the builder: /ops/brokers and /ops/policies are pre-existing N+1 reads (120 s and 149 s on the check database from a laptop), to be measured on production by the reviewer; / keeps the workspace shell; F-UI-02 (one visible sandbox sentence on / and /login) still waits for Yoann's answer.

Delegates running: reviewer-b11 (docs/reviews/b11-mcp.md), builder-b12-1 (demo path, one production rehearsal with the demo key, agent-raised request rejected on purpose afterwards), rereviewer-b8, reviewer-ui-polish. Yoann's rule refined at 19:10Z: reviewers and builders launch without his go; the decisions a brief embeds are put to him before launch.

## 2026-09-08T19:46:36+00:00 | Three verdicts (B8 re-review PASS, interface polish PASS, B11 PASS); policy page rebuilt as a product screen (b9636c2) and deployed

B8 re-review (docs/reviews/b8-corrections.md sections 11 to 16, at 2755d11): PASS. F-B8-01, 02, 03, 04, 05, 07 and F-B4-13 resolved on check lines (statements 48/48, correction replay 55/55, 36 pre-existing rows byte-identical) and on production (the five correction entry points answer 400/400/400/404/404, no correction created). Open: F-B8-06 (by decision) plus two new LOW, F-B8-08 (statement premium base of a split correction refund) and F-B8-09 (document date fields, fixed below). Interface polish review (docs/reviews/ui-polish.md, c5bcf2a plus 41be7fc): PASS; business logic untouched (five count reads, zero writes), 168 form entries identical apart from the money fields, guards identical, counts equal to the reviewer's own SQL (20 open breaks on the trial database), 27 pages and 17 refusals measured, /ops/brokers and /ops/policies at 0.25 s on production (the builder's 120 s was the 671-broker check database). F-UI-12 MEDIUM: the mask stripped what the server refuses ("1 200,50" became 120,050); fixed below. B11 review (docs/reviews/b11-mcp.md, at 5f2c841): PASS; tenant refusals and bypass attempts measured on production with six keys created and revoked, the write tool proven to move no money on both sides of the threshold, four doors against agent approval, guards 184/184 on corgi_test without contention. F-B11-01 MEDIUM: an agent-raised payment at or below $1,000 creates no approval request and the claim screen does not say a machine asked; a money rule put to Yoann at 19:46Z. Six LOW in the register.

Policy page rebuilt by the coordinator at Yoann's request (19:36Z, "revoir entièrement le layout", with his screenshot, F-YA-06): components/detail-layout.tsx (DetailHeading, DetailGrid, Panel, Facts, AsideList, Chip, Empty) and its CSS; the page is now an identity band with status chips and the actions as buttons (Pay, Bind, Endorse, Cancel the policy, Open a claim), then two columns: terms in force as a facts grid, endorsement in progress, endorsement schedule with the explanations and the correction form folded per row, cancellation, refunds, claims, corrections, as it stood on a date, timeline and journal as titled panels on the left; ledger sums (cash in, cash out, commission payable, unearned premium, as plain sums of the journal lines), broker and KYB, documents and the folded "how to read this page" on the right. The endorse, cancel and open-a-claim forms moved to /policies/[id]/endorse, /cancel and /claims/new with the same field names; the two preview pages render the form when opened without parameters; every server check unchanged. Money mask: only "$", spaces and its own commas are removed, anything else reaches the server untouched (F-UI-12), leading zeros dropped from the echo (F-UI-19). Checks: typecheck, build, 414 tests (413 pass, 1 skipped). Pushed alone; /api/health reported b9636c2 at 19:45:26Z. Next screens in the same pattern: policy lists (/broker, /ops/policies), claim page (F-UI-20), approvals, reconciliation, statements. Independent review of the rebuild after the next screens.

## 2026-09-08T20:07:48+00:00 | Every screen rebuilt in the detail pattern (1b946de, 0a9821b, e341f83), deployed; F-B11-01 screen half done

Yoann's feedback on the rebuilt policy page (21:50 local: "vraiment pas mal par rapport à ce qu'on avait avant", colour on the actions, the ledger easier to read with a show-all, the same everywhere, business verification not right) and his screenshot of the journal (22:05, tags broken over three lines). Done by the coordinator: actions coloured (a change in orange, stopping cover in red on white, money in black; components and CSS); the journal as compact entry blocks (components/journal-table.tsx: one header line per entry with a tone tag, then account, debit and credit rows with fixed amount columns, newest first, the older ones behind a native show-all fold), used by the policy page, the claim page and the corrections section; the broker, staff policies, claims, brokers, customer and business verification screens, then the claim page, approvals, reconciliation, the two statements lists and the statement detail, all with the heading band, chips, panels, tables and folded explanations. The claim page shows the actions as open forms in the side column (F-UI-20) and marks a payment raised through the MCP endpoint with the key prefix on its payments table (F-B11-01, screen half; lib/claims/payments.ts reads requested_through from the payment_requested event). Forms, field names and server checks unchanged; no money computed in the browser. Checks: typecheck, build, 414 tests after each batch; claims and approvals check 71/71 after the payments view change. Deployed: 1b946de at 19:59Z, 0a9821b at 20:05Z, e341f83 at 20:07Z; the nine main screens answer 200 for the right identities from outside. Independent review of the rebuild launched on e341f83, measured on production. Still open for Yoann: the F-B11-01 money rule (rule 21), F-UI-02 (visible sandbox sentence on / and /login), the customer read-only policy page question.

## 2026-09-08T20:27:24+00:00 | Rebuild review FAIL at e341f83 (F-UI-22, the typed decimal comma), fixed at fea572c and deployed; re-review running; what-needs-you enriched; T+24h email rewritten

docs/reviews/ui-rebuild.md (reviewed e341f83 on production): business logic untouched (db, scripts, app/api, vercel.json empty; one read-only sub-select in lib/claims/payments.ts), 170 form contracts against 168 with exactly the three intended moves and the two F-B8-09 defaults, guards additions only (282 lines), 105 production requests across five identities and the three new form pages with every refusal right, the four "so far" figures recomputed from the 24 rendered journal lines, 22 entries and 48 lines audited with every entry balancing and the folds consistent, the agent marker measured on CLM-00212. FAIL on F-UI-22 (MEDIUM): the mask reformatted on every keystroke, so "1200,50" typed one character at a time still became 120,050, the F-UI-12 defect on the typed path. Fixed by the coordinator at fea572c: the field keeps what was typed until it loses focus, then groups the thousands only when the text is a plain amount with no comma; the echo accepts thousands commas only in their proper places. Also fixed: F-UI-24 (cancel date clamped inside the term), F-UI-25 (journal columns under 580 px). F-UI-23 accepted, F-UI-26 (the notices expression) to B13-2. Deployed at 20:26:55Z; re-review requested from the same reviewer (append-only section).

Earlier in the hour: the what-needs-you block now counts the broker's real work (policies to pay, endorsement deltas to pay, correction differences to collect, quotes waiting for the customer) and the customer's (endorsements and corrections to approve), with the readers the policy page uses (0e1dfb9, deployed 20:15Z; Yoann asked whether the block worked: it did, and for broker@ nothing is waiting). The T+24h email draft was rewritten short and plain at Yoann's request (docs/checkpoints/t-plus-24h-email.md, 20:22Z): the URL, four demo roles, the two live Stripe paths with today's identifiers (CGP-01707 collected, re_3UDN8aK6R3v50tIy0J6CmRy3 refunded), the Stripe Connect verification with the disclosure that Sumsub and Middesk were gated, the two simulators, the ledger sentence, one paragraph on the rest, the next 24 hours. Yoann sends it before 07:50 Europe/Zurich.

## 2026-09-08T20:32:34+00:00 | Rebuild re-review PASS at fea572c

docs/reviews/ui-rebuild.md, re-review section: F-UI-22 resolved at the cause (the mask never sees its own output); the eight named strings traced character by character; an exhaustive sweep of 69,904 strings of length 1 to 4 over the digits, point, comma, minus, dollar, space and "e" found zero cases where the mask changes the server's answer and zero echo/server disagreements; F-UI-24 resolved on production; F-UI-25 resolved in code, not verified visually (no browser tool); F-UI-23 and F-UI-26 open, LOW. No regression: three presentation files touched, 170 form contracts identical apart from the clamp, 26 guard measurements unchanged, the four ledger figures still recompute from the rendered journal. Noted for Yoann: separators now appear on blur and on server-rendered defaults, not while typing, which is the price of not letting a mask rewrite a half-typed amount. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T20:41:43+00:00 | T+24h email sent by Yoann (22:34 local); rule 21 coded and deployed (F-B11-01 closed); F-UI-02 closed; customer change requests specified (YOA-634)

Yoann sent the T+24h email on the candidate thread at 22:34 Europe/Zurich (20:34 UTC), the short version of docs/checkpoints/t-plus-24h-email.md (checkpoint file updated, YOA-615 Done, YOA-631 item 1 done). His answers at 20:35Z (DECISIONS): rule 21, a claim payment requested by an agent always waits for a human approver; a customer read-only policy page with change requests on selected lines, visible to the policy writer; one light sandbox sentence on / and /login. Coded by the coordinator: requestClaimPayment queues every agent-raised payment whatever the amount, sendClaimPayment refuses an agent-raised payment without a request (read from the immutable payment_requested event), the tool description says so, scripts/check-mcp.ts asks for $500 on a quiet claim with the agent key first and proves the request is queued and the operation stays 'requested' (53/53 on corgi_test behind a local server on port 3800 pointed at the test database); README money rules and ARCHITECTURE section 6 carry the rule. The sandbox sentence is on / and /login (F-UI-02). Checks: typecheck, build, 414 tests. Pushed alone; deploy verification in the next entry. Customer change requests: shape proposed to Yoann and written in YOA-634 (read-only policy page for the customer, a request naming the lines it is about with a comment, append-only, no money effect, answered by the broker with a reply, counted in the broker's what-needs-you); builder after his confirmation of the shape.

## 2026-09-08T20:43:43+00:00 | Housekeeping at Yoann's request: eighteen delegate worktrees removed

Every delegate worktree under .claude/worktrees was checked (branch merged into main, working tree clean) and removed with its branch: builders B4, B7, B8, B10, B11, B12-1, UI polish and the PDF pass, reviewers B1 to B11, the design review, the interface reviews, the rebuild review. No .env.local copy remains under .claude/worktrees (a B14 item done early). The two Codex worktrees are Yoann's and stay: .worktrees/corgi-interface (branch merged at 856e75c) and .worktrees/corgi-illustrations (one unmerged commit, an illustration library started on the Spark, not part of the submission). No delegate is running; the finished ones in the roster hold no process.

## 2026-09-09T06:17:03+00:00 | Day 2 opens: the scheduled reconciliation ran on its own at 06:00 UTC

Production at 4ddb33b, healthy. The Vercel cron called /api/jobs/daily at 06:00 UTC: two reconciliation runs stored at 06:00:12Z, stripe complete and claims_rail complete, run_by empty (the screen shows "scheduled job"). No approval waiting. Freeze in about 23 hours 30 minutes (September 10, 07:50 Europe/Zurich).

## 2026-09-09T06:24:14+00:00 | Three builders launched on Yoann's go

builder-b12-2-3 (YOA-625, YOA-626: explain-this-amount folds and as-of steps on the rebuilt pages), builder-change-requests (YOA-634: migration 0019 on corgi_test only, customer read-only view, request and reply routes, broker panel, what-needs-you count, a check script), builder-low-batch (YOA-628: F-B8-08, F-B11-02/03/05/06/07, F-B12-01, F-B7-13, F-B4-07/10/12, F-B10-08 sentence, F-B10-09, F-UI-03/13/16/17/18; items touching the other builders' files skipped and listed). File ownership split to avoid conflicts: the policy page panels to the first, its ownership block and a customer view file to the second, neither file to the third. Each merges origin/main before reporting; the coordinator merges, applies 0019 to the trial database, runs the checks and the guards on an ephemeral database, deploys, and launches the reviews.

## 2026-09-09T06:35:24+00:00 | Two more builders on Yoann's morning feedback: PDF quality (YOA-635), notification centre (YOA-636)

Agency bill kept (DECISIONS). builder-pdf-quality: declarations page, endorsement schedule and statement PDF as real documents (issuer, insured and producer blocks, coverage table, premium summary, signature block, watermark SPECIMEN TEST DATA, page X of Y), deterministic double render, PNG samples under docs/evidence/pdf/; only the three PDF files. builder-inbox: lib/inbox/read.ts and /inbox per role with one row per pending item and the link to the exact object, sidebar counts pointing at /inbox; what-needs-you.tsx untouched until the change-requests builder merges. Five delegates running.

## 2026-09-09T06:36:32+00:00 | Live step 6 by Yoann: endorsement above $500 on CGP-01707, approved by the customer, delta paid, in force

Yoann as broker@ requested the endorsement at 06:28:25Z (annual premium $1,200 to $2,400, limits doubled, effective 2026-10-08, $1,127.24 to settle: prorated premium 110136 = floor(120000 x 335 / 365), tax 2588, commission 16520, every figure equal to the coordinator's hand calculation of the evening), approved it as customer@ at 06:29:33Z from the customer's list (the orange button), paid the delta as broker@ with the test card; payment_intent.succeeded at 06:34:34.327Z, endorsed event at 06:34:34Z, four entries in the trial database: endorsement_premium_written 110136 and endorsement_tax_billed 2588 effective 2026-10-08, endorsement_premium_collected 112724 and endorsement_commission_earned 16520 effective 2026-09-09. The "so far" side list adds up (collected 238044, commission 34520, unearned 230136). Two findings: F-YA-07 (the "Terms in force" panel shows the October terms in September) and F-B2-21 (the late checkout.session.completed appended provider_accepted after succeeded on the delta collection: the F-B2-20 guard covered issuance only; fix in progress on the endorsement and correction writers). Walkthrough status: DONE BY YOANN; his explanation of "priced from the effective date" pending.

## 2026-09-09T06:49:47+00:00 | F-B2-21 fixed (369671d); B12-2 and B12-3 merged (a6aca56); customer change requests merged (b40e803); migration 0019 on the trial database; deploying

F-B2-21: every posting of a Stripe success (issuance, parking, endorsement delta, correction difference) now takes one advisory lock per money operation and the session-completed handler takes the same lock before its not-exists check, so the two events arriving within 60 ms cannot interleave any more; the endorsement and correction readers prefer the final status. Replay checks after the change: payment 34/34, endorsement 80/80, correction 55/55.

B12-2 and B12-3 (builder, docs/handoffs/b12-2-3-notes.md): lib/money/explain.ts and components/amount-explained.tsx render each figure with a fold showing the formula in integer cents, the days and the rate, the rounding rule and the proving journal entries; twenty-three folds (policy terms, endorsement delta, seven cancellation figures, four ledger sums, claim paid/reserve/incurred, five statement totals); three mechanisms against disagreement (stored formula lines reused, the same accountSumCents the figure used, a render-time comparison that prints an alert); 12 new unit tests. As-of steps: policyAsOfSteps offers the term start, every effective date of a non-superseded event and today as links, the current one marked. Customer change requests (builder, docs/handoffs/b13-6-notes.md): migration 0019 (two append-only tables with the 0018 guards, a CHECK on the seven line names, one reply per request), lib/policy/change-requests.ts, two routes, the customer read-only view (no journal, no sums, one action), the broker's panel and count, scripts/check-change-requests.ts 35/35. Coordinator checks on the merged tree: typecheck, 426 tests (425 pass, 1 skipped), build, change-requests 35/35; 0019 applied to the trial database at 06:47Z; money guards on an ephemeral database migrated to 0019: running. README: sections "Explain this amount" and "Customer change requests", two check lines. Pushed b40e803 alone; two reviewers launched (B12-2/3, change requests), each measuring on production once the deploy lands. Still building: the LOW batch, the PDF quality pass, the notification centre.

## 2026-09-09T06:53:30+00:00 | PDF quality pass merged (4dbeef9) and deployed at 06:53Z; B12-2/3 and change requests deployed at 06:49Z

PDF pass (builder, four presentation files, docs/evidence/pdf/ with a PNG of every page including the edge cases): the declarations page reads like a document (title band with the policy number, headline strip, insured and producing broker blocks, coverage table, boxed premium summary with a shaded total, endorsements in force, an empty signature block labelled specimen, a diagonal SPECIMEN TEST DATA watermark, page X of Y); the schedule and the statement in the same language, the statement's revision, cutoff, hash and callouts verbatim; Helvetica kept because the repository holds WOFF2 only and an embedded TrueType breaks the text-extraction tests; determinism proven with cmp; the coordinator looked at the declarations PNG. Checks on the merged tree: typecheck, 426 tests, build. Money guards on an ephemeral database migrated to 0019: 184/184 PASS at 06:55Z, database dropped afterwards (the two change-request tables are probed by scripts/check-change-requests.ts, nine refusal lines, not yet by the guards script). Deployed 4dbeef9 at 06:53:17Z. Presentation only: its review is folded into the final integration review.

## 2026-09-09T07:02:57+00:00 | Reviews of B12-2/3 and of the customer change requests: both PASS; two fix cycles running

docs/reviews/b12-explain.md at b40e803: PASS. All 23 folds end on their figure (classified: eight render-time comparisons including the five statement totals re-summed from the stored lines, eight identities from the events' stored figures, four calls of the same sum function, three echoes); 47 folds rendered on production with zero disagreement alerts; no client file added; forms, field names, role checks and uuid guards byte-identical; as-of steps right on every observable dimension, PDFs following the date; the reversed-entry case checked live on a statement. Two MEDIUM (F-B12-02: the account-sum fold lists lines the rule does not sum; F-B12-03: the cancellation earned line prints a single-segment ratio, unreachable on today's data) and six LOW, including F-B12-08 which is the visible face of F-YA-07. docs/reviews/b13-6-change-requests.md at b40e803: PASS. Migration additive and append-only on both databases (triggers read from pg_get_triggerdef), zero money rows written during the exercise, 30 measured requests on production with every refusal right, the customer view clean, the broker's count exact; one request and one reply created on the voided policy CGP-01061 by the reviewer (ids in the record). Seven LOW. Fix cycles dispatched at 09:03 local to the two builders on their branches (F-B12-02 to 07 and 09, F-YA-07; F-B13-01, 02, 03, 04, 06); F-B12-08 resolves with F-YA-07; F-B13-05 and 07 accepted.

## 2026-09-09T07:11:29+00:00 | Change-requests fixes merged (06999e3) and deployed at 07:11Z; YOA-637 created for the animated explanation

Builder fixes, one commit each: F-B13-01 (the answer notice on the policy page), F-B13-02 (an ownership refusal sends the person to their own home with the sentence), F-B13-03 (answered decided on reply_id), F-B13-04 (a repeated line refused by the application, the database gap stated in the check), F-B13-06 (PolicyTimeline takes an audience; the customer reads neutral sentences, never an operator's free text or a provider reference). check:change-requests 43/43 (run twice by the builder, disclosed). Coordinator checks on main: typecheck, 426 tests, build. Re-review requested from the same reviewer. Yoann's morning request at 09:05 local: the explanation folds must become an animated proof (YOA-637, B12-4): click a figure, the formula reveals line by line, the result counts up from the server-rendered text, a connector draws down to the proving journal entry which pulses; the browser animates and never computes; builder after the B12 fix cycle merges (same file).

## 2026-09-09T07:13:01+00:00 | B12 explanation fixes and the terms panel merged (19baf15) and deployed

Builder fixes, one commit each: F-B12-02 (a sum fold lists only the lines its rule sums, the side named is the rule's side, the empty state exists), F-B12-03 (the cancellation earned line prints the per-segment rule the ledger ran and says the event stores totals, not segments), F-B12-04 (today is a step only inside the term; the step builder is a pure module with its own tests), F-B12-05 (signed cents joined as subtractions), F-B12-06 (an unknown statement format gets the amount alone), F-B12-07 (the claim paid fold lists the two entry types that move paid), F-B12-09 (wording), F-YA-07 (the panel is "Terms in force on <today>" from the as-of fold, with one line per applied endorsement not yet in force). Coordinator checks on main: typecheck, 434 tests (433 pass, 1 skipped), build. Deployed; on CGP-01707 the panel now reads today's terms with the line naming the endorsement effective 2026-10-08 (measured below in this entry's log). Re-review requested from the B12 reviewer. The animated explanation builder (YOA-637) launched now that the file is free.

## 2026-09-09T07:22:31+00:00 | Two re-reviews PASS; LOW batch merged (82591a0); two more LOW fixed by the coordinator; deployed 85b40b3

Re-reviews: B12-2/3 PASS at 19baf15 (every finding resolved on production: the "Collected at Stripe" fold lists debits only, the earned line prints the per-segment rule, today's step clamped, signs joined as subtractions, unknown statement format unexplained, claim paid evidence adds up, the terms panel dated with the October line; one new LOW F-B12-10) and change requests PASS at 06999e3 measured on 19baf15 (all five fixes intact; one new LOW F-B13-08). LOW batch (builder, docs/handoffs/b13-2-notes.md): fifteen findings closed one commit each (F-B8-08 refund lines carry their own premium; F-B11-02 bounded and fixed sentences in mcp_calls; F-B11-03 the run note names the key that launched it; F-B11-05 a call whose audit row cannot be written is refused; F-B11-06 arguments validated against the closed schema; F-B12-01; F-B7-13; F-B4-07 the quote hash is now compared by both posting paths; F-B4-10 unique violations answered with a notice; F-B4-12 policy lock plus re-read, taken before the operation lock of F-B2-21, order documented; F-B10-09 a shared provider reference no longer pairs; F-UI-03, 13, 16, 18), builder checks statements, mcp 58/58, endorsement 80/80, claims 72/72, reconciliation 39/39, correction 55/55; coordinator checks after the merge: typecheck, 436 tests, build, endorsement replay 80/80. Coordinator fixes: F-B12-10 (the tax and fee folds cite only entries effective on or before the panel's date), F-B13-08 (the broker and staff homes print a refusal notice), README lines for F-B11-07 and F-B10-08. Deployed 85b40b3. Reviewer launched on the LOW batch. Running: the inbox builder, the animated explanation builder.

## 2026-09-09T07:40:31+00:00 | Inbox merged and deployed (08f2493); animated explanation merged and deployed (db1762a); console v1 building on Yoann's answers

Inbox (builder, docs/handoffs/b13-8-notes.md): lib/inbox/read.ts and sections.ts (pure grouping, 8 tests), /inbox per role with one row per waiting item and the link that does the work, the sidebar counts and the what-needs-you block pointing at it (the block's two lines applied by the coordinator), three count helpers now the length of the list they point at (the change-request count had disagreed with its list on corgi_test by one), scripts/check-inbox-counts.ts read-only (staff_ops 1184 counted and listed on corgi_test); one finding for the register: correctionsOfPolicy throws on old correction_rebook payloads on corgi_test (none on the trial database), the inbox names the unreadable policies instead of blanking. Animated explanation (builder, docs/handoffs/b12-4-notes.md): components/amount-explained-motion.tsx, the one new client component, reveals the server-rendered lines, counts the result up from the digits of the server text and writes the text back verbatim, draws a connector to the proving entry block (ids added to journal-table.tsx) and pulses it; runningSubtotals server-side, null unless the lines add up; with JavaScript off nothing is hidden and no role button is advertised; 11 tests including one asserting the client source does no arithmetic on cents; 12 frames under docs/evidence/b12-4/ captured on a throwaway fixture, deleted before the commit. Coordinator checks: typecheck, 455 tests (454 pass, 1 skipped), build, inbox counts 4/4. Deployed 08f2493 at 07:38Z and db1762a at 07:39Z. One reviewer launched for both slices. Console: Yoann's answers recorded (DECISIONS) and sent to the builder, an infrastructure view added to v1.

## 2026-09-09T07:41:17+00:00 | LOW batch review PASS at 82591a0 (measured on 85b40b3)

docs/reviews/b13-2-low-batch.md: all twelve advisory lock sites enumerated, only the endorsement posting takes two (policy then operation), no transaction takes them the other way, so no cycle; F-B8-08 proved by the unique refund_operation_id and the absence of an unearned_premium line on a correction refund; the append-only sentence measured on /ops; Redwood 2026-09 revision 3 still ties to the ledger on production; no migration, no UPDATE or DELETE, no form change, the deleted CSS unreferenced. Checks: typecheck, 436 tests, endorsement replay 80/80, statements 50 PASS then an abort in the PDF section caused by the reviewer's own npm ci mid-run (disclosed; the in-scope lines passed first), MCP 58/58 behind a local server, reconciliation 39/39. Five new LOW: F-B13-09 accepted, F-B13-10 to 12 in the backlog, F-B13-13 a decision for Yoann (bump the canonical statement version to 3 after the F-B8-08 change; recommended). The inbox builder's finding renumbered F-B13-14; the inbox and animation reviewer told to number from F-B13-15.

## 2026-09-09T07:52:54+00:00 | Statement format version 3 (decision 22), migration 0020 on both databases; Codex illustration prompt and manual retest tasks created

Yoann said yes to F-B13-13 at 09:47 local: CANONICAL_STATEMENT_VERSION is 3, migration 0020 widens the CHECK on statement_runs.canonical_version to 3 (widening only, applied to corgi_test and to the trial database), collectedFigures branches on "version 2 or later" so a version 2 run keeps its reading, the statement page explains the totals of versions 2 and 3 only, the pinned test and the check script follow the constant; statements check 53/53, 455 tests, build. On the trial database no run is affected; the next Redwood 2026-09 run will be revision 4 in version 3, flagged "format changed" against revision 3. Two tasks created: YOA-639 (the prompt Yoann pastes into his Codex session to bring the 150-illustration library into the empty states, welcome and banner spots, presentational files only) and YOA-640 (his manual retest of everything before the freeze, checklist to come). Running: the console builder, the inbox and animation reviewer.

## 2026-09-09T08:07:24+00:00 | Console v1 merged (020fd86) and deployed (5d405d2); its eleven documented limits verified against the live provider pages

Builder delivery (docs/handoffs/b13-9-console-notes.md): lib/console/read.ts (feed of ten event kinds newest first with a since cursor and kind filters, latency readers with p50, p95 and max computed by Postgres over 24 hours, the errors and unknowns readers with the 15-minute rule, the reference search over nine shapes, the 360 readers), lib/console/infra.ts, safe-read.ts (a failing panel prints an error line, never blanks the page), access.ts and guard.ts, the seven pages under /ops/console, scripts/check-console.ts (48 lines, read-only; one assertion rewritten after another agent's rows moved a global count mid-run, disclosed). Coordinator: navigation entry "Console" for staff, pages highlighted; checks on main: typecheck, 455 tests, build, check:console 48/48; deployed 5d405d2 at 08:02:31Z. A peer session verified the eleven documented free-plan limits the infra page cites against the live Vercel, Neon and Stripe pages: all accurate; two wording caveats (Neon "100 CU-hours/project" carries no "per month" in the cell; "public network transfer" is Neon's term) and one inference (Stripe's 25 rps per endpoint is not split by mode) passed to the console reviewer. Reviewer launched; a recheck agent launched on Yoann's request to go through B0 to B14 in order (docs/reviews/recheck-day2.md). Yoann's rule of 10:00 local: broad verifications and reviews go to sub-agents, the coordinator keeps merges, deployments and the docs of record.

## 2026-09-09T08:14:33+00:00 | Inbox and animation reviews PASS (db1762a), two MEDIUM in fix cycles; AF-05 history scan clean

docs/reviews/inbox-and-motion.md: inbox PASS (anonymous refused, ownership from the session, the three rewritten count helpers equal to the SQL they replaced on corgi_test, check 4/4 read-only, no table, no write, no client file) with F-B13-15 MEDIUM (the what-needs-you block links to sidebar sections instead of inbox anchors) and five LOW; animation PASS (server HTML at rest with no role button and no reveal attribute, fold counts and figures identical to the previous review, 455 tests) with F-B12-11 MEDIUM (the reveal opens every ancestor fold including the journal's show-all and draws the connector off screen) and seven LOW, one being the wording of "the browser never computes money" (it animates digits of the server text; the invariant that no money figure is computed in the browser holds and was verified line by line). Both fix cycles dispatched at 10:12 local to the two builders on their branches. The animation was never run in a browser by a reviewer (no browser tool); the committed frames come from a fixture. AF-05 evidence for the recheck: gitleaks git over the whole history from the main checkout, 332 commits, no leaks; a directory scan finds 81 hits, all in untracked or ignored locations (delegate worktrees, the Codex worktree, .next, the two .env files), zero in tracked files.

## 2026-09-09T08:22:00+00:00 | Operations console v1 reviewed: FAIL on one blocking finding, fix cycle running

- Independent review of the console (docs/reviews/b13-9-console.md, main at 5d405d2 deployed): read-only, staff-only, honest and masked all proven on production; page p50 between 0.33 s and 0.45 s; all eleven documented provider limits re-fetched and accurate; check:console 48/48 on corgi_test. Verdict FAIL on F-B13-21: `?since=999999d` computes an instant in year -712, Postgres refuses it and the feed and errors panels go dark (the safety net keeps the page at HTTP 200). Eight more findings, F-B13-22 to F-B13-29, two accepted as recorded. The record was renumbered from the reviewer's F-B13-20..28 because the inbox record already holds F-B13-15 to 20.
- Fix cycle launched at 08:21Z on a builder worktree (clamp in the pure parser plus one check assertion, bounded readers, one read per render, the search failure branch, the broker 360 fold, two quoted limits). Re-review by the same reviewer after the merge, then deploy.
- Post-PASS review widened with sections F (b1a0bce, the read query behind the open-or-resolved state of a break) and G (4cef315, the PDF rewrite), on the B7 to B10 audit's findings. Two review headers (ui-polish, b12-explain) carried local time with a Z suffix; corrected to UTC with the local time in parentheses and the correction noted in the header.
- Guards proof on an ephemeral database migrated to 0020 running (previous proof: 184/184 at 0019).
- README: console section and the check:console line.

## 2026-09-09T08:26:00+00:00 | Animated explanation fix cycle merged (5590242), deploy in progress

- The motion builder closed F-B12-11 (MEDIUM) and F-B12-12 to F-B12-18: the reveal points at the proving entry only when it is already on screen and never opens a fold nor scrolls; "Trace to the ledger" is the one action that opens, scrolls and pulses; the README and handoff now say exactly what the browser does during the 600 ms count (arithmetic on the digits of the server text, never a money figure); entry ids carry the panel key, which caught a fourth JournalTable call site on the console 360 page. 455 tests (454 pass, 1 skipped), typecheck and build clean on the merged tree, 18 evidence frames re-recorded under docs/evidence/b12-4/.
- Merged fast-forward to main at 5590242 and pushed; Vercel skipped that build because the merge commit has the builder branch as first parent, so its first-parent diff was docs only. Deployed as 0b92476 (the same code plus one comment in the login route, F-RC-04) at 08:35Z, health check confirmed from outside. Builder branches are merged with --no-ff from now on. Re-review of docs/reviews/inbox-and-motion.md requested once the inbox fix cycle (F-B13-15 to 20) lands, so one re-review covers both.
- First thing to look at on the deployed data: CGP-01274, the "Unearned premium, refunded" fold, whose proving entry sits behind the show-all fold.

## 2026-09-09T08:29:00+00:00 | Day-2 recheck of B0 to B14 delivered (docs/reviews/recheck-day2.md)

- Fifteen status lines at 5d405d2: B0 and B1 done and reviewed at their current code (the ledger immutability core has zero commits since its PASS); B2 to B11 done, each with material code after its last PASS and no re-review at the time of the recheck (the drift is exactly what docs/reviews/post-pass-changes.md is re-reviewing: sections A to G); B12 and B13 partial; B14 not started. Live fire: three of seven driven by Yoann (test-card payment, cancellation with an open claim, endorsement above the customer threshold); webhook replay and initiator refusal proven by check scripts only; backdated fix not yet done; as-of between two endorsements impossible until a policy carries two endorsements; planted mismatch partly (the $42.42 probe shows as a provider-only break on the deployed board). AF-01, 02, 04, 05 PASS (gitleaks over 347 commits, no leaks); AF-03 PASS on code with the guards proof one migration behind (running now on an ephemeral database at 0020); AF-06 not satisfied: every record ends NOT REVIEWED WITH YOANN.
- Twelve findings F-RC-01 to F-RC-12 registered; six already fixed or fixed before the recheck was read (F-RC-01, 02, 03, 04, 10, 11; the guards proof at migration 0020 is 184/184 PASS at 08:32Z on an ephemeral database), three in cycles (06, 08, 09), two in review or drafting (05, 12), one decision put to Yoann (07, structured redacted logs: build small or cut with disclosure).
- Launched: the B13-1 screens review (three key screens, five states, 375 px and 1280 px, on production once 5590242 is deployed; findings F-B13-30 upward), the compliance matrix refresh (drafted by a sub-agent, merged by the coordinator), the F-RC-06 fix. The integration review REVIEWER.md requires is launched after the console and inbox fix cycles land, so it reads the tree that will be frozen.

## 2026-09-09T08:36:00+00:00 | Bare corrections page fixed (7b8431c), integration review launched

- F-RC-06: /policies/{id}/corrections/new opened without parameters now renders the correction form (which endorsement, the date it should have carried, why) in the endorse page's shape, then the existing preview and confirmation path; one file, no lib change. Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 08:36Z. This is the entry point of the live-fire backdated correction Yoann drives later today.
- The integration review REVIEWER.md requires (docs/reviews/integration.md) launched at 08:35Z on 0b92476: one policy through its whole life on the deployed data with every figure compared across screens, the same pure function for preview, execution and explanation, ledger invariants read on production, maker-checker traced from the MCP tool to the trigger, role boundaries probed per role, statements and reconciliation checks once each on corgi_test, the AF gate for the whole tree, and the known gaps stated. Findings F-INT-nn.
- Note from the builder: the repository has no lint script and no ESLint configuration; typecheck and the test runner are the automated gates.

## 2026-09-09T08:40:00+00:00 | Console fix cycle merged (d786644), deploy in progress, re-review requested

- The console builder closed F-B13-21 (the since cursor is clamped to ten years back and says so, one database-free assertion added), F-B13-22 to F-B13-27 and F-B13-29, plus F-RC-08 (an integration-mode line on all seven console screens and a rail field on every money row, LOCAL SIMULATOR printed on every non-Stripe one) and F-RC-09 (infra wording quoted from the providers). Nine files, no migration, no write path. check:console 49 of 49 on corgi_test, typecheck clean, 455 tests (454 pass, 1 skipped) on the merged tree.
- Merged with --no-ff at d786644 and pushed as code alone; deployed and confirmed by /api/health at 08:40Z. Re-review by the console reviewer requested at that revision; new findings, if any, from F-B13-50.

## 2026-09-09T08:41:00+00:00 | Compliance matrix refreshed (F-RC-12): 51 rows in six sections

- docs/COMPLIANCE-MATRIX.md now has one row per control: the general non-negotiables, the AUTOMATIC-FAILS safeguards, the Track 1 gauntlet and live slots, the seven live-fire items with who performed each, the build's own additions (endorsements, cancellation, corrections, statements with format 3 and migration 0020, the B12 items, change requests, inbox, console, MCP demo, animated explanation) and the delivery controls. Each row cites its record and the reviewed SHA; 25 rows say "re-review of later changes in progress" and name the pending post-PASS record; four requirements have no evidence at all yet: structured logs (decision pending), the integration review (running), the five screen states (review running), as-of between two endorsements (no production policy carries two).
- Drafted by a sub-agent at 9e839bf, merged by the coordinator with three rows brought up to date (console fix deployed at d786644, integration review launched, screens review running).

## 2026-09-09T08:46:00+00:00 | Codex illustration work merged (08b3678), cross-model review launched

- Yoann relayed Codex's report on YOA-639: branch codex/corgi-interface finished at 9ffc2c5 with 150 WebP illustrations (19.7 MB) under public/illustrations/library and a catalog under docs/illustrations, banners and empty states on the home, login, error, 404, broker, customer, inbox and statements screens, overflow checked at 375, 768 and 1440 px, seven demo accounts walked without any business action, typecheck, build, 454 tests and gitleaks passing, and a review record Codex wrote about its own work.
- Coordinator checks before the merge: the branch was exactly on top of main (0 commits behind), the diff touches nothing under lib/, db/ or app/api/, the only ops-page changes are two empty-state illustrations (claims list, reconciliation with no run), gitleaks over the 22 commits found no leak, 22 em or en dashes in the three Codex-written records and none in code or UI copy. Merged with --no-ff at 08b3678, typecheck clean, 455 tests (454 pass, 1 skipped), pushed as code alone, deployed and confirmed by /api/health at 08:47Z.
- A Claude reviewer was launched on the deployed revision for the cross-model review (docs/reviews/illustration-cross-review.md, findings F-IL-nn): call sites, image weight per page, accessibility and honesty of the visuals next to the integration-mode labels, layout at three widths, the error and 404 pages, and the Codex records' claims. It also rewrites the three Codex records without dashes. The Codex worktrees stay untouched.

## 2026-09-09T08:48:00+00:00 | Console re-review PASS at d786644, one MEDIUM follow-up

- The console reviewer re-measured the deployed revision: the whole since-cursor boundary set renders 0 failed panels on 8 of 8, no details element nested in a link on any of the seven pages, the integration-mode line on all seven screens with 0 unlabelled money or webhook rows in the 7-day feed, infra wording quoted from the providers, check:console 49 of 49, hostile inputs 11 of 11 at HTTP 200, role probes unchanged, no secret, personal data behind folds, p50 between 0.30 s and 0.48 s. Verdict PASS. Record appended to docs/reviews/b13-9-console.md.
- Two new findings: F-B13-50 (MEDIUM) the F-B13-23 bound tied the unknown-outcome reader to the feed window, so the default 60-minute view shows nothing unresolved while two such operations exist; F-B13-51 (LOW) the errors panel prints its cap as the count. Both sent to the console builder for a short fix cycle, with a measurement by the reviewer afterwards.

## 2026-09-09T08:51:00+00:00 | Console follow-up merged (e117a61): unresolved operations keep a 7-day floor

- F-B13-50 and F-B13-51 fixed by the console builder: the accepted-and-unconfirmed reader no longer takes the feed cursor and reads over a named 7-day floor (an unconfirmed operation stays visible until a human resolves it, whatever window the operator reads the feed in), the "Being checked" note and the errors disclosure say so, one assertion proves a stuck operation older than the window still appears; the errors panel prints its cap and the chip says "60 or more". Three files. check:console 50 of 50 (run twice by mistake, no contention), typecheck clean, 455 tests. Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 08:52Z; confirmation measurement by the console reviewer requested at e117a61.

## 2026-09-09T08:56:00+00:00 | Console v1 confirmed PASS at e117a61: no open finding on the console

- The console reviewer measured e117a61 on production: 2 unresolved operations visible at the default window and at every wider one (0 before the fix), both sentences rendered, the errors chip reads "60 or more to look at" with the cap sentence above the cap and the true count below it, check:console 50 of 50. Verdict PASS, record docs/reviews/b13-9-console.md (three sections: review, re-review, confirmation). One residual observation, not a finding: at 24 h and wider the 60-row cap of the merged errors panel pushes the older of the two unresolved operations off the list; the cap sentence says to narrow the window and that brings both back.
- What remains on the console is a recommendation, not a defect: the AF-06 reading map of lib/console/read.ts (six section line ranges in the handoff note, two hardest lines rehearsed). Console v2, the activity log on every route, is a separate decision against the remaining time.

## 2026-09-09T08:57:00+00:00 | Inbox fix cycle merged (41ea2c5), joint re-review of inbox and animation launched

- The inbox builder closed F-B13-15 (MEDIUM) and F-B13-16 to F-B13-20: every task in the what-needs-you block links to the exact inbox anchor, the twelve anchor names live once and a unit test checks each is rendered, the check compares anchor by anchor and refuses a duplicate id, the per-policy catch keeps only a refused stored value and prints which figure was refused, endorsements paid and not applied are listed oldest first. Measured correction reported by the builder: walking every owner on corgi_test took over twenty minutes (434 brokers, 380 customers accumulated by every check run), so the check compares the 25 most recent of each role and prints how many it left out. check:inbox-counts 53 of 53 (staff_ops 1330 counted and 1330 listed, the old "policies 42" now read as policies 10 plus endorsements 37, which is what F-B13-16 was about). 456 tests (455 pass, 1 skipped), typecheck and build clean. Eight files, no migration. Notes in docs/handoffs/b13-8-notes.md; one gap stated there: refusedValueOrRethrow has no unit test of its own because lib/inbox/read.ts opens the pool on import.
- Merged with --no-ff at 41ea2c5, pushed as code alone, deployed and confirmed by /api/health at 08:57Z. A fresh reviewer re-checks both fix cycles (inbox F-B13-15 to 20, animation F-B12-11 to 18) at that revision and appends to docs/reviews/inbox-and-motion.md.

## 2026-09-09T09:01:00+00:00 | Post-PASS re-review delivered: six of seven changes PASS, statement format v3 FAIL on one stale assertion

- docs/reviews/post-pass-changes.md (693 lines, reviewed at cc7bdda) covers every money-path change made after its slice's PASS: A rule 21 (4ddb33b) PASS, with a probe on corgi_test showing a hand-built agent-raised operation without approval refused by the rule-21 sentence; B the late checkout.session.completed fix and the per-operation locks (0fa828d, 5f2c841, 369671d) PASS, twelve advisory-lock sites enumerated, no transaction takes the operation lock before a policy lock, the race closed with no gap between check and commit; C statement format v3 with migration 0020 FAIL on F-PP-05; D the tax fold and home notices PASS; E the first LOW batch with migration 0013 PASS (an apparent deletion of the endorsement-delta guards in one diff was a merge-base artefact, the real diff is three small changes); F the resolved-break filter (b1a0bce) PASS; G the PDF rewrite (4cef315) PASS.
- F-PP-05 (MEDIUM): scripts/check-mcp.ts pinned the statement format version to 2, so check:mcp reads 57 PASS and 1 FAIL since the bump to 3, and the "53/53" evidence closing F-B11-01 was stale. The application is right, the evidence was not. The coordinator changed the assertion to import CANONICAL_STATEMENT_VERSION where it runs (ea204d7); check:mcp rerun on the disposable database behind the app on port 3800: 58 PASS, 0 FAIL, the line reads format v3. Confirmation of section C requested from the reviewer. Eleven LOW findings F-PP-01 to F-PP-12 registered (two accepted, two disclosed, the rest on the B13 backlog).
- Checks run by the reviewer: check:claims-and-approvals 72/0, check:payment-replay 34/0, check:endorsement-replay 80/0, check:correction-replay clean, check:statements all passed, 454 unit tests. Production measured at 7b8431c: the agent chip on CLM-00212, CGP-01707 succeeded, the Redwood 2026-09 statement revision 3 with five explain folds. The reviewer notes the guards proof it cites was at 0019; the 0020 proof (184/184 at 08:32Z) postdates its brief.

## 2026-09-09T09:07:00+00:00 | Post-PASS re-review: overall PASS for A to G at ea204d7

- The reviewer confirmed F-PP-05 with its own run (58 PASS, 0 FAIL behind its own server on the disposable database, "format v3, 2 lines"), turned section C to PASS and the overall verdict to PASS at ea204d7. Every money-path change made after a slice's PASS now carries a verdict; the eleven remaining F-PP findings are LOW and none blocks. Note kept from the record: the guards proof it cites is the 0019 one; the coordinator's 0020 proof (184 of 184 at 08:32Z) postdates the brief. The 27 compliance-matrix rows that named the pending record now cite the PASS.

## 2026-09-09T09:09:00+00:00 | Illustrations: cross-model review FAIL on weight only, fix cycle launched

- A Claude reviewer measured the Codex illustration work at 08b3678 (docs/reviews/illustration-cross-review.md): 22 call sites in 13 files, zero image on the policy journal, the approvals queue, the claim detail, the published statement, the breaks table and every console screen (87 measurements over 29 routes at three widths), seven demo accounts signed in, typecheck and tests reproduced, no leak in the changed content. Verdict FAIL on two MEDIUM about bytes: F-IL-01 four banner panoramas ship at 3456 or 4032 px (472 to 694 KB) into a 220 px box with no srcset, the same bytes on a phone; F-IL-02 the eager 404 image is preloaded on all 29 routes, a 404 also pulls the home banner it never shows, and /login at 375 px fetches a hidden image. Five screens above the 400 KB yardstick (broker 744 KB, customer 691 KB, ops 525 KB, home and 404 501 KB). Five LOW: 137 of 150 assets referenced by nothing (served, never fetched), the "all-clear" key on the "no run yet" state (sentence exact), names and dimensions retyped from the catalog, a dead class, four ops screens with illustrations rather than two.
- The reviewer also rewrote the three Codex-written records without dashes (25 removed). Both commits merged.
- Fix cycle launched on a builder for F-IL-01, F-IL-02, F-IL-05 and F-IL-06; F-IL-03, F-IL-04 and F-IL-07 disclosed as recorded.

## 2026-09-09T09:10:00+00:00 | Screens review and integration review delivered, both FAIL, fixes in flight

- docs/reviews/b13-screens.md at 08b3678 (36 frames, three screens, 375 px and 1280 px): 1444 amount cells right-aligned, no stack trace or secret over 28 rendered states, every empty and error state carries a sentence, five hostile as-of values refused by name, the as-of control changes the figures. FAIL on F-B13-30 (MEDIUM): one click on "Explain this amount" made the policy detail and the statement run scroll sideways at 375 px because the fact grid had no explicit column. Fixed by the coordinator at 3e9d095 (one CSS line), pushed as code alone, deployed and confirmed by /api/health at 09:11Z, confirmation by the screens reviewer requested. Four LOW registered (no loading state named anywhere, a repeated as-of parameter printed two ways, counter-generated scroll-region labels, no per-slot mode chip on two screens). Three states unreachable on the trial data (a policy with no entries, a window with no breaks, a break older than 24 hours), recorded as gaps.
- docs/reviews/integration.md at 08b3678: 24 of 24 figures for CGP-01707 and CGP-01274 recomputed by hand and matched to the cent across the journal, the policy page, the explanation folds, the approvals screen and the reconciliation board; ledger invariants hold on production; role boundaries hold; maker-checker traced from the MCP tool to the trigger. FAIL on F-INT-01 (HIGH): a staff_approver can create the operator's MCP key, raise a money-out with it and approve it as themselves. Four MEDIUM: the customer's own policy page prints policy_current as the terms in force today (F-YA-07 was applied to the staff page only); the console lists the live-paid CGP-01707 as unknown outcome because it does not prefer a terminal status; no published statement contains either live policy; the endorsement fold's agreement check compares a field to itself. Seven LOW.
- Fix cycle launched on one builder: F-INT-01 (staff_ops only may mint keys, one check assertion, README sentence), F-INT-02, F-INT-03, F-INT-05 (a real recompute-and-compare in the fold), F-INT-07 (PDF caption), F-INT-10 (a test of rule 21's fail-closed branch). Put to Yoann: the trigger-level rule "the key creator cannot decide" as a second layer, one statement run and one reconciliation run on production (F-INT-04, F-INT-06), the threshold base alignment (F-INT-12, fails safe, disclosed).

## 2026-09-09T09:14:00+00:00 | Inbox PASS and animation PASS at 41ea2c5; README paragraph on the animation rewritten

- The joint re-review (docs/reviews/inbox-and-motion.md) measured 41ea2c5 on production and corgi_test: every anchor link lands on the section listing its items (22 counted, 22 on the chip, 22 rows for the reconciliation section), the anchor-by-anchor check 53 of 53, 17 trace links with 0 absent target and 0 duplicate id over five pages, 0 disagreement alert on 30 folds. Inbox PASS. Animation PASS with F-B12-13 still open: the honest wording ("the browser never computes a money figure; during the count it does arithmetic on the digits of the server text") was in the component header and the handoff but not in README.md. The coordinator rewrote that README paragraph. Two new LOW registered (F-B13-60, F-B12-20). The reviewer could not run the animation in a browser (no Playwright in the tree); the frames under docs/evidence/b12-4 remain the builder's.

## 2026-09-09T09:16:00+00:00 | Screens review PASS at 3e9d095

- The screens reviewer re-measured the fix on production after a real click on "Explain this amount": scrollWidth 375 against innerWidth 375 on the policy detail and on the statement run at 375 px (731 before), the fold panel 249 px wide instead of 668, and 0 overflow on the 14 states at 1280 px with 722 amount cells still right-aligned. Slice verdict PASS (docs/reviews/b13-screens.md, confirmation section; both the overflowing and the fixed frames kept as evidence). Four LOW stay open on the B13 backlog.

## 2026-09-09T09:33:00+00:00 | Illustration weight fix merged (5fa3d49), production re-measurement requested

- The builder moved the decorative illustrations to next/image with static file imports (intrinsic width and height from the bytes, which closes F-IL-05 without shipping the 444 KB catalog to the browser), per-variant sizes, no eager or priority anywhere, the dead class removed; seven files, nothing under lib/, db/ or app/api/. Local measurement with the reviewer's method: the home from 500.6 KiB to 2.8 KiB, /broker from 743.6 KiB to 7.7 KiB, /customer from 691.2 KiB to 7.6 KiB, /login at 375 px from 141.6 KiB to nothing, the 404 from 39.7 KiB to 2.6 KiB, 0 preload links on every route, 21 of 21 layout readings without overflow. Typecheck, 456 tests and the production build clean. The builder reports one incident: a stray checkout reverted its tree mid-session and it rewrote the same edits before measuring again.
- Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 09:34Z; the illustrations reviewer re-measures on production (Vercel image optimisation) at 5fa3d49.

## 2026-09-09T09:40:00+00:00 | Illustrations PASS on production at 5fa3d49

- The illustrations reviewer re-measured production after the fix: 3,288,716 bytes of images over the seven routes before, 43,642 after at 1280 px and 32,544 at 375 px (98.7 percent less), zero image preload on 21 loads, 21 of 21 layouts without horizontal overflow at 375, 768 and 1280 px, no image in any table, every decorative alt empty, the sandbox sentence intact, optimised variants served with a one-year immutable cache (MISS then HIT). Verdict PASS, F-IL-01, 02, 05 and 06 closed; F-IL-03, 04 and 07 stay disclosed as LOW. YOA-639 done.

## 2026-09-09T09:48:00+00:00 | Integration fix cycle merged (a1e525d), re-reviews requested

- The builder closed F-INT-01 (create and revoke of MCP keys are staff_ops only, the sidebar link hidden from approvers, README updated, check:mcp signs a real approver session and proves the POST is refused and writes no key), F-INT-02 (the customer view reads the terms as they stand today through a helper shared with the staff page, so the two screens cannot drift again), F-INT-03 (one SQL expression prefers a terminal status in every console reader that derives an operation's status, with an assertion), F-INT-05 (the endorsement fold reprices server-side from the inputs stored on the endorsed event and compares the six printed figures, three unit tests), F-INT-07 (both PDF tables print the charged total, premium plus tax, summed in the fold), F-INT-10 (a check builds a $10 agent-raised payout with no approval request and proves rule 21's fail-closed branch refuses it). 24 files, no migration. Builder counts: check:mcp 59 of 59, check:console 53 of 53, check:claims-and-approvals 74 of 74, check:endorsement-replay 80 of 80, 459 tests, build clean.
- The merge conflicted on three docs of record (the builder's branch carried an older copy of FINDINGS, STATUS and the matrix); main's versions were kept, nothing of the builder's docs was needed. Typecheck and 459 tests clean on the merged tree, pushed as code alone, deployed and confirmed by /api/health at 09:48Z. The two original reviewers could not be resumed (their worktrees were removed at the 11:43 clean-up), so one fresh re-reviewer covers the six findings and appends to both records. The trigger-level key rule stays a decision put to Yoann.

## 2026-09-09T09:50:00+00:00 | Yoann's answers on the day-2 decisions (DECISIONS 24 to 27)

- Customer threshold cumulative per policy (24), structured logs and the console activity log both built (25), MCP key rule one line only, trigger in week two (26), parked money disclosed (27). Two builders launched: the threshold base (one function for the preview and the payment gate, a money path, re-review required) and the observability piece (migration 0021 activity_log, the log helper, every route, the console v2 panels; the coordinator applies the migration on the trial database and re-proves the guards on an ephemeral database before deploying).

## 2026-09-09T09:57:00+00:00 | Desktop UI audit delivered as JSON

- Scope receipt: read-only UI/navigation and branding audit requested by Yoann, with mobile explicitly excluded and no Linear writes. Actual reads across this audit: AGENTS.md, AUTOMATIC-FAILS.md, READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, docs/BRIEF-REFERENCE.md, README.md, docs/PLAN.md, relevant STATUS/DECISIONS/COMPLIANCE-MATRIX/GAP-REVIEW entries and docs/reviews/b13-screens.md. Applied the web-design-guidelines skill. The mandatory kit and released brief were available. After context reset, the operating instructions and current status were reread. No application implementation was performed.
- Acceptance criterion for this task: inspect available desktop pages and disclosures, retain only observed issues, and provide an English JSON inventory with reproduction steps and screenshot evidence. Report: docs/ui-audit-2026-09-09.json. It contains 35 retained findings across 30 page groups (7 priority 1, 19 priority 2, 9 priority 3), supported by the 122 desktop/reference captures indexed in docs/evidence/ui-audit-2026-09-09/observations.json. The connected Corgi portal supplied the branding reference. Seven synthetic demo accounts across four roles were inspected. Initial evidence was captured from 09:12Z; the closing recheck ended at 09:52Z.
- Deployment changed during inspection: 3e9d095, then 5fa3d49, then a1e525ddd02e. Closing browser checks removed the corrected console status contradiction (UI-027) and narrowed UI-035/UI-036 to the remaining customer list/wording issues. Earlier findings remain tied to their timestamps. This audit does not certify a single immutable revision or approve a feature's financial, security or compliance controls.
- Actual checks: desktop browser navigation, disclosure/preview inspection and timestamped screenshots; inline Python JSON/unique-ID/evidence-link/image-decode verification PASS for 35 issues and 122 JPEGs; `gitleaks dir docs/evidence/ui-audit-2026-09-09 --redact --no-banner --no-color` PASS; `gitleaks stdin --redact --no-banner --no-color < docs/ui-audit-2026-09-09.json` PASS. The initial artifact check caught incorrect image filename extensions and a stale mobile manifest entry; both were corrected before the passing recheck. No application tests/build/provider round trip were run for this read-only audit. No approval, financial action, KYC submission, key change, reconciliation/statement run, commit, push or deployment was performed by this audit.
- Limits: two approval detail routes had no pending request to open; PDF inspection was blocked by Chrome ERR_BLOCKED_BY_CLIENT; no application exception was induced. These are explicit coverage gaps, not passing checks. AF-01 has observed reachable UI evidence only; AF-02/03/06 product gates are NOT RUN in this audit, AF-04 was maintained by inspection-only use of synthetic trial accounts, and AF-05 evidence is the scoped scans above, not a history-wide certification. Yoann's understanding was not assessed.
- Handoff: local branch main at 329499bfd4e45dafa232eed0c7410d8dbbc25d90 when checked; concurrent development continued throughout. This session changed audit artifacts and this status entry only. Browser restored to staff operations at /ops, 1512 x 698 desktop. Next executable step: use the retained issue IDs to select a bounded desktop fix, then recheck affected screens against its deployed revision. Trial-wide completion and independent feature review remain governed by the existing records; this JSON is an audit deliverable only.

## 2026-09-09T10:14:00+00:00 | Integration re-review PASS at a1e525d; threshold change merged (20439ad), re-review requested

- The re-reviewer confirmed the six fixes on production and in the code: the key-minting attack replayed end to end as the approver (303 refused, 7 keys before and after, sidebar link gone), the customer terms identical on both pages through the shared helper, zero unknown outcome on the console at every window with both 360 pages reading succeeded, the endorsement fold printing "Recomputed today from the same inputs: identical", $1,127.24 under "Amount charged" on both PDFs, rule 21's fail-closed branch refusing a $10 agent-raised payout. Integration verdict lifted to PASS; AF-06 stays NOT SATISFIED until the walkthroughs. Three LOW registered (F-INT-20 to 22). Register lines for F-INT-02, 03, 05, 07 and 10 brought up to date (they still read open after the merge).
- Decision 24 implemented: one pure function endorsementNeedsCustomerApproval and one query additionalPremiumOfTheTerm, used by the preview, the request standing and the payment gate; the preview line names the running total and the threshold; unit tests (three-step example, exact $500.00, a decrease, tax excluded) and check:endorsement-replay section 3b (90 of 90) prove the gate refuses to collect the second endorsement before the customer's approval. Eleven files, no posting amount changed, no migration. Merged with --no-ff at 20439ad, 463 tests, pushed as code alone, deployed and confirmed by /api/health at 10:15Z; re-review requested from the integration re-reviewer (money path). The builder notes the correction path keeps its own base (F-B8-02), out of scope of decision 24.

## 2026-09-09T10:30:00+00:00 | Decision 24 re-review PASS at 20439ad; three UI builders launched on the desktop audit

- The re-reviewer read the running-total query clause by clause against its comment, confirmed the one predicate is read by the preview, the request standing and both payment gates (checkout start and the payment recording inside the advisory lock), that every old base name is gone from the tree, that the diff of lib/payments, lib/ledger and db/migrations between a1e525d and 20439ad is empty, and on production (GET only) that a preview of a further $42.60 endorsement on CGP-01707 names the running total $1,143.96 and asks for the customer. check:endorsement-replay 90 of 90, 463 tests. Verdict PASS, F-INT-12 closed; one LOW on the preview wording (F-INT-23). The correction path's separate base is recorded as an accepted scope line for week two.
- Yoann received a desktop UI audit (docs/ui-audit-2026-09-09.json, 35 issues, 122 captures, 1512 px) and asked for the P1 and P2 items fixed by three builders splitting the work: A ops tables and console (9 issues), B policy detail and forms (9), C shell, login, inbox and homes (8), on disjoint files with one builder allowed on globals.css. Linear YOA-643. One UI reviewer re-measures the 26 items on production after the merges.

## 2026-09-09T10:30:00+00:00 | Observability merged locally (2985df4): activity log, structured logs, console v2; guards proof at 0021 running before any deploy

- The builder delivered decision 25: migration 0021 activity_log (append-only, the money-table guards for every role, app_runtime with SELECT and INSERT only, no payload column, server-clock trigger on recorded_at, indexes on recorded_at, subject and correlation id), lib/observability/log.ts and redact.ts with tests, the 34 route handlers and the MCP endpoint wrapped in one line each (correlation id from x-request-id or a uuid, timing, outcome ok, refused with the rule, or error with a sanitised sentence, one JSON line per request, a failed insert never breaks the request), console v2 panels (refused and failed actions, latency by route, an Activity panel on the four 360 pages, correlation id in the search), the table added to the money-guards proof with two extra assertions. Two deliberate choices recorded by the builder: refusals matched by instanceof because the production bundle mangles class names, and the MCP endpoint declaring its own outcome because a refused tool call answers HTTP 200. Builder counts on corgi_test: check:console 65 of 65, check:mcp 59 of 59, check:payment-replay 34 of 34, check:claims-and-approvals 74 of 74; 471 tests, typecheck and build clean.
- Merged with --no-ff at 2985df4 and kept local until the guards were proven: 192 of 192 PASS on an ephemeral database migrated 0001 to 0021 at 10:42Z (eight new assertions for activity_log), the database dropped afterwards. Migration 0021 applied to the trial database at 10:42Z (npm run migrate, one applied, twenty skipped). The push that followed ended on a docs commit and Vercel skipped it; a one-line code fix (F-PP-06) pushed at 10:43Z carried the build, deployed and confirmed by /api/health at 10:44Z as f7e81aa. Independent review of the slice launched (docs/reviews/b13-11-observability.md, findings F-OB-nn).

## 2026-09-09T10:52:00+00:00 | Observability review PASS at f7e81aa; four LOW sent back to the builder

- docs/reviews/b13-11-observability.md: 34 route files, 37 handlers, 37 wrappers, 0 unwrapped; rows read back for seven routes and the rest verified by reading the uniform wrapper; 99 rows read as the runtime role on both databases with 0 hits for ten secret or personal-data shapes; UPDATE refused for the runtime role; fail-open proven twice (unreachable database, refused constraint) with the response unchanged; check:console 65 of 65, check:mcp all passed, redact tests 8 of 8. Verdict PASS. Seven findings, none blocking: F-OB-01 the rule column stays null on route refusals (only the throw branch names a rule), F-OB-02 the stored status code is 400 or 500 rather than the one the caller got, F-OB-03 the console's failure line is built without the redaction function so a driver error could print a connection-string password on the console screens, F-OB-05 the duration excludes the user lookup and the insert; F-OB-04 disclosed (only the API routes are wrapped, the README says so), F-OB-06 and 07 accepted. The four are a short fix cycle for the observability builder.

## 2026-09-09T11:06:00+00:00 | UI audit cycle C merged (ce60fb2): login, approvals, inbox ordering and links, broker and customer homes

- Builder C closed UI-003 (the login layout no longer moves when the demo help opens: an auto margin cancelled the flex stretch, the form's box is identical closed and open at six widths), UI-008 and UI-009 (approvals gutter, and the disclosure rewritten from rule 21: below the threshold only an agent-raised request appears), UI-016 (sections with work first, empty ones folded to one line, anchor ids unchanged), UI-017 (a break's Investigate link lands on /ops/reconciliation#break-<key>, every link distinct on 1325 open breaks), UI-031 (a broker whose verification blocks binding is told first, linking to the KYB page), UI-034 (customer rows link to the policy detail where the change request lives), UI-035 (the customer list shows the premium in force today through the shared helper). Ten files, 473 tests, build clean, check:inbox-counts all passed (staff_ops 1590 counted and 1590 listed). Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 11:06Z.
- The builder found a broken gate: check:inbox-counts cannot start on main since the illustration component's static .webp imports (F-IL-08, MEDIUM); a builder moves the pure task code into lib/inbox/tasks.ts. It also notes that the staff inbox never finished rendering in a browser on corgi_test (1325 open breaks plus one query per claim): a read-cost item to look at, outside this cycle.

## 2026-09-09T11:17:00+00:00 | UI audit cycle A merged (a2068c1): ops tables and console readable at desktop width

- Builder A found the root cause of the six crushed tables: overflow-wrap anywhere on every cell let the browser size a column one character wide. A dedicated stylesheet (app/styles/ops-tables.css) gives each column role a minimum width and lets the detail grid stack so the tables get 1098 px instead of 736. Measured on a production build at 1512 px: 28 tables across nine screens, zero horizontal overflow, zero hidden column, no natural-language word broken beyond two lines. UI-025: the console's meta refresh replaced by a client timer calling router.refresh() every 10 seconds, cleared on unmount (measured: two refreshes in 23 s on the console, none after navigating to /ops); the Refresh button unchanged. UI-028: the reference trail's broker clause applies only when the subject is the broker (32 journal entries before, 5 after on a policy). Every reconciliation break row carries id break-<key> for the inbox links of cycle C. Eleven files, 473 tests, build clean, check:console once all passed. Merged with --no-ff, pushed as code alone, health check polling. The console record and handoff describe the old meta refresh; a dated note was appended to the handoff, the record stays historical and the UI reviewer will note it.

## 2026-09-09T11:19:00+00:00 | Observability follow-up merged: F-OB-01, 02, 03 and 05 fixed

- The console failure line now goes through the sanitiser, so a driver error carrying a connection string prints its credentials masked (asserted in check:console); the eleven business imports and the instanceof table are gone from the wrapper, replaced by a closed rule union each route declares, applied only where it is true (a refusal before the route's gate reads "sign in", a 400 names no rule); the stored status code is the response's own and a throw records 500 as error; the clock starts on the wrapper's first line and stops when the answer is ready, the insert alone stays outside. README and the console disclosure updated. 466 tests on the builder's tree, 473 on the merged one, check:console 68 of 68, check:mcp 59 of 59. Merged with --no-ff behind UI cycle A, pushed as code alone, deployed and confirmed by /api/health at 11:18Z; a confirmation by a reviewer follows with the UI review.

## 2026-09-09T11:23:00+00:00 | check:inbox-counts gate repaired (22babed)

- F-IL-08: the pure task-building code (workspaceTasks and its types) moved byte for byte into lib/inbox/tasks.ts, which imports nothing from components or app; the what-needs-you component re-exports it; the check script imports the lib module. check:inbox-counts runs again from the repository with no stub: 53 of 53 (staff_ops 1596 counted and 1596 listed, approver 1533 and 1533, 50 of 1004 owners). Five pages still reach workspaceTasks through the component's re-export, a follow-up. 473 tests, build clean. Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 11:23Z.

## 2026-09-09T11:30:00+00:00 | UI audit cycle B merged (06166cb): the 26 P1 and P2 items are all merged, review launched

- Builder B closed UI-004 (the policy list total goes through the terms-in-force helper, a row moved from $1,867.30 to $1,253.20 equal to its page), UI-007 (a card holding an open fold spans the row: 174 by 1693 px became 738 by 582), UI-019 (five fixed-share columns), UI-020 (the as-of field keeps the clicked date; still a GET form), UI-021 (references as an inline fold, 17 px inside the container instead of 151 px past it), UI-022 (journal sums exclude reversed pairs, the voided policy reads $0.00 collected and refunded, a sweep of 40 policies with reversals shows only one-sided cash sums move), UI-023 (prefilled date accepted, a refused date re-renders the form), UI-024 (label column 44 px to 329 px), UI-036 (wording). Nine files, 473 tests, build clean, 375 px scrollWidth equal to innerWidth with folds open. Builder cost note: the ops policies list now folds each policy, about 0.13 s per policy on a 1077-policy test database (4 policies on production). Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 11:31Z.
- Two reviewers launched on 06166cb: the UI audit review (the 26 items replayed at 1512 px, the 24 integration figures re-read, 375 px regressions, docs/reviews/b13-13-ui-audit.md) and the observability confirmation (F-OB-01, 02, 03, 05).

## 2026-09-09T11:33:00+00:00 | LOW backlog sweep launched on two builders; the rest disclosed

- Yoann at 13:30 local: finish every development point, then he takes over the UI and UX review. Two builders on disjoint files: screens and wording (13 LOW: named loading states, the repeated as-of parameter, scroll-region labels, per-slot mode chips on the policy detail and the statement, the per-role inbox anchor assertion, one arrival per journal entry in the animation, the console 360 reason dating, the MCP keys card hidden from approvers, three wordings, Stripe ids behind the references fold, the refund banner state, the notices shape) and library reads and checks (9 LOW: claim events ordered by sequence number, the policy lock taken first on the issuance posting, the index named in the code comment, bounded resolved-break reads with a correlated not exists, annotated rather than dropped resolved breaks, constraint names checked on 23505, the MCP call id, a tolerant correction read, three refusal proofs in check:correction-replay). Each ends with the check scripts once on corgi_test; the library batch gets an independent re-review.
- Disclosed rather than built, with the reason in the register: a column-level grant, a journaled mismatch refusal, the migration numbering, a trigger on role promotion, the migration comments, guards on the rate tables, deriving the agent marker inside the request function, the policy notices refactor; the day-1 aggregate lines F-B2-L and F-B5-L partly fixed and the rest disclosed; the F-UI-03 to 19 lines superseded by the screen rebuilds.

## 2026-09-09T11:42:00+00:00 | Observability follow-up confirmed PASS at 06166cb

- The re-reviewer measured production: the approver's refused POST reads refused, 303, rule maker-checker on the row and on the console panel; six unauthorised cron calls read cron secret; an anonymous document GET reads sign in; 8 of 8 refusals since the fix name a rule; 12 of 12 rows store the status the caller received; the sanitiser masks a connection string in the exact shape the finding described; the wrapper clock now includes the session query (about 5 ms). 60 rows grepped for ten secret shapes, 0 hits; check:console all passed; no money row written by the walk. F-OB-01, 02, 03 and 05 closed. Three new LOW or INFO (F-OB-10 to 12) on the B13 backlog.

## 2026-09-09T12:06:00+00:00 | Desktop UI audit review: 24 of 26 resolved, FAIL on one MEDIUM, five items sent to the LOW screens builder

- docs/reviews/b13-13-ui-audit.md at 06166cb: the 26 P1 and P2 items replayed on production at 1512 px, 24 RESOLVED, 2 PARTLY, 0 still open; 30 of 30 figures unchanged on the two live policies (only the voided policy's summary moved, which is the UI-022 fix); 375 px scrollWidth equal to innerWidth on the policy page with folds open, reconciliation, statements and console; 32 role-and-path renders with 0 stack trace; inbox anchors byte-identical; the approvals form unchanged; badge counts equal to the lists. FAIL on F-UA-01 (MEDIUM): the broker home still prints the future endorsement's premium for CGP-01707, UI-004 was closed on the ops list only. F-UA-04 (MEDIUM, pre-existing since the first rebuild): at 375 px the journal block hides 363 px of its table with no scroller. Three LOW (the approvals sentence must also name the cumulative per-claim rule, a fixed-layout header overflow, the approvals table without the new stylesheet), one disclosed (the ops list folds each policy, 203 ms median at the delivered scale), one for week two (stylesheet conventions). The reviewer discloses one accidental POST to the logout route while probing the policy page (no financial effect; the affected measurements were redone signed in).
- The five actionable items were added to the running LOW screens builder; Yoann can start LIVE-0 now (the two production runs), the money flows are unaffected by the two display findings.

## 2026-09-09T12:23:00+00:00 | LOW library sweep merged (bf36b27): nine findings, six checks green, re-review launched

- Claim events ordered by sequence number for the agent-marker reads; the issuance posting takes the policy lock then the operation lock like every other path; the unique index named in the code comment; resolved breaks correlated in SQL with a bounded open-breaks reader; resolved breaks sharing a reference with an open one annotated rather than dropped; constraint names tested on 23505; the MCP not-recorded answer echoes the request id; a correction read tolerant of missing refund totals; three refusal proofs added to check:correction-replay (58 of 58). Builder counts on corgi_test: payment-replay 34, correction-replay 58, reconciliation 39, claims-and-approvals 74, mcp 59, inbox-counts 53, all 0 FAIL; 473 tests, typecheck clean. Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 12:24Z; an independent reviewer re-reads the lock order (twelve sites), the reconciliation reads and the three new proofs.
- Incident reported by the builder: while starting a dev server, a quoted value in its copy of .env.local produced an invalid URL and Next printed the DATABASE_URL_TEST_APP string, including the app_runtime password of corgi_test, into its terminal and a scratch log; the log is deleted, nothing reached the repository (gitleaks clean on every commit), the credential is the disposable database's runtime role. The coordinator rotates it with scripts/set-runtime-role-password.ts once the last builder using corgi_test has finished, and updates .env.local.

## 2026-09-09T12:27:00+00:00 | LOW screens sweep merged (3890300): the two remaining UI MEDIUM and twelve LOW closed, screens stable

- Builder: the broker list on today's terms ($1,253.20 for CGP-01707, F-UA-01), the journal lines scrolling in their own container at 375 px with the page at 375 (F-UA-04), the approvals sentence naming both sub-threshold cases, headers that wrap, the approvals table on the shared stylesheet, one refusal for a repeated as-of parameter, named scroll regions, mode chips on the policy detail and the statement, a non-vacuous per-role inbox test, one arrival per journal entry, the dated earlier attempt on the console 360, no MCP keys card for approvers, the preview wording, the refund banner waiting state, one notices shape. Not built, with proof: named loading states, because seven loading.tsx files answered refusals with HTTP 200 and the loading body (the old F-UI-01); the measurement is recorded in app/layout.tsx and the item re-scoped for week two. 474 tests, build clean, 34 of 34 routes dynamic. Disclosure: corgi_test was unusable for the screen checks (only ops@example.com seeded there, the staff workspace read over 1109 policies never returned), so the builder's local server read the trial database through the read-only runtime role, GET only.
- Merged with --no-ff, pushed as code alone, deployed and confirmed by /api/health at 12:28Z. A confirmation reviewer measures the seventeen items on production. The screens are now stable for Yoann's own UI and UX pass and for the live steps.

## 2026-09-09T12:45:00+00:00 | LIVE-0 by Yoann: reconciliation run twice, the statement re-run hit the wrong affordance; three findings from his comments

- Yoann ran "Reconcile both sources now" twice on production (12:42:38Z and 12:43:00Z): Stripe 34 provider records against 7 ledger records, 7 matched (the endorsement payment of CGP-01707 now matched, F-INT-06 closed), 28 provider-only breaks, all probe payments created in the shared Stripe sandbox by the check scripts of the day's reviews (22 at the 06:00Z cron, 6 more at 12:42Z); claim rail 3 matched, no break. He did not understand the count and its change: the board explains nothing about probes (F-YA-10, decision put to him). He then used "Re-run with this knowledge cutoff" on the Redwood 2027-09 run page: revision 5, identical to revision 4, cutoff 2026-09-08T17:26:29Z kept by design (rule 10), so the September 2026 statement is still to run from the "Run a statement" panel with the month 2026-09 and an empty cutoff (F-YA-11 wording). No success notice appeared after the click (F-YA-09, MEDIUM, a builder launched: one notices pattern after every action, a per-button loading state).

## 2026-09-09T12:48:00+00:00 | LOW library sweep re-review PASS at bf36b27

- The reviewer enumerated the thirteen advisory-lock sites (twelve before the sweep): exactly two transactions hold two locks, both policy then operation, no cycle possible; the decisive agent-marker reads order by sequence number and the two remaining recorded_at orderings are display only; the constraint names tested on 23505 exist in the database with those names; 49 of 117 correction_rebook payloads on corgi_test lack the totals and 90 policies now read without a throw; the three new refusal proofs build the states they claim and the refund figures were recomputed by hand (122653 corrected against 121307 uncorrected). Checks on corgi_test: payment-replay 0 FAIL, reconciliation 39 of 39, correction-replay 58 of 58, claims-and-approvals 74 of 74; production figures unchanged. Verdict PASS. Four LOW registered (F-LS-01 to 04), the first folded into the breaks-board decision F-YA-10.

## 2026-09-09T12:51:00+00:00 | Screens sweep confirmed on production: 16 of 17, one residual item in a short fix cycle

- docs/reviews/b13-14-low-screens.md at 3890300: the broker and ops lists identical cell for cell ($1,253.20, "on the policy record"), the approvals text naming both sub-threshold cases, 0 px header overflow and a 122.5 px references fold, the policy page at 375 px with 17 of 17 amounts reachable inside the journal's own scroller, one value named on a repeated as-of, no counter-named region left in the repository, one scrollIntoView on a two-fold entry, the per-role inbox test proved non-vacuous by mutation, 32 renders with 0 stack trace, anchors and the decision form byte-identical. One FAIL: the console 360 timeline table still prints an earlier decline beside the succeeded chip (F-LU-01). Four LOW to fix with it (a thead for the journal lines at phone width, a role on the scroller, the mode chips on the customer view, the customer list wording) sent to the feedback builder already on app/; three LOW disclosed. The reviewer agrees with leaving route-level loading states out (F-B13-31 re-scoped for week two).

## 2026-09-09T13:00:00+00:00 | Interface handed to a separate Claude session; the last UI builder stopped

- Yoann at 14:55 local: the interface is being redone end to end in another Claude session working directly, without sub-orchestration, because the screens as they stand would lose the case. He asked to stop the running UI builder (notices after every action, loading submit buttons, the five residual screen items): stopped at 13:00Z; whatever it committed stays on its worktree branch, unmerged, for the UI session to pick up or ignore. From here the coordinator launches backend builders only; every UI change comes from that session on its own branch and is merged into main with --no-ff by the coordinator after an independent review. Seven register lines re-assigned to the UI session: F-YA-09, F-YA-11, F-LU-01 to F-LU-05.
- Backend track proposed to Yoann while the interface is redone: the honest breaks board (probe classification, explained annotation), two more MCP tools (explain_amount, list_my_activity), automatic monthly statements on the daily cron, corrections aligned with decision 24, guards on the rate tables, the remaining backend LOW items.

## 2026-09-09T13:05:00+00:00 | Backend track launched: five slices in parallel, one adversarial reviewer each (decisions 28 to 32)

- While the interface is redone in Yoann's separate session, the coordinator runs backend work only: the honest breaks board (probe classification plus append-only explanations, migration 0022), two read-only MCP tools (explain_amount, list_my_activity), automatic monthly statements on the daily job, corrections aligned with decision 24, guards on the rate tables (0023) and the key-creator layer of the maker-checker trigger (0024). Each slice is built in its own worktree, reviewed adversarially at its SHA by an independent agent that tries to break it, with one fix round if the review fails. The coordinator then merges with --no-ff, re-proves the guards on an ephemeral database migrated to 0024, applies 0022 to 0024 to the trial database, deploys, and launches a production confirmation.

## 2026-09-09T13:10:00+00:00 | Interface redesign session coordinated: branch ui-system

- The session redoing the interface (started 15:15 local on Yoann's approval: a new two-level shell, a sticky page band, common components, all 34 routes) works on branch ui-system cut from 1b37034, never commits on main, merges origin/main before handover; the coordinator merges with --no-ff and runs the independent UI review on production afterwards. It takes F-YA-09, F-YA-11 and F-LU-01 to F-LU-05 into its common layer (Toaster, SubmitButton). It keeps the three touches the backend track makes under app/ (the explain-this-break form and the two reconciliation headings, the generic inbox over lib/inbox/sections.ts, the running-total sentence on the two correction pages) and the measurable invariants (five demo logins, the sandbox sentence on / and /login, the LIVE SANDBOX and LOCAL SIMULATOR labels on every screen). It touches nothing under lib/, db/, app/api or scripts/ except two new read-only files, lib/ledger/read.ts and lib/ui/views.ts.

- Incident, 15:13 local: that session created its branch by switching the shared checkout for two minutes, so the first commit of this entry landed on ui-system; it cherry-picked it back onto main and now works only in its own worktree (.claude/worktrees/ui-system). The duplicated entry that followed was removed here.

## 2026-09-09T15:16:00+00:00 | Backend track, first workflow done: three slices PASS and merged, two in a second fix round; migration 0024 on the trial database

- Workflow corgi-backend-five, 14 agents, two hours: monthly statements PASS at ffa1b69 (docs/reviews/backend-monthly-statements-r1.md, seven LOW and one INFO), corrections on decision 24 PASS at 0510c1b (backend-corrections-rule-24-r1.md, five LOW and one INFO), the key-creator layer PASS at 845443a (backend-guards-and-key-trigger-r1.md, three LOW and four INFO; migration 0023 on the rate tables was not written because the builder and the reviewer verified independently that brokers and state_tax_rates already carry the append-only guards, F-INT-09 was wrong on that point, two assertions added to the guards proof instead). The three merged with --no-ff into main at 19c4edf, typecheck and 479 tests clean, migration 0024_mcp_key_creator.sql applied to the trial database at 15:07Z, pushed and deployed (health check 19c4edf). Guards proof on an ephemeral database migrated 0001 to 0024: 194 of 194 PASS at 15:22Z (two more assertions than at 0021: the owner cannot change brokers.commission_rate_bps nor state_tax_rates.rate_bps), the database dropped afterwards.
- The breaks board (FAIL twice: an explanation keyed on the break alone never expired, fixed by binding the note to what it explained; then the daily window no longer widened back to explained breaks, plus a duplicate migration number 0024) and the MCP tools (FAIL twice: the figure gate decided which key a customer may name, never what the answer contains, so commission lines and journal ids could reach a customer key) go to a third round: the breaks slice renumbers its later migration to 0025 and restores the wide window rule for the daily job, the MCP slice refuses customer keys on explain_amount outright. The second-round workflow was cut by the session limit at 17:08 local and is relaunched after the login.

## 2026-09-09T15:20:00+00:00 | Interface rework merged (ui-system at e4cb118, main 489a36d), deploy in progress

- The interface session handed over branch ui-system at e4cb118 with main merged in and no conflict: 83 files, a two-level shell, a sticky page band, common components, all 34 routes, decisions 33 to 41 recorded. Coordinator checks before the merge: the branch exactly on top of main, nothing under lib/, db/, app/api or scripts except two read-only readers (lib/ledger/read.ts, lib/ui/views.ts), no secret over the branch, no dash, the sandbox sentence on / and /login, LIVE SANDBOX and LOCAL SIMULATOR labels present. Its independent review (docs/reviews/ui-system.md, PASS at 2af6ebe): no HIGH or MEDIUM, eight LOW of which six fixed on the branch; the reviewer ran check:inbox-counts (all passed), compared 49 to 52 forms with every POST contract identical, refusals 307 and 404 unchanged, AF-02 labels visible on 25 screens, 30 of 30 figures unchanged on CGP-01707 and CGP-01274, ledger debits equal credits at 4262730 cents. Closed by the rework: F-YA-05, F-YA-09, F-YA-11, F-LU-01 to F-LU-05. Merged with --no-ff at 489a36d, typecheck clean, 496 tests, pushed as code alone, deployed and confirmed by /api/health at 15:21Z. The interface session runs its adversarial screenshot loop against production after the deploy, as a fix cycle on a new branch.
- Pending on main from the backend track, not yet in the rebuilt pages: the explain-this-break form and the two reconciliation headings, the two inbox sections of the monthly statements, the running-total sentence on the correction pages; the rebuilt pages leave room for them.

## 2026-09-09T15:25:00+00:00 | ui-fixes b928f2e merged (aa76888): Yoann's 17:30 feedback on the homes

- The interface session verified production at 489a36d from outside (22 screens across the three roles, refusals 307 and 404 unchanged, the sandbox sentence verbatim on / and /login) and handed over branch ui-fixes: icons on the overview section cards, the landscape banners moved to a footer band on /ops, /broker and /customer, the band vignette without its framed square, no header row over an empty statement table. Six files under app/ and components/, no form or route touched, no dash, no leak; merged with --no-ff, typecheck clean, 496 tests, pushed as code alone, deployed and confirmed by /api/health at 15:26Z. The adversarial screenshot loop starts on that branch at 18:00 local when its session limit resets; further batches are merged on the same terms (presentation-only batches on typecheck and screenshots, anything touching a form, a figure, a role or a label through the independent review).

## 2026-09-09T15:28:00+00:00 | A 375 px defect of the rework found on production and fixed (944003a)

- The interface session re-measured production after the merge with the freeze invariants: three staff screens (reconciliation, console, console ledger) scrolled sideways at 375 px because the hidden data table behind each chart kept its 640 px minimum width; the policy page held. Fix on ui-fixes (1997f1c, two files: the hidden data in a div, the band static under 800 px), merged with --no-ff at 944003a, typecheck clean, 496 tests, pushed as code alone, deployed and confirmed by /api/health at 15:29Z. Registered as F-UIS-09 (MEDIUM, fixed after the merge); the screenshot loop re-measures it on production.

## 2026-09-09T16:08:00+00:00 | Backend round 3 PASS on both slices, merged locally, guards proof at 0025 running before the trial migration and the push

- Breaks board PASS at 48d6e63 (docs/reviews/backend-breaks-board-r3.md, five INFO and one LOW open): the daily window back on the wide rule, the console 360 on the shared rule, the probe rule's residual stated, the README without a deployment claim, the check scoped to its own policies, the reopened-row sentences, the later migration renamed 0025 with idempotent statements, the guards fixture carrying the new columns. MCP tools PASS at ce42a3c (docs/reviews/backend-mcp-tools-r3.md, four LOW and two INFO open, three LOW carried from round 1): customer keys refused on explain_amount before any read, the enum validated by the transport, the tool annotations no longer overridable, the visibility rule exported and tested, check:mcp 78 of 78; fe18216 on top is a one-line comment correction, merged as the branch tip. The reviewer of the MCP slice notes the branch was edited by a second process during the round; every figure in the code was re-run by the builder and the reviewer read the whole diff.
- Merge conflict on app/ops/reconciliation/page.tsx between the breaks branch (the explain form, the probes and explained headings, the reopened-row sentences) and the interface rework: resolved by keeping the rebuilt page; the interface session, which owns app/ now, ports those touches from the branch's version of the page onto the rebuilt one. Typecheck clean on the merged tree. The two stale worktrees of the run killed at 17:08 removed. Next, in order: the guards proof on an ephemeral database migrated to 0025, migrations 0022, 0023 and 0025 on the trial database, the push, the deploy, the production confirmation of the two slices.

## 2026-09-09T16:31:00+00:00 | Guards 200 of 200 at migration 0025; migrations 0022, 0023, 0025 on the trial database; main pushed (100ef41)

- The guards proof on an ephemeral database migrated 0001 to 0025 passed 200 of 200 (six assertions more than at 0024: reconciliation_break_notes with the three columns of 0025, UPDATE, DELETE and TRUNCATE refused for every role, the runtime role can insert). The first run was stopped by the machine's low-memory manager (the interface session's dev server and three headless browsers hold most of the memory); the second ran detached and completed. Migrations 0022 (break notes), 0023 (probe classification) and 0025 (what a note explained) applied to the trial database at 16:31Z. Main pushed as code alone at 100ef41 with the two round-3 slices and one new reader, countOpenBreaksBySource, for the board tiles (the same rule as the badge, grouped by source; the interface session asked whether tiles should count the rows drawn or the open set: the open set). The explain form stays on the list of breaks to act on only. Deployed and confirmed by /api/health at 16:32Z; the interface session merges origin/main into its port branch after the deploy.

## 2026-09-09T16:45:00+00:00 | Production confirmation of the backend track: everything PASS except the probe count, which needs one reconciliation run

- docs/reviews/backend-production-confirmation.md at 100ef41: the seven MCP tools with honest annotations, explain_amount on CGP-01707 returning $28.20 of tax and $345.20 of commission payable with the same formulas and the same three journal entry ids as the policy page, an unknown figure key refused by naming the closed list, list_my_activity returning exactly the reviewer's six calls; the monthly close last in the daily route with both inbox sections present; the correction preview naming $1,160.54 of additional premium against $500.00; the approver refused on the keys page (307) with the card absent, the deployed trigger carrying the key-creator refusal; the 24 figures unchanged; 34 artefacts with no secret shape and no stack trace, mode labels and the sandbox sentence in place. The one write of the review: one $42.42 probe break explained by ops, the note row read back with the database clock and the break gone from the badge, the inbox, the board and the MCP tool (36 to 35); UPDATE refused for the runtime role.
- FAIL on F-BP-01, expected: the probe classification is stored by a run, and no reconciliation has run since the deploy (the latest complete Stripe run is 16:25Z), so the badge still counts the 35 probes. One "Reconcile both sources now" on production closes it; that click is Yoann's (LIVE-0, YOA-645), and the cron of the 10th falls after the freeze. F-BP-02 and F-BP-03 (the explained and probe headings absent from the rebuilt page, the README ahead of production) close with the interface session's port of the page.

## 2026-09-09T17:08:00+00:00 | Live audit of every posting form (Yoann's request): the toasts and the working state sound, three route redirects completed

- The interface session ran a live audit of every posting form on a disposable database of its own on the test host (created, migrated, dropped afterwards by the coordinator; the trial database verified untouched before and after): every refusal gives an alert toast plus the inline notice, the parameter is stripped from the URL, the working state lands about 30 ms after the click. Four successes said nothing; the three route-side ones fixed by the coordinator: POST /api/policies redirects with ?created=<policy number>, POST /api/statements/run with ?produced=<revision>, and the wrong-role refusals of /api/policies and /api/brokers/kyb redirect to the broker home with ?error=<the refusal sentence> (which also closes F-OB-10: the activity log now records those refusals). The page-side items (the claim reserve's ?reserved= ignored by its page, three toast bodies reading a bare "1", a dead rule on /ops/statements, no toasts on the endorsement approval page, two plain buttons in the console parts) go to the interface session's cycle 2. Pushed as code alone, deployed and confirmed by /api/health at 17:09Z.

## 2026-09-09T18:04:00+00:00 | Interface cycle 2 merged (ui-fixes 0d2b50d, main c408cb3), deployed; independent review launched

- The interface session handed over cycle 2 with the breaks-board port: one sidebar with folding submenus, the AF-02 mode words as one line in the top bar of every signed-in screen plus LOCAL SIMULATOR on simulated rows, a staff search field in the top bar, the inspector and the amount explanation as a right-hand drawer, chips and references that never break mid-word, the restating charts removed, the journal lines as an aligned table, the overview on one screen, a filterable inbox, the claim in two views, the documents folded into the policy overview, the MCP keys page as a provider page (endpoint, header, client snippet, tool list read from the code, never-delegated list), the New broker card rendered disabled pending Yoann's answer, the toast rules for the new outcome parameters; and the reconciliation page with the explain form in the row menu, the probe and explained tables, "N probes, M breaks to act on", the cap sentence, the superseded-note lines and the per-source tiles on countOpenBreaksBySource. Its checks on the merged tree: typecheck, build, 517 tests, the break script at 1024 and 1440 on every screen (1280 and 1920 on the money page), form contracts compared by script on every changed file, the AF-02 line on 15 screens by probe, 375 px readings on four screens with one regression found and fixed before handover (the top-bar line wrapped). Coordinator checks: nothing outside app/, components/, public/ and docs/; 34 distinct form actions on both sides; no money row touched; no dash; no leak; the sandbox sentence and the mode words present. Merged with --no-ff at c408cb3, pushed as code alone, deployed and confirmed by /api/health at 18:04Z. An independent reviewer measures the revision on production (form contracts, the breaks-board port closing F-BP-02 and F-BP-03, AF-02 on every screen, 375 px, the 24 figures, the provider page, the disabled card, regressions).

## 2026-09-09T18:35:00+00:00 | inspect_reference (decision 42) PASS after one fix round, merged and deployed; the investigation script drafted

- Workflow: built at 0b9202d (twelve files, no migration, every query a SELECT; one argument, eight reference shapes recognised before any read, the operational file through the console readers, every list bounded to 20; a customer key refused before any read, a broker key opening only its own book, staff keys everything; the never-delegated list unchanged; check:mcp 93 of 93). Adversarial review FAIL on F-INSPECT-01 (a reference resolving to a broker published one arbitrary policy as the file's policy while the lists spanned the whole book); fixed at 9b9902a (a broker header with null policy fields and the book span stated first, five unit tests, check:mcp 96 of 96), re-review PASS (docs/reviews/backend-inspect-reference-r1.md and r2.md). Merged with --no-ff at a530b84, 534 tests. Vercel created no deployment for that push (no entry in the project's deployment list, a GitHub-side miss); a one-line code commit f4885f3 retriggered it, deployed and confirmed by /api/health at 18:34Z. Open: F-INSPECT-05 (a broker key can tell a refused reference from one that matches nothing; one sentence for both is put to Yoann), F-INSPECT-02, 03, 04, 08 LOW on the backlog. The rehearsed investigation script is in ~/Downloads/corgi-revision-enquete-agent.md (209 lines, figures from the records, the ones depending on the new tool marked to confirm). The coordinator now tests the deployed endpoint with the demo key and documents the hosting.
- Three sessions opened by Yoann at 20:20 local: the live-fire co-pilot (corgi-work-trial-e1, branch live-fire-evidence: expected figures computed read-only, LIVE-0 running on c408cb3), the freeze package (corgi-work-trial-3a, branch freeze-package: evidence pack index, video script draft, final email draft, week-two plan draft, freeze checklist), the Linear scribe (blocked at first, no session reaching Linear since the account change at 17:14 local; Yoann reconnected the connector at about 18:40Z and the scribe applied the whole change set directly: YOA-644, 651, 652, 653, 654, 655 to Done, YOA-656 to In Progress with a comment, five comments from STATUS, three issues created (YOA-657 inspect_reference, YOA-658 the freeze package under the B14 parent, YOA-659 LIVE-3 broker KYB shown live), a project update naming f4885f3 as the deployed revision; two duplicate twins from a Linear 502 at 13:05, YOA-649 and 650, marked Duplicate of 654 and 655). The freeze session found the two inspect_reference records untracked: committed here.

## 2026-09-09T18:38:00+00:00 | Interface cycle 2 review PASS at c408cb3; the coordinator's own test of the deployed MCP endpoint

- docs/reviews/ui-cycle-2.md: 94 rendered POST instances in 49 contracts joined to 35 handlers, no form became a link or a script, every money form with its hidden fields; the breaks-board port closes F-BP-02 and F-BP-03 (the explained row under its heading, the tiles counting the open set: 35 against 0 rows drawn on a filter); the mode line verbatim on 42 of 42 signed-in renders and the LOCAL SIMULATOR word on every simulated row; 375 px equal on 8 of 8 screens with folds open and 0 of 2,932 wide elements without a scroller; 24 of 24 figures; the provider page with seven tools then, no secret shape; the New broker card disabled with its sentence; 52 renders with 0 stack trace; anchors byte-identical; badge counts equal to the lists. Verdict PASS. Seven LOW: one is F-BP-01 (the probes wait for a reconciliation run), one is the New broker decision, one asks the interface session to surface the verbatim sandbox sentence outside the fold on / and /login (a different sandbox sentence is visible), the rest disclosed.
- The coordinator tested the deployed endpoint at f4885f3 with the demo key, read tools only: tools/list answers the eight tools with run_reconciliation and request_claim_payment marked as writes; explain_amount on CGP-01707 returns premium tax 2820 cents with the formula floor(120000 x 235 / 10000) and the rounding sentence; inspect_reference on CGP-01707 resolves to the policy with 2 money operations and 9 journal entries, bounds stated; on the probe pi_3UDQN7 it returns the latest report (provider_only, $42.42, open 26 hours) and no ledger record; an unknown figure key is refused with the closed list; list_my_activity returns the calls just made with their outcome, rule and correlation id; a call without a key answers 401; GET answers 405 with allow: POST. Latencies 190 to 740 ms. Incident, reported honestly: the coordinator's first test script read the whole key file as the header value and the runtime's error message echoed the file, secret included, into the coordinator's own tool output (not into the repository, not to the terminal, not to any document); the script was corrected to extract the token only. Revoking and re-minting the demo key before the debrief is put to Yoann (a revocation is a new row; the handoff names the prefix cmk_e96f88a4, which would change).

## 2026-09-09T18:40:00+00:00 | Interface finishing round merged (ui-fixes 9674296, main 92379e2), deployed

- The interface session's finishing round: the sidebar fits 900 px with the console submenu open, readable band illustrations for Inbox, MCP keys and Search, the stat tiles packed left, the created and produced toasts naming the policy number and the revision (every integer printed), a text option on the toast rules with a test, the search trail's references opening the drawer with their instants, a dead class removed, and Yoann's item: a plain description line under every journal entry on the policy money view and the claim journal with the legend "Credit: where the money comes from. Debit: where it goes. Every entry takes as much as it brings." (the console 360 journal reader returns no description, a lib line for later). Coordinator checks: nothing outside the interface files and lib/ui, 34 form actions on both sides, no dash, no leak. Merged with --no-ff at 92379e2, typecheck clean, 535 tests, pushed as code alone, deployed and confirmed by /api/health at 18:39Z. F-UI2-04 closed as not reproduced by the coordinator's own read of production (the verbatim sandbox sentence is a visible paragraph on / and /login, zero details element open before it).

## 2026-09-09T18:52:00+00:00 | Evidence defect in the cycle 2 review, recapture ordered

- While indexing the evidence pack, the freeze-package session found five of the 24 screenshots of docs/reviews/ui-cycle-2.md to be byte-identical copies of the signed-out login page (a lost cookie mid-capture, the trap the b13-13 reviewer had documented and guarded against); the coordinator verified the md5 values. The record's sentence that every file was opened before being committed is false for those five, two of which are the named illustration of the breaks-board item; the DOM measurements behind the verdict are not in question, and the genuine recon-explained.png carries the F-BP-02 evidence. The reviewer was resumed to recapture the five signed in, list the 24 md5 values and date the correction in its record; registered as F-UI2-08. The evidence pack records it in its Gaps section until the recapture lands.

## 2026-09-09T22:44:00+00:00 | New broker route deployed: /api/health reports 756db05; every item of tonight's list is merged

- Health at 756db05 at 22:44Z. Anonymous checks: `POST /api/brokers` answers 303 to /login (no session), `POST /ops/brokers/reveal/consume` answers 401, /login renders. Migrations 0026 and 0027 are on the trial database, both proven by a 200 of 200 guards run on an ephemeral database before they were applied.
- **Everything on the night's list is now merged and deployed**: interface batch 1 and 2, the demo-account switcher and its hotfix, the New broker route, the F-INSPECT-05 sentence, six live-fire steps, the live-integration screenshot pack, the evidence pack, the US and California rules check, the compliance matrix.
- Remaining before the freeze, none of it code: Yoann's demo key revocation and re-mint (a token may now carry an expiry), his validation of the three drafts and the copy of the week-two plan into the repository, the walkthrough rehearsals, the video, the final email. Coordinator at the freeze: the F-NEWBROKER-02 union line if time allows, the co-pilot's 54-figure re-read on 756db05, the corgi_test runtime password rotation, worktree cleanup, gitleaks over the history, and the SHA in the email.

## 2026-09-09T22:44:00+00:00 | New broker route merged and pushed (756db05): decision 52 delivered, three review rounds

- `POST /api/brokers` for staff_ops only: broker and sign-in user in one transaction, a 20-character one-time password stored as a scrypt hash and shown once through an HttpOnly SameSite=Strict cookie scoped to /ops/brokers, consumed on paint by `POST /ops/brokers/reveal/consume` with a 120 s ceiling; seeded demo users keep the shared demo password (migration `0027_users_password_hash.sql`, nullable column). The disabled New broker card is live.
- **Three review rounds**: r1 FAIL (one MEDIUM, the cookie could be broken by a crafted contact address), fix, r2 FAIL (the fix missed the vertical bar, which is the value's own separator), coordinator fix plus two wording fixes, r3 **PASS** at 2998117 with two LOW that are also fixed (d3e45b3). Records `docs/reviews/backend-new-broker-r1.md`, `-r2.md`, `-r3.md`; register F-NEWBROKER-01 to 10.
- Coordinator before the push: **guards proof 200 of 200 on an ephemeral database migrated 0001 to 0027** (database dropped), `npm run migrate` applied 0027 to the trial database at 22:43Z, merge conflicts on README.md and the login route resolved by keeping both sides (the shared refusal sentence kept, the duplicated secureFlag dropped since batch 2 exports it once), typecheck clean, **572 tests (571 pass, 1 skipped)**, `check:new-broker` 45 PASS 0 FAIL 1 SKIP.
- One line stays open for the next session: F-NEWBROKER-02, adding "broker creation" to the ActivityRule union in `lib/observability/log.ts` and naming it on the two descriptors, so the console Rule column stops reading "none" on their own refusals.

## 2026-09-09T22:11:00+00:00 | Interface batch 2 deployed: /api/health reports 8f3cd7c

- Health at 8f3cd7c at 22:11Z. Anonymous checks after the deploy: POST /api/mcp without a key answers 401, /ops/mcp-keys (Access tokens) answers 307 to the session read. Migration 0026 is on the trial database, so a token may now carry an expiry.

## 2026-09-09T22:00:00+00:00 | Interface batch 2 merged and pushed (8f3cd7c): access tokens with expiry, billing view, correction entry point, claims and statements

- Five independent reviews PASS on `origin/ui-evening-2` at 79a96c7 (records merged under `docs/reviews/ui-evening-2-*.md`). Coordinator before the push: the last em dash removed from a source comment (F-LT-14), typecheck clean, 558 tests (557 pass, 1 skipped), **guards proof 200 of 200 on an ephemeral database migrated 0001 to 0026** (log `guards-ephemeral-0026.log`, database dropped), `npm run migrate` on the trial database applied `0026_mcp_key_expiry.sql` at 22:00Z, then the code pushed alone at 22:00Z.
- Decisions 55 to 69 recorded. README corrected on the two stale key sentences (a key may carry an expiry; unknown, revoked and expired answer the same detail-less 401), closing F-TK-04. Open LOW carried to week two: F-EV2-09 (drawer over the AF-02 line between 801 and 887 px, so no submission screenshot below 888 px with a drawer open), F-BL-14 (tests for the Billing pure functions), F-LT-05 (disclosed in the README), F-LT-13, plus the CHECK on mcp_api_keys.label and the ActivitySubjectKind "user" row.
- Still in flight: the New broker route (decision 52). Its independent review returned **FAIL** at 512c826 with one MEDIUM (F-NEWBROKER-01: the reveal cookie value is written into Set-Cookie unescaped and the address check accepts a semicolon or a comma, so a crafted contact address can truncate or extend the cookie) and four LOW; the workflow's fix round is running.

## 2026-09-09T21:44:00+00:00 | Batch 2: five PASS verdicts on origin/ui-evening-2 at 79a96c7, ready for the next session's merge

- Correction to the wrap-up: the screens and late-slices reviews finished PASS after the handover; five records on the branch. Remaining before the merge: an em dash in a source comment (F-LT-14, app/policies/[policyId]/page.tsx line 243), the guards proof with 0026, the trial migration, then merge, push code alone, docs. The F-LT-05 sentence (a session becomes the approver without a second credential on this demo) is in the README now. Details in `docs/handoffs/interface-batch-2-handover.md`.

## 2026-09-09T21:40:00+00:00 | SESSION WRAP-UP at the usage limit (coordinator session corgi-track-policy-admin): state of everything, for the next session

**Production**: ff88934 on /api/health at 21:37Z; 66fb863 (the F-INSPECT-05 sentence) pushed at 21:39Z and deploying. **main == origin/main** after this docs push. Freeze at 2026-09-10T05:50Z; runbook `docs/handoffs/freeze-checklist.md`.

**Done tonight and merged**: six live-fire steps by Yoann (LIVE-0, 8, 9, 7, 3, 10; `docs/handoffs/live-fire-day2.md`), the interface batch 1 (d002f77), the demo-account switcher and its hotfix (4f10705, ff88934), the live-integration screenshot pack, the evidence pack index, the US and California rules check, the compliance matrix section 4, decisions 43 to 54.

**In flight at the limit, NOT merged, for the next session**:
1. Interface batch 2, branch `ui-evening-2` (interface session corgi-ui-redesign): migration `0026_mcp_key_expiry.sql`, access tokens with the reveal cookie and the consume step, F-LIVE-01 (224150f), F-EV-02 fix, Billing view (43), KYB wording, refund toasts, F-LT fixes. Three reviews were running (late slices, tokens on the built server, billing and screens re-reviews). To merge: wait for or re-run the reviews, run the guards proof on an ephemeral database with 0026 and 0027 (`scratchpad guards-ephemeral-0026.ts` pattern), run `npm run migrate` on the trial database BEFORE the push, merge --no-ff, push code alone, read /api/health, then the docs commit with the DECISIONS text (from 55), the register lines and the two stale README lines on keys (an MCP key may expire; unknown, revoked and expired answer the same 401).
2. New broker route (decision 52), branch `new-broker-route` at 6aa11e4 (opus builder, 543 tests reported): migration `0027_users_password_hash.sql`, `lib/auth/password.ts`, login check, POST /api/brokers, reveal cookie and consume route, `scripts/check-new-broker.ts`, README sentence. Its independent review was running in workflow wf_bd4e916d-509 when the limit hit; the record, if written, is in that worktree. Not merged without a PASS. The KYB wording rule for app/ops/brokers/page.tsx (F-LIVE-03) is to apply on that branch.
3. F-INSPECT-05 stays PARTIAL: the existence-probe half (F-INSPECT05-01) awaits Yoann's choice.
4. The co-pilot's re-read of 54 figures on the final SHA (script on `live-fire-evidence`), the freeze-package session's final refresh of `docs/EVIDENCE-PACK.md` (asked at 21:39Z), and the Linear last pass by the scribe.

**Yoann's own list**: revoke the demo MCP key `cmk_e96f88a4` on /ops/mcp-keys, mint the replacement after batch 2 (with an expiry) and paste the secret into `.local/mcp-demo-key.txt` only; read and validate the three drafts (`docs/checkpoints/t-plus-48h-email.md` on branch `freeze-package`, `~/Downloads/corgi-video-script.md`, `~/Downloads/corgi-week-two-plan-DRAFT.md`, which must be copied into the repository as `docs/WEEK-TWO-PLAN.md` once validated); the five disclosure sentences from `docs/reviews/us-rules-check.md`; the walkthrough sessions with `~/Downloads/corgi-revision-trois-histoires.md` (now carrying tonight's production figures); the video.

**Freeze-time list for the coordinator** (in the checklist): rotate the corgi_test `app_runtime` password with `scripts/set-runtime-role-password.ts` once no builder uses corgi_test; remove the `.env.local` copies and `.local` directories from the agent worktrees when every session has finished; gitleaks over the history; `/api/health` SHA in the email.

## 2026-09-09T21:38:00+00:00 | F-INSPECT-05 built and reviewed PASS, merged locally (66fb863); New broker route built, under review

- Workflow of 22:48 local (two opus builders in worktrees, one adversarial reviewer each): `inspect_reference` answers one sentence when nothing matches (decision 53), test and check-mcp assertion updated, review PASS (`docs/reviews/backend-inspect-05-r1.md`); coordinator checks on the merged tree: typecheck clean, 541 tests (540 pass, 1 skipped). The New broker route (decision 52) is built at 6aa11e4 on new-broker-route (543 tests reported by the builder) and its reviewer is running; merge on PASS, guards proof on an ephemeral database with 0026 and 0027, then deploy in the batch-2 window.

## 2026-09-09T21:37:00+00:00 | Hotfix deployed: /api/health reports ff88934

- The sidebar account name truncates again on every signed-in screen. Production ff88934; main and origin at 3318a70 plus this entry. Next deploys: interface batch 2 (migration 0026) and the New broker route (migration 0027), each after its review; then the co-pilot's single re-read of 54 figures on the final SHA.

## 2026-09-09T21:36:00+00:00 | LIVE-10 done by Yoann on production (4f10705), evidence merged (2205ac9); hotfix ff88934 pushed

- **Claim payout path on CGP-01707** (cancelled policy, loss 2026-09-09 during cover): CLM-00214 opened, reserve $3,000.00 at 21:13:53Z then $2,500.00 at 21:17:27Z (two rows); bank account verified on the labelled simulator (routing 110000000); request A $1,200.00 and B $500.00 waiting for approval, C $2,000.00 refused by the reserve ceiling sentence; ops saw "your own request" on its own row; approver Alex approved A (21:22:38Z), B (21:23:43Z) and a later D $300.00 (21:29:53Z); A, B and D sent on the simulated rail; A force-settled through the simulator control then returned (account_closed) at 21:30:45Z, claim_payment_returned posted; final position paid $800.00, reserve $1,700.00, incurred $2,500.00, limits untouched. Every figure agrees with the read-only prediction.
- **Honest gap, stated in the handoff**: the approver's reject was not driven tonight (Yoann approved A, B and D and declined a fifth request: "c'est bon, ça y est"); a rejection by Alex exists from day 1 on CLM-00212 (2026-09-08). Not readable before the freeze: the automatic settlement of B and D by the daily job on 2026-09-11. Two interface items sent to the interface session (bank account form only in the row menu; the Approval column naming the requester instead of the decider).
- The switcher hotfix (ff88934) was pushed alone at 21:36Z right after "LIVE-10 committed"; /api/health watched.

## 2026-09-09T21:17:00+00:00 | Switcher hotfix merged (switch-fix fe6b9d1, merge ff88934): account name wrapping on every signed-in screen, F-LT-05 disclosed

- The late-slices reviewer of batch 2 found a regression on 4f10705: the sidebar account name wraps into the role line at 1024 and 1440 (F-LT-04, MED). Hotfix: CSS only plus two wording changes on the switch route (F-LT-06, F-LT-07). Coordinator checks: no dash, typecheck clean, 537 tests (536 pass, 1 skipped). Pushed in the next window the co-pilot gives (LIVE-10 in progress on 4f10705).
- F-LT-05 (MED, disclosure): the switch lets any demo session become the approver in one click; the README now says that the maker-checker control on the demo rests on the closed list of six accounts and that the ledger-side proof is unaffected. Hardening (refuse a switch into staff_approver from a non-staff session, or an environment flag) is a week-two line for Yoann's plan.

## 2026-09-09T21:11:00+00:00 | Switcher deployed: /api/health reports 4f10705

- Health at 4f10705 at 21:11Z. Anonymous checks: POST /api/session/switch signed out answers 303 to /login with no Set-Cookie, GET answers 405. The co-pilot re-reads the LIVE-10 before-state on this revision and gives Yoann the go.

## 2026-09-09T21:10:00+00:00 | Demo-account switcher reviewed PASS, F-SWITCH-01 fixed, pushed at 4f10705

- Independent review `docs/reviews/demo-switch-r1.md` (PASS, five findings, no HIGH): the email list is a code constant checked before the single select, the cookie comes from the same signSessionCookie as login with the same flags, refusals log as refused with rule "sign in". Measurement limit stated by the reviewer: no node_modules in its worktree, so claims rest on code and the shipped classifier, not on curl against a built server.
- F-SWITCH-01 (MED) fixed by the coordinator before the push: the actor must be a demo account too, otherwise a broker created tonight through POST /api/brokers could become staff in one POST. Typecheck clean, 537 tests (536 pass, 1 skipped). Pushed with the switch-only merge (8d567cc), the docs (d30d99d), the screenshot pack (c4618e5) and the fix (4f10705) as HEAD; /api/health watched for 4f10705. The co-pilot gets the SHA for LIVE-10.

## 2026-09-09T21:07:00+00:00 | Live integration screenshot pack imported (docs/evidence/live-integration-2026-09-09/)

- Prepared by a desktop session at Yoann's request (Stripe dashboard captures through the browser, 20:12Z to 20:23Z), reviewed by an independent Codex reviewer (its record `REVIEW-NOTES.md` inside the folder, PASS for the bounded claims), handed over by Yoann at 23:07 local. 14 images (10 fresh Stripe captures, 4 earlier application captures byte-identical to their repository sources), a manifest with SHA-256 per file, `SHA256SUMS.txt` verified on import (14 of 14), `00-START-HERE.md` as the reading guide.
- Coordinator checks before import: gitleaks clean on the text files, no dash, four sensitive captures opened (webhook destinations, delivery responses, the Harbor Point connected account): sandbox banner visible on each, no signing secret, no API key, only event and account identifiers and the synthetic broker2@example.com contact. Imported unchanged, `.DS_Store` excluded. The freeze-package session indexes the folder in `docs/EVIDENCE-PACK.md`.
- What the captures prove: the deployed `/api/webhooks/stripe` URL is registered twice on the sandbox (connected accounts, platform payments), the issuance ($1,253.20), the cancellation refund ($2,081.09) and the correction collection ($53.84) events were delivered with HTTP 200 and the application's `received: true, status: done` response, and the Harbor Point account.updated event reached the endpoint. The claim payout rail and the bank check stay LOCAL SIMULATOR and are not claimed as live.

## 2026-09-09T21:03:00+00:00 | Demo-account switcher merged locally (switch-only, merge 8d567cc), focused review running, deploy before LIVE-10

- Yoann asked for the switcher on production ahead of batch 2. Two commits cherry-picked by the interface session onto origin/main a7279a6 (menu, route, lib/auth/demo-accounts.ts with test, activity row naming both roles, and the F-EV-08 375 px fix). Coordinator checks: gitleaks clean, no dash, typecheck clean, 537 tests (536 pass, 1 skipped). Decision 54 recorded, README sentence added, F-EV-08 fixed. An independent reviewer (docs/reviews/demo-switch-r1.md, on its way) attacks the route on the built server; the push waits for its verdict and for the co-pilot's confirmation that Yoann has not started LIVE-10 clicks.

## 2026-09-09T20:57:00+00:00 | LIVE-3 done by Yoann on production (d002f77), evidence merged (2443843): the five live-fire steps of day 2 are done

- **Two refusals read first**: Harbor Point (failed, verification_failed_tax_id_match) draft CGP-01708, Pay disabled with the Stripe reason; Sierra Crest (never submitted) draft CGP-01709, Pay disabled with "No verification on file for this broker".
- **Live pending to approved**: Sierra Crest submitted at 20:50:41Z (EIN 000000000, address_full_match, the production URL as website): connected account created, status pending with the settling sentence, history row "verification pending"; account.updated webhook received 20:51:44Z; an early "Check at Stripe" inside the window answered "still pending, nothing appended"; at 20:53:53Z approved, second history row, binding allowed, ops list "approved", Pay active.
- **Beyond the brief, disclosed**: Yoann then paid CGP-01709 with the test card at 20:54:28Z ($12,000.00 annual premium typed, charge $12,307.00, commission $1,800.00, bound, effective 2026-09-26); sandbox only. Sierra Crest's September statement is no longer empty and the next reconciliation matches one more payment; CGP-01708 stays a refused draft.
- Two LOW registered (F-LIVE-02, F-LIVE-03): the broker's Webhooks view on the console says no provider reference while the account id is on the broker; the policy Broker card prints a "seeded placeholder" KYB sentence for a broker that never submitted. Both sent to the interface session. Every figure of LIVE-3 agrees. Interface batch 2 may deploy; the co-pilot re-reads all figures on the batch-2 SHA.

## 2026-09-09T20:46:00+00:00 | Yoann's four answers: decision 44 to week two, demo key to re-mint, New broker route built tonight (decision 52), F-INSPECT-05 one sentence (decision 53)

- Recorded in DECISIONS. The New broker slice starts now as an opus builder with an independent reviewer (route, migration adding a nullable password hash to users, login check, reveal cookie, check script); F-INSPECT-05 is a second small builder in the same run. The demo key is revoked by Yoann on /ops/mcp-keys and re-minted after interface batch 2 is on production. LIVE-3 (KYB live) is briefed by the co-pilot on d002f77.

## 2026-09-09T20:39:00+00:00 | LIVE-7 done by Yoann on production (d002f77), evidence merged (3d23e92): the four live-fire steps of day 2 are done

- **Cancellation of CGP-01707 with the open claim**, confirmed by the broker 20:32:36Z effective 2026-11-08: 61 of 365 days, earned 38629, unearned 224877 in four refund_requested entries (81374 + 5260 + 28108 + 110135), tax back 5285, refund 230162 ($2,301.62), clawback 33731 ($337.31), identical to cancellationBreakdown on the three segments and to the preview; refund_payable credited, queue 4 waiting.
- **Maker-checker**: ops on /ops/approvals refused (four rows "not an approver", no decision form); approver Alex Kim approved the four at 20:34:28Z to 20:34:32Z with the intent text and hash; ops sent the four refunds at about 20:36Z ($287.69, $832.85, $1,127.24, $53.84, newest collection first); webhooks refund.created and refund.updated (second delivery answered "already posted"), charge.refunded ("confirmation only"); four refund_completed at 20:36:27Z to 20:36:28Z; requested versus completed explicit on the money view, clearing list empty.
- **Commission clawback** 4216 + 12206 + 16520 + 789 = 33731; commission payable after $57.94 (15 percent of the 38629 earned, floored); unearned premium $0.00; claim CLM-00213 open, reserve $2,000.00 and paid $0.00 untouched.
- Every figure of the four steps agrees with the read-only predictions; the day's one slip is the co-pilot's $2,722.00 brief figure in LIVE-9, disclosed. The handoff carries the freeze checklist section D lines for LIVE-0, 8, 9 and 7. Two more interface items went to the interface session (no ops notification for approved refunds, no per-send feedback). Interface batch 2 may now deploy.

## 2026-09-09T20:22:00+00:00 | Interface batch 1 deployed: /api/health reports d002f77

- Pushed alone at 20:21Z, health at d002f77 at 20:22Z. Anonymous checks after the deploy: /ops/ledger and the old /ops/console/ledger answer 307 to the session read, POST /api/mcp without a key answers 401, the sandbox sentence is on /login. Docs pushed after the code, per the Vercel rule. The co-pilot starts LIVE-7 on d002f77.

## 2026-09-09T20:21:00+00:00 | LIVE-9 done by Yoann on production (92379e2), evidence merged (73f528c); interface batch 1 pushed (d002f77), deployment in progress

- **Second endorsement on CGP-01707**, $2,400.00 to $2,700.00 effective 2026-10-01, requested 20:11:09Z: preview identical to the read-only one (342 of 365 days, premium 28109, tax 660, delta 28769, commission 4216, running total $1,435.06 above the $500 threshold so customer approval); approved at about 20:15Z, paid 20:17:43Z (payment_intent.succeeded): premium written 28109 and tax 660 effective 2026-10-01, collected 28769 and commission 4216 effective 2026-09-09. Cash on the policy $2,721.97 (125320 + 112724 + 5384 + 28769), commission payable $395.25, unearned premium $2,635.06.
- **As it stood** on 2026-09-15: $1,200.00 / $28.20 / $1,253.20 / $1M / $2M; on 2026-09-25: $2,400.00 / $56.40 / $2,481.40 / $2M / $4M; on 2026-10-05: $2,700.00 / $63.45 / $2,788.45 / $2M / $4M. Every figure agrees. The co-pilot disclosed one slip in its brief to Yoann (cash written $2,722.00, the page and the sum say $2,721.97).
- Four more interface items went to the interface session (latest-terms tile, pending endorsement row, broker notification after approval, doubled count on the broker home). LIVE-7 starts after the new SHA is confirmed on /api/health; the refund figures are computed read-only meanwhile.

## 2026-09-09T20:18:00+00:00 | Interface batch 1 merged locally (ui-evening c120944, merge d002f77), deploy held for the LIVE-9 boundary

- Independent review PASS (`docs/reviews/ui-evening-1.md`): 68 files, nothing under db/ or app/api, lib touched only in lib/documents and lib/statements/pdf.tsx; forms unchanged; AF-02 line on nine pages; refusal matrix 12/12; withActivity 38/38; 30/30 figures; hash covers the canonical text. Coordinator checks on the merged tree: gitleaks clean on the 17 commits, no dash, typecheck clean, 535 tests (534 pass, 1 skipped, 0 fail). Decisions 45 to 51 recorded; register F-EV-01 to F-EV-07 (F-EV-02 MED pre-existing: sidebar count badges are dead links in Chromium, fix in batch 2).
- Push and deploy wait for the co-pilot's "LIVE-9 committed" so a live-fire step never straddles two revisions. Code is pushed alone, /api/health read, then this docs commit.

## 2026-09-09T20:30:00+00:00 | Local screenshot pack ready for Yoann's review, not hosted

B14 packaging scope only. Folder: `/Users/yoannabriel/Downloads/Corgi-Live-Integration-Evidence-2026-09-09`. It contains 14 images (10 fresh Stripe dashboard captures through Computer Use and 4 byte-identical earlier deployed-application captures), an English `00-START-HERE.md`, provenance manifest, image SHA-256 list and independent `REVIEW-NOTES.md`. The fresh images show payment/refund events, actual webhook delivery responses (HTTP 200, received=true/status=done), two active deployed webhook destinations and the synthetic Harbor Point Connect account. Earlier images show approved/failed app KYB, correction/ledger and reconciliation. They are explicitly dated as earlier evidence. No payment, refund, approval, replay, credential creation, upload, sharing change, commit, push or deployment was performed by this packaging session.

Checks passed: all 14 images visually inspected; Python/Pillow inventory, dimensions, formats, byte sizes and SHA-256 verification 14/14 with 14 unique hashes; four earlier captures compared byte-for-byte against repository sources, 4/4 identical. JPEG files have their actual .jpg extension; no pixels were edited. Gitleaks 8.30.1 `gitleaks dir /Users/yoannabriel/Downloads/Corgi-Live-Integration-Evidence-2026-09-09 --redact --no-banner` exited 0 with no leaks. This is a text scan, not OCR or a repository/history scan. Independent reviewer `/root/evidence_pack_review` opened all 14 images, independently repeated the identity checks and ran the scanner before and after its report; verdict at 20:27 UTC: PASS for this local pack for Yoann's review only, with no material pack issue identified. This does not close B14 or the integrated trial gate.

Exact gaps remain visible in the index and review: no pending-KYB screenshot, no fresh Redwood provider verification-detail screenshot, four historical app captures including a stale correction badge, and the reconciliation image's four unresolved provider-only breaks plus 32 probes. Restricted Connect capability status is not labeled as the app's KYB decision. Both live slots use Stripe; bank verification and claim payout remain LOCAL SIMULATOR. Upload, read-only sharing and verification from a reviewer session are NOT RUN. AF-01 final deployment/two-role checks, AF-03 product invariants and AF-06 candidate understanding are NOT RUN here; no global AF PASS is asserted.

Diagnostic incident: an initial Stripe accessibility inspection exposed a test PaymentIntent client secret into tool output. Its value was not saved in the pack or reproduced in files. Raw payload output stopped; payloads were collapsed and subsequent diagnostic output filtered. Yoann was informed. The saved images and pack text were checked separately; no credential was found there. The diagnostic exposure is not erased by those checks; no global AF-05 PASS or completed credential remediation is claimed.

After context compaction, reread AGENTS, AUTOMATIC-FAILS, WORKFLOW-48H, REVIEWER and current scope-relevant STATUS before this documentation-only closeout. No mandatory kit file found missing. Current observed branch main at `5269397dc14a291bfa2ddcb3d7f10f379f80e2ac`, clean before this note; concurrent sessions advanced it from the initial `81e9680`. About 9 h 20 remained before the freeze. Product tests were not rerun because no product code changed. Next executable step: Yoann reviews the Downloads folder, then selects the sharing service; upload the reviewed files with viewer-only access, exclude Finder metadata and verify the final link from a viewer session. Any later submission claim must retain the disclosed evidence gaps or close them with actual captures.

## 2026-09-09T20:08:15+00:00 | Local live-integration screenshot pack requested by Yoann

Scope: B14 evidence packaging only, through Computer Use, into Downloads for Yoann's review before hosting. Initial branch main at 81e9680; initial working tree clean. About 9 h 42 remained to the 2026-09-10T05:50Z freeze. No product implementation or feature completion claim. Startup reads: AUTOMATIC-FAILS.md, AGENTS.md, READABLE-CODE.md, WORKFLOW-48H.md and REVIEWER.md; general and Track 1 official Notion pages through the open browser; structured BRIEF-REFERENCE; README and PLAN; relevant sections/searches of STATUS, DECISIONS, COMPLIANCE-MATRIX, EVIDENCE-PACK, GAP-REVIEW (20/21), freeze checklist and B2/B3/B5 review records. No mandatory kit file found missing; separately supplied kickoff sandbox rules remain unavailable as already disclosed. Local records are used for this packaging task; Linear readiness tickets have not been queried or modified.

Next acceptance evidence: genuine Stripe premium collection/refund and Connect KYB screenshots, including delivery logs to the deployed webhook endpoint. Planned checks: visible sandbox context, stable object references, screenshots actually opened, no credentials or real personal data captured, image inventory and hashes. The main risk is presenting an old/local fixture or a dashboard summary as proof of a delivery. AF-01/02/04/05 are checked only for captured surfaces; AF-03 product invariants and AF-06 candidate understanding are NOT RUN here. No payment, refund, approval, replay, credential creation, upload, sharing change, commit or push is part of the collection step.

## 2026-09-09T20:04:00+00:00 | LIVE-8 done by Yoann on production, evidence merged (01d6f3e)

- **Backdated correction on CGP-01707**: preview identical to the read-only one (351 of 365 days, 110136 to 115397, difference 5261 + 123 = 5384 cents, commission 789, customer approval required at a running total of $1,153.97); confirmed 19:33:07Z as a correction_reversal effective 2026-10-08 superseding the wrong endorsement plus a correction_rebook effective 2026-09-22; reversal entries 110136 and 2588 at the old date, re-booked 115397 and 2711 at the right date, cash untouched. As it stood on 2026-09-15: $1,200.00 premium, $28.20 tax, $1M / $2M; on 2026-09-25: $2,400.00, $56.40, $2M / $4M. PDFs as of both dates downloaded.
- **Approval and collection**: customer approved $53.84 at about 19:39Z; broker collected, payment_intent.succeeded at 19:50:43Z, correction_premium_collected 5384 and correction_commission_earned 789; cash on the policy $2,434.28, commission net $353.09.
- **Redwood 2026-09 statement**: revision 5 (19:36:34Z) identical to 4 at $429.56 before the collection; revision 6 (19:59:14Z, run 8df46119, 18 lines) cash $8,382.45, premium $8,116.72, commission $1,217.50, clawback $780.05, net due $437.45, hash `5c991578…e84d8` identical to the co-pilot's read-only prediction of 19:55:16Z; revision 7 (20:03:04Z) identical to 6, one more immutable row, disclosed.
- Interface findings of the step went to the interface session (F-LIVE-01 entry point, preview colours, collect button first, Billing view, statement totals as a subtraction, one statement row per month). Next: LIVE-9, second endorsement as broker.

## 2026-09-09T19:54:00+00:00 | LIVE-8 in progress: correction confirmed, customer approved, collection pending; decisions 43 and 44 relayed

- Correction confirmed at 19:33:07Z (reversal and re-book events, entries 110136 and 2588 reversed at effect 2026-10-08, re-booked 115397 and 2711 at effect 2026-09-22); customer approved the $53.84 difference at about 19:39Z; collection waits on Yoann's broker click. Redwood 2026-09 revision 5 at 19:36:34Z is identical to revision 4 ($429.56, same hash), the right answer since the correction alone posts neither cash nor commission; the revision after the collection is expected at $437.45.
- Decisions 43 (Billing view, presentation) and 44 (direct bill by the customer through the same Checkout, a money path) recorded as relayed by the co-pilot session, not built until Yoann confirms them here; 44 is a backend slice with its own review, after LIVE-7.

## 2026-09-09T19:41:00+00:00 | US and California rules consistency check (workflow, 21 agents)

- Asked by Yoann at 21:30 local: many small checkers, one rule each. Record `docs/reviews/us-rules-check.md`; register lines F-US-01 to F-US-07. Checkers on the haiku family alias, refuters and synthesis on the opus alias; 14 checks, 6 refuters, 1 synthesis, 0 errors, nothing written by the agents.
- **Consistent (7)**: the 2.35% California rate and its basis (a checker's medium finding on scope was refuted: Ins. Code 1775.5 taxes the surplus line broker, a different taxpayer); tax base premium only with return premiums deducted (RTC 12221); the itemised tax line as a disclosed interpretation; pro-rata return premium with short-rate representable (Ins. Code 481); incurred = paid + reserve and the limit guard; fiduciary funds avoided because the broker never holds the money (Ins. Code 1733); endorsement premium pro rata for the remaining term.
- **Not modeled (5), disclosure sentences put to Yoann for the README not-built list and the week-two plan**: cancellation notice and grounds (Ins. Code 676.2 and 677.2, LOW); claim acknowledgement and decision clocks (10 CCR 2695.7, MED); producer licence and appointment behind the KYB (Ins. Code 1631 and 1704, MED); sanctions screening of payees before a claim payment or a refund (OFAC, MED); consent to electronic delivery of documents (Ins. Code 38.5 and 38.6, MED).
- **Unverifiable (2)**: the flat $25 policy fee fully earned at issuance (no public source fetched on whether rate regulation permits it; the decision of 2026-09-08T09:29Z already calls the fee an assumption of this build; the record rates it HIGH because a panel would ask); daily pro rata earning against SSAP No. 53, paywalled.
- No code change tonight. This is a consistency check against public sources, never a legal PASS.

## 2026-09-09T19:23:00+00:00 | F-LIVE-01: the backdated correction has no visible entry point (found by Yoann during LIVE-8)

- On 92379e2 the policy band offers Endorse, Cancel and Open a claim; the `Correct` sub-menu item appears only on the corrections route; the sole link is the one-line form under the Endorsements table. Yoann could not find it on his first LIVE-8 attempt. Registered MED (the panel's live-fire item 2 depends on reaching it), passed to the interface session for tonight's batch as its own commit. Not blocking: the direct URL `/policies/<id>/corrections/new` and the Endorsements line work, LIVE-8 continues on 92379e2.

## 2026-09-09T19:07:00+00:00 | LIVE-0 done by Yoann on production, evidence merged (3c67d45)

- **Reconciliation click at 18:38:23Z** as `ops` in the interface: 42 Stripe records against 7 ledger records, 32 probes, 4 breaks to act on ($100.00, two of $12.61, one refund of $8.98), claims rail 1 provider record, 3 ledger, 0 break. Board chip, probes tile and inbox badge agree. F-BP-01 and F-UI2-01 closed on that run.
- **Redwood 2026-09 statement, revision 4 at 18:45:13Z** (run c602abcf, version 3, 16 lines): cash collected $8,328.61, premium collected $8,064.11, commission earned $1,209.61, clawback $780.05, net due $429.56, content hash `7eddb01a…78c` identical to the hash the co-pilot predicted read-only before the click. A first click produced Harbor Point 2026-09 revision 2 ($0.00, identical), disclosed in the handoff.
- Evidence under `docs/evidence/live-fire-day2/LIVE-0/{before,after}` and `docs/handoffs/live-fire-day2.md`, merged with `--no-ff`. LIVE-8 briefed to Yoann on 92379e2 (preview read by GET: 351 of 365 days, $1,153.97, difference $53.84, commission $7.89, customer approval required); LIVE-9 and LIVE-7 not started.

## 2026-09-09T19:06:00+00:00 | Cycle-2 evidence corrected, freeze checklist on main, interface batch announced

- **F-UI2-08 fixed and merged** (c8d679d, merge b33edbc): six PNGs recaptured with a login guard, 24 distinct md5 values in section 11 of `docs/reviews/ui-cycle-2.md`, defect and cause in its section 12. The record stamped the correction `20:55Z`, which is local time (18:55Z); coordinator note added on the record. The evidence pack rows for that folder are stale and the freeze-package session is refreshing them on its branch.
- **F-UI2-04 reopened as LOW, wording only**: the coordinator's closure of 20:39 local read the HTML source; the reviewer's DOM measurement stands and was confirmed in the markup at 19:05Z (verbatim sentence inside a closed details, visible sentence `Work-trial build on sandbox providers and test data. No real money moves here.`). AF-02 holds on the visible sentence; no code change before the freeze.
- **F-BP-01 and F-UI2-01 closed by a production run at 18:38:24Z**: a human staff_ops actor ran `/api/jobs/reconcile` from the browser (activity_log), the Stripe run stored probe_count 32, provider_only 4, matched 7 over 42 provider records, and the board reads 4 breaks to act on. The reviewer transcript holds only login clicks, so the click was a person; the co-pilot session has been asked whether it was LIVE-0 with Yoann. Side effect: the explained break is a probe again, the explained table is empty on 92379e2 (the F-BP-02 closure evidence stays at c408cb3).
- **Freeze package**: deliverable 5, `docs/handoffs/freeze-checklist.md`, cherry-picked onto main at e1e1fe1 (one command and one expected result per line; it states that the cron fires at 06:00 UTC on September 10, after the deadline, so nothing waits on the cron). The T+48h email draft (43233e4) stays on the `freeze-package` branch until Yoann validates it; the week-two plan draft is in `~/Downloads`, not committed, settled with Yoann at the end; WORKFLOW-48H.md expects the plan in the repository, so it is copied in after that session and before the freeze.
- **Interface batch announced by the interface session** (five slices: sidebar and `/ops/ledger` route, inbox empty states, statements bands, brand in PDFs, Access tokens screen with migration `0026_mcp_key_expiry.sql`). Conditions set before merge: an expired token gets the same detail-less 401 as unknown and revoked with a refused `mcp_calls` row and an assertion in `scripts/check-mcp.ts`; the statement hash covers `canonicalText`, so the PDF wordmark does not touch reruns; the one-time secret cookie is Secure, SameSite=Strict, path-limited, server-cleared on first display and redacted; docs of record stay untouched on the batch. Migration 0026 runs on the trial database at deploy time.

## 2026-09-09T18:55:00+00:00 | Evidence pack index merged (docs/EVIDENCE-PACK.md)

- Built by the freeze-package session (14bdd81, merged with --no-ff): 84 records walked (51 reviews, 29 handoffs, 2 checkpoints, STATUS, the matrix), 50 carrying a verdict; 263 evidence files (26.5 MB, 8 directories), 0 orphan, 0 dangling citation among 93 cited paths; 88 real sandbox references (20 pi_, 3 re_, 10 cs_, 23 acct_, 18 evt_, 2 we_, 12 cmk_ prefixes) and 33 Linear ids; one section per review record in slice order with the verdict quoted verbatim and the reviewed SHA, the evidence walk with a line per file, the live-fire steps driven by Yoann, the gaps (F-UI2-08 recapture in progress; five expected duplicate groups, two of which are evidence by construction), the reference inventory. Two notes acted on: a dated coordinator note appended to docs/reviews/b13-13-ui-audit.md and b13-14-low-screens.md pointing at the records that lifted their FAIL, no line of the originals edited. The live-fire table of the pack is re-read once tonight's steps land.

## Earlier status (kept as history)

Updated at 2026-09-08T07:13:45+00:00. Track: **1: POLICY ADMINISTRATION**, explicitly selected by Yoann. Stack and providers: **UNSELECTED**. Repository currently contains instructions and preparation only; there is no product implementation or deployment.

Startup reads: original kit CLAUDE.md, AGENTS.md, WORKFLOW-48H.md and REVIEWER.md; released general and three track briefs supplied in the conversation, extracted in BRIEF-REFERENCE.md. Automatic-fail and readability rules were independently reviewed and corrected. Missing: separately supplied kickoff sandbox rules, chosen stack/providers, actual provider access and product acceptance evidence.

Local documentation is the working status source for this update; the Linear project overview now identifies Track 1, the pending stack discussion and the next-session handoff. Earlier general-rule and readability review corrections remain in force. Tickets YOA-593 (scope), YOA-595 (approval/MCP), YOA-599 (webhooks), YOA-600 (ledger) and YOA-601 (reconciliation) now carry the relevant released-brief overrides, with their existing unchecked scenarios preserved; there is no automatic synchronization. Its 162 checks are not completed tests.

Three independent Sol instruction reviews completed: brief coverage, financial/security rules, practical workflow/readability. Material findings were corrected and re-reviewed with scoped design PASS verdicts; see reviews/instructions-2026-09-08.md for initial findings, coordinator dispositions and minor post-review wording fixes. Product AF-01 through AF-06 verification: NOT RUN. Documentation changes do not establish passing product controls.

Next session: follow HANDOFF-PROMPT.md. Discuss at most two stack options, verify required sandbox access, then agree the stack and prepare the one-page attack plan. This session prepares the handoff only; no product implementation or finalized attack plan was requested. Logging is currently explicit, manual, sanitized entries in DECISIONS.md; no automatic prompt-capture hook or background review process is installed. At a material decision, scope change, completed slice or checkpoint, append a timestamped record identifying user decision versus assistant proposal and checks actually run.

Documentation verification: mandatory relative links resolve, original preparation archive SHA-256 values match its import manifest, and the three active kit copies agree. Gitleaks 8.30.1 was installed locally; a manual `gitleaks dir . --redact --no-banner --no-color` scan passed with no findings. This is a documentation-repository scan, not proof of product secret controls. No automatic hook/CI enforcement has been installed.

The Linear central checklist now has a released-brief override banner. Existing scenario states were preserved; no control was marked passed during these documentation edits.

## 2026-09-08T07:10:53+00:00 | Track 1 handoff

Yoann selected Track 1. Desired extension: one combined impact-preview and amount-explanation flow, after mandatory core correctness. Other brainstormed enhancements are optional. The next agent owns the stack discussion and attack-plan drafting with Yoann. Confirmation email is a draft for Yoann to send on the existing thread; no email was sent by the assistant. Personal explanatory HTML remains outside the repo and is not added to the decision log.

Linear handoff update: project renamed to Track 1: Policy administration, overview simplified into current action/core/differentiator/checkpoints, and YOA-593 linked to HANDOFF-PROMPT.md with its ten unchecked scenarios preserved. The 16 domain tickets remain reference material; detailed execution-backlog creation is deliberately assigned to the next session after stack and attack-plan agreement.


## 2026-09-09T13:06:24+00:00 | Read-only access investigation requested by Yoann

Scope: determine whether the deployed app was used between September 8, 22:40 and September 9, 08:00 Europe/Paris (2026-09-08T20:40:00Z inclusive to 2026-09-09T06:00:00Z exclusive). Branch main; initial revision 1b37034, observed revision at recording f995d48; concurrent work advanced the branch. No application implementation, deployment, provider side effect, test fixture, commit or push performed by this investigation. Existing work preserved. No feature completion or integrated PASS is claimed.

Startup/read receipt for this diagnostic scope: AUTOMATIC-FAILS.md, AGENTS.md, READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, structured released brief docs/BRIEF-REFERENCE.md; README and scope-specific sections/searches of STATUS, PLAN, DECISIONS, COMPLIANCE-MATRIX, GAP-REVIEW, observability/console/back-control review records and the T+24h checkpoint draft. Inspected login/session handling, activity logging and migration 0021, deployment project linkage and cron configuration. No mandatory kit file was found missing; the separately supplied original kickoff sandbox rules remain unavailable as disclosed in BRIEF-REFERENCE. This is an access diagnostic, not a new acceptance-criterion implementation. Local records used; Linear was not queried. Planned/actual checks: timestamp-bounded Vercel request logs, read-only queries against the deployed trial database and separation of browser prefetch, technical health checks and durable business events.

Evidence: Vercel CLI 59.11.7 command `npx --offline vercel logs --project corgi-work-trial --environment production --since 2026-09-08T20:40:00Z --until 2026-09-09T06:00:00Z --limit 1000 --json` exited 0 and returned 30 distinct requests. The same exact interval through Vercel's request-log API returned HTTP 200, 30 rows, hasMoreRows=false. All were GET/200: 27 declared Chrome 152 on macOS (16 explicitly marked prefetch, 11 not marked prefetch); three /api/health requests declared node. Browser requests span 20:40:36.641Z to 20:41:29.458Z: operations home, brokers, claims, approvals, statements, MCP keys, policies, policy 3c3697b7-33f8-45a4-beaf-5a1892fc9483 and its documents. The last non-prefetch browser request fetched its endorsement-schedule PDF. A declarations-PDF request also appears, but is marked prefetch and does not prove a click. Last technical health request: 20:41:42.751Z. No later request was returned before 06:00Z. Metadata was restricted to request IDs, timestamps, paths, method/status, user-agent, prefetch flag, internal referrer and deployment IDs; no cookies, credentials, payloads or IP addresses exported. Sanitized local evidence: /tmp/corgi-access-2026-09-08-sanitized.json and /tmp/corgi-access-2026-09-08-metadata.json (temporary files, not submission artifacts). A supplementary broader pre-window query repeated rows across page numbers and was not used for counts or a completeness claim.

Database checks executed with app_runtime on neondb inside explicit READ ONLY transactions (transaction_read_only=on): every one of 31 public-table creation/recording timestamp queries returned count 0 for the overnight interval, including policy/claim events, money operations/events, journal entries, approvals, statements, reconciliation, webhooks and MCP calls. activity_log's earliest row is 2026-09-09T10:44:03.939Z, after this interval; it cannot establish overnight login coverage. The two scheduled reconciliation runs at 06:00:11.432Z and 06:00:12.387Z were complete with run_by=null, consistent with the daily cron configured for 06:00 UTC (08:00 Paris), outside the requested interval. Narrow project-local agent-history inspection found illustration work and coordinator shell calls around the browser activity; it did not establish attribution of those browser requests.

Finding: browser access to internal pages is evidenced; new business writes and MCP calls were not observed. A browser user-agent and a non-prefetch flag do not prove a human clicked, identify the person, establish that the visitor was external, or connect the session to the T+24h email. The historical login route records no durable session identity; current login activity is anonymous by construction, and only API routes have activity wrappers. Identity and manual-versus-browser-automation attribution remain UNRESOLVED. No product checks rerun because no product changed. Next diagnostic step, only if further attribution is needed: correlate a separately available session identifier or visitor-provided timestamp; existing evidence alone cannot supply one. Trial completion gates and existing feature status are unchanged.

## 2026-09-09T13:41:37Z | Read-only explanation of the 28 reconciliation breaks

Yoann requested an explanation of the deployed reconciliation page and repeated $42.42 amounts. Scope: observation and explanation, no feature implementation. Branch main, inspected revision 0ee1b6e772d205136107899eb3b7ee053acc6663; working tree initially clean. About 16 hours remained before the September 10 05:50 UTC freeze. Actual reads: AGENTS.md, AUTOMATIC-FAILS.md, READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, structured brief docs/BRIEF-REFERENCE.md; README, PLAN and relevant portions/searches of STATUS, DECISIONS, COMPLIANCE-MATRIX, GAP-REVIEW, B10 review and implementation notes. No mandatory kit file was found absent; separately supplied kickoff sandbox rules remain unavailable as already disclosed. Local records were used for this diagnostic; Linear was not queried. Planned checks: count visible distinct references, trace the planted payment in the test script, and distinguish comparison runs from payment creation.

Observed on the existing deployed /ops/reconciliation browser tab: 28 open rows with 28 distinct provider references, all classified provider_only. Exact amount distribution: 24 payments of 4242 cents, one payment of 10000 cents, two payments of 1261 cents, and one refund of -898 cents; every row says no ledger record. Visible Stripe runs progressed from 20 breaks on September 8 at 17:58 UTC to 22 on September 9 at 06:00 UTC and 28 at 12:42/12:43 UTC. Latest visible Stripe comparison reports seven matched and 28 provider-only; the simulated claim rail reports three matched and zero breaks. These are observations of the displayed saved runs, not a newly executed reconciliation or an independent re-fetch of all Stripe objects.

Cause verified in scripts/check-reconciliation.ts: PLANTED_PAYMENT_CENTS = 4242; lines 548-559 create and confirm a fresh sandbox PaymentIntent using pm_card_visa, deliberately without operation metadata, with description corgi_probe: reconciliation check, a payment with no operation id (slice B10). The script and B10 handoff explicitly disclose one persistent provider-only probe per execution reaching this step. The test database isolation does not isolate these provider objects from the sandbox account read by the deployed reconciliation. app/api/jobs/reconcile/route.ts reads both sources and appends comparisons; pressing the UI comparison button does not create the probe payment. lib/reconciliation/read.ts selects one latest report per break key, so rerunning a comparison does not multiply the open count for the same reference. The existing open-break explanation confirms that unresolved records remain visible and cannot be dismissed on this screen.

Checks executed: browser accessibility inspection and exact row/reference/amount counting; read-only rg/sed source and record inspection; git status and revision inspection. No product tests, provider mutations, database writes, reconciliation runs, commit, push or deployment were performed; only this diagnostic note was appended. No new feature or integrated PASS is claimed. Residual issue: persistent test probes clutter the deployed operational queue (existing F-B10-08); detecting them does not mean the account is reconciled. Next executable step within this request: explain the screen and the test accumulation to Yoann. Any later probe-handling change must preserve financial history and undergo the existing review workflow. Human understanding remains unconfirmed until Yoann confirms it.


## 2026-09-10T01:04:50.310318+00:00 | Submission preparation: public publication BLOCKED by committed session credential

Yoann authorized adding the screenshot pack to the repository, making it public, and filling the portal without submitting. Actual reads: AUTOMATIC-FAILS, WORKFLOW-48H, REVIEWER, READABLE-CODE, structured BRIEF-REFERENCE; AGENTS supplied in conversation; README and relevant STATUS, PLAN, DECISIONS, COMPLIANCE-MATRIX, screenshot index and independent REVIEW-NOTES. No mandatory kit file identified absent; original kickoff sandbox rules remain unavailable. Scope is B14 packaging, not product implementation. Local records used; Linear not queried.

Copied the previously reviewed 14-image pack into docs/evidence/live-integration-submission, excluding .DS_Store and adding a navigation README. No image edited. Publication and push stopped: commit 24f4606, already contained in origin/main, includes a signed corgi_session credential in docs/evidence/live-fire-day2/tools/cookies/ops@example.com. Embedded expiry is 2026-09-10T12:48:25Z; server acceptance was not tested. A diagnostic mistakenly printed that committed credential into tool output; its value is not reproduced here. Standard gitleaks full-history scan returned exit 0 (729 commits), demonstrating that this pattern escaped detection, not AF-05 PASS. AF-05 is FAIL for this observed committed credential; public propagation BLOCKED.

Required recovery: credential owner rotates SESSION_SECRET and redeploys to invalidate existing signed sessions; remove the cookie from tracking while preserving the ignored local file, add an explicit session-cookie scanner rule and rescan history/current content. No history rewrite is authorized or performed; removing the latest copy does not erase the original incident. Reassess public publication after invalidation and documented remediation. Other working cookie files preserved. No commit, push, visibility change, Drive upload or final submission in this session. Portal application/repository/video URL fields were present before this request; credentials and evidence URL remain to complete. No global trial completion claim.


## 2026-09-10T01:08:43.481525+00:00 | Session rotation verified; publication review in progress

Production SESSION_SECRET updated through Vercel CLI using stdin, then deployment dpl_5TicLqfqDqyZAiHVW2cdz4kUKZft reached Ready and took the public alias. Runtime code remains 756db05. GET /api/health returned 200, database ok. The committed cookie now returns 307 /login on /ops. Fresh broker, staff operations and customer logins returned 303 to their correct home and each home returned 200. Sanitized evidence: docs/evidence/live-integration-submission/session-rotation-checks.json. No money moved in these checks.

Signed-session scanner rule added to .gitleaks.toml extending defaults. Full-history scan with --all returns exactly one known historical credential (729 commits), rather than a misleading clean result. Cookie file removed from index while local file is preserved; cookie directory ignored. Pack image SHA256SUMS: 14/14 match. Scope review by independent publication_review agent in progress. Historical AF-05 incident remains disclosed and is not erased by rotation.


## 2026-09-10T01:11:57.829219+00:00 | Public evidence available; portal filled without submission

Independent scoped remediation/publication PASS: docs/reviews/submission-publication.md. Commit a9013a7 pushed to origin/main and repository visibility confirmed PUBLIC. Anonymous HTTP checks returned 200 for repository, evidence folder and webhook delivery JPEG. Evidence URL: https://github.com/YoannAbriel/corgi-work-trial/tree/main/docs/evidence/live-integration-submission. Project-wide Vercel protection is all_except_custom_domains; prior immutable deployment with the exposed cookie returns 302 to Vercel authentication. Production rejects it and fresh three-role logins work, as recorded above.

Computer Use filled deployed URL, repository URL, four demo role credentials and evidence URL; existing video https://youtu.be/zWF8vi1d2f4 retained. Submit work trial button remains available and was never clicked. Public evidence and form preparation complete; this is not final submission or integrated-trial PASS. Historical AF-05 remains FAIL, with credential invalidation documented. Next user step: final submission decision; agent remains prohibited from submitting. No new money scenario or full regression suite run for documentation/configuration-only work.


## 2026-09-10T01:14:56.006794+00:00 | Concise root README

Yoann requested a very concise English README preserving trial requirements. Startup: clean main at 75d3751; reread AUTOMATIC-FAILS, READABLE-CODE, WORKFLOW-48H, REVIEWER; AGENTS and structured brief read in this continuous session; README and relevant current STATUS/PLAN/DECISIONS/review records inspected. No mandatory kit file found missing; original kickoff sandbox rules remain unavailable. Documentation-only B14 adjustment: detailed former README moved to docs/IMPLEMENTATION-GUIDE.md with relative links rebased; root becomes a short entry point retaining integration modes, setup, money rules, evidence and limitations. Planned checks: local links/anchors, npm script names, staged secret scan and scoped documentation review. No product behavior or completion claim changed.

Checks: 26 relative links/anchors resolved, README npm script names exist, git diff --check clean, staged gitleaks exit 0. Full history scan (731 commits) reports only the known invalidated cookie at 24f4606; historical AF-05 remains FAIL. Independent scoped documentation PASS: docs/reviews/readme-concision.md. README reduced from 6,265 to 397 words; previous technical content preserved in the linked guide. Product tests not rerun because no executable code changed. Next step: publish this documentation checkpoint; portal stays unsubmitted.
