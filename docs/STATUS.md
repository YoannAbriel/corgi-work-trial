# Current status

Updated at 2026-09-08T07:40:27+00:00. Track: **1: POLICY ADMINISTRATION**, explicitly selected by Yoann. Stack and providers: **SELECTED** in the planning discussion of 2026-09-08 (see DECISIONS.md): Next.js on Vercel, Postgres on Neon, Stripe test mode (live), Sumsub sandbox KYB (live, Middesk fallback), local simulators for bank verification and claim payout, real PDF generation. Repository still contains instructions, planning and preparation only; no product code, deployment or provider account exists yet.

## 2026-09-08T07:40:27+00:00 | Startup receipt and planning session

Files actually read in full this session: CLAUDE.md, AGENTS.md, AUTOMATIC-FAILS.md, READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, HANDOFF-PROMPT.md, README.md, docs/BRIEF-REFERENCE.md, docs/STATUS.md, docs/DECISIONS.md, docs/PROVENANCE.md. Read by targeted search only: GAP-REVIEW.md, docs/reviews/instructions-2026-09-08.md. Not read: READINESS-CHECKLIST.md, STRESS-TEST-PLAN.md, READINESS-BACKLOG.json (advisory, consulted later per scope). Absent files: none of the kit files are missing; docs/PLAN.md and docs/COMPLIANCE-MATRIX.md did not exist at session start (PLAN.md created now, COMPLIANCE-MATRIX.md to be created with B1).

Provider facts verified today against official documentation: Stripe refunds and refund webhooks work in test mode; Vercel Hobby cron runs at most once per day; Plaid sandbox is free and self-serve; Sumsub has an official MCP server, a trial sandbox and a review-state simulation API; Middesk issues a sandbox key at account creation with business-name triggers for verified, in_review and failed; Persona KYB requires contacting their team. Not yet verified: actual signup success for Sumsub or Middesk, Middesk webhook signature scheme, the modeled state's premium tax rate and source.

Linear: authenticated in this session; the 16 domain tickets were listed read-only and left unchanged. Execution issues B0 to B14 created as YOA-609 to YOA-623 with acceptance criteria, blocking relations, evidence and estimates; a project status update was posted. No scenario was marked passed.

Written this session: docs/ATTACK-PLAN.md, docs/checkpoints/t-plus-2h-email.md (draft, Yoann sends), docs/PLAN.md, four decision entries in docs/DECISIONS.md. T+2h email: SENT by Yoann at 09:49 Europe/Zurich (07:49 UTC) on the candidate thread, reported by him and recorded at 2026-09-08T08:07:11+00:00. Checkpoint met; the thread header is authoritative.

## 2026-09-08T08:20:34+00:00 | B0 bootstrap and deploy: in progress, deployment live

Done: Next.js 16 app hand-written at the repository root (no create-next-app boilerplate), TypeScript strict, plain CSS; `db/client.ts` pool on the Neon pooled endpoint; `scripts/migrate.ts` runner applying `db/migrations/*.sql` once each inside a transaction; `GET /api/health`; gitleaks pre-commit hook in `.githooks/` (activated locally with `git config core.hooksPath .githooks`, to be documented in README); `.env.example` with placeholders. Vercel project `corgi-work-trial` linked and connected to the GitHub repository; Neon `corgi-trial-db` provisioned through the Vercel Marketplace and connected to all environments. Production URL: https://corgi-work-trial-iota.vercel.app

Checks actually run: `npm run typecheck` PASS; `npm run build` PASS; `npm run migrate` against Neon PASS (schema_migrations created, no migration yet); `curl https://corgi-work-trial-iota.vercel.app/api/health` from the dev machine returned HTTP 200 with database ok and revision 1e40dc7 (AF-01 evidence for this revision only; the app has no product behaviour yet). Stripe key verified as test mode (`sk_test_` prefix, account US, test balance call); no key value was printed. Assistant choices, minor: npm instead of pnpm (corepack broken locally), no Tailwind, no ORM.

Pending in B0: broker KYB provider access (Sumsub trial verified by signed API call at 2026-09-08T08:28:41+00:00 but KYB is Enterprise-only there, so Middesk signup is being attempted next; see DECISIONS.md), Stripe webhook endpoint (after B2 route exists), history secret scan result below, README setup section. Not started: everything from B1.

## 2026-09-08T08:59:18+00:00 | KYB decision, Stripe proofs, design review

Broker KYB: Stripe Connect Accounts v2 business verification in test mode, decided by Yoann (DECISIONS.md, 08:55Z entry). Verified on the sandbox: three test company accounts created (acct_1UDKmAK6R3nWBqMW verified fixture, acct_1UDKmDK6R39jqMi4 failed fixture showing `verification_failed_tax_id_match`, acct_1UDKmIK6R3fxfHN9 pending fixture), all `livemode: false`, v2 events emitted. Premium collection: test payment pi_3UDKq0K6R3v50tIy1mwQmeWT ($12.61, succeeded) and partial refund re_3UDKq0K6R3v50tIy11aPmuHK ($8.98, succeeded) through the Refunds API; balance transactions show a 67 cent Stripe fee, to be classified explicitly by reconciliation. These are feasibility probes, not product evidence; the probe objects stay in the sandbox with metadata `corgi_probe`. One synthetic probe applicant also exists in the Sumsub sandbox.

Design review: DESIGN FAIL recorded in docs/reviews/architecture.md (21 findings). docs/ARCHITECTURE.md revised at 08:50Z for the material findings; five items stay OPEN for Yoann (worked-example date, negative endorsement delta, closed-month revision rule, commission clawback rounding, modeled state and tax rate). Re-review requested after the open items are decided. Reviewer verdict allows B0 and B1 to proceed.

B0 remaining: the Stripe webhook endpoint on the deployed URL, delivered with slice B1a (protected tables, webhook inbox, route, endpoint registration, replay test) before the pro-rata functions (B1b).

## 2026-09-08T09:12:00+00:00 | B1a done: ledger core, webhook inbox, Stripe endpoint live

Commit 3c9ef3a deployed (health check reports it). Migration 0001 applied on Neon: chart of accounts, append-only journal with deferred balance trigger and server-set recorded_at, immutable webhook_events plus mutable webhook_processing, role app_runtime with SELECT and INSERT only on money tables (password set by script, never committed). The app now connects as app_runtime in production and locally.

Checks executed: `npm run check:ledger-guards` 10/10 PASS on the trial database, every check rolled back (owner UPDATE and DELETE refused by trigger; unbalanced entry and entry without lines refused at commit; recorded_at server-set; app_runtime lacks UPDATE, DELETE, TRUNCATE; app_runtime can insert; global debits = credits). Stripe webhook endpoint we_1UDKzYK6R3v50tIybe5BIytW registered on the production URL (test mode, 9 event types). Against production: invalid signature 400; live-mode event 400; same event delivered twice sequentially and twice concurrently: two events stored for four deliveries, second deliveries answered as duplicate; a real Stripe delivery (evt_3UDL2qK6R3v50tIy0VZVRxVz for test payment pi_3UDL2qK6R3v50tIy0hthuI6E) received by the deployed app, stored once, marked ignored with the reason "no handler yet". Processing lease expires after five minutes so a killed function cannot leave an event stuck (to be demonstrated in B2).

Ledger practice check against Modern Treasury, Square Books and Stripe Ledger write-ups recorded in ARCHITECTURE.md section 1; created_by added to journal headers. Probe objects left in the Stripe sandbox carry metadata corgi_probe. B0 is complete. Independent review of B1a is grouped with the B1b/B2 money-path review. Walkthrough status: NOT REVIEWED WITH YOANN.

## 2026-09-08T09:17:36+00:00 | B1b done: premium arithmetic with tests (commit ea5085d)

`lib/money/dates.ts` (calendar dates, term end on the same date next year or February 28, real day counts) and `lib/money/premium.ts` (earned and unearned premium floored/ceiled, endorsement delta over remaining days from the effective date, state tax floored when charged and ceiled when refunded, commission on collected premium, pro-rata cancellation refund breakdown). All arithmetic in bigint cents. `npm test` (node --test through tsx): 14/14 PASS, including earned + unearned = written on every day of a 366-day term, both example dates (January 1: 32786 / 87214; March 1: 32876 / 87124), backdating changing the delta (43606 vs 48524), the refund breakdown 87214 + 2617 + 0 = 89831. docs/COMPLIANCE-MATRIX.md created with the ledger, webhook, sandbox, secrets and deployment rows.

B1 status: implementation complete (B1a + B1b), tests PASS; independent review requested next; walkthrough NOT REVIEWED WITH YOANN. Open for Yoann: which example date to recite (both are tested), commission clawback rounding (B5), negative endorsement delta handling (B4), closed-month revision rule (B9), modeled state and tax rate source (B2).

## 2026-09-08T09:34:54+00:00 | B1 review FAIL fixed (a94f091), re-review requested; B2 in progress by delegate

Independent B1 review (docs/reviews/b1-ledger-core.md): FAIL on F-B1-01, app_runtime could append balanced lines to a committed entry. Fixed by migration 0003 (entries sealed at commit: lines only in the creating transaction; TRUNCATE triggers; received_at server-set; CHECK livemode = false) and a fail-closed db/client.ts. Proven by `npm run check:ledger-seal` 4/4 on the disposable database corgi_test (same Neon project, created for tests that must commit rows); trial database guards still 10/10. Register of all findings: docs/reviews/FINDINGS.md. Re-review by the B9 reviewer at 17:32Z: PASS at 8b5b490 (docs/reviews/b9-statements.md section 10; production reports 8e6d468, the last code commit, with an empty code diff to 8b5b490). F-B9-09 resolved and verified on production; revision 3 ties on the reviewer's own ledger read; entryRecordedAt kept in the hash on the reviewer's judgement. Open LOW: F-B9-05, F-B9-06 (half), F-B9-08, F-B9-11. Walkthrough: NOT REVIEWED WITH YOANN. Two synthetic replay-test events remain in the production inbox, marked ignored; to be disclosed in README.

Decisions since the last entry: recited example March 1, 2028 (365 days); California premium tax 2.35 percent from official sources (DECISIONS.md). B2 is being built by a delegate in an isolated worktree (branch worktree-agent-*, no push, no deploy); it will be reviewed and merged by the coordinator.

## 2026-09-08T09:39:11+00:00 | B1 independent review PASS (re-review at 8f253a7)

Reviewer confirmed the seal, TRUNCATE and livemode guards and tried bypasses (owner, session settings, pause between header and lines, timezone, trigger disable): all refused or without effect. Residue disclosed, not a finding: the owner can still drop a trigger, and database restores sit outside the guards. .env.example corrected (no owner fallback wording; test database placeholders). B1 is DONE technically; walkthrough NOT REVIEWED WITH YOANN. Note: the shared database already holds migrations 0002 and 0004 applied from the B2 worktree; the repository is behind the schema until B2 merges.

## 2026-09-08T10:19:18+00:00 | B2 merged, deployed and live-proven (commit 0115796)

Slice B2 built by a delegate in a worktree (docs/handoffs/b2-implementation-notes.md), read by the coordinator on the money path, merged with two trivial conflicts, deployed (health reports 0115796). Checks after merge: typecheck, build, `npm test` 56/56, check:ledger-guards 10/10 and check:money-guards 43/43 on the trial database (rolled back), check:payment-replay 15/15 and check:ledger-seal 4/4 on corgi_test.

Live proof at 10:16Z: Yoann paid policy CGP-01062 ($3,450.75 premium, California, effective 2026-10-01, charge 355684 cents) with a Stripe test card on the deployed URL. Stripe delivered payment_intent.succeeded (evt_3UDM4KK6R3v50tIy0S6PIO4D, pi_3UDM4KK6R3v50tIy0F5xaBbu, livemode false) and, after it, checkout.session.completed (evt_1UDM4MK6R3v50tIyurIo0iug): both stored once and processed; four journal entries posted once (cash 355684, unearned premium 345075, tax 8109, fee 2500, commission 51761), policy bound. The operation's lifecycle reads requested, provider_accepted, failed (an earlier simulated failure from the delegate's local run), succeeded, provider_accepted (session completion): every step is an appended event. The T+24h requirement (money moving on a live sandbox rail at the deployed URL) is met at T+4h26; the checkpoint email is drafted in B6 and sent by Yoann.

Delegate findings adopted: webhook payloads were stored as jsonb strings by the B1a code (fixed with sql.json(); the two probe rows keep the old shape and are ignored by queries); Next.js 16 appends a block to AGENTS.md on dev runs (agentRules turned off). Demo data now in the trial database: broker Redwood Commercial Brokers (15%), users broker@, customer@, ops@, approver@example.com sharing DEMO_PASSWORD, policies CGP-01061 (bound through a locally signed webhook) and CGP-01062 (bound through the real Stripe delivery above). Independent review of B2 in progress (docs/reviews/b2-issuance-and-collection.md). Walkthrough NOT REVIEWED WITH YOANN.

## 2026-09-08T11:06:08+00:00 | B2 review FAIL (data), documents and KYB adapter merged (c548ccb), B5 in progress

B2 independent review: FAIL on F-B2-01 (CGP-01061 bound on a locally fabricated payment; Yoann decided reversal plus dated correction event, done as B8a after B5 merges), MEDIUM F-B2-02 (503 while a lease is in flight, stuck-events view), F-B2-03 (Checkout expiry: new intent per attempt, decided), F-B2-04 (a fresh draft must be paid on the deployed URL). Code controls all passed. Register: docs/reviews/FINDINGS.md.

Merged from the documents/KYB delegate: lib/documents (pure fold of policy events as of a date, declarations and endorsement schedule PDFs, text-asserted tests) and lib/kyb (Stripe Connect Accounts v2 adapter, eligibility mapping with captured fixtures, opt-in live test PASS). `npm test` 103 pass, 1 skipped. Findings for Yoann: Stripe's business identity check lands about 45 to 50 seconds after account creation (settling window rule), the published pending EIN fixture yields approved, the ToS attestation and business URL are mandatory. Fourteen test accounts tagged corgi_probe exist in the Stripe sandbox. B5 (cancellation with a real refund) is being built by a delegate, including F-B2-02 and F-B2-03.

Walkthrough status: B1 QUESTIONS OPEN, B2 QUESTIONS OPEN (Yoann asked for shorter explanations; the Code page was rewritten with a flow diagram).

## 2026-09-08T11:33:27+00:00 | B5 merged and live-proven (e120f69), CGP-01061 corrected by reversal (B8a)

B5 (delegate, 9 commits, notes in docs/handoffs/b5-implementation-notes.md) merged: `npm test` 130 (129 pass, 1 skipped), guards 10/10 and 51/51, payment replay 22/22, refund replay 27/27, seal 4/4. Live: CGP-01062 cancelled effective 2026-10-31 through the deployed app; real Stripe refund re_3UDM4KK6R3v50tIy0scSGaps of 324156 cents succeeded on pi_3UDM4KK6R3v50tIy0F5xaBbu; refund.updated received by the deployed app posted refund_completed (324156) and commission_clawback (47506); refund_payable back to 0; ledger balanced. The refund.* events delivered before the deployment are stored as ignored (disclosed); a new refund.updated was made to fire by updating the refund's metadata. Disclosed: this refund crossed the $1,000 money-out threshold with no approver because B7 does not exist yet.

Yoann paid a fresh policy CGP-01274 ($2,312 premium, tax 5433, fee 2500, charge 239133) from a Checkout Session created by the deployed app (closes F-B2-04). The customer email he typed was his real address; replaced by a synthetic example.com address on the nonfinancial customers table (AF-04).

B8a: CGP-01061 (bound on a locally fabricated webhook) corrected by `npm run void:fabricated-binding`: the guard asked Stripe and confirmed the payment intent does not exist, then one transaction wrote a correction_reversal policy event superseding the issuance and four reversal entries mirroring the originals (lib/ledger/reverse.ts, lib/policy/void-fabricated-binding.ts). Originals untouched; the fold skips superseded events; ledger cash_stripe now equals what Stripe holds (270661 cents); the same run on corgi_test showed net zero per account and a refused second attempt. B2 re-review requested. B3 wiring is being built by a delegate; B5 independent review in progress.

## 2026-09-08T12:05:27+00:00 | Walkthrough progress, B2 re-review status, B5 review PASS

Walkthrough with Yoann (four questions on the money path, his own words): B2 outbox order (half right, then explained: crash safety and the stable key), which Stripe event posts (right, with the reason), replay twice is one (inbox by event id right, journal by operation id explained and confirmed back), B5 refund liability (refund_payable as a clearing account explained with his own policy's figures). Status: B2 and B5 EXPLAINED, QUESTIONS OPEN until a second pass tomorrow; B1 QUESTIONS OPEN.

B2 re-review at 109dafb: four fixes verified; new finding F-B2-13 (voided policy could still be paid): first door closed (session expired at Stripe, void fixed at 5b14830), second door (attempt 2 on a voided policy, unique-violation mapped to already_posted) assigned to the B3 delegate with the reviewer's exact spec; re-review after B3 merges. B5 review: PASS with four MEDIUM (F-B5-01 approval on refunds and F-B5-02/03 refund re-issue and recovery assigned to the B7 delegate; F-B5-04 a deployed-app cancellation after B7). B7 (claims, reserves, simulated rail, maker-checker) being built by a delegate.

## 2026-09-08T12:41:45+00:00 | B3 merged and live-proven (b3ba15b, 09e71e5): broker KYB on Stripe Connect

Delegate build (docs/handoffs/b3-implementation-notes.md): migration 0006 (broker_kyb_submissions, EIN last4 only, terms acceptance with date and IP) and 0007 (voided status), broker KYB screen with the decided terms checkbox, submission committed before the provider call, account.updated handling that re-reads the account and appends a status row only on change, settling window of at least 2 minutes, eligibility rechecked at binding time (money arriving for an ineligible broker is not bound, shown in red, staff can bind once approved), staff view /ops/brokers with a re-read action, and the F-B2-13 second-door fixes (voided status, startCheckout refusal, unique violations no longer read as replays, seven replay lines). Checks: 152 tests, money guards 58/58, kyb replay 22/22, payment replay 29/29, refund replay 27/27.

Live: demo broker submitted through the app to Stripe Connect (acct_1UDNobK6R3ohMVag): pending at 12:07:57Z, approved at 12:10:34Z (the settling window held it 2 min 37 s); failed path on corgi_test (acct_1UDNv2K6R3y2nGVW, verification_failed_tax_id_match, Pay disabled, direct POST refused). Webhook leg: the account endpoint cannot receive connected-account events, so a Connect endpoint (we_1UDOHHK6R3v50tIyfbZP2ohD, connect=true, same route, own secret STRIPE_CONNECT_WEBHOOK_SECRET) was registered and the route now verifies against either secret (09e71e5); a contact-email change on the connected account produced account.updated evt_1UDOJoK6R3ohMVaghFAAnpEU, received by the deployed app one second later, processed done, "broker KYB unchanged at approved: nothing appended" (Vercel log: POST /api/webhooks/stripe 200). Stripe refused https://example.com as a business URL (recorded as a failed status row at 12:07:45Z). Fourteen probe accounts plus the demo account exist in the sandbox. Independent review: PASS at 09e71e5, unchanged at faa12a8 (docs/reviews/b3-broker-kyb.md): typecheck, build, 152 tests, money guards 58/58, kyb replay 22/22, payment replay 29/29, fresh database migrated 0001 to 0007, gitleaks on 17 commits clean, trial rows and Stripe read back; three MEDIUM (F-B3-01 suspense account, decided by Yoann at 12:54Z; F-B3-02 misleading retry comments, fixed; F-B3-03 no pending or failed broker visible on the deployed app, fixed at 13:03Z: two more demo brokers seeded, broker2@example.com submitted through the deployed URL with the failing tax-id fixture, pending at 13:02:33Z, then account.updated evt_1UDOgLK6R3FpfF2DhpwJYnqR reached the Connect endpoint at 13:03:26Z and the app appended failed at 13:03:27Z, which is the first webhook-driven KYB transition observed on the deployed app; /ops/brokers now shows approved, failed and never submitted side by side; broker3@example.com is kept unsubmitted so the pending state can be shown live at the debrief) and six LOW in FINDINGS.md. B2 re-review of the F-B2-13 fixes: PASS at b3ba15b (12:44Z). Walkthrough: NOT REVIEWED WITH YOANN. The demo broker's connected account is real product data, not a probe; it carries a corgi_connect_webhook_probe metadata key from the Connect endpoint proof.

## 2026-09-08T13:34:00+00:00 | Rule 14 built: suspense account for money received after eligibility loss

Migration 0010 (account unapplied_customer_cash, status paid_not_bound), lib/ledger/policy-entries.ts (unappliedCashReceivedEntry, collectedFrom on the issuance entries), lib/payments/collection.ts (parking at receipt in the same transaction as the succeeded status; the bind applies the parked cash; the replay guard now reads the premium_collected entry, not any entry), lib/payments/checkout.ts (expireOpenCheckoutSessionsOfBroker) called from the KYB refresh when a broker stops being approved, screens (policy page wording, no Pay button on paid_not_bound, disclosure line on /broker/kyb and /ops/brokers, placeholder label only with a placeholder row). Checks: typecheck, build, 155 tests (154 pass, 1 skipped), kyb replay 23/23 (parking, no double parking on redelivery, bind applies the cash, suspense back to zero, cash_stripe once), payment replay 29/29, money guards 58/58, ledger guards PASS. Migration 0010 applied to corgi_test and to the trial database before the deploy. Not built yet: the refund of parked money for a broker who fails for good (money out, waits for the B7 approval queue). Independent re-review requested from the B3 reviewer. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T14:21:00+00:00 | B7 merged (e77baa6, plus 59f2fcd staff landing): claims, reserves, simulated payout rail, maker-checker

Delegate build (docs/handoffs/b7-implementation-notes.md): migration 0008 (claims, claim_events, approval_requests and decisions, rail records, bank verification records, two money_operations columns), pure claim rules (incurred = paid + reserve, per-occurrence and aggregate limits), claim entries (reserve set and adjusted, payment sent, settled, returned, reserve restored), two local simulators labeled LOCAL SIMULATOR (payout rail with delayed settlement and returns; bank ownership check), approval queue with a hashed intent (a changed destination or amount needs a fresh approval), maker-checker enforced in code and by a database trigger (initiator cannot approve, only staff_approver decides), the refund path of B5 gated above $1,000 (F-B5-01), re-issue of a dead refund under the same key with a Stripe listing first (F-B5-02), recover-stuck-operations job and a resend action (F-B5-03), screens /ops/claims, /ops/claims/[id], /ops/approvals, and the two job endpoints protected by CRON_SECRET. Deviations recorded by the delegate: per-claim lock with pg_advisory_xact_lock (app_runtime has no UPDATE privilege, so SELECT FOR UPDATE is impossible by design); a claim payment counts as paid from the moment it is sent, settled money reported separately; reserve_adjusted carries the new outstanding reserve. Coordinator additions: schema_migrations record renamed on both databases from the old 0007 file name to 0008 (the runner then skips everything), staff landing page /ops with the login redirect for staff roles.

Checks on main after the merge: typecheck, build (31 routes), 220 tests (219 pass, 1 skipped), claims and approvals 55/55, kyb replay 23/23, payment replay 29/29, refund replay 27/27, money guards on corgi_test 110/110 on a solo rerun (a first run under shared load had one Neon CONNECTION_CLOSED, not a guard failure); the delegate's own runs: 110/110 on corgi_test, and on the trial database a catalogue read of every trigger and grant plus an exercised run reported as 108/110 under contention (deadlocks on TRUNCATE probes against the live app), which the delegate was re-running when the session limit stopped it. Not verified: the screens were never rendered by the delegate (type-checked and built only), the job endpoints never called over HTTP. Independent review to be launched once the session limit resets (16:20 local). Decided by Yoann at 14:32Z: a claim payment is paid when sent (DECISIONS.md). Walkthrough: NOT REVIEWED WITH YOANN.

Parallel interface branch (noted 14:29Z): Yoann runs a separate Codex session in the git worktree .worktrees/corgi-interface (branch codex/corgi-interface, cut at 59f2fcd) that adds a portal-style dressing only (navigation, cards, responsive styles, two illustrations generated on his own machine): no change under lib, db, scripts, app/api or the Vercel configuration; existing forms, access checks and business conditions kept; nothing pushed, deployed or merged. Integration into main happens after the B4 screens land; the overlap to watch is app/policies/[policyId]/page.tsx and the global styles. AF-06 is NOT confirmed for that dressing until Yoann has explained the path (server page, PortalShell, CSS, WorkspaceOverview for the ops home) back; the directory is git-ignored at the root so it can never be committed by accident. At 16:25Z Yoann also gave that session the visual pass on the two PDFs (F-B4-04): scope limited to lib/documents/render.tsx plus an optional theme file and bundled font, no change to the snapshot, the fold or the formatting helpers, render tests unchanged, no network at render, a demo issuer name with a test-data label and never a real company's identity; the prompt lives outside the repository.

## 2026-09-08T15:34:03+00:00 | B10 merged (28a361c); B7 review FAIL then fixed (65b06be); decisions 16 and 17

B10 reconciliation, delegate build (docs/handoffs/b10-implementation-notes.md): migration 0011 (reconciliation_runs, reconciliation_items, append-only, guarded), the Stripe source (PaymentIntents, Refunds, BalanceTransactions, paginated) and the claims rail source, the ledger side with the runtime role, a pure diff (matched, local-only, provider-only, amount mismatch, stale; Stripe fee noted only; parked payment matched; reversed operation matched at zero), one run per source and window stored in one transaction after the fetch (a failed fetch stores a failed run with no items and never reads clean), POST /api/jobs/reconcile and GET or POST /api/jobs/daily (recover, settle, reconcile) under CRON_SECRET, vercel.json with one cron at 06:00 UTC, the staff screen /ops/reconciliation (runs, open breaks with age, resolved breaks, Run now), scripts/check-reconciliation.ts 29/29 (planted provider-only PaymentIntent of $42.42 with no metadata found on the real sandbox; a rail transfer with no ledger entry found; a simulated fetch failure produces a failed run). Migration 0011 applied to corgi_test and the trial database. Decisions by Yoann (DECISIONS.md): stale at 24 hours for Stripe and 72 hours for the rail, order recover then settle then reconcile, one cron at 06:00 UTC; "run a reconciliation" goes to the MCP surface in B11. Not yet verified: the run from the deployed app and the cron (after this deploy).

B7 independent review (docs/reviews/b7-claims-and-approvals.md): FAIL on F-B7-01 (HIGH: a refund above $1,000 rejected by the approver could be re-issued by the maker as a new operation with no approval request, and nothing on that path checked maker-checker before Stripe; proven on corgi_test), F-B7-02 (MEDIUM: threshold per payment, so $1,200 out of a claim as two $600 lines with no approver, proven), F-B7-03 (MEDIUM: no cron, no Run now), nine LOW; rule 14: PASS with two LOW. Fixed at 65b06be: the maker-checker gate now sits inside issueRefundsAtStripe so every road to Stripe passes it and a refusal appends nothing; a rejection is an approval-stage failure, and a re-issue above the threshold raises a new approval request and returns to the queue; the claim threshold is cumulative per claim (decision 17); the rejection is worded on the policy page; a Stripe error while expiring a broker's sessions no longer fails the webhook. B10's cron and Run now close F-B7-03. Checks on main at 65b06be: typecheck, build, 276 tests (275 pass, 1 skipped), claims and approvals 63/63 (eight new proof lines), refund replay 27/27, reconciliation 29/29, kyb replay 23/23, payment replay 29/29, money guards on corgi_test 127 PASS lines under shared load (128 claimed by the delegate; rerun pending). Re-review by the B7 reviewer at 16:16Z: PASS for the B7 scope after the fixes, rule 14 stays PASS (docs/reviews/b7-claims-and-approvals.md section 12); F-B7-01 and F-B7-02 re-probed on corgi_test and, for the threshold, live on CLM-00212; F-B7-03 agreed closed by B10; one new LOW F-B7-13. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T15:39:30+00:00 | Production caught up (6e8805e): B10 and the maker-checker fixes live; daily job and reconciliation proven on the deployed app

Deploy incident, disclosed: the pushes carrying the B10 merge (28a361c) and the maker-checker fixes (65b06be) each ended with a docs commit, and the Vercel Ignored Build Step compares only the last commit with its parent, so both builds were canceled and production stayed on 4decb8c from 14:26Z to 15:38Z. Found by reading the deployment list, fixed by pushing a code commit on its own (6e8805e); rule from now on: push code alone, confirm the revision on /api/health, then push docs; Yoann to switch the build step to compare with the last successful deployment (VERCEL_GIT_PREVIOUS_SHA). The B7 reviewer's HTTP checks ran on 4decb8c, which carried all the B7 and rule-14 code it reviewed, so its findings stand.

Deployed evidence at 15:38Z: GET /api/jobs/daily answers 401 with no credential and 401 with a wrong bearer, 200 with CRON_SECRET (so the secret is set in the deployment); POST /api/jobs/reconcile answers 401 with no credential; the daily job ran recover (0 stuck), settle (0 due) and reconcile over the last 7 days: Stripe run 91473119 complete, matched 4, provider-only 8 (the probe PaymentIntents of the checks and of earlier sandbox probes, with no operation id, disclosed in README), amount mismatch 0, stale 0, 11 provider records against 4 ledger records, fees kept by Stripe 18376 cents noted as information; rail run complete, 1 matched (the reviewer's settled payout on CLM-00212), no break. Staff login lands on /ops; /ops/reconciliation renders the runs, the open breaks with their age and meaning, the resolved breaks, the thresholds as assumptions and the LOCAL SIMULATOR label. The cron itself (06:00 UTC) is declared in vercel.json and visible only in the Vercel dashboard; not yet observed firing.

B7 reviewer, trial-database evidence (read-only plus one synthetic claim): all 16 protected tables carry their triggers and app_runtime holds INSERT and SELECT only; claim CLM-00212 on CGP-01274 created through the deployed screens (reserve $5,000, one $1,200 payment approved by approver@example.com, sent and settled on the simulator, reserve left at $3,800 so the cancel-with-open-claim preview stays demonstrable); maker-checker refused the initiator, a broker, a customer and an anonymous caller over HTTP; a changed destination after approval broke the intent hash; the screen's SHA-256 recomputed independently matched; both job endpoints 401 without the secret and 200 with it; a throwaway database migrated 0001 to 0010 in order matched the trial catalogue. Money guards on corgi_test: the solo rerun hit deadlocks on the TRUNCATE probes while the B4 builder and the B10 reviewer used the same database; a quiet rerun is pending.

## 2026-09-08T16:13:06+00:00 | B4 merged (dfdca38) and deployed: endorsements, customer approval, delta collected or refunded, segments, as-of PDFs

Delegate build (docs/handoffs/b4-implementation-notes.md; first builder cut by the session limit at 15:58Z, resumed on opus in the same worktree): migration 0009 (endorsement events, endorsement_collections, unique indexes for applied once and approved once), lib/money/endorsement.ts (the one pure function: price, approval threshold, quote hash, formula lines), lib/policy/endorse.ts (request under a lock, stale hash refused, reduction applied in the same transaction with its refund operation), lib/payments/endorsement-collection.ts (hosted page for a positive delta under endorsement-checkout:<event>[:attempt], four entries and the applied event on payment_intent.succeeded, rule-14 parking when the endorsement cannot be applied), lib/ledger/endorsement-entries.ts, the fold with written premium segments so an endorsed policy can be cancelled (decision 18), customer approval page and /customer list, preview page with formula lines, schedule and explanation on the policy page, declarations and endorsement schedule PDFs as of any date; a negative delta above $1,000 goes through B7's approval queue. Checks on main after the merge: typecheck, build (42 routes), 313 tests (312 pass, 1 skipped), endorsement replay 69/69, payment replay 29/29 (one run failed to start under shared load, rerun clean), refund replay 27/27, kyb replay 23/23, claims and approvals 63/63, reconciliation 29/29, money guards 136/136 PASS at 16:22Z on an ephemeral database created on the same Neon server, migrated 0001 to 0011 in order with the runtime role, then dropped (the shared corgi_test runs kept hitting TRUNCATE-probe deadlocks while three agents used it; those were contention, not guard defects). Migration 0009 applied to corgi_test and to the trial database before the deploy (it alters no CHECK constraint, closing F-B7-12). Deployed: /api/health reports dfdca38; policy CGP-01274 renders the documents block; GET /api/policies/<id>/documents/declarations?asOf=2026-10-01 and .../endorsement-schedule answer application/pdf. Decisions: 18 (segment by segment). Open items tracked: F-B4-01 (refund of parked money, B13), F-B4-02 (suspense account missing from the clearing balances list, B13). Not yet done: a live endorsement and a live delta refund from the deployed app with Yoann's card (planned in the walkthrough session), independent review (launched). Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T16:53:59+00:00 | B9 merged (44188bd) and deployed: broker monthly statement tied to the ledger, with cutoff and revisions

Delegate build (docs/handoffs/b9-implementation-notes.md): migration 0012 (statement_runs and statement_lines, append-only, guarded), lib/statements/compute.ts (lines and totals from journal lines only, net due = the commission_payable movement of the broker in the month, reversals with their sign, parked-then-applied cash counted, canonical text hashed), lib/statements/run.ts (read up to the knowledge cutoff with the runtime role, one transaction, revision N+1 with supersedes_run_id, identical re-runs stored and flagged, changes against the previous revision computed on read), screens /ops/statements and /broker/statements plus the shared detail page with a live "ties to the ledger" line, PDF with revision, cutoff and hash, scripts/check-statements.ts. Merge conflict in scripts/check-money-guards.ts (B4 and B9 both added probes) resolved by keeping both. Checks on main: 328 tests (327 pass, 1 skipped), build, statements 29/29 (the delegate counted 30, asked), endorsement 69/69, payment 29/29, refund 27/27, kyb 23/23, claims 63/63, reconciliation 29/29; money guards 155/155 on an ephemeral database migrated 0001 to 0012 (16:44Z). Migration 0012 applied to corgi_test and the trial database before the deploy; /api/health reports 44188bd. Decisions by Yoann at 16:47Z (decision 19): month by effective date, two money columns, provisional month; the delegate is adding the column and the label on its branch. Independent review launched. Not yet verified: the deployed screens and PDF, a month containing an endorsement, a parked-then-applied payment end to end. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T16:57:31+00:00 | B4 review FAIL (3 MEDIUM) and B10 review FAIL (1 HIGH): fixes dispatched to the original builders

B4 (docs/reviews/b4-endorsements.md, at dfdca38): every recited figure reproduced from independent arithmetic, 312 tests, endorsement replay 69/69, HTTP refusals and both PDFs verified on production, F-B7-12 closed (0009 additive, throwaway database migrated 0001 to 0011). FAIL on F-B4-04 (threshold per endorsement, repeated sub-$1,000 reductions refund without an approver), F-B4-05 (a delta paid after cancellation or void is refused and journaled nowhere; cancellation does not expire open pages), F-B4-06 (no endorsement on the deployed app and no user for the customer of CGP-01274). Six LOW. Actions: cumulative per-policy rule for refunds and the rule-14 parking of a late delta dispatched to the B4 builder; customer2@example.com attached to CGP-01274's customer on the trial database and listed in README; the live endorsement with Yoann's card is planned in the walkthrough session.

B10 (docs/reviews/b10-reconciliation.md, at dfdca38 on production): typecheck, 276 tests, reconciliation 29/29 solo, guards settled at 128 for the B10 revision, gitleaks clean, HTTP matrix on production including one staff Run now (Stripe 14 provider records against 4 ledger: 4 matched, 11 provider-only probes; rail 1 against 2, no break; CRON_SECRET proven configured). FAIL on F-B10-01 (a break outside the compared window leaves the open list and shows as resolved; reproduced, 14 open rail breaks became 0 under a narrower window; the 7-day cron default self-resolves anything older than a week), F-B10-02 (break key includes the classification), F-B10-03 (clearing balances list not built, same as F-B4-02). Six LOW. Actions: resolution only by a covering later run, daily window widened to the oldest open break, key without classification, account-level clearing balances list, dispatched to the B10 builder. Both re-reviews follow the merges. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T16:59:47+00:00 | LOW findings delegate merged (b956950 plus the uuid guard follow-up): ten findings closed

Delegate build (docs/handoffs/low-findings-notes.md, one commit per finding): F-B7-08 pair check between the claim and the operation in the URL; F-B7-10 settleClaimPayment checks its actor (operator or the scheduled job); F-B7-06 a loss dated in the future or before the cover is refused in the pure coverage rule; F-B7-05 claim payouts recovered by the stuck-operations job; F-B7-07 lib/http/path-ids.ts, a malformed id answers 400 on a route and 404 on a page (the coordinator added the same one-line guard to the nine endorsement, document and statement routes and pages created after the delegate's branch point); F-B2-19 partial unique index on the succeeded status per operation (migration 0013) with on conflict do nothing; F-B2-18 replay guard aligned on policyWasVoided so a re-booked policy's replay reads as already posted; F-B3-05 advisory lock per broker and refusal while a submission is in flight; F-B3-04 settling window measured to now; F-B7-09 foreign key journal_entries.claim_id (0013). Delegate checks: 283 tests, claims and approvals 71/71, payment replay 32/32, kyb replay 26/26, refund replay 27/27, reconciliation 29/29; guards 127 PASS plus one contention deadlock. Migration 0013 applied to corgi_test and the trial database before the deploy. Demo consequence of F-B7-06: CGP-01274's cover starts on 2026-09-17, so no new claim can be opened on it until then; the walkthrough session issues a policy effective today for the claim scenarios (CLM-00212 stays). Coordinator checks after the merge: recorded below when they finish.

## 2026-09-08T17:08:33+00:00 | B9 follow-ups merged (9dc1775, renumbered 51883fe): decision 19 implemented

The delegate implemented points 2 and 3 of decision 19: migration 0015_statement_commission_base.sql (renumbered by the coordinator from the delegate's 0013, which collided with 0013_low_findings.sql; the schema_migrations record on corgi_test renamed accordingly, 0015 applied to the trial database, which held zero published runs) adds cash_collected_cents on a run and commission_base_cents on a line, premium_collected_cents now holding the premium alone, read as the unearned_premium movement of the same money operation as the cash entry under the same cutoff, never a rate applied backwards; the canonical text moves to v2 (no published run existed); a month still running at the cutoff is labeled provisional on the lists, the statement page and the PDF, derived from the run's own dates so it stays provisional forever. Also fixed: B4's endorsement_premium_collected and endorsement_commission_earned entries now read as collected premium rather than adjustments. Delegate checks: 333 tests, statements 34/34. The earlier 29 versus 30 count: the summary line "ALL CHECKS PASSED" was counted as a PASS line by the delegate. Coordinator checks on the merged tree: 340 tests (339 pass, 1 skipped), build; the replay sequence after the LOW merge: claims 71/71, payment 32/32, kyb 26/26, refund 27/27, endorsement 69/69, statements and reconciliation pending at the time of writing.

## 2026-09-08T17:15:29+00:00 | B9 review: FAIL at 44188bd (F-B9-01, fixed by the follow-ups) and FAIL at afeba97 (F-B9-09, a format change over existing rows)

docs/reviews/b9-statements.md. Verified good: ties to the ledger to the cent everywhere, including on the deployed app against the trial ledger (Redwood, 2026-09: premium 594817, commission 86441, clawback 47506, net due 38935, matching the reviewer's independent read of the commission_payable movement); a voided binding nets to zero with all four lines visible; a re-run with the same cutoff reproduced the hash as revision 2 flagged identical; a correction after the cutoff is invisible to that cutoff and a fresh run is revision N+1 with the change list; append-only proven; authorisation over HTTP with five identities; month boundary correct (reversals keep the original effective date; clawbacks are dated on the day the cash left Stripe). F-B9-01 (HIGH at 44188bd): endorsement cash dropped from the statement, fixed by the follow-ups at 51883fe and re-verified by the reviewer's probe (cash 169904, premium 163561, commission 24534). F-B9-09 (HIGH at afeba97): migration 0015 changed the meaning of premium_collected_cents while three runs already existed on the trial database, created by the reviewer at 17:03Z after the delegate's zero-runs check: 2026-09 revision 1 and 2 and 2027-09 revision 1 for Redwood, 20 lines, all appends. No money figure is wrong (net due, lines and hashes untouched) but those rows would render "Cash collected $0.00". Fix dispatched to the delegate: a format version column, old rows rendered with their v1 meaning, fresh revisions of those months run on the deployed app after the deploy; the three v1 runs stay and are disclosed here. Also dispatched: F-B9-02 (footer sentence), F-B9-03 (handoff note month), F-B9-04 (form validation). The money guards must be re-proven on an ephemeral database after 0015 and 0016 (columns added to guarded tables): done at the next merge. Walkthrough: NOT REVIEWED WITH YOANN.

## 2026-09-08T17:26:09+00:00 | B9 format-version fix merged (8b5b490): F-B9-09 closed, plus F-B9-02, 03, 04, 06, 07, 10

Migration 0016_statement_format_version.sql adds canonical_version (old rows default to 1, which is what they are; the migration carries the read-only query to run before applying it anywhere holding runs, its answer for the trial database, three runs all v1-shaped, and the rule: nullable, never NOT NULL DEFAULT). One function, collectedFigures, is the only place that knows what an old column meant: a v1 run renders as the cash it holds with premium "not stored" and a visible note, the PDF branches the same way, and revisions of different formats read "format changed, not comparable by hash". Proved on the disposable database with a hand-inserted v1 row. Also: the page and PDF now name the one case that does not reproduce (entryRecordedAt is in the hashed text and deterministic); the handoff note's parked-cash month corrected; the run form's broker id validated. Checks on main: typecheck, build, 341 tests (340 pass, 1 skipped), statements 38/38. Migration 0016 applied to corgi_test and the trial database before the deploy. Deployed at 17:27Z (/api/health 8b5b490). Fresh revisions run on the deployed app as staff_ops at 17:26Z: Redwood 2026-09 revision 3 (premium collected $5,762.75, cash collected $5,948.17, commission $864.41, clawback -$475.06, net due $389.35, listed as "format changed, not comparable by hash" against revision 2, provisional because the month is running) and 2027-09 revision 2 (zeros, format changed); the three v1 runs (2026-09 revisions 1 and 2, 2027-09 revision 1, created by the B9 reviewer at 17:03Z to 17:05Z) stay and render "format v1: premium not stored". Re-review requested. Money guards re-proven at 17:33Z on an ephemeral database migrated 0001 to 0016 (15 files) with the runtime role: 155/155 PASS, database dropped afterwards.

## 2026-09-08T17:30:32+00:00 | B10 fixes merged (f9fc17f, renumbered 010a6fe): F-B10-01 closed, clearing balances built

The B10 builder fixed the review findings on its branch: migration 0017_reconciliation_item_record_date.sql (renumbered by the coordinator from the builder's 0016, which collided with B9's 0016; the schema_migrations record on corgi_test renamed, 0017 applied to the trial database) stores each item's record date; a break resolves only when a later complete run of the same source whose window contains that date stops reporting it, otherwise it stays open with its age; the daily job widens its window back to the oldest open break, capped at 31 days, and says when it could not reach one (F-B10-01). The break key is source plus reference, our operation id first (F-B10-02). The four clearing accounts (premium_receivable, refund_payable, claims_payable, unapplied_customer_cash) are listed on /ops/reconciliation per policy or claim, read from the journal with no window, with the oldest entry and its age (F-B10-03, closes F-B4-02). A storage failure stores a failed run and does not stop the next source; one banner per source; the run note counts the PaymentIntents not compared (F-B10-04 to 06). Left open on purpose: F-B10-07 (unbounded open-breaks read, so no row is ever dropped), F-B10-08, F-B10-09. Builder checks: 344 tests, reconciliation 39/39, claims 63/63, guards 155/155. Expect a longer board: breaks that used to age out now stay. Coordinator checks after the merge and the deploy: recorded below.

## 2026-09-08T17:38:16+00:00 | B4 fixes merged (dcad358, 26dede3) and B8 merged (f8c904f, e7b5913): corrections by reversal and re-book

B4 fixes (docs/reviews/b4-endorsements.md findings): refundNeedsApproval in lib/approvals/threshold.ts asks whether the refund alone is above $1,000 or takes the policy's refunds already sent plus on their way above it, read at request time by the endorsement and cancellation plans, at send time in assertRefundMaySend excluding the operation being sent, and on re-issue (F-B4-04); expireOpenCheckoutSessionsOfPolicy after a cancellation commits, issuance and endorsement pages alike, failures returned not thrown (F-B4-05); a delta arriving for a cancelled or voided policy, or whose request cannot be read, is parked with the rule-14 entry (F-B4-05); the endorsement standing derives approvalRequired from the events (F-B4-08); the $500 customer threshold counts the additional premium still waiting for that customer (F-B4-09). Builder checks: 335 tests, endorsement replay 80/80, refund 27/27, claims 63/63. F-B4-07, 10, 11, 12 stay open (B13).

B8 (docs/handoffs/b8-implementation-notes.md): lib/money/correction.ts (one pure function: re-prices the endorsement with only the date changed, signed difference, commission on it, both approval questions), lib/ledger/correction-entries.ts, lib/policy/correct-endorsement-date.ts (preview, then one transaction: correction_reversal superseding the wrong endorsed event, reversal entries at the originals' effective date, correction_rebook with fresh premium and tax at the corrected date, then the settlement of the difference through the B4, B5 and B7 machinery: Stripe refund with the approval queue above $1,000, or a hosted Checkout with customer approval above $500; the cash entries are never touched, the difference lands in premium_receivable), lib/payments/correction-collection.ts, lib/policy/correction-read.ts, migration 0014_corrections.sql (correction_collections, guarded), five screens (preview at /policies/{id}/corrections/new, explanation, timeline with effective and recorded dates side by side and superseded events named, as-of view) and three routes, scripts/check-correction-replay.ts 47/47. Deviations: the correction supersedes the wrong endorsed event only (the request is named in the payload); only the most recent endorsement in force can be corrected; a correction is refused while an earlier difference is unsettled; only an endorsement that collected premium can be corrected (one that refunded is refused with a sentence, disclosed); collecting a difference has no broker eligibility gate (it settles a receivable, binds nothing). Builder checks: 328 tests, correction replay 47/47, every other replay unchanged, guards 141 plus contention. Merge conflict in scripts/check-money-guards.ts resolved by keeping both sides. Migration 0014 applied to corgi_test and the trial database before the deploy. Decision points for Yoann: the commission on a correction rounds per piece (same rule as decision 18); corrections limited to endorsements that collected premium. Coordinator checks on the merged tree: typecheck, 370 tests (369 pass, 1 skipped), build, correction replay 47/47, endorsement replay 80/80, reconciliation 39/39, claims and approvals 71/71, statements 38/38, payment replay 32/32, refund replay 27/27, kyb replay 26/26 (all at 17:50Z); money guards 161/161 at 17:45Z on an ephemeral database migrated 0001 to 0017 (17 files) with the runtime role, then dropped.

## 2026-09-08T18:05:00+00:00 | B10 re-review PASS; two follow-up findings fixed (b1a0bce)

The B10 reviewer re-ran its own probes at e7b5913: 32 rail breaks stay open instead of resolving by silence, the scheduled window widens to the oldest open break, caps at 31 days and reports when it cannot reach one, the clearing balances list reads the journal with no window and includes the suspense account (F-B4-02 closed); 60 reconciliation unit tests, check 39/39; deployed run as staff_ops: Stripe 23 provider records against 4 ledger, 20 breaks (sandbox probes), rail 1 against 2, none. Verdict PASS on the six findings. Two new: F-B10-11 (MEDIUM, items written under the old key shape showed a pre-existing break twice: open with a fresh age and resolved under the old key) fixed in the read by excluding from the resolved list any reference still open, no stored row touched; F-B10-12 (LOW, stale 0016 references) corrected. Coordinator checks: 370 tests, reconciliation 39/39. Deploying.

## 2026-09-08T18:07:00+00:00 | B4 re-review PASS

The B4 reviewer re-read the fixes at e7b5913 (docs/reviews/b4-endorsements.md section 11): refundNeedsApproval decisive at send time (a refund written under the threshold cannot leave once another reduction has gone out; failed refunds count for nothing; re-issue uses the same rule); cancellation closes every open hosted page and a delta paid anyway is parked; customer2@example.com signs in on production and sees CGP-01274; approvalRequired recomputed from the events; the $500 threshold cumulative per policy with the deployed preview saying so. 370 tests, endorsement replay 80/80 including the eight new lines, HTTP previews recomputed independently. PASS for the B4 scope. One new LOW passed to the B8 reviewer (F-B4-13, the correction plan's per-operation approval questions). Open LOW unchanged: F-B4-07, 10, 11 (the only remaining case where a delta payment is journaled nowhere: an amount mismatch), 12. Walkthrough: NOT REVIEWED WITH YOANN.

Next: verdicts of the B4, B10 and B9 reviews; B9 follow-ups (premium column, provisional label) to merge; B8 review; the Codex interface branch and the PDF visual pass integrated after B8; then B11 MCP (needs B9 and B10, both merged), B12 polish, B13 hardening and walkthrough, B14 freeze package. T+24h email to be sent by Yoann. Refund leg of rule 14 deferred and disclosed.

## Earlier status (kept as history)

Updated at 2026-09-08T07:13:45+00:00. Track: **1 — POLICY ADMINISTRATION**, explicitly selected by Yoann. Stack and providers: **UNSELECTED**. Repository currently contains instructions and preparation only; there is no product implementation or deployment.

Startup reads: original kit CLAUDE.md, AGENTS.md, WORKFLOW-48H.md and REVIEWER.md; released general and three track briefs supplied in the conversation, extracted in BRIEF-REFERENCE.md. Automatic-fail and readability rules were independently reviewed and corrected. Missing: separately supplied kickoff sandbox rules, chosen stack/providers, actual provider access and product acceptance evidence.

Local documentation is the working status source for this update; the Linear project overview now identifies Track 1, the pending stack discussion and the next-session handoff. Earlier general-rule and readability review corrections remain in force. Tickets YOA-593 (scope), YOA-595 (approval/MCP), YOA-599 (webhooks), YOA-600 (ledger) and YOA-601 (reconciliation) now carry the relevant released-brief overrides, with their existing unchecked scenarios preserved; there is no automatic synchronization. Its 162 checks are not completed tests.

Three independent Sol instruction reviews completed: brief coverage, financial/security rules, practical workflow/readability. Material findings were corrected and re-reviewed with scoped design PASS verdicts; see reviews/instructions-2026-09-08.md for initial findings, coordinator dispositions and minor post-review wording fixes. Product AF-01 through AF-06 verification: NOT RUN. Documentation changes do not establish passing product controls.

Next session: follow HANDOFF-PROMPT.md. Discuss at most two stack options, verify required sandbox access, then agree the stack and prepare the one-page attack plan. This session prepares the handoff only; no product implementation or finalized attack plan was requested. Logging is currently explicit, manual, sanitized entries in DECISIONS.md; no automatic prompt-capture hook or background review process is installed. At a material decision, scope change, completed slice or checkpoint, append a timestamped record identifying user decision versus assistant proposal and checks actually run.

Documentation verification: mandatory relative links resolve, original preparation archive SHA-256 values match its import manifest, and the three active kit copies agree. Gitleaks 8.30.1 was installed locally; a manual `gitleaks dir . --redact --no-banner --no-color` scan passed with no findings. This is a documentation-repository scan, not proof of product secret controls. No automatic hook/CI enforcement has been installed.

The Linear central checklist now has a released-brief override banner. Existing scenario states were preserved; no control was marked passed during these documentation edits.

## 2026-09-08T07:10:53+00:00 | Track 1 handoff

Yoann selected Track 1. Desired extension: one combined impact-preview and amount-explanation flow, after mandatory core correctness. Other brainstormed enhancements are optional. The next agent owns the stack discussion and attack-plan drafting with Yoann. Confirmation email is a draft for Yoann to send on the existing thread; no email was sent by the assistant. Personal explanatory HTML remains outside the repo and is not added to the decision log.

Linear handoff update: project renamed to Track 1: Policy administration, overview simplified into current action/core/differentiator/checkpoints, and YOA-593 linked to HANDOFF-PROMPT.md with its ten unchecked scenarios preserved. The 16 domain tickets remain reference material; detailed execution-backlog creation is deliberately assigned to the next session after stack and attack-plan agreement.
