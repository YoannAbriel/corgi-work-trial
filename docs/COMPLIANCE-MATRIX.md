# Compliance and control matrix

Created at 2026-09-08T09:25:00+00:00. One row per requirement or control actually implemented or planned, with its source, the implementing control, the code location, the evidence and its status. Source types: trial requirement (the released brief), engineering safeguard (AGENTS.md, AUTOMATIC-FAILS.md), provider contract, law or guidance. Proposed rules are marked as such. Status: PASS (evidence for the stated revision), NOT RUN, BLOCKED, N/A with rationale. A PASS here is an engineering check, never a legal certification.

Refreshed at 2026-09-09T08:45:00+00:00 against `main` at `9e839bf`, closing F-RC-12. Rules this refresh follows, so a reader can trust every cell:

- Every status names the review record that issued the verdict and the SHA the verdict was measured on. A verdict no record states is never written here.
- Where a slice's own code changed materially after its last PASS, the status reads `PASS at <sha>, re-review of later changes in progress` and never "verified". The drift itself is listed in `docs/reviews/recheck-day2.md` slice by slice.
- `docs/reviews/post-pass-changes.md` is the record of that re-review (sections A to G). **It is being written and is not in the tree at `9e839bf`**; rows citing it name a record that now exists (delivered 2026-09-09T09:01Z at cc7bdda). `docs/reviews/integration.md`, which `REVIEWER.md` requires, does not exist either and is tracked as REV-01.
- Where nothing independent was reviewed, the status says "not reviewed". `docs/reviews/recheck-day2.md` is an audit record: it establishes what each verdict said and what changed since, and it issues no verdict of its own, so a row backed only by it says so.
- A review PASS is a scoped engineering check. It is never a legal certification, and a sandbox integration is never proof of production regulatory compliance.

The table is split into sections for reading; every section uses the same eight columns.

## 1. General non-negotiables of the brief

| ID | Requirement | Source (type, checked) | Applicability | Control | Code | Evidence | Status (revision) |
|---|---|---|---|---|---|---|---|
| DEP-01 | Deployed accessible URL and demo credentials for at least two roles | Brief non-negotiable 1 and automatic fail 1 (trial requirement, 2026-09-08) | The whole submission | Vercel production deployment; seven demo accounts across broker, customer, staff maker and staff approver; `/api/health` reports the built revision | `app/api/health/route.ts`, `app/api/session/login/route.ts`, `scripts/seed.ts` | Day-2 audit at `5d405d2`: `/api/health` HTTP 200, `database: ok`, revision equal to `main`, from outside the dev machine; all seven demo logins HTTP 303; twelve staff routes 200 in 0.21 s to 0.39 s (`docs/reviews/recheck-day2.md`) | Not reviewed: no review record takes the deployment package as its scope. AF-01 audited PASS at `5d405d2` in `docs/reviews/recheck-day2.md`, an audit record that issues no verdict. F-RC-04 (customer landed on a refusal page) fixed at `7bc6ff4` |
| LED-01 | Own double-entry, append-only ledger; every displayed balance derivable from entries, including as of past dates | Brief non-negotiable 2 (trial requirement, 2026-09-08) | All money in the product | `journal_entries` plus `journal_lines`, balances summed over lines filtered by `effective_at`; `policy_current` declared a rebuildable cache, never an authoritative money record | `db/migrations/0001_ledger_core_and_webhook_inbox.sql`, `lib/ledger/post.ts`, `scripts/rebuild-policy-current.ts` | `npm run check:ledger-guards` 10/10; `check:ledger-seal` 4/4; `check:money-guards` 184/184 on an ephemeral database migrated 0001 to 0020 with the runtime role, 2026-09-09T08:32Z (`docs/reviews/FINDINGS.md`, F-RC-02); trial-database read at `5d405d2`: 35 entries, 77 lines, debits 4,262,730 = credits 4,262,730 (`docs/reviews/recheck-day2.md`) | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`). The immutability core carries no commit since that revision (`docs/reviews/recheck-day2.md`, B1) |
| INT-01 | At least two genuinely live third-party sandbox slots, real API calls and real webhooks received by the deployed system | Brief non-negotiable 3 and Track 1 live-required list (trial requirement, 2026-09-08) | Premium collection and broker KYB | Two LIVE SANDBOX slots, both Stripe test mode: hosted Checkout plus the Refunds API, and Connect Accounts v2 business verification; the README integration inventory names the mode of all five slots | `lib/payments/*`, `lib/kyb/stripe-connect.ts`, `app/api/webhooks/stripe/route.ts`, README integration inventory | Live card payments on the deployed URL (CGP-01062 at 10:16Z, CGP-01707 at 18:56Z on 2026-09-08); real refund `re_3UDN8aK6R3v50tIy0J6CmRy3`, 208,109 cents; `account.updated` `evt_1UDOgLK6R3FpfF2DhpwJYnqR` received by the deployed app; 65 webhook events on the trial database, all `livemode: false` and signature verified (`docs/reviews/recheck-day2.md`) | PASS at `b3ba15b` for collection (`docs/reviews/b2-issuance-and-collection.md`) and at `09e71e5` for KYB (`docs/reviews/b3-broker-kyb.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). Disclosed: both live slots run on one provider and Stripe is not a dedicated KYB vendor (README; DECISIONS 2026-09-08T08:54:29Z) |
| WHK-01 | Verified webhook signatures, idempotency, out-of-order tolerance; polling is fallback, not the design | Brief non-negotiable 4; Stripe webhook contract (docs.stripe.com/webhooks, provider contract, checked 2026-09-08) | Stripe events, both the platform and the Connect endpoint | Signature verified on the raw body against either secret; immutable `webhook_events` unique per provider event; mutable leases kept in the separate nonfinancial `webhook_processing`; live-mode events refused; one advisory lock per money operation so a late `checkout.session.completed` never regresses a final status | `app/api/webhooks/stripe/route.ts`, `lib/payments/webhook-inbox.ts`, `db/migrations/0001`, `0013_low_findings.sql` | `npm run check:payment-replay` 34/34; production probes on 2026-09-08, sequential and concurrent, two events stored for four deliveries; invalid signature 400, live-mode 400 | PASS at `b3ba15b` (`docs/reviews/b2-issuance-and-collection.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `369671d`, `0fa828d` and `b956950` with migration 0013 landed after that verdict |
| LED-06 | Backdated correction by reversal entries plus re-book, never an edit; the corrected statement still reconciles | Brief non-negotiable 5 (trial requirement) | Backdated errors on an applied endorsement | One transaction: `correction_reversal` superseding the wrong event, reversal entries at the originals' effective dates, `correction_rebook` with the corrected premium and tax, then settlement of the difference through the existing Stripe rails; original rows never touched | `lib/policy/correct-endorsement-date.ts`, `lib/ledger/reverse.ts`, `lib/ledger/correction-entries.ts`, `db/migrations/0014_corrections.sql` | `npm run check:correction-replay` 55/55 with 36 original rows compared byte for byte before and after; the B8 reviewer reproduced the worked example from scratch (38630 and 907 booked, 43561 and 1023 corrected, 5047 to collect, 739 of commission) | PASS at `2755d11` (`docs/reviews/b8-corrections.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `369671d` added an advisory lock inside the correction-difference posting. The live rehearsal has not been performed, see LF-2 |
| MKC-01 | Money out above a threshold requires a distinct human approver; the initiator cannot approve, and neither can an agent | Brief non-negotiable 6 (trial requirement) | Claim payments, cancellation and endorsement refunds, correction differences | Approval request carrying a sha256 of the immutable intent written before the operation; decision restricted to `staff_approver` and refused again by a database trigger; the gate sits inside `issueRefundsAtStripe` and `sendClaimPayment` so every road to Stripe passes it; threshold cumulative per claim (DECISIONS 2026-09-08T15:34:03Z, decision 17); rule 21, an agent-raised payment always queues whatever the amount (DECISIONS 2026-09-08T20:38:26Z) | `lib/approvals/threshold.ts`, `lib/claims/payments.ts`, `lib/payments/refunds.ts`, `db/migrations/0008_claims_and_approvals.sql`, `0018_mcp_api_keys.sql` | `npm run check:claims-and-approvals` 72/72; HTTP refusals proven on production against the initiator, a broker, a customer and an anonymous caller; live: a further $10 after $1,200 paid on CLM-00212 waits for an approver | PASS at `65b06be` (`docs/reviews/b7-claims-and-approvals.md`, section 12), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `4ddb33b` rewrote the gate for rule 21 and `b956950` carried F-B7-05 to F-B7-10 plus migration 0013, neither inside a review's declared scope. The $1,000 threshold is an assumption of this build, not a Corgi rule |
| REC-01 | Reconciliation job pulls provider truth by API and diffs it against the ledger; a screen shows breaks; a planted break is found; a failed fetch is never reported clean | Brief non-negotiable 7 (trial requirement) | Stripe and the claim payout simulator | Pure diff over stable references, amount, currency and lifecycle status; runs and items append-only; fetch before write so a failed fetch stores a failed run with no items; daily cron under `CRON_SECRET` plus a staff Run now | `lib/reconciliation/*`, `db/migrations/0011_reconciliation.sql`, `0017_reconciliation_item_record_date.sql`, `vercel.json` | `npm run check:reconciliation` 39/39 (every classification once, the planted $42.42 PaymentIntent found, a failed fetch stored failed); production at `5d405d2`: 22 open breaks with provider reference, classification, difference, first seen and age; ten runs, all complete | PASS at `e110a7c` (`docs/reviews/b10-reconciliation.md`), re-review of later changes in progress (`docs/reviews/post-pass-changes.md`, section F): `b1a0bce` changed `lib/reconciliation/read.ts`, the very read behind the finding that caused the original FAIL, and no record verifies that fix |
| MCP-01 | Working MCP surface: at least three read tools with authorization and tenant isolation, one write tool that only enters the human approval queue, and a written list of operations never delegated | Brief non-negotiable 8 (trial requirement) | Agents holding a per-user API key | Streamable HTTP JSON-RPC endpoint; per-user key (sha256 stored, secret shown once, public prefix); reads scoped to the key holder's visibility; `request_claim_payment` only queues; `run_reconciliation` appends a run and moves no money; the `agent` role is refused at login, as a decider by a 0008 trigger, and as an approver key by a 0018 trigger | `lib/mcp/*`, `app/api/mcp/route.ts`, `app/ops/mcp-keys/page.tsx`, `db/migrations/0018_mcp_api_keys.sql` | `npm run check:mcp` 58/58; deployed endpoint exercised from outside by the reviewer (six keys, revoked, 401 after) and by MCP Inspector 2.5.0; 70 recorded calls on production, 25 refusals, 9 unauthorised | PASS at `5f2c841` (`docs/reviews/b11-mcp.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `4ddb33b` rewrote the maker-checker gate the write tool depends on and is in no review's declared scope |
| LED-04 | Money never float: integer minor units, a stated rounding rule and deterministic pro-rata penny allocation | Brief non-negotiable 9 (trial requirement) | All money arithmetic | `bigint` cents in SQL, bigint arithmetic in `lib/money`; the insurer eats the penny, charges floored and refunds ceilinged, decided by Yoann (DECISIONS 2026-09-08T08:04:03Z) | `lib/money/premium.ts`, `lib/money/cents.ts`, `db/migrations/0001` | `npm test` worked examples: 32786 / 87214, 2617, 43606 and -43607; recited example 32876 / 87124 on the 365-day term | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`). `premium.ts` and `dates.ts` stopped changing before the B4 re-review recomputed their arithmetic by hand at `e7b5913` (`docs/reviews/recheck-day2.md`, B1) |
| DEC-01 | Running timestamped decision log, including assumptions and cuts | Brief non-negotiable 10 (trial requirement) | The whole build | `docs/DECISIONS.md`, 38 timestamped entries, each labelled explicit user decision, assistant proposal accepted, or fact recorded; per-slice entries in `docs/STATUS.md`; the cut list in the README and `docs/ATTACK-PLAN.md` | `docs/DECISIONS.md`, `docs/STATUS.md`, `README.md` | Entries run from 2026-09-08T06:29:32Z to 2026-09-09T07:45:59Z, written as the work happened; every review record lists the log in its startup receipt | Not reviewed as a control in its own right. Read and relied on by each per-slice reviewer, for example `docs/reviews/b4-endorsements.md` at `e7b5913` |
| SEC-01 | No secrets committed, sandbox credentials included | Brief automatic fail 5; AUTOMATIC-FAILS.md AF-05 (engineering safeguard) | Repository and history | gitleaks pre-commit hook on staged content; full-history scan before a push; `.env.example` placeholders only | `.githooks/pre-commit`, `.env.example` | `gitleaks detect --redact --no-banner` at `5d405d2`: 347 commits scanned, no leaks, exit 0; working-tree scan 89 findings, all in git-ignored paths; `.env.example` the only tracked env file (`docs/reviews/recheck-day2.md`, AF-05) | Not reviewed: no review record issues a verdict on this control. AF-05 audited PASS at `5d405d2` (`docs/reviews/recheck-day2.md`, audit record). Still owed before the freeze: the literal `gitleaks git --redact --no-banner` from an unrestricted shell |
| AF2-01 | No simulated integration presented as live | Brief automatic fail 2; AUTOMATIC-FAILS.md AF-02 (engineering safeguard) | Every integration slot and every screen that shows one | README inventory of five slots with honest modes; mode wording on the reconciliation screen, the claim screen and the overview; reconciliation run notes carry the source mode | `README.md`, `app/ops/reconciliation/page.tsx`, `lib/reconciliation/source.ts` | Labels present in the deployed HTML: "Stripe: LIVE SANDBOX" and "claim payout rail: LOCAL SIMULATOR"; the UI reviewer checked the inventory labels against the deployed HTML at `856e75c` | PASS at `856e75c` for the labels in the tree at that revision (`docs/reviews/ui-main-merge.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). Open gap F-RC-08 (MEDIUM): the seven console screens carry no mode label while rendering simulated rail records beside Stripe references; fix cycle running (`docs/reviews/recheck-day2.md`) |

## 2. Engineering safeguards from AUTOMATIC-FAILS.md

| ID | Requirement | Source (type, checked) | Applicability | Control | Code | Evidence | Status (revision) |
|---|---|---|---|---|---|---|---|
| LED-02 | Never UPDATE or DELETE money rows, anywhere, ever | Brief automatic fail 3; AUTOMATIC-FAILS.md AF-03 (engineering safeguard) | The protected tables listed in `docs/ARCHITECTURE.md` section 1 | Trigger `forbid_change_of_financial_record` on every protected table; role `app_runtime` with SELECT and INSERT only and no TRUNCATE; UPDATE granted on exactly two declared caches, `policy_current` and `webhook_processing` | `db/migrations/0001`, `0003_seal_journal_entries_and_truncate_guards.sql`, `0004`, `db/client.ts` | `check:money-guards` 184/184 on an ephemeral database migrated 0001 to 0020 with the runtime role, 2026-09-09T08:32Z, database dropped afterwards (F-RC-02 closed); source sweep at `5d405d2`: no `delete from`, no `truncate` outside the guard definitions; 28 of 34 base tables carry the guard triggers | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`), re-confirmed independently by the B9 reviewer at `8b5b490`; the guarded core is byte-identical to the reviewed revision (`docs/reviews/recheck-day2.md`, B1) |
| LED-03 | Balanced entries and atomic posting | Brief non-negotiables 2 and 9; AGENTS.md financial invariants (engineering safeguard) | Every journal entry | Deferred constraint trigger `check_journal_entry_balances`: debits equal credits and at least one line, checked at commit; entries sealed at commit | `db/migrations/0001`, `0003` | Guard checks: an unbalanced entry and an entry without lines are both refused; global debits equal credits on the trial database | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`); F-B1-01 (a balanced line appended to a committed entry) was the FAIL at `2b15397` and is closed at this revision |
| LED-05 | Effective time distinct from recorded time, with the recorded time set by the server | Brief Track 1 as-of and correction items; AGENTS.md historical queries (engineering safeguard) | Journal headers and policy events | `effective_at` date column; `recorded_at` set by a trigger from the database clock, so a client value cannot win | `db/migrations/0001`, `lib/policy/current.ts` | Guard check: a client value of 2000-01-01 is replaced by the server clock; the timeline renders both clocks side by side | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`); the two-clock semantics were re-read end to end by the B8 reviewer at `2755d11` (`docs/reviews/b8-corrections.md`) |
| SBX-01 | Sandbox only, no live-mode keys, no real money and no real personal data | Brief automatic fail 4; AUTOMATIC-FAILS.md AF-04 (engineering safeguard) | Every provider path, including jobs, scripts and MCP | `lib/stripe.ts` throws on any key that is not `sk_test_`; the balance call checks `livemode`; the webhook route rejects live-mode events with 400; a `livemode = false` CHECK in the database | `lib/stripe.ts`, `app/api/webhooks/stripe/route.ts`, `db/migrations/0003` | All 65 webhook events on the trial database are `livemode: false` and signature verified; `.env.local` key is `sk_test_`, zero `sk_live` occurrences, checked by count and never printed (`docs/reviews/recheck-day2.md`, AF-04) | PASS at `8f253a7` for the key and route guards (`docs/reviews/b1-ledger-core.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). AF-04 audited PASS at `5d405d2` (`docs/reviews/recheck-day2.md`, audit record) |
| SUS-01 | Money that arrives when the policy cannot be bound is journaled at once, never left unbooked | Brief non-negotiable 2; DECISIONS 2026-09-08T12:54:22Z, rule 14 (trial requirement plus a candidate decision) | Collection after a broker loses eligibility | `unapplied_cash_received` posted at receipt into the suspense account, applied to the policy when staff bind it; status `paid_not_bound`; open Checkout Sessions expired on eligibility loss | `lib/payments/collection.ts`, `lib/ledger/policy-entries.ts`, `db/migrations/0010_unapplied_customer_cash.sql` | `npm run check:kyb-replay` 26/26; ledger cash equals Stripe cash at every instant on the trial database | PASS at `9a69fd5` (`docs/reviews/b7-claims-and-approvals.md`, rule 14 part), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). Known gap F-B4-01 (MEDIUM): the refund leg for parked money is not built and is disclosed in the README |

## 3. Track 1 gauntlet and required live slots

| ID | Requirement | Source (type, checked) | Applicability | Control | Code | Evidence | Status (revision) |
|---|---|---|---|---|---|---|---|
| PAY-01 | Premium collection live on a real sandbox rail, with an actual refund API | Brief Track 1 live-required list and non-negotiable 3 (trial requirement) | Issuance, endorsement deltas, correction differences, cancellation | Durable money operation with a stable idempotency key before any side effect; Stripe hosted Checkout; journal posting on `payment_intent.succeeded` keyed on the money operation; real Refunds API on cancellation; replay treated as already posted | `lib/payments/checkout.ts`, `lib/payments/collection.ts`, `lib/payments/refunds.ts`, `lib/ledger/policy-entries.ts` | Test card paid on the deployed URL, four entries posted once; `check:payment-replay` 34/34, `check:refund-replay` 27/27; refund `re_3UDN8a...` in the sandbox with three webhooks received at 18:27:59Z | PASS at `b3ba15b` (`docs/reviews/b2-issuance-and-collection.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): 47 files and about 25 commits landed after the verdict, `369671d`, `0fa828d`, `b956950`, `9a69fd5` and `65fb3f4` among them |
| KYB-01 | Broker KYB live, showing pending and failed as well as approved, and gating binding | Brief Track 1 live-required list (trial requirement); AGENTS.md KYC section | Brokers | Stripe Connect Accounts v2 business verification in test mode; eligibility derived only from Stripe's answer, never from browser input; a measured settling window; binding refused at checkout and again at payment; append-only status rows | `lib/kyb/stripe-connect.ts`, `lib/broker/eligibility.ts`, `lib/broker/kyb.ts`, `db/migrations/0006_broker_kyb_wiring.sql` | `npm run check:kyb-replay` 26/26; three demo brokers on the trial database show approved, failed on Stripe's tax-id fixture and never submitted; `account.updated` received by the deployed app | PASS at `09e71e5` (`docs/reviews/b3-broker-kyb.md`, one verdict, no re-review section), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `lib/broker/` moved +212 lines inside `b956950`, which no review record names. Disclosed: Stripe is not a dedicated KYB vendor and a sandbox verification is not proof of a KYB policy |
| T1-01 | Written versus daily earned premium and the unearned liability | Brief Track 1 domain list (trial requirement) | Every policy term and segment | Pure functions earning pro-rata daily over the real day count, per written segment from its own effective date | `lib/money/premium.ts` (`earnedPremiumCents`, `unearnedPremiumCents`, `earnedPremiumOfSegment`, `earnedPremiumAcrossSegments`) | `npm test`: earned plus unearned equals written on every day of the term, both worked examples | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`); the segment functions were re-read and recomputed by hand by the B4 reviewer at `e7b5913` (`docs/reviews/b4-endorsements.md`) |
| T1-02 | Annual calendar term with real leap years, not a fixed 365 | Brief Track 1 domain list (trial requirement) | Term computation | `computeTermDays` over the calendar; February 29 to February 28 | `lib/money/dates.ts` | `npm test`: 365 against 366 days; recited example 32876 / 87124 on the March 1, 2028 term and 32786 / 87214 on the January 1, 2028 term | PASS at `8f253a7` (`docs/reviews/b1-ledger-core.md`) |
| T1-03 | Endorsement premium delta priced from the effective date, never the entry date | Brief Track 1 domain list (trial requirement) | Mid-term endorsements | `endorsementDeltaCents` over the remaining days from the effective date; the quote hash and policy version bind the customer approval to the priced intent | `lib/money/endorsement.ts`, `lib/money/premium.ts`, `lib/policy/endorse.ts`, `db/migrations/0009_endorsements.sql` | `npm test`: 43606 on day 100, 48524 when backdated 30 days, -43607 when the limit is lowered; `check:endorsement-replay` 80/80 | PASS at `e7b5913` (`docs/reviews/b4-endorsements.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `369671d` landed on the delta collection path after the verdict |
| T1-04 | A wrong effective date three weeks ago corrected by reversal and re-book, old rows untouched, the month still reconciling | Brief Track 1 domain list and live-fire item 2 (trial requirement) | Applied endorsements | One transaction: supersede, reverse at the originals' effective dates, re-book at the corrected date, settle the difference; the statement classifies the correction entry types so its cash is not dropped | `lib/policy/correct-endorsement-date.ts`, `lib/money/correction.ts`, `lib/statements/journal.ts` | `check:correction-replay` 55/55 with 36 original rows compared byte for byte; `check:statements` 53/53 with the correction difference carried into the totals | PASS at `2755d11` (`docs/reviews/b8-corrections.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). The rehearsal on the deployed application has not happened, see LF-2 |
| T1-05 | Pro-rata cancellation with the other method representable | Brief Track 1 domain list (trial requirement) | Cancellation | Pro-rata unearned premium and refundable tax; the method is stored on the cancellation event and a short-rate penalty account exists, so short-rate is representable but is not computed in v0 | `lib/policy/cancel.ts`, `lib/ledger/cancellation-entries.ts`, `lib/money/premium.ts` (`cancellationBreakdown`), `db/migrations/0005_cancellations_and_refunds.sql` | `npm test`: refund 87124 on the recited example, tax refunded 2048, fee 0, total $891.72; live cancellation of CGP-01274 refunded 208,109 cents through the real Refunds API | PASS at `e120f69` (`docs/reviews/b5-cancellation-and-refund.md`, one verdict, no re-review section), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): 19 commits and +2,315/-420 landed after the verdict and no B5 reviewer read them |
| CLM-01 | Claims money: reserves adjusted append-only, payments reduce the reserve, incurred equals paid plus reserve, no payment past the limit | Brief Track 1 domain list and live-fire item 4 (trial requirement) | Claims | Pure limit rules for the per-occurrence and aggregate limits; six claim entry types; per-claim advisory lock; the identity is asserted against the journal after each transition; paid when sent on the rail (DECISIONS 2026-09-08T14:32:14Z, decision 15) | `lib/claims/limits.ts`, `lib/claims/payments.ts`, `lib/ledger/claim-entries.ts`, `db/migrations/0008_claims_and_approvals.sql` | `npm run check:claims-and-approvals` 72/72; on production CLM-00212 renders paid $1,200.00 against a reserve outstanding of $3,800.00, so the identity holds on the deployed data | PASS at `65b06be` (`docs/reviews/b7-claims-and-approvals.md`, section 12), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `4ddb33b` and `b956950` landed after the verdict |
| T1-07 | Commission on collected premium, with clawback on refunds, on a defended basis | Brief Track 1 domain list (trial requirement) | Broker commission | Commission earned on collected premium and clawed back on refunded premium, rounded down (DECISIONS 2026-09-08T09:57:02Z); the statement reads the commission movement from the journal, never a rate applied backwards | `lib/ledger/policy-entries.ts`, `lib/ledger/cancellation-entries.ts`, `lib/statements/journal.ts`, `db/migrations/0015_statement_commission_base.sql` | `check:refund-replay` 27/27 (clawback posted once); `check:statements` 53/53; recited example clawback 13068 cents | PASS at `e120f69` for the clawback entries (`docs/reviews/b5-cancellation-and-refund.md`) and at `8b5b490` for the statement reading (`docs/reviews/b9-statements.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7) |
| T1-08 | Monthly broker statement of collected premium, commission, clawback and net due, tied to the ledger to the cent, a closed-month rerun identical forever | Brief Track 1 domain list (trial requirement) | Brokers, per month | Statement runs and lines append-only with a content hash over a canonical text; a knowledge cutoff plus revisions (DECISIONS 2026-09-08T10:02:00Z); a rerun that changes nothing is stored and flagged identical; a corrected month produces a new revision | `lib/statements/compute.ts`, `lib/statements/run.ts`, `db/migrations/0012_broker_statements.sql`, `0016_statement_format_version.sql`, `0020_statement_format_v3.sql` | `npm run check:statements` 53/53 including the ledger tie; five statement runs on production, revision 2 flagged identical to previous; revision 3: premium 576,275, commission 86,441, clawback 47,506, net due 38,935 | PASS at `8b5b490`, scoped to the fix batch (`docs/reviews/b9-statements.md`), re-review of later changes at cc7bdda (`docs/reviews/post-pass-changes.md`, section C): format version 3 (`919cd51`) and migration 0020 reviewed, FAIL on one stale check assertion only (F-PP-05, `check:mcp` pinned to version 2), fixed at ea204d7 with `check:mcp` 58 of 58 reading format v3, confirmed by the reviewer (section C PASS, overall post-PASS verdict PASS at ea204d7). The PDF rewrite `4cef315` is section G, PASS |
| T1-09 | Historical coverage, limits, premium and documents as of any date, including between endorsements | Brief Track 1 domain list and live-fire item 5 (trial requirement) | Policies with endorsements or corrections | The policy is a fold over `policy_events` that skips events superseded by a correction; `?asOf=YYYY-MM-DD` is a fresh server render, so the address can be bookmarked; the two PDFs follow the same date | `lib/policy/current.ts`, `lib/documents/policy-as-of.ts`, `lib/documents/from-database.ts` | The as-of panel on CGP-01707 offers the term start, every effective date still in force and today; the figures change across the endorsement date ($1,200 with $1M/$2M against $2,400 with $2M/$4M) | PASS at `2755d11` for the as-of fold (`docs/reviews/b8-corrections.md`) and at `19baf15` for the as-of steps (`docs/reviews/b12-explain.md`, re-review section), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). Reachability gap: no policy carries two endorsements, so "between two endorsements" has no data, see LF-5 |
| T1-10 | Deterministic rounding penny on pro-rata allocation | Brief Track 1 domain list and non-negotiable 9 (trial requirement) | Every split of a money figure | The insurer eats the penny: charges rounded down, refunds rounded up, commission rounded down; the rule is named on screen in each explanation fold | `lib/money/premium.ts`, `lib/money/refund-allocation.ts`, `lib/money/explain.ts` | `npm test` worked examples 32786 / 87214 and 2617; every fold prints the rounding rule by name next to the integer-cent formula | PASS at `8f253a7` for the arithmetic (`docs/reviews/b1-ledger-core.md`) and at `19baf15` for the folds that state the rule (`docs/reviews/b12-explain.md`) |
| T1-11 | At least one US state's effective-dated premium taxes and fees, separated from premium, earned premium and refundability | Brief Track 1 domain list (trial requirement); California premium tax rate, Cal. Const. art. XIII s. 28(d) (law, read by Yoann 2026-09-08, DECISIONS 09:29:25Z) | The one modeled state, California | `stateTaxCents` floored on charge and `refundedTaxCents` ceilinged on refund, capped at the tax charged; the $25 flat policy fee is an assumption of this build, fully earned at issuance and never refunded | `lib/money/premium.ts`, `lib/ledger/policy-entries.ts`, `lib/ledger/cancellation-entries.ts` | `npm test`: 3600 charged, 2617 refunded, fee 0; the policy page shows premium, tax and fee separately, each with its own fold | PASS at `8f253a7` for the arithmetic (`docs/reviews/b1-ledger-core.md`) and at `e120f69` for refundability (`docs/reviews/b5-cancellation-and-refund.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). The rate is a candidate's reading of a cited official source and is not a legal certification or a tax filing |
| DOC-01 | Declarations and endorsement PDFs generated from data, supporting historical dates | Brief Track 1 live-required list (trial requirement) | Documents | `@react-pdf/renderer` over the policy's event fold, so a document is produced for any business date rather than stored | `lib/documents/from-database.ts`, `lib/documents/policy-as-of.ts`, `lib/statements/pdf.tsx`, `app/api/policies/[policyId]/documents/[document]/route.ts` | Both documents answer `application/pdf` on production for a given `asOf`; rendered pages committed under `docs/evidence/pdf/` | Not reviewed for the current code: the PDF quality pass (`4dbeef9`, +593 lines) has no review record, and the earlier layout work was explicitly deferred by the B4 reviewer to an integration review that does not exist (`docs/reviews/recheck-day2.md`, B12). The generation path itself was PASS at `e7b5913` (`docs/reviews/b4-endorsements.md`), re-review of later changes in progress (`docs/reviews/post-pass-changes.md`, section G) |

## 4. Live-fire items of the brief, and who performed each

Performed-by is established by `docs/reviews/recheck-day2.md` (live-fire table at `5d405d2`). "Live by Yoann" means he drove it himself on the deployed application. "Check only" means the control is proven by a check script or an agent HTTP probe, which is evidence of the control and not the rehearsal the brief asks for.

| ID | Requirement | Source (type, checked) | Applicability | Control | Code | Evidence | Status (revision) |
|---|---|---|---|---|---|---|---|
| LF-1 | Issue and pay with a test card, and watch the journal | Brief Track 1 live-fire item 1 (trial requirement) | Issuance | Four journal entries posted once on `payment_intent.succeeded`; the policy binds only after the webhook | `lib/payments/collection.ts`, `lib/ledger/policy-entries.ts` | **Live by Yoann**: CGP-01062 at 10:16Z and CGP-01707 at 18:56Z on 2026-09-08, four entries each, every figure equal to the recited example (`docs/reviews/recheck-day2.md`) | Performed. The control behind it is PASS at `b3ba15b` (`docs/reviews/b2-issuance-and-collection.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7) |
| LF-2 | Backdated fix on the deployed application | Brief Track 1 live-fire item 2 (trial requirement) | Corrections | See LED-06 and T1-04 | `lib/policy/correct-endorsement-date.ts` | `check:correction-replay` 55/55 and the reviewer's from-scratch reproduction; **no correction has ever been performed on the deployed application and no production policy carries one** (`docs/reviews/recheck-day2.md`) | NOT RUN: not done by anyone. The mechanism is PASS at `2755d11` (`docs/reviews/b8-corrections.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). F-RC-06 (the bare correction page renders no form) is in a fix cycle |
| LF-3 | Replay the payment webhook twice | Brief Track 1 live-fire item 3 (trial requirement) | Webhook inbox | Unique constraint per provider event plus a business guard on the money operation | `app/api/webhooks/stripe/route.ts`, `db/migrations/0001`, `0013` | **Check only**: coordinator HTTP probes on production on 2026-09-08, sequential and concurrent, two events stored for four deliveries, and `check:payment-replay` 34/34; not driven by Yoann (`docs/reviews/recheck-day2.md`) | Control demonstrated, rehearsal not performed by Yoann. PASS at `b3ba15b` (`docs/reviews/b2-issuance-and-collection.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7) |
| LF-4 | Cancel with an open claim, and explain refund, commission and reserve | Brief Track 1 live-fire item 4 (trial requirement) | Cancellation with a claim open | Pro-rata refund through the real Refunds API, commission clawback, the reserve untouched | `lib/policy/cancel.ts`, `lib/payments/refunds.ts`, `lib/claims/*` | **Live by Yoann**: CGP-01274 on 2026-09-08 from 18:22Z to 18:28Z; refund `re_3UDN8a...` 208,109 cents succeeded, three webhooks received, clawback posted, claim CLM-00212 and its $3,800 reserve untouched (`docs/reviews/recheck-day2.md`) | Performed. **Yoann's own spoken explanation of why the reserve stays is still pending**, so the AF-06 half of this step is open. Controls PASS at `e120f69` and `65b06be`, re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7) |
| LF-5 | The policy as it stood between two endorsements | Brief Track 1 live-fire item 5 (trial requirement) | As-of reconstruction | See T1-09 | `lib/policy/current.ts`, `lib/documents/policy-as-of.ts` | **Not done and not currently possible**: no production policy has two endorsements; CGP-01707 has exactly one, effective 2026-10-08. As-of *across* that one endorsement works and was verified (`docs/reviews/recheck-day2.md`) | NOT RUN, blocked on data. A second endorsement on CGP-01707 is the recorded fix. The as-of machinery is PASS at `2755d11` and `19baf15`, re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7) |
| LF-6 | Detect a planted payout mismatch on the breaks screen | Brief Track 1 live-fire item 6 (trial requirement) | Reconciliation | See REC-01 | `lib/reconciliation/*` | **Partly, and not as worded**: a planted $42.42 Stripe probe sits on the deployed breaks screen as a **provider-only** break with its age, among 22 such probes; the claim payout **rail** reports zero breaks on production; `local_only`, `amount_mismatch` and `stale` have never been produced on production; no human planted one and watched it appear (`docs/reviews/recheck-day2.md`) | PARTIAL. The classifications and the failed-fetch rule rest on `check:reconciliation` 39/39 on `corgi_test`, a CHECK constraint and code reading, never on an observation on the deployed application. Control PASS at `e110a7c` (`docs/reviews/b10-reconciliation.md`), re-review of later changes in progress (`docs/reviews/post-pass-changes.md`, section F) |
| LF-7 | The initiator refused on the approvals queue | Internal rehearsal item, from the live-fire table of `docs/reviews/recheck-day2.md`, backing brief non-negotiable 6; not one of the six Track 1 live-fire steps | Maker-checker | See MKC-01 | `lib/approvals/*`, `lib/claims/payments.ts` | **Check only**: `docs/STATUS.md` at 18:30Z records "not done by Yoann, to be done on the next refund", and it never was. The B7 reviewer proved the refusal over HTTP on production against the initiator, a broker, a customer and an anonymous caller | Control demonstrated, rehearsal not performed by Yoann. PASS at `65b06be` (`docs/reviews/b7-claims-and-approvals.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7) |

## 5. The build's own additions

| ID | Requirement | Source (type, checked) | Applicability | Control | Code | Evidence | Status (revision) |
|---|---|---|---|---|---|---|---|
| BLD-01 | Endorsements: request, price, customer approval above $500, delta collected or refunded, schedule and declarations as of any date | `docs/PLAN.md` slice B4 (trial requirement, Track 1 core) | Mid-term changes | Approval bound to the quote hash and the policy version; a positive delta through Checkout, a negative delta refunded immediately (DECISIONS 2026-09-08T09:57:02Z); the policy lock is taken before the operation lock | `lib/policy/endorse.ts`, `lib/payments/endorsement-collection.ts`, `lib/ledger/endorsement-entries.ts`, `db/migrations/0009` | `check:endorsement-replay` 80/80; a live endorsement above $500 approved by the customer and paid by Yoann on CGP-01707, 2026-09-09 at 06:34Z | PASS at `e7b5913` (`docs/reviews/b4-endorsements.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): 17 commits since, `369671d` among them. F-B4-11 (LOW) open |
| BLD-02 | Cancellation with a real refund and commission clawback | `docs/PLAN.md` slice B5 (trial requirement, Track 1 core) | Cancellation | Refund on the original payments through the Refunds API; `refund.*` webhooks keep requested and completed apart and surface a failed refund; maker-checker above $1,000 | `lib/policy/cancel.ts`, `lib/payments/refunds.ts`, `lib/money/refund-allocation.ts`, `db/migrations/0005` | `check:refund-replay` 27/27; two production policies cancelled, one voided; the real refund and its three webhooks in the sandbox | PASS at `e120f69` (`docs/reviews/b5-cancellation-and-refund.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `3e6b9b4`, `1f540d2`, `d850f68`, `4a48a26`, `01a0bd7` and `65b06be` were read piecemeal by other slices' reviewers and never by a B5 reviewer |
| BLD-03 | Corrections: preview, one transaction, explanation, timeline with both clocks | `docs/PLAN.md` slice B8 (trial requirement, Track 1 core) | Applied endorsements | See LED-06; a correction is refused while an earlier difference is unsettled, and only the most recent endorsement in force can be corrected, both disclosed | `lib/policy/correct-endorsement-date.ts`, `lib/policy/correction-read.ts`, `lib/payments/correction-collection.ts`, `db/migrations/0014` | `check:correction-replay` 55/55; the correction entry point answers 200 on production | PASS at `2755d11` (`docs/reviews/b8-corrections.md`), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7). F-B8-06 (LOW) open by the coordinator's decision |
| BLD-04 | Broker statements, format version 3, migration 0020 | `docs/PLAN.md` slice B9 (trial requirement, Track 1 core); DECISIONS 2026-09-09T07:45:59Z, decision 22 | Broker monthly statements | Canonical version 3 after the refund-line premium fix; migration 0020 widens the CHECK on `statement_runs.canonical_version`; a revision keeps the version it was written under, and two revisions of different versions read "format changed, not comparable by hash" | `lib/statements/compute.ts`, `lib/statements/run.ts`, `db/migrations/0020_statement_format_v3.sql` | `check:statements` 53/53, 455 unit tests, build clean; migration 0020 applied to `corgi_test` and the trial database; guards re-proven 184/184 at 0020 | PASS at `8b5b490` for the earlier fix batch (`docs/reviews/b9-statements.md`), re-review of later changes at cc7bdda (`docs/reviews/post-pass-changes.md`, section C): format version 3 (`919cd51`) and migration 0020 reviewed, FAIL on one stale check assertion only (F-PP-05, `check:mcp` pinned to version 2), fixed at ea204d7 with `check:mcp` 58 of 58 reading format v3, confirmed by the reviewer (section C PASS, overall post-PASS verdict PASS at ea204d7). The PDF rewrite `4cef315` is section G, PASS |
| BLD-05 | "Explain this amount" folds and the "as it stood on" steps | `docs/PLAN.md` slice B12, items 2 and 3; DECISIONS 2026-09-08T18:52:54Z (candidate differentiator, accepted by Yoann) | 23 money figures across the policy, claim and statement screens | Server-rendered from the same pure functions the ledger posted with; the component asserts on every render that the explanation ends on the figure above it and prints an alert instead of hiding a disagreement | `lib/money/explain.ts`, `components/amount-explained.tsx` | 47 folds rendered on the deployed revision with zero disagreement alerts; 12 new unit tests; nothing computed in the browser | PASS at `19baf15` (`docs/reviews/b12-explain.md`, re-review section), re-review of later changes PASS (`docs/reviews/post-pass-changes.md`, A to G at ea204d7): `85b40b3` (F-B12-10) landed after the verdict, and the record's own requirement that the list of client files be identical no longer holds since `components/amount-explained-motion.tsx` was added |
| BLD-06 | Customer read-only policy page and change requests | `docs/PLAN.md` slice B13-6; DECISIONS 2026-09-08T20:38:26Z (accepted by Yoann) | Customers of a policy | Both tables append-only under the same guards as the money tables, one answer per request enforced by the database; the request moves no money and changes no policy | `lib/policy/change-requests.ts`, `db/migrations/0019_policy_change_requests.sql`, `app/policies/[policyId]/customer-view.tsx` | `npm run check:change-requests` 43/43; two rows created by the reviewer on production and nothing else | PASS at `06999e3` (`docs/reviews/b13-6-change-requests.md`, new verdict). `countOpenChangeRequests` was rewritten afterwards, and that rewrite was checked against the SQL it replaced by the inbox reviewer at `db1762a` (`docs/reviews/inbox-and-motion.md`). F-B13-05, 07, 08 accepted or fixed |
| BLD-07 | `/inbox`, the per-role notification centre | `docs/PLAN.md` slice B13-8; DECISIONS 2026-09-09T06:35:24Z (accepted by Yoann) | Every signed-in role | Every count is the length of the list it points at, read with the same readers the screens use, so a badge and its list cannot disagree | `lib/inbox/read.ts`, `lib/inbox/sections.ts`, `app/inbox/page.tsx`, `components/what-needs-you.tsx` | `npm run check:inbox-counts` 4/4, read-only, on `corgi_test`; anonymous refused, ownership from the session, no write and no client file | PASS at `db1762a` (`docs/reviews/inbox-and-motion.md`). Open: F-B13-15 (MEDIUM), the what-needs-you block links to sidebar sections instead of inbox anchors; fix cycle running, no fix merged at `9e839bf` |
| BLD-08 | `/ops/console`, the staff operations console v1 | `docs/PLAN.md` slice B13-9; DECISIONS 2026-09-09T07:40:31Z (accepted by Yoann) | Staff only | Read only, staff only, latency figures computed by Postgres over a stated window, personal data masked behind a reveal fold, every reader wrapped so a failing query prints one named line | `lib/console/read.ts`, `lib/console/infra.ts`, `lib/console/safe-read.ts`, `app/ops/console/**` | `npm run check:console` 48/48 on `corgi_test`; read-only and staff-only proven three ways on production; the eleven documented provider limits re-fetched and all accurate | **FAIL at `5d405d2`** (`docs/reviews/b13-9-console.md`) on F-B13-21; fix cycle merged at `d786644` and deployed 2026-09-09T08:40Z (F-B13-21 to F-B13-27, F-B13-29, F-RC-08 mode line on all seven screens, F-RC-09), `check:console` 49/49; re-review by the same reviewer in progress at `d786644`, verdict pending |
| BLD-09 | Rehearsed live MCP agent demonstration | `docs/PLAN.md` slice B12, item 1; DECISIONS 2026-09-08T18:52:54Z (candidate differentiator) | The debrief | A real MCP client against the deployed endpoint, ending on an agent-raised payment that a human rejects | `app/api/mcp/route.ts`, `lib/mcp/tools/`, `docs/handoffs/b12-1-agent-demo.md` | MCP Inspector 2.5.0 (protocol 2025-11-25) on the deployed app on 2026-09-08: five tools, then a $1,200.00 claim payment queued as agent-raised request `8148a717` and rejected by a human approver 87 seconds later; nothing moved; screenshots in `docs/evidence/b12-1/`; session transcript in `docs/handoffs/b11-mcp-session.md` | Not reviewed: B12-1 has an evidence pack and a handoff note, and no review record (`docs/reviews/recheck-day2.md`, B12). The surface it exercises is MCP-01 |
| BLD-10 | Animated explanation of a money figure | `docs/PLAN.md` slice B12, item 4; DECISIONS 2026-09-09T06:35:24Z (accepted by Yoann) | The 23 explained figures | Every string the animation shows is server-rendered and arrives in a prop or a data attribute; a unit test asserts the client source does no arithmetic on cents; JavaScript off and reduced motion both show the finished state | `components/amount-explained-motion.tsx`, `lib/money/amount-explained-motion.test.ts` | Server HTML at rest carries no reveal attribute; fold counts and figures identical to the previous review; 455 tests; 18 frames re-recorded under `docs/evidence/b12-4/` | PASS at `db1762a` (`docs/reviews/inbox-and-motion.md`), re-review of later changes pending in that same record: F-B12-11 (MEDIUM) and F-B12-12 to F-B12-18 were fixed and merged at `5590242`, and the reviewer's re-review is held until the inbox fix cycle lands so one pass covers both (`docs/STATUS.md` 2026-09-09T08:26:00Z) |
| BLD-11 | Decorative illustrations (Linear YOA-639, written by Codex, reviewed cross-model by Claude) | `docs/reviews/illustration-cross-review.md` (engineering safeguard: a reviewer never reviews its own author's work) | Home, login, error, 404, broker and customer homes, inbox, statements list, two empty states on ops pages | No visual on any money table, journal, approvals queue, breaks table or console screen; decorative images carry an empty alt; delivered through next/image with intrinsic sizes and no preload off-route | `components/decorative-illustration.tsx`, `public/illustrations/library/`, `docs/illustrations/catalog.json` | 87 placement measurements over 29 routes at three widths; image bytes per route before and after under the record | **PASS at 5fa3d49** (FAIL at 08b3678 on weight, fixed): 3,288,716 B of images over seven routes before, 43,642 B after, zero preload, 21 of 21 layouts without overflow; three LOW disclosed (137 unreferenced assets, one key name, record accuracy) |

## 6. Delivery controls of this repository's own workflow

| ID | Requirement | Source (type, checked) | Applicability | Control | Code | Evidence | Status (revision) |
|---|---|---|---|---|---|---|---|
| REV-01 | Independent review of the integrated system before submission, recorded in `docs/reviews/integration.md` | `REVIEWER.md` and `AGENTS.md` completion gates; `docs/PLAN.md` slice B13 acceptance criterion (engineering safeguard) | The whole system before the freeze | An independent reviewer scoped at the frozen revision, pointed at what nothing else covers | not applicable, a review record | None | **PASS at a1e525d** (`docs/reviews/integration.md`, FAIL at 08b3678 then re-review 2026-09-09T10:08Z): 24 of 24 figures agree to the cent across five screens for the two live policies; ledger invariants hold on production; role boundaries hold; the maker-checker bypass through key minting closed and replayed on production (7 keys before and after); the customer terms, the console status, the endorsement fold's real recomputation, the PDF charged total and rule 21's fail-closed test confirmed; F-INT-04 and 06 are two production runs assigned to Yoann; decision 24 (threshold base) merged at 20439ad, re-review pending; AF-06 not satisfied until the walkthroughs |
| SCR-01 | Three key screens with default, loading, empty, error and one edge state | Brief scoring note on the three screens that matter; `docs/PLAN.md` slice B13 (trial requirement) | Policy detail with as-of, reconciliation breaks, broker statement | Not built as a stated set of five states | `app/policies/[policyId]/page.tsx`, `app/ops/reconciliation/page.tsx`, `app/ops/statements/**` | None for the five states. "Loading state" and "error state" appear in none of the seven UI records; the only loading-state work deleted the root loading boundary (F-UI-01). No screen that exists today has been rendered at 375 px: the one narrow-viewport session was at `af08635`, before the polish, the rebuild, the customer view, the folds, the animation, the inbox and the console (`docs/reviews/recheck-day2.md`, B13) | **PASS at 3e9d095** (`docs/reviews/b13-screens.md`, FAIL at 08b3678 then confirmed after the fix; 36 frames plus two fixed-state frames under `docs/evidence/b13-screens/`): default, error and edge states measured on the three screens at 375 px and 1280 px, 1444 amount cells right-aligned, no stack trace, every empty and error state carries a sentence, scrollWidth 375 at 375 px after opening an explanation; four LOW open (no loading state named anywhere, a repeated as-of parameter printed two ways, counter-generated scroll-region labels, no per-slot mode chip on two screens); three states unreachable on the trial data, recorded as gaps |
| LOG-01 | Structured, redacted logs with correlation ids | `AGENTS.md` operations and security section; `docs/PLAN.md` slice B13 acceptance criterion (engineering safeguard) | Money routes, webhooks, jobs | **Not implemented.** No logging module, no correlation id, five `console.*` calls in the whole of `lib/` and `app/`, one of which logs a raw thrown error (`lib/mcp/jsonrpc.ts:268`) | none | None | **Not implemented and not yet disclosed as cut** (F-RC-07, MEDIUM, `docs/reviews/recheck-day2.md`). The decision, build a small redacted logger with a request id or cut it in DECISIONS and the README, was put to Yoann on 2026-09-09 at 08:30Z. Not reviewed |

## Open items this matrix records rather than resolves

- **AF-06 is not satisfied.** Every one of the eighteen review records ends "Candidate walkthrough status: NOT REVIEWED WITH YOANN". Two live scenarios were driven by Yoann and both left his own explanation pending (`docs/reviews/recheck-day2.md`, AF-06 table). No engineering PASS in this matrix substitutes for that.
- **Ten slices carry material code changes after their last PASS.** They read "re-review of later changes in progress" above; the record that will close them, `docs/reviews/post-pass-changes.md`, sections A to G, is being written and is not in the tree at `9e839bf`.
- **Four requirements have no implementation or no evidence at all**: LOG-01 (structured redacted logs, not built), REV-01 (integration review, no record), SCR-01 (five screen states, never verified at any revision or width), and LF-5 (as-of between two endorsements, no policy carries two).
