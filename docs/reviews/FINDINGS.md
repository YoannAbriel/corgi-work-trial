# Review findings register

One line per finding from the independent reviews (design and implementation). Full records stay in the review files; this is the running list to check before each push. Severity: HIGH blocks the slice, MEDIUM must be fixed before submission, LOW is fixed when cheap or disclosed. Status: OPEN, FIXED (with the commit), DISCLOSED (known limitation written in README), DECIDED (Yoann's call recorded in DECISIONS.md).

| ID | Sev | Finding (one line) | Fix | Status |
|---|---|---|---|---|
| F-01 | HIGH | Webhook retry after a processing failure was acknowledged and never processed; no lease | Store event, lease with one UPDATE (pending, failed, or processing older than 5 min), 500 on failure | FIXED 3c9ef3a |
| F-02 | HIGH | Journal entries keyed on the webhook event would double-post (several Stripe events per money effect) | Key entries on (money_operation, operation id, entry type) | FIXED in design; code in B2 |
| F-03 | HIGH | Simulated claim rail had no provider-truth source, so a planted mismatch could not be found | Simulator keeps its own provider-side records, reconciled like Stripe | FIXED in design; code in B7/B10 |
| F-04 | MED | Worked example counted 366 days for a March 1 term (365) | Recited example redone with 365 days | DECIDED 29312ca |
| F-05 | MED | Earning window per written segment and negative endorsement delta undefined | Segments earn from their effective date; negative delta: refund now or credit | Segments FIXED in design; negative delta DECIDED: immediate Stripe refund (DECISIONS.md) |
| F-06 | MED | Chart of accounts could not book claims | Accounts added with a worked claim example | FIXED in design; code in B7 |
| F-07 | MED | Closed-month rerun vs corrected month undefined | Statement runs carry a knowledge cutoff; corrections create a new revision | DECIDED: knowledge cutoff plus revisions (DECISIONS.md), code in B9 |
| F-08 | MED | Sandbox checks only on Stripe webhooks | Key prefix check in lib/stripe.ts; livemode CHECK in the database; startup account check still to add before B2's first outbound call | Partly FIXED 8bb9063 and 0003; startup check OPEN (B2) |
| F-09 | MED | Seed and test databases not addressed | Seed refuses a non-empty database; disposable corgi_test database for committing tests | corgi_test FIXED; seed in B2 |
| F-10 | MED | Human-approver check location unspecified | Trigger on approval_decisions plus session-only approve route | FIXED in design; code in B7 |
| F-11 | MED | Recovery "by idempotency key" is not a Stripe capability | Recovery per kind (same key resend, list by client_reference_id, refunds by PaymentIntent) | FIXED in design; code in B2/B5 |
| F-12 | MED | Refund allocation across several payments undefined | Newest collection first, one Stripe refund per PaymentIntent | FIXED in design; code in B5 |
| F-13 | MED | Stripe fees not journaled | Excluded explicitly by reconciliation and disclosed | DISCLOSED (README in B6) |
| F-17 | MED | Commission clawback rounding undecided | Floor | DECIDED: rounded down (DECISIONS.md) |
| R-01 | MED | Design said a failed Stripe refund reverses the refund liability (would restore unearned premium while the customer is still owed) | No entry on refund.failed; liability stays open in the clearing list; staff re-issue | FIXED in design (this commit); B5 review confirms |
| R-02..R-06 | LOW | KYB paragraph read as conditional; lease expiry undocumented; claims_paid row; recorded_at wording; negative-delta tax, allocation and cap | Design text aligned | FIXED in design (this commit) |
| R-07 | LOW | Stale comment in premium.test.ts about clawback rounding; .env.example still lists Sumsub and Middesk placeholders | Comment and placeholders updated | FIXED 0115796 |
| R-08 | LOW | Operation id must reach the PaymentIntent (payment_intent_data.metadata) so payment_intent.succeeded resolves its operation | payment_intent_data.metadata.operation_id set at session creation | FIXED 0115796 (B2 review to confirm) |
| F-B2-01 | HIGH | CGP-01061 bound on a locally fabricated payment in the shared database; ledger cash exceeds what Stripe holds | Reversal entries plus dated correction event (Yoann's decision); fabricated events confined to corgi_test | FIXED: correction event plus 4 reversal entries, ledger cash equals Stripe (B8a, this commit) |
| F-B2-02 | MED | A delivery arriving while another is in flight gets 200, so a killed function can leave an event stuck with Stripe no longer retrying; no stuck-events view | 503 while a lease is in flight; stuck and ignored events view with replay | 503 FIXED e120f69; stuck-events view OPEN (B13) |
| F-B2-03 | MED | Expired Checkout Session leaves the draft unpayable | checkout.session.expired marks failed; new operation and key per attempt | FIXED e120f69 |
| F-B2-04 | MED | The only real payment used a session created by the delegate's local server; no session created by the deployed app yet | Yoann creates and pays a fresh draft on the deployed URL | FIXED: CGP-01274 paid from a session created by the deployed app at 11:24Z |
| F-B2-L | LOW | latest status regresses when session.completed arrives after succeeded; amount mismatch terminal ignored, currency not compared; paidOn uses PI created; concurrent first Pay click 500s on the unique key; KYB checked at session creation not at binding; worktree .env.local copies; explicitness items | Fix with B5/B13; KYB-at-binding with B3 | PARTLY FIXED: the status regression and the late session event closed by F-B2-20 and F-B2-21 (post-PASS B PASS); the remaining items of these day-1 aggregate lines are disclosed (13:33 local) |
| F-B2-13 | HIGH | After a void, the operation's Checkout Session stayed open at Stripe and the attempt was not dead: a later payment would arrive with nothing journalable | Session expired at Stripe immediately (12:47Z); void now expires the session first and marks the attempt dead (reason expired); collection.ts guard (refuse a payment whose entries are reversed) assigned to the B3 branch | Session FIXED 11:47Z; void FIXED 5b14830; second door (voided status, unique-violation handling, replay lines) FIXED b3ba15b; re-review PASS 12:44Z |
| F-B2-17 | MEDIUM | A payment received while the broker is no longer eligible is stored but not journaled until staff bind: ledger cash below Stripe until then | Suspense account unapplied_customer_cash (Yoann, 12:54Z): unapplied_cash_received posted at receipt, premium_collected debits the suspense account at the bind, status paid_not_bound, open Checkout Sessions expired on eligibility loss; kyb replay 23/23 | FIXED 13:34Z, receipt and bind legs built; the refund of parked money for a broker who fails for good is deferred to B13 and disclosed in README |
| F-B2-18 | LOW | The replay guard would refuse a genuine replay after a B8 re-book instead of reading it as already posted | Align the guard on policyWasVoided in B8 | FIXED b956950: aligned on policyWasVoided |
| F-B2-19 | LOW | appendSucceededEventOnce is not serialized on the refused path | Serialize or document in B13 | FIXED b956950: partial unique index on succeeded per operation (0013), on conflict do nothing |
| F-B2-20 | MEDIUM | checkout.session.completed delivered after payment_intent.succeeded appended provider_accepted after succeeded, so the policy page read "last status: provider_accepted" on a bound policy (CGP-01707, live session, 18:57:00Z); ledger and policy status were right | Never append a step after a final status | FIXED 0fa828d: session-completed appended only while no final status exists, two replay-check lines (payment replay 34/34) |
| F-B2-21 | MEDIUM | The F-B2-20 regression exists on the two other Checkout paths: the endorsement delta and the correction difference collections append provider_accepted after succeeded when checkout.session.completed arrives second (seen on CGP-01707's delta on 2026-09-09 06:34:34Z, 60 ms after the success); ledger right, status display of the collection wrong | Same guard as issuance on both writers; readers prefer the final status | FIXED 369671d (one advisory lock per money operation on every collection path; replay checks 34, 80, 55), independent re-review in docs/reviews/post-pass-changes.md |
| F-B3-01 | MEDIUM | Money arriving for an ineligible broker is not journaled (same fact as F-B2-17) | Same fix as F-B2-17 | FIXED 13:34Z, receipt and bind legs built; the refund of parked money for a broker who fails for good is deferred to B13 and disclosed in README |
| F-B3-02 | MEDIUM | Comments in migration 0006 and kyb-onboarding.ts promised a same-key retry after a lost answer that the code cannot do | Comments corrected: the key only collapses a double-sent request; a resubmission is a new attempt with a new key | FIXED 13:10Z |
| F-B3-03 | MEDIUM | The deployed app could show neither pending nor failed: one broker, approved, every submission from a local server | Two demo brokers added (seed and trial database); broker2 submitted from the deployed URL with the failing fixture: pending 13:02:33Z, account.updated evt_1UDOgLK6R3FpfF2DhpwJYnqR received 13:03:26Z, failed appended 13:03:27Z; /ops/brokers shows approved, failed and never submitted; broker3 kept for the live pending demo | FIXED 13:03Z |
| F-B3-04 | LOW | Settling-window corner: an approval recorded inside the window with clock skew is held forever and resubmission refused | Time-relative hold | FIXED b956950: settling window measured to now |
| F-B3-05 | LOW | Double-click guard narrower than claimed: a second click after the first insert commits creates a second account | Serialize per broker | FIXED b956950: advisory lock per broker plus an in-flight submission refusal |
| F-B3-06 | LOW | README lacks the Connect endpoint, STRIPE_CONNECT_WEBHOOK_SECRET and check:kyb-replay | README update | FIXED: README carries the Connect endpoint, STRIPE_CONNECT_WEBHOOK_SECRET and check:kyb-replay; register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B3-07 | LOW | A paid-but-refused policy reads awaiting_payment in lists and the broker is offered to continue the payment | Status paid_not_bound: no Pay button, list reads paid_not_bound | FIXED 13:34Z |
| F-B3-08 | LOW | The "not a dedicated KYB vendor" disclosure is in README but not on the screens | Disclosure line on both screens | FIXED 13:34Z |
| F-B3-09 | LOW | The trial database holds two migrations numbered 0007 (the B7 branch applied its file at 12:38Z) | B7 file renamed 0008; schema_migrations record corrected by the coordinator at merge | FIXED at the B7 merge (duplicate 0007 numbering resolved); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B3-10 | LOW | A broker with no submission at all shows the label "KYB: not yet live: seeded placeholder" on /ops/brokers (seen on broker3 at 13:04Z); the wording belongs to the seeded placeholder row, not to an absent one | Placeholder label shown only when a placeholder row exists | FIXED 13:34Z |
| F-B7-01 | HIGH | A rejected or failed refund above $1,000 could be re-issued by the maker as a new operation with no approval request; nothing on that path checked maker-checker before calling Stripe | Gate moved inside issueRefundsAtStripe (every road to Stripe passes it, a refusal appends nothing); a rejection is an approval-stage failure; a re-issued operation above the threshold carries a new approval request and goes back to the queue; five proof lines in check:claims-and-approvals | FIXED 65b06be, re-review PASS 16:16Z (reviewer replayed its probe: rejected refund re-issue queued, direct Stripe call refused) |
| F-B7-02 | MEDIUM | The threshold applied per claim payment: $1,200 out of one claim as two $600 payments with no approver | Yoann's decision 15:34Z: threshold per claim; claimPayoutNeedsApproval counts paid and pending on the claim, at request and at send; three proof lines and unit tests | FIXED 65b06be, re-review PASS 16:16Z (proven live on CLM-00212: a further $10 after $1,200 paid waits for an approver) |
| F-B7-03 | MEDIUM | Neither job endpoint was scheduled or reachable from a screen | B10: vercel.json daily cron on /api/jobs/daily (recover, settle, reconcile) and the Run now form on /ops/reconciliation | FIXED 28a361c (B10 merge), agreed by the reviewer 16:16Z; README notes that Run now covers reconciliation only |
| F-B7-04 | LOW | A rejected refund displayed as "requested, not completed" with a Re-issue button | failureStage on the refund view: "rejected by the approver, nothing sent" and "Raise a new approval request" | FIXED 65b06be, re-review PASS 16:16Z |
| F-B7-05 | LOW | claim_payout operations are outside recoverStuckOperations | Add the kind to the recovery job | FIXED b956950: claim_payout in stuckOperations, recoverStuckOperation resends through sendClaimPayment |
| F-B7-06 | LOW | A claim can be opened for a loss dated in the future | Refuse occurredAt after today | FIXED b956950: occurredAt after today or before the cover refused in the pure coverage rule (consequence: no new claim on a policy whose cover has not started) |
| F-B7-07 | LOW | A malformed uuid in any id path returns HTTP 500 (pre-existing, now on five more routes) | Validate ids at the route edge | FIXED b956950 and the follow-up commit: lib/http/path-ids.ts on every id route (400) and page (404), including the endorsement, document and statement routes added later |
| F-B7-08 | LOW | The payment-stage route does not check that the operation belongs to the claim in its URL | Check the pair in the route | FIXED b956950: assertPaymentBelongsToClaim |
| F-B7-09 | LOW | journal_entries.claim_id has no foreign key, and B7 is its first writer | Additive migration with the constraint, or accepted | FIXED b956950: foreign key in migration 0013 |
| F-B7-10 | LOW | settleClaimPayment has no actor check of its own | Add the operator check | FIXED b956950: settledBy actor or scheduled job checked inside settleClaimPayment |
| F-B7-11 | LOW | A Stripe error while expiring a broker's open sessions turned a committed KYB transition into a webhook failure the retry could not repair | Error caught and reported on the outcome; the status stays recorded | FIXED 65b06be, re-review PASS 16:16Z |
| F-B7-12 | LOW | Migration numbering skips 0009, so B4's file applies after 0010 on the existing databases and before it on a fresh one | All migrations additive and independent; 0009 must not touch the policy_current CHECK; verified at the B4 merge | DISCLOSED: applied migrations are never renumbered; the order is the same on every database that exists (0009 is simply unused) (13:33 local) |
| F-B7-13 | LOW | A payment below the threshold when requested cannot be sent while a second request waits on the same claim (cumulative rule, fails closed); the refusal message does not say that rejecting the waiting request releases it | Say it in the refusal message | FIXED d3152ea (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B4-01 | MEDIUM | Money parked in unapplied_customer_cash (rule 14 at issuance, or a delta paid for an endorsement that can never be applied) has no refund action: returning it is money out and needs the approval queue | One shared "refund parked money" action through B7's queue | OPEN (B13), disclosed in README |
| F-B4-02 | LOW | The reconciliation screen lists one clearing account per operation kind and never unapplied_customer_cash, so parked cash shows no open clearing balance with its age (ARCHITECTURE section 1 asks for that list) | Add the suspense account to the clearing balances list | FIXED f9fc17f with F-B10-03 |
| F-B4-03 | LOW | Cancelling an endorsed policy returns a cent or two more than pricing the cover in force over the remaining days, because each written premium segment rounds in the customer's favour; ties to the premium_written entries | Yoann to confirm (asked 16:05Z) or switch to pricing on the annual premium in force | DECIDED by Yoann 16:09Z: segment by segment, ties to the journal (decision 18) |
| F-B4-PDF | LOW | Declarations PDF prints "Mailing address not collected by this build" followed by an empty ", CA" line; no issuer block and no page number on either PDF | One clear address line; issuer header and page footer in the react-pdf components only (visual pass, no data change) | FIXED c9064d8 (PDF visual pass): one address line, issuer block, page footer |
| F-B4-04 | MEDIUM | The money-out threshold is read per endorsement, so repeated premium reductions under $1,000 on one policy send refund after refund with no second approver (the door F-B7-02 closed for claims) | Cumulative per policy: refundNeedsApproval counts refunds sent and waiting on the policy, at request and at send | FIXED dcad358 (merged 26dede3): refundNeedsApproval, cumulative per policy at request (endorsement and cancellation plans), at send and on re-issue; proven in the endorsement replay (two $800 reductions, the second queued; a cancellation of $538.41 queued after an endorsement refund); re-review PASS 18:07Z |
| F-B4-05 | MEDIUM | A delta paid after the policy is cancelled or voided is refused and journaled nowhere; cancellation does not expire the open hosted pages | Cancellation expires the policy's open Checkout Sessions; a late delta is parked with the rule-14 entry, never refused unjournaled | FIXED dcad358: expireOpenCheckoutSessionsOfPolicy after the cancellation commits (issuance and endorsement pages, failures returned not thrown); a late delta is parked with the rule-14 entry, never refused unjournaled; proven (one parking entry, no double parking); re-review PASS 18:07Z |
| F-B4-06 | MEDIUM | No endorsement on the deployed app and the customer of the only bound policy (CGP-01274) had no user, so the approval leg above $500 could not be demonstrated | customer2@example.com attached to that customer on the trial database 16:57Z (README updated); live endorsement planned in the walkthrough session | FIXED for the data, verified 18:07Z (customer2 signs in on production and sees CGP-01274); the live endorsement is done with Yoann |
| F-B4-07 | LOW | A dead quote_hash column is described as a control | Drop the description or use the column | FIXED 644e8da (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B4-08 | LOW | An approval flag is trusted from a payload instead of being derived from the events | Derive from the events | FIXED dcad358: the standing derives approvalRequired from the events; the stored flag is only printed; re-review PASS 18:07Z |
| F-B4-09 | LOW | The $500 customer-approval threshold is splittable across requests | Cumulative per policy | FIXED dcad358: the $500 customer threshold counts the additional premium still waiting for that customer plus this one; re-review PASS 18:07Z |
| F-B4-10 | LOW | Concurrent Approve or Pay on the same request answers HTTP 500 | Catch the unique violation and answer with a message | FIXED 0e87174 (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B4-11 | LOW | An amount mismatch on a delta payment is refused but not journaled | Park or record with a reason | DISCLOSED (week two): journaling a refused mismatch is a posting change; the refusal itself is proven by check:endorsement-replay (13:33 local) |
| F-B4-12 | LOW | The endorsement posting takes no advisory lock | Lock per policy like claims | FIXED 7d18e28 (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B4-13 | LOW | The correction plan (lib/money/correction.ts, correction-read.ts) asks the approval questions per operation instead of cumulatively; not a bypass (the send gate is cumulative), but the preview can say no approver is needed and the refund then cannot be sent | Adopt refundNeedsApproval and the cumulative customer threshold in the correction plan | MERGED into F-B8-02 (refund side, MEDIUM) and F-B8-04 (collection side, LOW) by the B8 reviewer; one fix, dispatched |
| F-B10-01 | HIGH | A break whose records fall outside the compared window leaves the open list and shows as resolved although nothing fixed it; the daily cron's 7-day default self-resolves anything older than a week (reproduced: 14 open rail breaks became 0) | Resolution only by a later run whose window covers the record's date; the daily job widens its window to the oldest open break | FIXED f9fc17f (merged 010a6fe): each item stores its record date (migration 0017); a break resolves only when a later complete run of the same source whose window contains that date stops reporting it; the daily job widens its window back to the oldest open break, capped at 31 days; re-review PASS 18:05Z |
| F-B10-02 | MEDIUM | The break key contains the classification, so a reclassified break resets its age and its old key reads as resolved | Key on source and reference only; first seen from the earliest non-matched report | FIXED f9fc17f: key on source and reference (our operation id first), classification from the latest run; re-review PASS 18:05Z |
| F-B10-03 | MEDIUM | The non-zero clearing balances list the architecture asks for is not built (same fact as F-B4-02) | Account-level list on /ops/reconciliation, window-immune, with the age of the oldest unmatched entry | FIXED f9fc17f: the four clearing accounts on /ops/reconciliation per policy or claim, read from the journal without a window, with the oldest entry and its age (closes F-B4-02); re-review PASS 18:05Z |
| F-B10-04 | LOW | An error after the fetch stores no run at all and stops the second source | Independent runs per source; a failed run stored on a store error | FIXED f9fc17f: a storage failure stores a failed run and does not stop the next source; re-review PASS 18:05Z |
| F-B10-05 | LOW | The failed-run banner claims the latest run failed even when that source completed runs since | Banner on the latest run of the source | FIXED f9fc17f: one banner per source from its latest run; re-review PASS 18:05Z |
| F-B10-06 | LOW | Only succeeded PaymentIntents are compared and the run note does not disclose the dropped statuses | Disclose in the note | FIXED f9fc17f: the run note counts the PaymentIntents it did not compare; re-review PASS 18:05Z |
| F-B10-07 | LOW | Unbounded reads behind the breaks screen | Limit and page | FIXED with F-PP-11 at 2b904a7 (the reader is bounded; the screen still calls openBreaks, follow-up) |
| F-B10-08 | LOW | The deployed board is eleven sandbox probes with no acknowledgement path | Acknowledge action (append-only) | DISCLOSED 85b40b3: README sentence, no dismiss button by design |
| F-B10-09 | LOW | Pairing by provider reference silently keeps the last of two ledger records sharing one reference | Report duplicates as a break | FIXED 99e4258 (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B10-11 | MEDIUM | Items written before the key change stay under the old key, so a pre-existing break showed twice on the production board: open with a fresh age and resolved under its old key | The read excludes from the resolved list any reference still present in the open list (no stored row touched) | FIXED b1a0bce |
| F-B10-12 | LOW | Stale "0016" references after the renumbering to 0017 (migration header, read.ts comment, handoff notes) | Corrected | FIXED b1a0bce |
| F-B9-01 | HIGH | A month containing an endorsement dropped the endorsement cash from the statement and filed its commission as an adjustment (premium collected 125320 instead of 169904, commission 18000 instead of 24534; net due still tied) | Follow-up 9dc1775 reads the endorsement collection entries as collected premium | FIXED 51883fe, verified by the reviewer's own probe |
| F-B9-09 | HIGH | Migration 0015 redefined premium_collected_cents (cash to premium) with cash_collected_cents defaulting to 0 while three runs already existed on the trial database (created by the reviewer at 17:03Z after the delegate's zero-runs check): those immutable rows would print "Cash collected $0.00" | Format version column (old rows v1, rendered with their v1 meaning), fresh revisions of the affected months on the deployed app, the three v1 runs disclosed | FIXED 8b5b490, re-review PASS 17:32Z: verified on production (v1 rows render with their v1 meaning and their hashes untouched; revision 3 ties on the reviewer's independent ledger read: cash 594817, premium 576275, net due 38935) |
| F-B9-02 | MEDIUM | The PDF footer promises unconditional hash reproduction; recorded_at is the transaction time | Exact sentence: same broker, month and cutoff reproduce the same lines and hash; recorded_at outside the hash | FIXED 8b5b490, re-review PASS 17:32Z (entryRecordedAt kept in the hash: it makes the hash cover the knowledge state) |
| F-B9-03 | MEDIUM | The handoff note names the wrong month for parked cash (parked on 2028-03-31 and bound later lands on March as revision 2, April empty); the code is right, the explanation is wrong | Note corrected | FIXED 8b5b490, re-review PASS 17:32Z |
| F-B9-04 | LOW | POST /api/statements/run answers 500 on a malformed brokerId from the form | Validate the field | FIXED 8b5b490, re-review PASS 17:32Z (clean 303 on production) |
| F-B9-05 to F-B9-11 | LOW | Six further LOW items in docs/reviews/b9-statements.md | B13 | F-B9-07, 10 FIXED; F-B9-08 FIXED c9064d8; F-B9-06 half; F-B9-05, 11 OPEN (B13) |
| F-B1-01 | HIGH | app_runtime could append balanced lines to a committed entry (history changed without UPDATE) | Trigger: lines only in the transaction that created the header; test on corgi_test | FIXED 0003 (a94f091), re-review PASS 8f253a7 |
| F-B1-02 | MED | db/client.ts fell back to the owner connection when DATABASE_URL_APP was unset | Fail closed: DATABASE_URL_APP required | FIXED a94f091 |
| F-B1-03 | MED | Owner could TRUNCATE protected tables | BEFORE TRUNCATE triggers | FIXED 0003 (a94f091), re-review PASS 8f253a7 |
| F-B1-10 | LOW | UPDATE grant on webhook_processing is table-wide (status, attempts, last_error, updated_at are all the app may change, but a column-level grant would document it) | Column-level grant in a later migration | DISCLOSED (week two): a column-level grant needs a migration; the table holds no money and is declared a mutable processing cache (13:33 local) |
| F-B1-L | LOW | received_at client-settable; no DB livemode check; refunded tax may exceed tax charged by one cent on early cancellation; claims_paid account missing; two synthetic replay-test events in the production inbox; README gaps | received_at and livemode fixed in 0003; tax cap in B5; account in B7; replay events disclosed in README; README in B6 | Partly FIXED; rest tracked in the slices named |
| F-B5-02 | MED | Staff re-issue after a create-stage refund failure could create a second Stripe refund (new operation, new key) | Retry the same operation and key for create-stage failures; new operation only after a Stripe-reported failure; list refunds first | FIXED e77baa6 (re-issue under the same key after a Stripe listing), reviewed PASS by the B7 reviewer at e77baa6 |
| F-B5-03 | MED | A refund operation stuck in requested has no resume action or recovery job | Staff "send to Stripe again" plus /api/jobs/recover-operations | FIXED e77baa6 (resend action and recover-operations job), reviewed PASS by the B7 reviewer at e77baa6 |
| F-B5-04 | MED | The live refund was issued from the delegate's local server; a cancellation confirmed on the deployed URL is still needed for the evidence pack | One cancellation on the deployed app after B7 (with approval) | FIXED 18:28Z: cancellation of CGP-01274 by Yoann on the deployed app (effective 2026-10-31, open claim CLM-00212 untouched), approved by approver@example.com (hash 04cf55bd), sent by ops@example.com, Stripe refund re_3UDN8aK6R3v50tIy0J6CmRy3 succeeded 208109 cents, webhooks refund.created, refund.updated and charge.refunded received at 18:27:59Z, refund_completed and commission_clawback posted |
| F-B5-L | LOW | Version check outside the transaction (move inside with a row lock before B4); cash date from refund.created; PaymentIntent fallback could adopt a dashboard refund; README wording for future-dated cancellation and payment_failed label on expiry; stale comment in read.ts; disclosures | Fix with B4/B6/B13 | PARTLY FIXED: the status regression and the late session event closed by F-B2-20 and F-B2-21 (post-PASS B PASS); the remaining items of these day-1 aggregate lines are disclosed (13:33 local) |
| F-B5-01 | MED | The live refund of $3,241.56 crossed the $1,000 money-out threshold with no human approver because maker-checker (B7) does not exist yet | B7 inserts the approval queue between recordCancellation and issueRefundsAtStripe; disclosed in README | FIXED e77baa6 (refund above $1,000 waits for a distinct staff_approver, hashed intent), reviewed PASS by the B7 reviewer at e77baa6 |
| B5-RP | LOW | refund.* events delivered before the B5 deployment stay stored as ignored; a staff replay for ignored events is needed | Replay action (B13); this time Stripe was made to emit a new event | DISCLOSED |
| F-UI-01 | MEDIUM | The interface iteration ui-04 added a root app/loading.tsx, which streamed a 200 shell for every route: anonymous requests to protected pages and malformed ids answered 200 with the redirect or not-found only inside the payload (no data leaked, verified) | Root loading boundary removed by the coordinator; error and not-found pages kept; statuses rechecked on production | FIXED af08635, reproduced and confirmed by the UI reviewer |
| F-YA-01 | MEDIUM | Yoann, walkthrough 18:27Z: pages stack every element as raw text; tables with row actions and dialogs or popovers for actions are missing, the screens read as unfinished | Information architecture pass on the main screens (policies, claims, approvals, reconciliation, statements) | OPEN (UI polish delegate after the UI review) |
| F-YA-02 | LOW | Stripe identifiers (pi_, cs_, re_) printed inline in the flow | Behind an info affordance labeled sandbox, kept for the evidence | CLOSED, no move needed (14:27 local): no app page prints a Stripe id inline any more; the reconciliation and console reference columns keep them on purpose (working identity during an incident), documented in a comment |
| F-YA-03 | MEDIUM | No per-role notification: an approver does not see what waits for them; no badge count on Approvals in the sidebar; same for bindings to make and open breaks for operations | Badge counts on the sidebar and a "what needs you" block on the overview, per role, server-rendered | OPEN (UI polish) |
| F-YA-04 | LOW | The policy page banner says "refund request(s) were sent to Stripe" when the refund is waiting for approval | Wording from the refund state | FIXED ec4c5c9, merged 3890300: the waiting state says "waiting for an approver, and nothing has been sent to Stripe"; the changed branch not renderable on trial data |
| F-YA-05 | MEDIUM | Yoann, 19:24Z on the deployed polish (c5bcf2a): the endorse, cancel, documents and claim forms are folded behind a disclosure line under their heading, so the page reads as empty and the primary action is hidden ("rien ici") | Primary actions visible as a button row or an open form; disclosures only for secondary detail | CLOSED (ui-system, 489a36d): the policy band carries Endorse, Cancel the policy, Open a claim as visible buttons |
| F-B8-01 | HIGH | The broker statement drops the cash of a collected correction difference (correction_premium_collected not classified) and files its commission as an unexplained adjustment with no premium base (probe: ledger cash 167380, statement 162333, missing the 5047 difference) | Classify the correction entry types in the statement, premium base read from the re-booked premium under source_kind correction; proof in check-statements | FIXED: re-review PASS at 2755d11 (docs/reviews/b8-corrections.md); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B8-02 | MEDIUM | The correction decides maker-checker per correction, not cumulatively per policy, and discards a refused send outcome, leaving an unsendable operation the operator was told was requested | refundNeedsApproval in the plan, a refund needing an approver queued with a request, refused or queued outcomes surfaced | FIXED: re-review PASS at 2755d11 (docs/reviews/b8-corrections.md); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B8-03 | MEDIUM | The five correction routes and pages answer 500 on a malformed policy id (they landed after the uuid-guard pass) | badPathIdResponse and isUuid from lib/http/path-ids.ts | FIXED: re-review PASS at 2755d11 (docs/reviews/b8-corrections.md); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B8-04 | LOW | The $500 customer threshold is per correction, not cumulative | Cumulative like F-B4-09 | FIXED: re-review PASS at 2755d11 (docs/reviews/b8-corrections.md); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B8-05 | LOW | The timeline prints "effective date ? put right to ?" for the void correction of CGP-01061 (no date keys on that payload) | Voided wording when the keys are absent | FIXED: re-review PASS at 2755d11 (docs/reviews/b8-corrections.md); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B8-06 | LOW | Three refusals rest on reading only (voided policy, refunded endorsement, cancellation after a correction) | Check lines | FIXED 2b904a7, merged bf36b27: three assertions in check:correction-replay (voided policy, refunded delta endorsement, cancellation after a correction on the corrected segment, 122653 against 121307), 58/58; re-review PASS at bf36b27 (docs/reviews/b13-14-low-sweep.md, 14:48 local) |
| F-B8-07 | LOW | The as-of field defaults to today while its min is the term start, so a policy whose term has not started ships a rejected value | Default to the term start when today is before it | FIXED: re-review PASS at 2755d11 (docs/reviews/b8-corrections.md); register line brought up to date 2026-09-09T08:29Z (F-RC-03) |
| F-B8-08 | LOW | Statement premium base of a correction refund split over several Stripe payments: each refund line carries the whole correction's premium (lib/statements/journal.ts subquery keyed on the re-book event); no total or ledger tie affected, the figure is printed and hashed | Read refund_allocations.refunded_premium_cents per line | FIXED 15d3efc (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B8-09 | LOW | The two document date fields on the policy page defaulted to today with min at the term start, so on a future-dated policy they refused to submit | Default inside the constraint | FIXED b9636c2 |
| F-UI-02 | MEDIUM | UI reviewer: the public home and the login page lost every visible sandbox sentence; the only app-level disclosure left is a collapsed control reading "Sandbox"; the first page the panel opens reads as a live product (per-integration labels intact, so not AF-02) | One visible sentence on / and /login; Yoann to confirm the 16:43Z Codex decision entry | FIXED (rule 21 commit): one light sentence on / and /login, Yoann's answer "oui, en léger, petit" |
| F-UI-03 to F-UI-10 | LOW | Eight LOW items in docs/reviews/ui-main-merge.md | UI polish or B13 | SUPERSEDED by the screen rebuilds of 2026-09-08 evening (ui-rebuild PASS at fea572c, ui-polish PASS at 41be7fc) and the day-2 audits; not re-verified line by line (13:33 local) |
| F-UI-11 | LOW | The login page renders inside the workspace shell (brand block, section label, two-link sidebar, breadcrumb, placeholder account block) for an anonymous visitor; raised by Yoann; the seven staff links do not reproduce, the reviewer measured anonymous /login on production | Bare layout for / and /login | SUPERSEDED by the screen rebuilds of 2026-09-08 evening (ui-rebuild PASS at fea572c, ui-polish PASS at 41be7fc) and the day-2 audits; not re-verified line by line (13:33 local) |
| F-UI-12 | MEDIUM | The money mask stripped every character the server refuses before submitting: "1 200,50" became 120,050 and would have been booked as $120,050.00; also 12.345, 1.2.3, -5, 1e3 | Strip only the mask's own characters, leave the rest for the server parser | FIXED b9636c2 for a paste; the typed path reopened as F-UI-22, fixed fea572c |
| F-UI-13 to F-UI-19 | LOW | Dead approval-details CSS, globals.css growth, / still in the workspace shell for anonymous, two count functions without a database parameter, silent count catch, one liveEndorsementRequest read per policy on every page, echo "$007.00" | UI rebuild (YOA-633) or B13-2 | SUPERSEDED by the screen rebuilds of 2026-09-08 evening (ui-rebuild PASS at fea572c, ui-polish PASS at 41be7fc) and the day-2 audits; not re-verified line by line (13:33 local) |
| F-UI-20 | LOW | The four primary actions on /ops/claims/[id] still served folded after 41be7fc | Claim page rebuild | FIXED 0a9821b (open forms in the side column) |
| F-UI-21 | LOW | The opened document lookup sat third on the policy page, above the payment and the journal | Policy page rebuild | FIXED b9636c2 (documents moved to the side column) |
| F-YA-06 | MEDIUM | Yoann, 19:36Z with a screenshot: the policy page was a document, eleven stacked sections in one column, a paragraph before each block, forms inline, 40% of the width empty ("le foutu vrac") | Identity band, actions as buttons, two columns, tables in panels, forms on their own pages, explanations folded | FIXED b9636c2 to e341f83: policy, claim, lists, verification, approvals, reconciliation, statements; rebuild review FAIL at e341f83 on F-UI-22 only (docs/reviews/ui-rebuild.md), fixed fea572c, re-review PASS 20:31Z |
| F-YA-07 | MEDIUM | Coordinator, live step 6 on 2026-09-09: the "Terms in force" panel shows the terms after the latest applied endorsement ($2,400 from 2026-10-08) on 2026-09-09, a month before it takes effect; the label says "in force" | Show the terms as they stand today from the as-of fold, and the endorsement that will change them with its date | FIXED 19baf15, re-review PASS 07:25Z |
| F-YA-08 | LOW | Production audit by the recheck's helper (08:15Z): both customer accounts landed on /broker after signing in, a page telling them they are on the wrong screen; the login route sent every non-staff user to /broker | Route by role: customer to /customer | FIXED (commit "a customer lands on /customer after signing in", 2026-09-09 10:20 local) |
| F-UI-22 | MEDIUM | The money mask reformatted on every keystroke, so a decimal comma typed one character at a time was swallowed: "1200,50" became 120,050 and $120,050.00 would have been booked (rebuild review at e341f83) | Format only on blur, and only a plain amount with no comma; anything else reaches the server untouched | FIXED fea572c, re-review PASS 20:31Z (69,904 strings swept, zero cases where the mask changes the server's answer) |
| F-UI-23 | LOW | A second field-less reconcile form (the heading button) was added; not a bypass, the route requires staff or the cron bearer | Note in the form inventory | ACCEPTED |
| F-UI-24 | LOW | The cancel form's date defaulted to today even past the term end, outside its own max (F-B8-09 class, pre-existing) | Clamp inside the term | FIXED fea572c |
| F-UI-25 | LOW | Journal blocks had no narrow-screen rule: two 128 px amount columns left little room for the account name at 375 px (not verified visually) | Narrower columns under 580 px | FIXED fea572c |
| F-UI-26 | LOW | The policy page's twelve notices are one 80-line inline expression | Readability (AF-06) | DISCLOSED: a refactor of the policy page notices; the page was rebuilt since and the notices read correctly (13:33 local) |

## B11 MCP surface (docs/reviews/b11-mcp.md, PASS at 5f2c841, 19:37Z)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B11-01 | MEDIUM | An agent-raised claim payment at or below $1,000 creates no approval request, and the claim screen shows only the key holder's human name; below the threshold no human is told a machine asked | Print requested_through on the claim payments table; money rule for Yoann: does any agent-raised payment queue regardless of amount | FIXED: claim screen marker 0a9821b; rule 21 decided by Yoann 20:35Z and coded (request always queued for an agent, send gate fails closed), check:mcp 53/53 at the time, 58/58 at ea204d7 after F-PP-05 (format v3) |
| F-B11-02 | LOW | Caller strings reach mcp_calls verbatim (tool name unbounded, refusals quote asOf, method and protocol version in detail) | Bound and sanitise before insert | FIXED 52bdd2b (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B11-03 | LOW | run_reconciliation is agent-callable with no rate limit and its run is attributed to the key holder's name without an agent marker | Agent marker on the run; disclosed limitation | FIXED 63520ac (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B11-04 | LOW | The 0018 trigger fires on key insert only; a later promotion of the holder to staff_approver would leave an agent key on an approver (no code path changes a role today) | Trigger on users.role change, or a check at call time | DISCLOSED (week two): a trigger on role promotion; no code path promotes a user in this build (13:33 local) |
| F-B11-05 | LOW | A failed mcp_calls insert is swallowed; the surface can answer with no audit row | Fail the call or log the failure | FIXED b3e9501 (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B11-06 | LOW | additionalProperties false is advertised but arguments are not validated against the schema | Validate | FIXED 2b78302 (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B11-07 | LOW | Keys never expire; not stated as a limitation | README limitation | FIXED 85b40b3: README limitation sentence |
| F-B12-01 | LOW | get_policy_as_of without asOf on a policy whose term starts later refuses with a terse sentence (builder B12-1) | Name the first effective date in the refusal | FIXED 8bb9fc0 (LOW batch, merged 82591a0), review PASS 07:42Z |
| F-B12-02 | MEDIUM | explainAccountSum lists every line on the account whatever the rule: "Collected at Stripe" shows a credit line against its debit sum (live on CGP-01274); the empty state can never appear | List only the lines the rule sums, each side named | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-03 | MEDIUM | The cancellation earned_premium fold prints a single-segment ratio; on an endorsed-then-cancelled policy it is not the arithmetic that produced the figure, and the cents comparison cannot catch it (not reachable on today's data) | Print the per-segment arithmetic or say the segments are not carried | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-04 | LOW | The "today" as-of step can fall before the term start on a future-dated policy (F-B8-07 class) | Clamp inside the term | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-05 | LOW | "34680 + -30499" double sign in a formula line | Print a subtraction | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-06 | LOW | canonicalVersion >= 2 would explain a future v3 with v2 semantics | Equality, refuse an unknown format | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-07 | LOW | The claim "Paid" evidence nets to zero under $1,200 | Show the lines that add to the paid figure | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-08 | LOW | The tax fold on CGP-01707 displays the F-YA-07 gap ($56.40 in force against $54.08 booked) | Resolved with F-YA-07 | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-09 | LOW | Handoff says the endorsement lines are stored on the event; they are rebuilt from the stored figures | Wording | FIXED 19baf15, re-review PASS 07:25Z |
| F-B12-10 | LOW | The tax fold's evidence was not filtered by the panel's date: the October endorsement tax sat under September's figure | Filter the evidence to entries effective on or before the panel's date | FIXED 85b40b3 |

## B13-6 customer change requests (docs/reviews/b13-6-change-requests.md, PASS at b40e803, 06:57Z)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B13-01 | LOW | The reply route redirects with ?changeRequest=answered but the policy page prints no notice for it | Add the notice | FIXED 06999e3, re-review PASS 07:22Z |
| F-B13-02 | LOW | A customer refused on a foreign policy is redirected to that policy page, which redirects to /customer and drops the error: silent refusal | Redirect straight to /customer with the error | FIXED 06999e3, re-review PASS 07:22Z |
| F-B13-03 | LOW | "answered" decided from the truthiness of joined columns including display_name | Test reply_id is not null | FIXED 06999e3, re-review PASS 07:22Z |
| F-B13-04 | LOW | The lines CHECK allows a repeated line on a direct INSERT; the application dedupes | Refuse duplicates in the application; the database gap noted (no redefinition of 0019) | FIXED 06999e3, re-review PASS 07:22Z |
| F-B13-05 | LOW | openChangeRequestsOfPolicy exported and used by the check only | Accepted | ACCEPTED |
| F-B13-06 | LOW | The customer's timeline reprints staff-written correction text verbatim (an internal payment-intent reference, a review finding id, the word "coordinator" on CGP-01061) | Customer-safe timeline summaries | FIXED 06999e3, re-review PASS 07:22Z |
| F-B13-07 | LOW | No bound on the number of requests a customer may send | Accepted for the trial, README limitation | ACCEPTED |
| F-B13-08 | LOW | The ownership refusal of a broker or staff landed on /broker or /ops with ?error, and neither page printed it | Both homes print the error notice | FIXED 85b40b3 |
| F-B13-09 | LOW | The three fail-closed branches added by the LOW batch (the two quote-hash comparisons of F-B4-07, the under-lock refusal of F-B4-12) have no negative evidence: unreachable by construction or needing a race no check produces | Accepted for the trial: documented as reasoning, not proof | ACCEPTED (docs/reviews/b13-2-low-batch.md) |
| F-B13-10 | LOW | Both new catch blocks treat any SQLSTATE 23505 as the expected unique index, so a future constraint would read as "already approved" | Match the constraint name | FIXED 2b904a7, merged bf36b27: both catch blocks test the constraint name through isViolationOf (lib/ledger/post.ts); re-review PASS at bf36b27 (docs/reviews/b13-14-low-sweep.md, 14:48 local) |
| F-B13-11 | LOW | The MCP "call not recorded" answer returns id null for a request whose id was read | Echo the request id | FIXED 2b904a7, merged bf36b27: notRecorded echoes the request id |
| F-B13-12 | LOW | When one of two ledger records sharing a provider reference pairs by operation id, the other still says the reference could not pair either | Wording of the classifier note | FIXED with F-PP-12 at 2b904a7: the note says which record was paired by the operation id |
| F-B13-13 | LOW | The F-B8-08 fix changes the canonical statement text inside format version 2 without bumping it: a pre-batch v2 revision of an affected month would not reproduce by hash and no chip would explain it (not reachable on the deployed data) | Yoann's decision: bump the canonical version to 3 (additive marker, old runs keep their meaning, the "format changed" chip explains) | FIXED: decision 22 (09:47 local), version 3, migration 0020 widening the CHECK, statements check 53/53 |
| F-B13-14 | LOW | correctionsOfPolicy throws on a correction_rebook payload written before the refund totals existed (39 of 70 such events on corgi_test, none on the trial database); workspaceTasks then empties a role's badge in silence, the inbox names the unreadable policies | Tolerate the missing totals in lib/policy/correction-read.ts | FIXED 2b904a7, merged bf36b27: missing totals read as zero with totalsNotStored on the view and the reason in the sentence; a corrupt value still throws; check:inbox-counts 53/53 with no unreadable policy |

## Inbox and animated explanation (docs/reviews/inbox-and-motion.md, PASS and PASS at db1762a, 10:10 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B13-15 | MEDIUM | The what-needs-you block links each task to /inbox#<sidebar section>, not the inbox anchor: "2 change requests to answer" lands on "Policies to pay" with 0 items (4 of 5 broker kinds, both customer kinds, 1 staff kind) | An anchor field per task, the block links to it | FIXED 97e738d, merged 41ea2c5 (10:56 local): WorkspaceTask carries an anchor beside its section, names live once in INBOX_ANCHORS, unit test that each of the twelve is rendered; check:inbox-counts 53/53 anchor by anchor; re-review PASS at 41ea2c5 (docs/reviews/inbox-and-motion.md, 11:13 local) |
| F-B13-16 to F-B13-20 | LOW | Five LOW on the inbox (the check folds anchors onto sidebar sections, and four smaller items in the record) | See the record | FIXED, all five, merged 41ea2c5 (10:56 local): anchor-by-anchor check over the 25 most recent owners per role (printing what it leaves out), the per-policy catch keeps only a refused stored value and names it, script header, endorsements paid and not applied oldest first; re-review PASS at 41ea2c5 (docs/reviews/inbox-and-motion.md, 11:13 local) |
| F-B12-11 | MEDIUM | Every reveal opens every ancestor details, the journal's show-all fold included, without scrolling: on CGP-01707 all five trace targets sit behind that fold, so a fold expands the whole journal under the reader and draws the connector off screen | Draw the connector only to a visible target; "Trace to the ledger" is the one action that opens, scrolls and pulses | FIXED d036fcb, merged 5590242 (10:28 local): the reveal never opens a fold nor scrolls, "Trace to the ledger" is the one action that opens, scrolls and pulses; re-review PASS at 41ea2c5 (docs/reviews/inbox-and-motion.md, 11:13 local) |
| F-B12-12 to F-B12-18 | LOW | Seven LOW on the animation, including the wording "the browser never computes money" (it does arithmetic on the digits of the server text for the count-up, never on a money figure), the fragment target with JavaScript off, duplicate entry ids when two panels render the same entry | See the record | FIXED, all seven, merged 5590242 (10:28 local): README and handoff say the browser does arithmetic on the digits of the server text during the count and never states a money figure; ids are journal-entry-<panel>-<entry>; no fallback amount; hidden steps out of the tab order; re-review at 41ea2c5: all fixed except F-B12-13, whose README paragraph had not been updated; README rewritten by the coordinator at 11:14 local (the browser never computes a money figure; during the count it does arithmetic on the digits of the server text) |

## Operations console v1 (docs/reviews/b13-9-console.md, FAIL at 5d405d2, 10:17 local; numbered from F-B13-21 because the inbox record holds F-B13-15 to 20)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B13-21 | MEDIUM | `parseSince` bounds the digit count but not the resulting instant, so `since=999999d` (year -712) makes Postgres refuse the parameter and the feed and errors panels both go dark; proven on production, boundary measured between 700000d and 740000d | Clamp the computed instant in the pure function, print that the window was clamped, and assert it in `check:console` | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |
| F-B13-22 | LOW | `openBreaksOfSubject` calls `openBreaks`, which has no `LIMIT`: every 360 page reads every open break in the system and filters in TypeScript, against the slice's own "every read is bounded" | A console-local bounded reader, or an explicit cap stated on the panel | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |
| F-B13-23 | LOW | The `latest` CTE of `acceptedAndUnconfirmedOperations` reduces the whole `money_operation_events` table with no time bound, and the feed page runs it twice per render, twelve times a minute under the 10-second refresh | Bound the CTE by time and read it once, passing the result into `operationsProblems` | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |
| F-B13-24 | LOW | `consoleSubject` is the only read on a 360 page not wrapped in `attempt`: its failure replaces the page with the framework error page instead of a named line | Wrap it and render the heading plus the failure line, or qualify the header claim in the handoff note | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |
| F-B13-25 | LOW | On a broker 360 page the masked customer name is a `<details>` nested inside a `<Link>`: invalid HTML, and the click navigates instead of opening the fold, so that one value can never be revealed | Put the fold and the link side by side, as the last column of the same table already does | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |
| F-B13-26 | LOW | When the search read fails, the page prints the red line and, under it, "Nothing in this database matches that reference"; the trail panel prints "Nothing to show yet" with no line at all: a failed read reported as a fact about the data | Branch on `found.ok` before the empty branch in both panels | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |
| F-B13-27 | LOW | `(payload ->> 'raised_by_agent')::boolean` is the one payload value cast rather than read as text; safe against every writer that exists, but a non-boolean string would take out the approvals panel and the feed | Compare as text (`= 'true'`), or accept and record the payload-shape dependency | ACCEPTED as recorded |
| F-B13-28 | INFO | The failure line prints the driver's raw message, which echoed the operator's own input in the case measured here | None: naming the real error is what makes the line useful; recorded as a known property | ACCEPTED |
| F-B13-29 | LOW | All eleven documented provider limits re-verified accurate, but two are summarised where the file's own rule says quoted: "per month" is added to Neon's "100 CU-hours/project", and Stripe's unqualified per-endpoint 25/second row is presented under the sandbox heading | Quote the two cells verbatim and move the inference into the `what` field | FIXED b652fde, merged d786644; re-review PASS at d786644 (10:47 local, docs/reviews/b13-9-console.md) |

## Day-2 recheck of every slice (docs/reviews/recheck-day2.md, at 5d405d2, 10:29 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-RC-01 | LOW | .gitignore re-ignored .env.example on a later .env* line; the file survived only because it was already tracked | A second !.env.example after that line | FIXED (08:28Z), git check-ignore confirms the file is not ignored |
| F-RC-02 | MEDIUM | The money guards have no proof on the current schema: last proof 184/184 at migration 0019, and 0020 changed a constraint on the protected table statement_runs | Re-prove on an ephemeral database migrated to 0020 | FIXED: 184/184 PASS on an ephemeral database migrated 0001 to 0020 with the runtime role, 2026-09-09T08:32Z, exit 0 (scratch log guards-ephemeral-0020.log); the database was dropped afterwards |
| F-RC-03 | LOW | Register stale: F-B2-21, F-B3-06, F-B3-09, F-B8-01 to 05 and 07 read OPEN, F-B5-01 to 03 read review pending, although the code and the re-reviews closed them | Bring the lines up to date | FIXED: F-B2-21 and F-B5-01..03 at d936b8f, the rest at 08:29Z |
| F-RC-04 | MEDIUM | A customer signing in on production landed on /broker with a red refusal alert | Send the customer to /customer | FIXED before the recheck was read: F-YA-08, 7bc6ff4, deployed 08:16Z; the route comment now gives the per-role rationale |
| F-RC-05 | MEDIUM | Statement format version 3 and migration 0020 unreviewed | Independent re-review | REVIEWED (docs/reviews/post-pass-changes.md, section C, 11:01 local): the application logic passed, one stale check assertion (F-PP-05) fixed at ea204d7 and confirmed: section C PASS, overall post-PASS verdict PASS at ea204d7 |
| F-RC-06 | LOW | /policies/{id}/corrections/new opened bare renders a heading and a red alert with no form, unlike the endorse and cancel pages; it is the entry point of the live-fire backdated correction | Render the form when opened bare | FIXED 6f14579, merged 7b8431c (08:36Z): select of the policy's endorsements, the date it should have carried, the reason, then the existing preview; a policy with no endorsement gets a sentence; staff_ops only |
| F-RC-07 | MEDIUM | B13's "structured redacted logs" criterion is neither implemented nor disclosed as cut: no logging module, no correlation id, five console.* calls, one logging a raw thrown error (lib/mcp/jsonrpc.ts:268) | Build a small redacted logger with a request id on the money routes and webhooks, or cut it in DECISIONS and PLAN | BUILT (decision 25): migration 0021 activity_log with the money-table guards, lib/observability with redaction tests, the 34 routes and the MCP endpoint wrapped, console v2 panels; guards 192/192 on an ephemeral database at 0021, migration applied to the trial database 10:42Z, deployed f7e81aa 10:44Z; review PASS at f7e81aa (docs/reviews/b13-11-observability.md, 12:50 local), four LOW in a short fix cycle |
| F-RC-08 | MEDIUM | The seven console screens carry no LIVE SANDBOX / LOCAL SIMULATOR label while rendering simulated rail records beside Stripe references (AF-02) | One mode line in the console shell, the mode word on every simulated row | FIXED b652fde, merged d786644 (08:39Z): IntegrationModes line on all seven screens, a required rail field on feed and 360 rows, LOCAL SIMULATOR printed on every non-Stripe money row; re-review at d786644: 25 money rows and 83 webhook rows in the 7-day feed, 0 unlabelled |
| F-RC-09 | LOW | Infra page wording: "public egress" where Neon says "public network transfer"; Stripe's mode-agnostic 25 rps row under a "sandbox (test mode)" heading | Quote the provider, move the row | FIXED b652fde, merged d786644 (08:39Z); re-review PASS at d786644 |
| F-RC-10 | LOW | Two review records stamped local Zurich time with a Z suffix (ui-polish; b12-explain twice) | Correct to UTC and say so in the header | FIXED: 5076e02 and 08:28Z for the second b12-explain line |
| F-RC-11 | LOW | .env.local (local file, never committed) set DATABASE_URL_APP and DATABASE_URL_TEST_APP unquoted with an & in the query string, so a shell source left them unset | Quote both values | FIXED locally 08:19Z, nothing to commit |
| F-RC-12 | LOW | COMPLIANCE-MATRIX.md has eleven rows at old revisions, some still "review pending", and no rows for endorsements, cancellation, corrections, statements, B12, change requests, inbox or console | Refresh the matrix, one row per control, verdict and SHA per row | FIXED: 51 rows in six sections at 1560c39, merged 08:41Z with three rows brought up to date (console fix deployed, integration review launched, screens review running); every cited path exists except the two records the matrix names as pending |

## Operations console re-review (docs/reviews/b13-9-console.md, PASS at d786644, 10:47 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B13-50 | MEDIUM | Bounding the in-flight CTE by the feed cursor (F-B13-23) tied the unknown-outcome family to the window: at the default 60-minute view the cockpit shows 0 unresolved operations while two accepted-and-unconfirmed operations exist on the trial database (2 rows at 24 h, 7 d and 3650 d) | Give that reader its own fixed 7-day floor, independent of the feed cursor, and keep the errors panel disclosure true | FIXED a19539b, merged e117a61 (08:51Z): UNRESOLVED_OPERATIONS_FLOOR_DAYS = 7, one read per render, the note and the disclosure say it, one assertion added (check:console 50/50); CONFIRMED PASS at e117a61 by the console reviewer (10:55 local): 2 unresolved operations visible at 60 m, 1 h and every wider window; residual observation: at 24 h and wider the 60-row cap of the merged errors panel pushes the older of the two off, the cap sentence says to narrow the window |
| F-B13-51 | LOW | The errors panel caps at 60 rows with no note and the chip prints the cap as the count ("60 to look at" at three different windows); pre-existing | Print the cap on the panel and say "60 or more" on the chip when the cap is reached | FIXED a19539b, merged e117a61 (08:51Z); CONFIRMED PASS at e117a61: chip reads "60 or more to look at" with the cap sentence, the true count below the cap |

## Post-PASS re-review of money-path changes made after their slice PASS (docs/reviews/post-pass-changes.md, at cc7bdda, 11:01 local): A rule 21 PASS, B posting locks PASS, C statement format v3 FAIL, D PASS, E LOW batch with migration 0013 PASS, F break filter PASS, G PDF rewrite PASS

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-PP-01 | LOW | `wasRaisedByAnAgent` orders `claim_events` by `recorded_at`, a timestamp two rows can share, where the schema provides `sequence_number` as the total order | Order by `sequence_number` | FIXED 2b904a7, merged bf36b27 (14:23 local): the agent-marker reads order claim_events by sequence_number; re-review PASS at bf36b27 (docs/reviews/b13-14-low-sweep.md, 14:48 local) |
| F-PP-02 | LOW | The rule-21 send gate runs a second `claim_events` query inside the money transaction, under the claim lock | Accepted: the alternative is trusting the mutable operation row | ACCEPTED as recorded |
| F-PP-03 | LOW | `postCollectionAndBind` writes `policy_events` and `policy_current` holding only the operation lock, unlike the endorsement path | Say why in the comment, or take the policy lock first (the order in B.3 permits it) | FIXED 2b904a7, merged bf36b27: postCollectionAndBind takes the policy lock then the operation lock, the order every other posting path uses; check:payment-replay 34/34; re-review PASS at bf36b27 (docs/reviews/b13-14-low-sweep.md, 14:48 local) |
| F-PP-04 | LOW | Policy, operation, broker and claim advisory keys share one 64-bit namespace through 32-bit `hashtext`; a collision would serialize unrelated transactions | Disclosed; no correctness or deadlock consequence | DISCLOSED as recorded |
| F-PP-05 | **MED** | `scripts/check-mcp.ts:454` hard-codes `formatVersion === 2`; commit 919cd51 bumped the canonical version to 3 without carrying it, so `check:mcp` is 57 PASS / 1 FAIL and the "53/53" evidence closing F-B11-01 no longer holds | Import `CANONICAL_STATEMENT_VERSION` and compare against it; rerun; restate the F-B11-01 register line | FIXED ea204d7 (09:04Z): the assertion imports CANONICAL_STATEMENT_VERSION where it runs; check:mcp 58 PASS, 0 FAIL on corgi_test, the line reads format v3; the F-B11-01 evidence line restated; CONFIRMED by the post-PASS reviewer at ea204d7 (11:07 local): section C PASS, overall PASS for A to G, the reviewer reproduced 58/58 behind its own server |
| F-PP-06 | LOW | `STATEMENT_VERSIONS_WITH_KNOWN_TOTALS` needs a `readonly number[]` cast at its only use site | Declare it as `readonly number[]` | FIXED f7e81aa (10:43Z): declared as readonly number[]; the redundant cast at the use site stays until the statement page builder finishes |
| F-PP-07 | LOW | The broker home wraps its refusal notice in `.notices`, the staff home does not; the two will not line up | One shape for both | FIXED ec4c5c9, merged 3890300: both homes render the notices shape; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-PP-08 | LOW | `?error` is reflected from the URL into a styled alert on both homes (escaped, session-gated) | Disclosed: the redirect-with-message pattern is application-wide | DISCLOSED as recorded |
| F-PP-09 | LOW | `appendSucceededEventOnce` infers its `on conflict` from the partial index of migration 0013, and neither end names the other | Name the index in the SQL comment | FIXED 2b904a7, merged bf36b27: the index money_operation_events_one_succeeded_per_operation named in the code comment (the applied migration is not edited) |
| F-PP-10 | LOW | `correctionThatReversedOperation` folds the whole policy on every refused payment | Accepted: asking the fold is what stops the two answers drifting (AF-06) | ACCEPTED as recorded |
| F-PP-11 | LOW | `resolvedBreaks` reads every open break and filters in the application, so the screen slows as the open-break count grows | A correlated `not exists` on the reference pair | FIXED 2b904a7, merged bf36b27: resolvedBreaks correlates the open list in SQL, oldestOpenBreakRecordDate is a min(), openBreaksPage returns rows, totalOpen and capped (the screen's cap sentence is a follow-up); check:reconciliation 39/39; re-review PASS at bf36b27 (docs/reviews/b13-14-low-sweep.md, 14:48 local) |
| F-PP-12 | LOW | A genuinely resolved break sharing a provider reference with an open one disappears from the resolved list instead of being annotated (cf. F-B13-12) | Annotate rather than drop | FIXED 2b904a7, merged bf36b27: a resolved break sharing a reference with an open break of another operation is listed and annotated; the same money under an older key stays excluded (F-B10-11); unit assertion; re-review PASS at bf36b27 (docs/reviews/b13-14-low-sweep.md, 14:48 local) |

## Cross-model review of the Codex illustration work (docs/reviews/illustration-cross-review.md, FAIL at 08b3678 on two MEDIUM then PASS at 5fa3d49, 11:40 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-IL-01 | MEDIUM | Four banner panoramas ship at 3456 or 4032 px and 472 to 694 KB into a 220 px CSS box, with no srcset, so /broker 743.6 KB, /customer 691.0 KB, /ops 524.5 KB and / 500.6 KB of decoration, identical at 375 px and 1280 px | Point the four banner entries at 440 px variants, or add a srcset | FIXED a64bd9b, merged 5fa3d49 (09:33Z): next/image with static file imports, per-variant sizes, no preload off-route, dead class removed; local figures 500 KiB to 2.8 KiB on the home, 744 KiB to 7.7 KiB on the broker home, 0 on /login at 375 px; CONFIRMED PASS on production at 5fa3d49 (11:40 local): 3,288,716 B of images over seven routes before, 43,642 B after at 1280 px and 32,544 B at 375 px, zero preload on 21 loads, 21 of 21 layouts without overflow, optimised variants cached (MISS then HIT) |
| F-IL-02 | MEDIUM | The eager 404 illustration makes React preload 40,640 B on all 29 routes including screens that show no image, the 404 also pulls the home banner's 471,938 B it never shows, and /login at 375 px fetches 104,334 B that display:none hides | Drop eager from not-found, error and the login art panel | FIXED a64bd9b, merged 5fa3d49 (09:33Z): next/image with static file imports, per-variant sizes, no preload off-route, dead class removed; local figures 500 KiB to 2.8 KiB on the home, 744 KiB to 7.7 KiB on the broker home, 0 on /login at 375 px; CONFIRMED PASS on production at 5fa3d49 (11:40 local): 3,288,716 B of images over seven routes before, 43,642 B after at 1280 px and 32,544 B at 375 px, zero preload on 21 loads, 21 of 21 layouts without overflow, optimised variants cached (MISS then HIT) |
| F-IL-03 | LOW | 137 of the 150 committed WebPs (about 19.1 MB) plus three older files are referenced by nothing; they are served but never fetched, so the cost is clone, build and deploy size only | Keep the 13 used files in public/, hold the library elsewhere, or disclose | DISCLOSED as recorded |
| F-IL-04 | LOW | app/ops/reconciliation/page.tsx:118 uses the key "all-clear" for the state where no reconciliation has ever run, which is the opposite claim; no user-visible false statement, the sentence beside it is exact | Give the never-run branch a neutral key | DISCLOSED as recorded |
| F-IL-05 | LOW | components/decorative-illustration.tsx retypes 13 file names and 26 dimensions from catalog.json with nothing checking the two agree (13 of 13 correct today) | A unit test reading catalog.json, or import it | FIXED a64bd9b, merged 5fa3d49 (09:33Z): next/image with static file imports, per-variant sizes, no preload off-route, dead class removed; local figures 500 KiB to 2.8 KiB on the home, 744 KiB to 7.7 KiB on the broker home, 0 on /login at 375 px; CONFIRMED PASS on production at 5fa3d49 (11:40 local): 3,288,716 B of images over seven routes before, 43,642 B after at 1280 px and 32,544 B at 375 px, zero preload on 21 loads, 21 of 21 layouts without overflow, optimised variants cached (MISS then HIT) |
| F-IL-06 | LOW | IllustrationBanner emits the class illustration-banner, which has no rule anywhere in app/globals.css | Remove the class or give it its rule | FIXED a64bd9b, merged 5fa3d49 (09:33Z): next/image with static file imports, per-variant sizes, no preload off-route, dead class removed; local figures 500 KiB to 2.8 KiB on the home, 744 KiB to 7.7 KiB on the broker home, 0 on /login at 375 px; CONFIRMED PASS on production at 5fa3d49 (11:40 local): 3,288,716 B of images over seven routes before, 43,642 B after at 1280 px and 32,544 B at 375 px, zero preload on 21 loads, 21 of 21 layouts without overflow, optimised variants cached (MISS then HIT) |
| F-IL-07 | LOW | Record accuracy: four operations screens carry illustrations, not two, and /ops and /ops/statements carry an always-visible banner; measured placement is nevertheless clean | Correct the count in the records | DISCLOSED as recorded |

## Three key screens in five states (docs/reviews/b13-screens.md, FAIL at 08b3678 on one MEDIUM then PASS at 3e9d095, 11:16 local; 36 frames under docs/evidence/b13-screens/)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B13-30 | MEDIUM | One click on "Explain this amount" makes the policy detail and the statement run scroll sideways at 375 px: `.fact` is a grid with no `grid-template-columns`, so its `auto` track sizes to max-content (668 px inside a 277 px box) and `documentElement.scrollWidth` goes 375 to 731 | `.fact { grid-template-columns: minmax(0, 1fr); }`, the same fix `08b3678` applied to `.detail-main`; re-measure with a fold open, not by re-reading the CSS | FIXED 3e9d095 (09:10Z): .fact gets grid-template-columns minmax(0, 1fr); CONFIRMED at 3e9d095 by the screens reviewer (11:16 local): scrollWidth 375 on both screens at 375 px after a real click, 0 overflow at 1280 px on 14 states, slice verdict PASS |
| F-B13-31 | LOW | No `loading.tsx` anywhere in `app/`, and nothing on screen names the wait: no spinner, no skeleton, no `aria-busy`, no sentence. The page is never blank (the shell and heading stream in first), so the behaviour is sound and only the label is missing | Add one `app/loading.tsx`, or record streamed rendering as the deliberate loading design in the handoff | NOT BUILT, with proof (14:27 local): seven loading.tsx files reproduced F-UI-01 exactly, nine refusals answered HTTP 200 with the loading body; removed, the measurement recorded in app/layout.tsx; re-scoped for week two as refusals above each page's Suspense boundary; the confirmation reviewer agrees (a Suspense fallback commits the response as 200, nine anonymous refusals answer 307 today) and asks for one line in the handoff |
| F-B13-32 | LOW | `asOf` is typed `string` but a repeated query parameter delivers `string[]`: the refusal is correct, but the same value prints two ways in one sentence ("on 2026-09-082026-10-08" then "2026-09-08,2026-10-08") | Type it `string \| string[] \| undefined` and take the first entry, or state that the array case is left to the calendar-date refusal | FIXED ec4c5c9, merged 3890300: a repeated as-of parameter is refused once, naming one value; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-B13-33 | LOW | Scroll regions are named by a counter (`Reconciliation table 3`, `Statements table 1`, `Policy details table 3/4/5`), and the shared `BreakTable` hardcodes one label for both the open and the resolved tables, so two regions will share a meaningless name once a break resolves | Pass the label in, as `ageColumn` already is | FIXED ec4c5c9, merged 3890300: no counter-named scroll region left, BreakTable takes a label; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-B13-34 | LOW | Only the reconciliation screen names its integration mode in words; the policy detail and the broker statement show LIVE SANDBOX Stripe money with no per-slot chip. Not an AF-02 violation: neither screen displays a simulated record | Add the `Stripe: LIVE SANDBOX` chip to both headings, or record that the chip is reserved for the reconciliation screen | FIXED ec4c5c9, merged 3890300: mode chips on the policy detail (Stripe LIVE SANDBOX, claim payout rail LOCAL SIMULATOR where a claim exists) and on the statement run; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |

## Integration review (docs/reviews/integration.md, FAIL at 08b3678, 11:10 local; 24 of 24 figures agree to the cent across the journal, the policy page, the explanation folds, the approvals screen and the reconciliation board)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-INT-01 | HIGH | A staff_approver can create an MCP key for the staff_ops user, raise a claim payment with it and approve it as themselves; no layer refuses, so one human makes and checks a money-out above $1,000 | Drop staff_approver from the create allowlist at app/api/mcp-keys/route.ts:22, keep revocation open to both, add a check line | FIXED 5fc56cb, merged a1e525d: create and revoke of keys are staff_ops only, the sidebar link hidden, README updated, check:mcp proves the approver's POST is refused with no key written (59/59); re-review PASS at a1e525d (12:08 local): the attack replayed end to end on production, 7 keys before and after; the trigger-level rule is decision 26, week two; second layer built (decision 32): migration 0024 records the key creator and the maker-checker trigger refuses a decision by that person, review PASS at 845443a, merged 19c4edf, proven on the trial database |
| F-INT-02 | MEDIUM | The customer's own policy page prints policy_current under "These are the terms in force today", so CGP-01707 shows $2,400.00, $56.40 of tax and $2M/$4M limits on 2026-09-09 while the endorsement is effective 2026-10-08 and the staff page says $1,200.00, $28.20 and $1M/$2M | Use the same termsToday fold as app/policies/[policyId]/page.tsx (Yoann's F-YA-07 fix) in customer-view.tsx:90 | FIXED 5fc56cb, merged a1e525d: the customer view reads the terms as they stand today through lib/policy/terms-in-force.ts shared with the staff page; re-review PASS: CGP-01707 prints $1,200.00, $28.20, $1M/$2M on both pages with the 2026-10-08 endorsement named below |
| F-INT-03 | MEDIUM | The console reads the last money_operation_event by sequence number without preferring a terminal status, so the live-paid CGP-01707 is listed on /ops/console as "unknown outcome, accepted and unconfirmed" 824 minutes old | Exclude operations carrying a succeeded or failed event, in acceptedAndUnconfirmedOperations and in latest_status (lib/console/read.ts) | FIXED 5fc56cb, merged a1e525d: statusPreferringTerminal used by the in-flight reader, the errors panel and the 360 tables, one assertion in check:console (53/53); confirmed on production: zero unknown outcome at 60 m, 24 h and 7 d, both 360 pages read succeeded, the two failed payouts still listed |
| F-INT-04 | MEDIUM | No published statement contains CGP-01707 or CGP-01274's cancellation (cutoff 2026-09-08T17:26:29Z precedes all of it), and every stored run is canonical_version 1 or 2 while the code writes 3 | Re-run Redwood 2026-09 once from /ops/statements with an empty cutoff, ideally after the backdated correction | YOANN: one click on /ops/statements to run September (appends a run, moves no money) |
| F-INT-05 | MEDIUM | The endorsement fold's agreement check compares figures.deltaTotalCents to itself, so the fold the README puts first can never raise its alert, and runningSubtotals returns null for it | Assert deltaTotalCents = deltaPremiumCents + deltaTaxCents in figuresFromPayload, or state the distinction in README.md | FIXED 5fc56cb, merged a1e525d: the fold reprices server-side with computeEndorsement from the inputs stored on the endorsed event and compares the six printed figures, three unit tests; re-review PASS: the fold prints "Recomputed today from the same inputs: identical" |
| F-INT-06 | LOW | CGP-01707's endorsement payment pi_3UDf5YK6R3v50tIy1GkJdCCi has no reconciliation item: it landed 34 minutes after the last run | One reconciliation run; the daily cron would also do it | YOANN: one click on the reconciliation screen (the 06:00Z cron of the 10th runs after the freeze) |
| F-INT-07 | LOW | Both PDFs print the premium-only delta ($1,101.36 on CGP-01707) under a caption reading "the prorated amount charged", while $1,127.24 was charged | Say "the prorated premium charged, before state premium tax", or print both | FIXED 5fc56cb, merged a1e525d: both PDF tables print the charged total under "Amount charged", summed in the fold; re-review PASS: $1,127.24 on both PDFs, two fetches identical apart from the generation second |
| F-INT-08 | LOW | Migrations 0008:236 and 0018:39 explain the agent rule by "a user per key with the role agent"; production has no such user and the key carries principal_kind instead | Correct both comments to describe the mechanism that exists | DISCLOSED: applied migration files are not edited; the comment is wrong on production's shape, the register says so (13:33 local) |
| F-INT-09 | LOW | brokers.commission_rate_bps and state_tax_rates.rate_bps determine money and are among the six tables of 34 with no UPDATE/DELETE guard; historical figures are safe because every rate is frozen on its event | Add the guards, or record in DECISIONS why they are configuration | CLOSED, finding was wrong (17:22 local): the builder and the reviewer of the guards slice verified independently that brokers and state_tax_rates already carry the append-only guards; two assertions added to check:money-guards, proven 194 of 194 on an ephemeral database at 0024 |
| F-INT-10 | LOW | Rule 21's fail-closed branch (lib/claims/payments.ts:484) has never been observed firing; no check builds the state and no constraint ties a claim payout to an approval request | One assertion that constructs the state and proves the refusal | FIXED 5fc56cb, merged a1e525d: check:claims-and-approvals builds a $10 agent-raised payout with no approval request and proves the send is refused with the rule-21 sentence (74/74); re-review PASS |
| F-INT-11 | LOW | requestedThrough is a parameter of requestClaimPayment rather than derived inside it from the principal; unspoofable today because only two callers exist | Derive it inside, or name the invariant in a comment | DISCLOSED (week two): deriving the marker inside requestClaimPayment; only two callers exist and both are reviewed (13:33 local) |
| F-INT-12 | LOW | The endorsement preview and the payment gate use two different bases for the $500 customer threshold (endorse.ts:685 against endorsement-requests.ts:138); it fails safe, but the preview can promise a path the gate refuses | One shared function for the base | FIXED 356b900, merged 20439ad, decision 24: one function endorsementNeedsCustomerApproval, the running total from additionalPremiumOfTheTerm, used by the preview, the request standing and both payment gates; re-review PASS at 20439ad (12:26 local): every old base name gone from the tree, the diff of lib/payments, lib/ledger and db/migrations empty, a read-only preview on CGP-01707 names the running total $1,143.96 and asks for the customer, check:endorsement-replay 90/90; the correction path's own base is an accepted scope line (week two) |

## Inbox and animation re-review (docs/reviews/inbox-and-motion.md, inbox PASS and animation PASS at 41ea2c5, 11:13 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-B13-60 | LOW | The anchor type and its unit test prove that each anchor exists somewhere, not that the role naming it renders it | A per-role assertion in the unit test | FIXED ec4c5c9, merged 3890300: a per-role test proved non-vacuous |
| F-B12-20 | LOW | Two folds can name one journal entry, so a fragment arrival runs open, scroll, pulse and connector once per fold (twice on CGP-01274), contradicting the code comment | Run the arrival once per entry | FIXED ec4c5c9, merged 3890300: arrival runs once per entry (scrollIntoView 2 before, 1 after on CGP-01274); CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |

## Integration re-review (docs/reviews/integration.md, PASS at a1e525d, 12:08 local; AF-06 stays NOT SATISFIED)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-INT-20 | LOW | The console 360 shows a green succeeded badge next to an older "Your card was declined." reason on an operation that later succeeded | Print the reason only for the terminal status, or date it | FIXED ec4c5c9, merged 3890300: the earlier declined attempt is dated under the succeeded badge; confirmation at 3890300: fixed on the operations table, STILL OPEN on the timeline table of the same screen (F-LU-01), fix cycle |
| F-INT-21 | LOW | The /ops home still offers an approver the MCP keys card (components/workspace-overview.tsx:73) while the page and the sidebar refuse it | Hide the card for staff_approver | FIXED ec4c5c9, merged 3890300: no MCP keys card for an approver on /ops; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-INT-22 | LOW | The customer terms panel says "on that date" when no date is given | Wording | FIXED earlier by UI cycle B (7220c0b, merged 06166cb): the voided policy reads "Figures on your policy record" |

## Decision 24 re-review (docs/reviews/integration.md, PASS at 20439ad, 12:26 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-INT-23 | LOW | The preview calls the running total the premium the policy "has since issuance", although it is scoped to the current term and includes the quote on screen | Wording: "of additional premium in this term, this quote included" | FIXED ec4c5c9, merged 3890300: "of additional premium in this term, this quote included"; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |

## Observability: activity log, structured logs, console v2 (docs/reviews/b13-11-observability.md, PASS at f7e81aa, 12:50 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-OB-01 | LOW | The `rule` column is null on every refusal a route answers: `RULE_OF_REFUSAL` is read only on the throw branch, and no shipped route lets a refusal class escape. Measured 1 of 6 refusals named on the trial database, and that one is set by hand by the MCP endpoint. Eleven modules imported into every bundle for a path nothing reaches | FIXED 2c7da4e (11:19Z merge): the console failure line goes through the sanitiser (asserted), the rule is a closed union declared per route and applied only where true (sign in, 400 names none), the stored status is the response own, the clock covers the params and the session lookup; check:console 68/68, check:mcp 59/59; CONFIRMED PASS at 06166cb (13:42 local): 8 of 8 refusals since the fix name their rule, 12 of 12 rows store the caller status, the sanitiser masks a connection string, the wrapper clock now includes the session query (5 ms measured) |
| F-OB-02 | LOW | `classify` writes 400 for a thrown refusal and 500 otherwise instead of the status the caller received; the console prints it as `HTTP n`. Latent while no route throws | FIXED 2c7da4e (11:19Z merge): the console failure line goes through the sanitiser (asserted), the rule is a closed union declared per route and applied only where true (sign in, 400 names none), the stored status is the response own, the clock covers the params and the session lookup; check:console 68/68, check:mcp 59/59; CONFIRMED PASS at 06166cb (13:42 local): 8 of 8 refusals since the fix name their rule, 12 of 12 rows store the caller status, the sanitiser masks a connection string, the wrapper clock now includes the session query (5 ms measured) |
| F-OB-03 | LOW | `lib/console/safe-read.ts` builds its on-screen failure line without `redact()`, which now sits in a sibling module; a driver error can carry a connection-string password onto all seven console screens. Raises F-B13-28 (INFO) now that the one-line fix exists | FIXED 2c7da4e (11:19Z merge): the console failure line goes through the sanitiser (asserted), the rule is a closed union declared per route and applied only where true (sign in, 400 names none), the stored status is the response own, the clock covers the params and the session lookup; check:console 68/68, check:mcp 59/59; CONFIRMED PASS at 06166cb (13:42 local): 8 of 8 refusals since the fix name their rule, 12 of 12 rows store the caller status, the sanitiser masks a connection string, the wrapper clock now includes the session query (5 ms measured) |
| F-OB-04 | LOW | Only `app/api` routes are wrapped, so no server-rendered page leaves a row; "latency by route" listed four API routes and no page after a walk that rendered six pages. Decision 25's "latency of every screen" overstates the build; the README is accurate | DISCLOSED: only the API routes are wrapped, the README says so; page latency stays measured by the console tiles |
| F-OB-05 | LOW | `duration_ms` is computed before the `currentUser()` lookup and before the activity insert, so the two costs the slice itself adds to every response are invisible in every figure on the latency panel | FIXED 2c7da4e (11:19Z merge): the console failure line goes through the sanitiser (asserted), the rule is a closed union declared per route and applied only where true (sign in, 400 names none), the stored status is the response own, the clock covers the params and the session lookup; check:console 68/68, check:mcp 59/59; CONFIRMED PASS at 06166cb (13:42 local): 8 of 8 refusals since the fix name their rule, 12 of 12 rows store the caller status, the sanitiser masks a connection string, the wrapper clock now includes the session query (5 ms measured) |
| F-OB-06 | INFO | The JSON line's `recordedAt` is the application clock, the row's `recorded_at` is the database clock; the same request carries two timestamps. The correlation id is the join key | ACCEPTED as recorded |
| F-OB-07 | INFO | `Masked` keeps the full name in the HTML inside a closed `<details>`, so the activity rows are decluttered and not redacted on screen. As decided (rule 23) and as stated in the v1 record; the stored rows and the log lines are genuinely redacted | ACCEPTED as recorded |

## Desktop UI audit fix cycle C (builder report, merged ce60fb2, 13:05 local; review after the three cycles)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-IL-08 | MEDIUM | Since the illustration component moved to static .webp imports (a64bd9b), npm run check:inbox-counts cannot start: the script imports components/what-needs-you, which imports the illustration component, and tsx cannot parse an image import; the gate was broken for every agent since 5fa3d49 (the inbox re-review at 41ea2c5 ran before it) | Move the pure task-building code into lib/inbox/tasks.ts with no component import, the script imports from there | FIXED ccf4d77, merged 22babed (11:23Z): the pure task code lives in lib/inbox/tasks.ts with no component import, the component re-exports it, the script imports the lib module; check:inbox-counts 53/53 without any stub (staff_ops 1596 counted and listed); five pages still import from the component through the re-export, a follow-up |

## Desktop UI audit fix cycles A and B (builder reports, merged a2068c1 and 06166cb, 13:29 local; review docs/reviews/b13-13-ui-audit.md pending)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| UI-006, 011, 013, 014, 026, 029, 030 | P2 | Tables crushed at 1512 px: overflow-wrap anywhere on every cell let the browser size a column one character wide | app/styles/ops-tables.css: minimum width per column role, the detail grid stacks so tables get 1098 px | FIXED (cycle A, 2a3e5bf); review at 06166cb: RESOLVED |
| UI-025 | P1 | The console's meta refresh kept firing after navigating away | A client timer calling router.refresh() every 10 s, cleared on unmount | FIXED (cycle A); review at 06166cb: RESOLVED |
| UI-028 | P1 | Reference trails included journal entries of other policies | The broker clause applies only when the subject is the broker (32 entries before, 5 after) | FIXED (cycle A); review at 06166cb: RESOLVED |
| UI-004, 007, 019, 021, 024, 036 | P2 | Policy list total on future terms; open explanation folds too narrow; endorsement schedule fragments; references popover clipped; correction preview labels one letter per line; voided policy wording | Shared terms-in-force helper on the list, the fact grid lets an open fold span the row, five fixed-share columns, references as an inline fold, text cells without the amount class, wording | FIXED (cycle B, 522b3e9); review at 06166cb: RESOLVED except UI-004 on the broker home (F-UA-01, fix cycle) |
| UI-020, 022, 023 | P1 or P2 | The as-of form kept the previous date after a step click; a voided local payment summarised as collected and refunded; the prefilled endorsement date refused and the form removed | A key on the as-of input, journal sums exclude reversed pairs (lib/policy/read.ts, only the voided policy's one-sided cash sums move), the form re-rendered with the message | FIXED (cycle B); review at 06166cb: RESOLVED, 30 of 30 figures unchanged |
| UI-003, 008, 009, 016, 017, 031, 034, 035 | P1 or P2 | Login layout shift; approvals gutter and rule 21 sentence; inbox ordering and break links; broker told about blocked binding; customer links and today's premium | Cycle C | FIXED (cycle C, a593499); review at 06166cb: RESOLVED except the UI-009 sentence (F-UA-02, fix cycle) |

## Observability re-review (docs/reviews/b13-11-observability.md, PASS at 06166cb, 13:42 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-OB-10 | LOW | A non-broker refused by a bare 303 to /broker with no ?error= is recorded ok, so the refused panel never shows it (app/api/policies/route.ts, app/api/brokers/kyb/route.ts) | Carry ?error= or declare the outcome on those two redirects | FIXED (19:08 local, with the live audit of the posting forms by the interface session): the two wrong-role redirects of /api/policies and /api/brokers/kyb carry ?error= with the refusal sentence, so the home renders a notice and the activity log records a refusal |
| F-OB-11 | LOW | The rules "webhook signature" and "ownership" on the statement PDF can never be recorded, those routes refuse with 400 and 404 | Declare the outcome on those refusals or drop the two rule names | OPEN (B13 backlog) |
| F-OB-12 | INFO | The latency disclosure still says "inside the handler" | Wording | OPEN (B13 backlog) |

## Desktop UI audit review (docs/reviews/b13-13-ui-audit.md, FAIL at 06166cb on one MEDIUM, 14:05 local; 24 of 26 resolved, 2 partly, 30 of 30 figures unchanged)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-UA-01 | MED | UI-004 fixed on /ops/policies and not on /broker: the broker list still prints $2,481.40 for CGP-01707 where the policy page and the staff list print $1,253.20, and CGP-01061 without the "on the policy record" note | Fold each policy on /broker with policyAsItStoodOn plus termsInForceOn, as app/ops/policies/page.tsx:29 already does, and carry its disclosure paragraph | FIXED ec4c5c9, merged 3890300 (14:27 local): the broker list goes through the terms-in-force helper, CGP-01707 reads $1,253.20; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-UA-02 | LOW | UI-009's new sentence names only the agent case; the $10.00 decided request is sub-threshold and not agent-raised, it is there under the cumulative per-claim rule (lib/approvals/threshold.ts:39) | Name the cumulative rule in the same sentence | FIXED ec4c5c9, merged 3890300: lead, empty state and disclosure name both cases (agent-raised, cumulative above the threshold); CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-UA-03 | LOW | table-layout: fixed on the endorsement schedule makes the nowrap header "New annual premium" (121 px) overflow its 118 px column into "Stripe", and leaves the UI-021 references fold 93 px wide | `table.endorsement-schedule thead th { white-space: normal; }`, the repair ops-tables.css:66 already uses | FIXED ec4c5c9, merged 3890300: headers wrap, provider column widened, the references fold 93 px to 123 px; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-UA-04 | MED | At 375 px `.entry-block { overflow: hidden }` (app/globals.css:1706) clips 363 px of the 640 px journal entry table with no scroller: every DEBIT and CREDIT amount on the policy page is unreachable on a phone. Pre-existing since e341f83, missed by b13-screens because its test was document-level scrollWidth | Give .entry-block the .table-scroll treatment, or overflow-x: auto | FIXED ec4c5c9, merged 3890300: the journal lines scroll in their own container (277 px scroller over 640 px content at 375 px, page scrollWidth 375); CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-UA-05 | LOW | /ops/approvals was edited by cycle C without ops-tables.css: its What column is 71 px and splits "payment" and "refund", the same defect class as UI-006/011/013/026/029/030. Pre-existing, visible in the audit's own ops-approvals.jpg | Add the stylesheet and the ops-table and col-* classes | FIXED ec4c5c9, merged 3890300: the approvals table imports ops-tables.css, What column 71 px to 121 px; CONFIRMED PASS at 3890300 (docs/reviews/b13-14-low-screens.md, 14:51 local) |
| F-UA-06 | LOW | The ops policy list folds every policy, two reads each, Promise.all with no LIMIT on policiesOfBroker. 203 ms median at the delivered scale (4 policies), against 167 to 223 ms on comparable ops screens, so not a defect now | DISCLOSED as a stated limitation; bound it if the estate grows | DISCLOSED |
| F-UA-07 | LOW | ops-tables.css argues for role classes "never on a column number" and policy-detail.css, same folder same cycle, uses th:nth-child percentages; and the col-* classes are global, not scoped under .ops-table as that file's header claims | Scope the col-* rules under .ops-table and note the convention, or align the two sheets | DISCLOSED (week two: one stylesheet convention, col classes scoped) |

## Yoann's own review on production, LIVE-0 (Linear YOA-645 comments, 14:40 and 14:43 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-YA-09 | MEDIUM | No visible confirmation after a click: the statement re-run produced revision 5 with no success notice, and the app has no consistent success, error or loading feedback on its actions ("we actually need to have the little badges that notify us about success, errors, and loading states on the UI for clicks") | One notices pattern after every POST redirect naming the outcome and the created id; a per-button loading state while a form submits (no route-level loading.tsx, see F-B13-31) | CLOSED by the interface rework (ui-system, merged 489a36d, 17:20 local): toastsFromQuery on 14 screens, SubmitButton working state; review PASS docs/reviews/ui-system.md |
| F-YA-10 | MEDIUM | The reconciliation board does not explain its own count: 22 open breaks became 28 after a run and a second run kept 28; every one is a provider-only probe payment ($42.42, no operation id) created in the shared Stripe sandbox by the check scripts of the reviews, and the endorsement payment did become matched (7 matched instead of 6), but nothing on the page says so | Recognise probe payments (amount, description, metadata) as their own classification "probe from a check run" with a sentence, or an append-only "explained" annotation on a break; decision put to Yoann | BUILT (decision 28): probe classification, append-only break notes (migrations 0022, 0023, 0025), the daily window kept wide; review PASS at 48d6e63 after three rounds; merged f90ffba; the rebuilt reconciliation page still has to render the form and the two headings (UI session) |
| F-YA-11 | LOW | "Re-run with this knowledge cutoff" on a run page keeps the old cutoff by design (rule 10), so it can never include new entries; Yoann used it expecting the September figures and got an identical revision of 2027-09 | The button says what it does ("reproduce this revision with the same cutoff") and the page points at "Run a statement" for a new month or a fresh cutoff | CLOSED by the interface rework (ui-system, 489a36d): the button reads "Reproduce this revision with the same cutoff", same action and hidden cutoff |

## LOW library sweep re-review (docs/reviews/b13-14-low-sweep.md, PASS at bf36b27, 14:48 local; 13 lock sites, no cycle possible)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-LS-01 | LOW | openBreaksPage (bounded, with totalOpen and capped) has no caller; the breaks board still calls the unbounded openBreaks and the comment claims a caller | Call it from the board with the cap sentence | FIXED with the breaks board slice (48d6e63, merged f90ffba) |
| F-LS-02 | LOW | The resolved-breaks limit now applies after the filter, an undisclosed paging change (58 rows against 6 at limit 100, none lost) | Say so in the comment | OPEN (B13 backlog) |
| F-LS-03 | LOW | The lock-order comment says "every other posting path" but issue.ts, cancel.ts and void-fabricated-binding.ts hold one lock only | Wording of the comment | OPEN (B13 backlog) |
| F-LS-04 | LOW | The resolved-break annotation names one open break when several share the reference | Name them all or count them | FIXED with the breaks board slice (48d6e63, merged f90ffba) |

## LOW screens sweep confirmation (docs/reviews/b13-14-low-screens.md, FAIL at 3890300 on one item of seventeen, 14:51 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-LU-01 | LOW | The console 360 timeline table still prints the declined reason beside a green succeeded chip, dated with the success time (lib/console/read.ts, detail: failureReason); the operations table is fixed | The branch console-360.tsx already uses: the reason only for the terminal status, otherwise "Earlier attempt, <date>: <reason>" | CLOSED by the interface rework (ui-system, 489a36d): console-360 prints "Earlier attempt, <date>: <reason>" on both tables |
| F-LU-02 | LOW | At 375 px the words debit and credit appear nowhere on the policy page: the entry columns are hidden under 580 px and the lines table has no thead | A thead on the lines table | CLOSED by the interface rework (ui-system, 489a36d): a thead with Account, Debit, Credit on the lines table |
| F-LU-03 | LOW | .entry-lines-scroll is a focusable div with an aria-label and no role | role="group" | CLOSED by the interface rework (ui-system, 489a36d): role="group" on the lines scroller |
| F-LU-04 | LOW | The Stripe: LIVE SANDBOX chip is on the staff and broker policy page, not on the customer view | Same chips on customer-view.tsx | CLOSED by the interface rework (ui-system, 489a36d): the Stripe chip on the customer view |
| F-LU-05 | LOW | The customer list drops the "on the policy record" wording the other lists carry on the voided policy | Align | CLOSED by the interface rework (ui-system, 489a36d): the customer list carries "on the policy record" |
| F-LU-06 to F-LU-08 | LOW | The inbox per-role test proves sections rather than tasks; duplicate region names remain on CGP-01274 (14 and 9); "2 is waiting for an approver" grammar | See the record | DISCLOSED as recorded |

## Monthly statements (decision 30), PASS at ffa1b69 (docs/reviews/backend-monthly-statements-r1.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-MONTHLYSTATEMENTS-01 | LOW | A close interrupted on the first of the month is never retried; the remaining brokers get no statement until a human runs one | Catch up on a later day, or set an explicit `maxDuration` and disclose the ceiling | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-02 | LOW | The cutoff is the web process clock while `runStatement` validates it against the database clock; forward skew refuses the whole month | Default the cutoff to the database clock, or clamp `now` once at the top of the job | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-03 | LOW | "Exactly once" holds for sequential calls only; two overlapping closes can publish two definitive runs, and the code comment and README promise more | Soften both sentences, or enforce it (re-check in the transaction, or an additive partial unique index) | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-04 | LOW | The new "ties to the ledger to the cent" assertion compares 0 with 0 by construction | Rename it to what it proves; the hash-reproduction assertion carries the real weight | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-05 | LOW | "A month already closed is not closed twice" passes vacuously: broker A has no policy in force and no movement in the closed month, so the job never considers it (probed on `corgi_test`) | Give broker A a policy in force, or relabel the assertion | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-06 | LOW | The staff inbox count caps at 25 with nothing saying so; it already reads 25/25 of 585 on `corgi_test` | Name the cap in the label and point at `/ops/statements` | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-07 | LOW | "A policy in force" is read at close time, so a brand-new broker gets an empty statement for a month in which it had no policy; three interpretations are unrecorded | Record the three calls in `docs/DECISIONS.md` (coordinator; `docs/` was out of the builder's scope) | OPEN (B13 backlog) |
| F-MONTHLYSTATEMENTS-08 | INFO | Per-broker serial queries with no index on `(broker_id, effective_at)`; `recordActivity` ignores the injected `database`; the summary embeds every produced broker | None required at trial scale; noted as the mechanism behind F-…-01 | OPEN (B13 backlog) |

## Corrections on decision 24 (decision 31), PASS at 0510c1b (docs/reviews/backend-corrections-rule-24-r1.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-CORRECTIONSRULE24-01 | LOW | The gate comment claims it "can only ever ask for MORE approval, never less"; the new base can shrink, so a difference the correction recorded as needing the customer can become collectable without one, and the screen then blocks the only button that would collect it. |
| F-CORRECTIONSRULE24-02 | LOW | The customer sentence names a tax-included amount and a tax-excluded running total side by side, so a reader cannot check the arithmetic from the sentence alone. |
| F-CORRECTIONSRULE24-03 | LOW | `additionalPremiumOfTheTermCents` means "before this change" in `CorrectionThresholdTotals` and "including this quote" in the endorsement plan: one name, two meanings, in the two files Yoann has to explain together. |
| F-CORRECTIONSRULE24-04 | LOW | A correction recorded before this change prints a sentence whose figures contradict its own verdict ("carries $50.47 of additional premium ... above $500.00"), qualified only by a caveat appended after it. |
| F-CORRECTIONSRULE24-05 | LOW | Decision 24 in `docs/DECISIONS.md` speaks of endorsements only; the extension to correction differences, which is this slice, is not recorded there. |
| F-CORRECTIONSRULE24-06 | INFO | The re-book payload still carries `customer_approval_required` computed with an empty base, beside the `difference_customer_approval_required` that decides. Nothing reads it to gate money. |
| F-CORRECTIONSRULE24-01 | LOW | The payment gate comment claims it can only ask for more approval than the correction recorded; a superseded open request makes it ask for less, and the screen then hides the only button that would collect the difference | Correct the comment and let the screen follow the gate | OPEN (B13 backlog) |
| F-CORRECTIONSRULE24-02 | LOW | The customer sentence names $117.77 (tax included) beside a $501.36 running total (premium only), so its arithmetic cannot be checked from the sentence | Name the premium part of the difference in the same sentence | OPEN (B13 backlog) |
| F-CORRECTIONSRULE24-03 | LOW | `additionalPremiumOfTheTermCents` means "before this change" in the correction totals and "including this quote" in the endorsement plan | Rename one of them, as the payload key already does | OPEN (B13 backlog) |
| F-CORRECTIONSRULE24-04 | LOW | A correction recorded before this slice prints "carries $50.47 of additional premium ... above $500.00" plus a caveat | Drop the figure when the running total was not recorded | OPEN (B13 backlog) |
| F-CORRECTIONSRULE24-05 | LOW | Decision 24 covers endorsements only; its extension to correction differences is not in `docs/DECISIONS.md`, and `integration.md` 12.5 still describes the old asymmetry | Coordinator records the decision and updates 12.5 | OPEN (B13 backlog) |
| F-CORRECTIONSRULE24-06 | INFO | The re-book payload carries a `customer_approval_required` computed with an empty base beside the verdict that decides | Leave, or stop writing it on a re-book | OPEN (B13 backlog) |

## Key-creator layer, migration 0024 (decision 32), PASS at 845443a (docs/reviews/backend-guards-and-key-trigger-r1.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-GUARDSANDKEYTRIGGER-01 | LOW | A key minted by `scripts/create-mcp-key.ts` has a null `created_by`, so a request raised through it escapes the fourth refusal | Decide and record: name the residual, or give script-minted keys an operator identity | OPEN (B13 backlog) |
| F-GUARDSANDKEYTRIGGER-02 | LOW | Requests raised through a key before 0024 keep a null `raised_through_key_id`; no backfill is legitimate on an append-only table | Clear the undecided MCP-raised requests before applying 0024 to the trial database | OPEN (B13 backlog) |
| F-GUARDSANDKEYTRIGGER-03 | LOW | `createApprovalRequest` now names a column absent from the trial database, which breaks all five approval paths, not only MCP | Apply 0024 before deploying this code | OPEN (B13 backlog) |
| F-GUARDSANDKEYTRIGGER-04 | INFO | 0022 and 0023 are free; a later file taking one and replacing the same function would undo the fourth refusal on databases where 0024 ran first | Confirm no in-flight migration replaces that function, or renumber | OPEN (B13 backlog) |
| F-GUARDSANDKEYTRIGGER-05 | INFO | The two new `check:money-guards` assertions are unexecuted by builder and reviewer alike (184 to 186 expected) | Run the script once on the ephemeral database and record the two lines | OPEN (B13 backlog) |
| F-GUARDSANDKEYTRIGGER-06 | INFO | `0023_rate_table_guards.sql` was not written; the premise of F-INT-09 is wrong and writing it as specified would have weakened the `brokers` guard | Correct F-INT-09 in `docs/reviews/integration.md` and note decision 26 was brought forward | OPEN (B13 backlog) |
| F-GUARDSANDKEYTRIGGER-07 | INFO | `keyId` joins the append-only `claim_events.payload` JSON with no format marker; absent means "old row" and "not raised through a key" alike | Note it, or version the stored channel shape when that payload next changes | OPEN (B13 backlog) |

## Breaks board round 1, FAIL at dee775b (docs/reviews/backend-breaks-board-r1.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-BREAKSBOARD-01 | MEDIUM | An explanation is keyed on `break_key` alone and never expires, so a break that later changes to a worse classification never returns to the count, the inbox, the daily job's window or the MCP tool; two comments claim it does | Bind the note to the classification and amounts it explained, and re-open when the latest report differs; correct the comments | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-02 | LOW | `openBreaksOfSubject` in the console still uses the old rule while the shared comment says both readers ask the same question | Use `IT_IS_A_BREAK_TO_ACT_ON` there, or reword the comment | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-03 | LOW | The probe rule's comment claims no forged marker can silence a real break; a provider-only record carrying a forged marker is silenced | Reword to the guarantee the code gives | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-04 | LOW | The README says the deployed board already separates probes; 0022 and 0023 are on `corgi_test` only | Reword, or land with the deploy | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-05 | LOW | The pre-existing global journal-entry assertion of `check:reconciliation` fails under shared-database contention (8514 to 8516 on my run) | Narrow it to this check's own policies, as the new assertion already is | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-06 | INFO | The "explained" notice is printed from the query string alone | One line if the panel is revisited | CLOSED by round 3 (see the round 3 table below) |

## Breaks board round 2, FAIL at 4cf3cc9 (docs/reviews/backend-breaks-board-r2.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-BREAKSBOARD-01 | MEDIUM | **RESOLVED** | Migration 0024 plus `THE_NOTE_EXPLAINS_THE_LATEST_REPORT`; three new live assertions, all passing on my own run |
| F-BREAKSBOARD-02 | LOW | **STILL OPEN, and wider than it was** | `openBreaksOfSubject` (`lib/console/read.ts:2612`) still filters on `not A_LATER_RUN_RE_EXAMINED_IT` alone, while the shared comment at `lib/reconciliation/read.ts:138-141` still says the console "must ask the same question this screen asks". The gap grew: the board's question now also excludes probes and explained breaks, so a 360 page shows as "open breaks" rows the board counts as zero, and shows them without the note that explains them |
| F-BREAKSBOARD-03 | LOW | **STILL OPEN** | `lib/reconciliation/diff.ts` still says "no forged marker can turn a real break into a line the board stops counting". A provider-only record with no operation id and a forged `probe=check-reconciliation` marker is classified `probe` and leaves the count. The guarantee the code actually gives is narrower: a record naming one of our operations is never a probe |
| F-BREAKSBOARD-04 | LOW | **STILL OPEN** | The README paragraph opens on "the deployed application" and then states "Since 2026-09-09 the board says which is which". Migrations 0022 to 0024 are applied to `corgi_test` only; nothing on this branch is deployed |
| F-BREAKSBOARD-05 | LOW | **STILL OPEN** | `scripts/check-reconciliation.ts:275,280` still uses the global `journalEntryCount()` for `a run posts no money and no journal entry`. The new assertion correctly uses `journalEntryCountForPolicies`, the pre-existing one was not narrowed. It passed on my run (8680 to 8680) but remains contention-sensitive |
| F-BREAKSBOARD-06 | INFO | **STILL OPEN, accepted** | The `?explained=` notice is still printed from the query string alone |
| F-BREAKSBOARD-01 | MEDIUM | A note was keyed on `break_key` alone and never expired | Migration 0024 records the classification and both amounts of the report the note explains; `THE_NOTE_EXPLAINS_THE_LATEST_REPORT` re-opens the break when the latest report differs | RESOLVED at 4cf3cc9, verified live |
| F-BREAKSBOARD-02 | LOW | `openBreaksOfSubject` still uses the old rule while the shared comment says both readers ask the same question; the gap widened, since the board now also excludes probes and explained breaks | Use `IT_IS_A_BREAK_TO_ACT_ON` there, or reword the comment and say what the 360 page shows | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-03 | LOW | The probe rule's comment claims no forged marker can silence a real break; a provider-only record with no operation id and a forged marker is silenced | Reword to the guarantee the code gives: a record naming one of our operations is never a probe | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-04 | LOW | The README says the deployed board already separates probes; 0022 to 0024 are on `corgi_test` only | Reword, or land the sentence with the deploy | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-05 | LOW | The pre-existing global journal-entry assertion of `check:reconciliation` is still global and contention-sensitive | Narrow it to this check's own policies, as the new assertion already is | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-06 | INFO | The "explained" notice is printed from the query string alone | One line if the panel is revisited | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-07 | MEDIUM | `oldestOpenBreakRecordDate` now excludes explained breaks, so the daily job stops widening its window to cover them and no later run can ever report them differently; the README and two comments promise that it does | Keep the widening for explained breaks (exclude probes only), or stop promising the break comes back and say for how long the promise holds | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-08 | LOW | The board tells the operator a break was "changed since" even when the note simply predates migration 0024 | Branch the sentence on a null `explained_classification` and use the migration header's honest wording | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-09 | LOW | Two different migrations numbered 0024 are applied to `corgi_test`, one from this branch and one from another slice | Coordinator decision at merge: renumber one, or record that file names and not numbers are the identity | CLOSED by round 3 (see the round 3 table below) |
| F-BREAKSBOARD-10 | INFO | `check-money-guards` never exercises the three new columns; "latest of N notes" counts superseded notes; the explain route has no CSRF token, like every other POST route of this build | None required; recorded so none of the three is read as reviewed-and-cleared | CLOSED by round 3 (see the round 3 table below) |

## MCP tools round 1, FAIL at 6a41c2a (docs/reviews/backend-mcp-tools-r1.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-MCPTOOLS-01 | MED | A customer key reads through explain_amount the broker's commission, the ledger sums, the cancellation figures and journal entry ids that the customer's own screen withholds | Gate the eleven non-customer figure keys on owning broker or staff inside the tool; add a customer-key refusal assertion to check:mcp | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-02 | LOW | explanationEndsOnTheFigure compares a number with itself for 12 of the 15 keys; the comment calls it the check made for every figure | Take the cancellation amount from the stored event figure, or say which keys the check can falsify | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-03 | LOW | Ten of the fifteen published figure keys are exercised by no test and no check; explain-figure.ts has no unit test | Cancel and endorse a fixture policy in check:mcp, or unit-test the module | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-04 | LOW | tools/list spreads a tool's own annotations after the derived hints, so a future write tool could publish readOnlyHint true | Spread tool.annotations first and let the derived hints win | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-05 | LOW | list_my_activity bounds its answer and not its work: Seq Scan plus Sort on activity_log, no index on actor_user_id | Additive index on (actor_user_id, recorded_at desc), or reword the comment | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-06 | LOW | list_my_activity tells the caller no amount is recorded, when it is the return shape that omits it (message is stored) | Say returned, not recorded | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-07 | LOW | explain_amount advertises an enum the shared argument validator does not enforce; jsonrpc.ts says the advertised schema is enforced there | Enforce enum in argumentsSchemaRefusal, or narrow the comment | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-08 | LOW | COMPLIANCE-MATRIX MCP-01 and BLD-09 and the README demo paragraph still describe a five-tool surface; the two new tools have no deployed-app evidence | Coordinator updates the records; exercise both tools on the deployed URL before the AF-01 claim | CLOSED by round 3 (see the round 3 table below) |

## MCP tools round 2, FAIL at 5976e4f (docs/reviews/backend-mcp-tools-r2.md, 17:16 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-MCPTOOLS-01 | HIGH | explain_amount applied only the policy visibility gate, so a customer key read every figure on its own policy | Second gate `figureVisibilityRefusal` by role; eleven of fifteen keys now refused to a customer key | PARTLY FIXED 5976e4f, reopened as F-MCPTOOLS-02 and F-MCPTOOLS-03 |
| F-MCPTOOLS-02 | HIGH | `endorsement_delta`, allowed to a customer key, returns the broker's commission movement and the 1500 bps commission rate in its formula lines; the refusal sentence, the description and the README all say it does not | Filter the returned lines and the recheck rows, or refuse the key to a customer; assert it in check:mcp on a fixture that has an endorsement; correct the three statements | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-03 | MED | `premium_tax` and `policy_fee`, allowed to a customer key, return journal entry ids, account names and the cancellation's 2813-cent tax refund, which the same key is refused as `cancellation_refunded_tax` | Decide what a customer key's answer may carry and drop the rest before returning; align the sentence, the description and the README | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-04 | LOW | `toolsListResult` spreads a tool's own annotations after the derived hints, so a future tool could publish a false `readOnlyHint` and the new check assertion would still pass | Spread `tool.annotations` first | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-05 | LOW | Nothing enumerates the fifteen figure keys against the four roles; one refused key and one allowed key are asserted, which is why F-MCPTOOLS-02 survived | Export `figureVisibilityRefusal` and add a table-driven test over all keys and roles | CLOSED by round 3 (see the round 3 table below) |
| F-MCPTOOLS-06 | INFO | The code calls this "slice B14"; `docs/PLAN.md` row B14 is the freeze package | Relabel, coordinator's call | CLOSED by round 3 (see the round 3 table below) |

## Interface rework, branch ui-system (docs/reviews/ui-system.md, PASS at 2af6ebe, merged 489a36d, 17:20 local; 495 tests, 49 to 52 forms with identical POST contracts, AF-02 labels on 25 screens, 30 of 30 figures unchanged)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-UIS-01 | LOW | `flowOfLine` is exported and carries eight assertions, but nothing calls it: the classification that actually runs is the `case` expression inside `dailyFlows`. The tests prove a copy of the rule, not the rule the screen uses, so the two can drift with a green suite | `lib/ledger/read.ts:88` and `:446` | either drive the SQL from the function, or say in the comment that the function is the specification and the SQL its transcription, and assert one row of each kind against a real read |
| F-UIS-02 | LOW | The ledger page catches every read failure with a bare `catch {}`, discards the reason and replaces the whole screen with one generic line. The codebase already has `attempt` (`lib/console/safe-read.ts`), which renders a redacted reason per panel; an operator on the one screen that proves AF-03 gets no reason at all | `app/ops/console/ledger/page.tsx:112` | route the five reads through `attempt` and print its sanitised sentence, as the console screens do |
| F-UIS-03 | LOW | `app/ops/console/ledger/page.tsx` imports `app/styles/landing.css` to get a `.ledger-screen` rule. A reader looking for the ledger's styles has no reason to open the landing stylesheet | `app/ops/console/ledger/page.tsx:26`, `app/styles/landing.css` | move the two parked rules into `system.css` or a `ledger.css`, and drop the import |
| F-UIS-04 | LOW | `app/styles/ops-tables.css` (171 lines) and `app/styles/shell.css` (61 lines) are imported by no file, and 12 of 21 and 5 of 6 of their classes appear in no component. Dead files that still read as live ones | `app/styles/ops-tables.css`, `app/styles/shell.css` | delete both, after checking the two classes each that are still named elsewhere |
| F-UIS-05 | LOW | `.band-icon` (two rule blocks, one responsive) and `.pop-menu button.destructive:hover` in `system.css` match no element in any component | `app/styles/system.css:515`, `:1271`, `:2279` | delete, or use them |
| F-UIS-06 | LOW | `Row` writes `data-href` on every `<tr>`; no CSS rule and no script reads it. The comment beside it describes the stretched-link technique, which is done by `a.dt-link::after`, not by this attribute | `components/ui/table.tsx:41` | drop the attribute, or say what reads it |
| F-UIS-07 | LOW | The brief's "what must not change" list still names `journal-table.tsx` and `amount-explained-motion.tsx`; this branch reworked both, correctly, to close F-LU-02 and F-LU-03 and to move the explanation out of the clipping table cell. The list is stale, so a later reader cannot tell an authorised change from a slip | `docs/handoffs/ui-system-brief.md` | record the two exceptions in the brief or in `docs/DECISIONS.md`, naming the findings that required them |
| F-UIS-08 | LOW | `Toaster` announces each notice twice: the visible toast carries `role="alert"` or `role="status"`, and a hidden region below repeats the same text with `role="status"` | `components/ui/toast.tsx:44` and `:73` | keep the hidden region for the review scripts but drop its role, or drop the role from the visible toasts |

## Interface rework, found after the merge by the interface session (17:28 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-UIS-09 | MEDIUM | On production at 489a36d and aa76888, /ops/reconciliation, /ops/console and /ops/console/ledger scrolled sideways at 375 px (documentElement.scrollWidth 672 against 375): every chart carries its data as a visually hidden table for screen readers, and a table's width is a minimum, so the 1 px hidden table still laid out at 640 px; the policy page held | The hidden data in a hidden div; the band static under 800 px | FIXED 1997f1c, merged 944003a (17:28 local): measured locally at 375 px on reconciliation, console, ledger and the policy page, scrollWidth 375; confirmed on production at 944003a (17:31 local): policy page, reconciliation, console and ledger all at scrollWidth 375 with every fold open, the inspector state included |

## Breaks board round 3 (decision 28), PASS at 48d6e63 (docs/reviews/backend-breaks-board-r3.md, 18:08 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-BREAKSBOARD-01 | MEDIUM | **RESOLVED** (already at 4cf3cc9, unchanged here) | `THE_NOTE_EXPLAINS_THE_LATEST_REPORT` plus migration 0025. Live on my run: `a break explained while it is stale leaves the list to act on`, `a break a later run reports differently is WORK AGAIN, and leaves the explained list`, `the superseded note is still on file and shown beside the break it no longer explains` (note written against `stale`, row now `provider_only`) |
| F-BREAKSBOARD-02 | LOW | **RESOLVED** | `lib/console/read.ts` imports and uses `IT_IS_A_BREAK_TO_ACT_ON`, and the comment now names the shared rule and says why the reader changed. Proved live: see "The two mechanisms, proved live" below, where the same break is 1 row on the 360 reader before the note and 0 rows after it |
| F-BREAKSBOARD-03 | LOW | **RESOLVED** | `lib/reconciliation/diff.ts` now states the rule exactly (a record naming one of our money operations is never a probe; a record naming none is a probe only with the check-run marker or with the planted amount AND the `corgi_probe:` description) and states the residual in its own paragraph (a real payment of exactly that amount with a forged description would be classified as a probe), with the reason probes stay listed under their own heading rather than dropped. The code matches the comment line by line |
| F-BREAKSBOARD-04 | LOW | **RESOLVED**, with an INFO residual | `Since 2026-09-09 the board says which is which` is gone; the sentence now describes the mechanism, and the window sentence at the end of the paragraph was corrected the same way. Residual wording: F-BREAKSBOARD-14 |
| F-BREAKSBOARD-05 | LOW | **RESOLVED** | `checkPolicyIds` is declared once beside the three policies and both journal assertions use `journalEntryCountForPolicies`; the global `journalEntryCount()` helper is deleted (grep: no caller left). Live: `a run posts no money and no journal entry ... (14 journal entries on this check's policies before the run, 14 after)` |
| F-BREAKSBOARD-06 | INFO | **STILL OPEN, accepted** as in rounds 1 and 2 | The `?explained=` notice is still printed from the query string alone. No state changes and React escapes the value |
| F-BREAKSBOARD-07 | MEDIUM | **RESOLVED** | `oldestOpenBreakRecordDate` is back to `not A_LATER_RUN_RE_EXAMINED_IT`, with a comment that says why the two questions differ (the desk of today versus how far back one run must look, since looking is what un-explains a break). **Proved live**, which round 2 could not do: see below |
| F-BREAKSBOARD-08 | LOW | **RESOLVED** | `app/ops/reconciliation/page.tsx` branches on `explainedClassification`: a stored classification prints "Explained before, and changed since."; a null one prints "Explained before this build recorded what was explained" and says the note predates the columns and the break needs explaining again as it stands today. The lateral that feeds it takes the LATEST note, and a note written after 0025 always records a classification, so a null value can only be a pre-0025 note: the branch cannot mislabel |
| F-BREAKSBOARD-09 | LOW | **RESOLVED** | The file is `db/migrations/0025_break_notes_say_what_they_explain.sql`; `ls db/migrations` shows 25 files and no repeated number; every statement is `add column if not exists`; the header says why the number changed and why the statements are idempotent. `npm run migrate -- --database=test` on my run: all 25 skipped, nothing applied, which is what the builder's "already applied under the new name" means. 0022 and 0023 untouched |
| F-BREAKSBOARD-10 | INFO | **ADDRESSED IN CODE, NOT EXECUTED** | The fixture of `scripts/check-money-guards.ts` now writes the three columns of 0025 with the same classification and the same two amounts as the reconciliation item beside it. `check:money-guards` is forbidden to me and was not run, so the guard proof over the real row shape is still unexecuted. The guard loop itself updates `id`, so the two amount columns are still not named in an UPDATE the way `brokers.commission_rate_bps` is at step 2b; that was not asked for and is not required |
| F-BREAKSBOARD-01 | MEDIUM | A note was keyed on `break_key` alone and never expired | Migration 0025 records the classification and both amounts of the report the note explains | RESOLVED at 48d6e63, verified live |
| F-BREAKSBOARD-02 | LOW | The 360 page asked a different question from the board | `openBreaksOfSubject` uses the shared `IT_IS_A_BREAK_TO_ACT_ON` | RESOLVED at 48d6e63, verified live |
| F-BREAKSBOARD-03 | LOW | The probe rule's comment claimed more safety than the rule gives | The comment states the rule exactly and its residual, and says why probes stay listed | RESOLVED at 48d6e63 |
| F-BREAKSBOARD-04 | LOW | The README stated as deployed fact something true only of the code | The dated claim is gone; the paragraph describes the mechanism | RESOLVED at 48d6e63; wording residual as F-BREAKSBOARD-14 |
| F-BREAKSBOARD-05 | LOW | A global journal-entry assertion was contention-sensitive | Scoped to `checkPolicyIds`; the global helper is deleted | RESOLVED at 48d6e63, verified live |
| F-BREAKSBOARD-06 | INFO | The `?explained=` notice is printed from the query string alone | One line if the panel is revisited | OPEN (B13 backlog) |
| F-BREAKSBOARD-07 | MEDIUM | The daily job stopped widening its window to explained breaks, so the re-open mechanism could never fire | `oldestOpenBreakRecordDate` is back to the wide rule, with the two questions distinguished in a comment | RESOLVED at 48d6e63, verified live with a rolled-back fixture |
| F-BREAKSBOARD-08 | LOW | A reopened row said "changed since" even when the note predated the columns | The heading branches on a null stored classification | RESOLVED at 48d6e63 |
| F-BREAKSBOARD-09 | LOW | Two migrations numbered 0024 | Renamed to 0025, idempotent statements, reason in the header; 0022 and 0023 untouched | RESOLVED at 48d6e63, verified by a migrate run and a schema read-back |
| F-BREAKSBOARD-10 | INFO | The guard fixture never wrote the three new columns | The fixture writes them with the item's own classification and amounts | ADDRESSED IN CODE; unproven until the coordinator runs `check:money-guards` |
| F-BREAKSBOARD-11 | LOW | The MCP tool's comment says the daily job reads `openBreaks` and excludes probes and explained breaks; it reads `oldestOpenBreakRecordDate`, which deliberately includes them | Reword to name the two questions, as the three other comments now do | OPEN (B13 backlog) |
| F-BREAKSBOARD-12 | INFO | "Explained before, and changed since" prints the same classification twice when only an amount moved, and never shows the amounts the note recorded | Name the amount that moved, or reword the heading | OPEN (B13 backlog) |
| F-BREAKSBOARD-13 | INFO | The window assertion passes identically with and without the F-BREAKSBOARD-07 bug on a database whose oldest break is inside the default window | Assert on a break this check owns and has explained | OPEN (B13 backlog) |
| F-BREAKSBOARD-14 | INFO | The corrected README sentence follows a sentence about the deployed application | One word ("this build"), or nothing once the slice is deployed | OPEN (B13 backlog) |
| F-BREAKSBOARD-15 | INFO | `docs/DECISIONS.md` rule 32 names migration 0023 for the rate-table guards; 0023 is the probe classification | Coordinator: correct the decision text or the future file name | OPEN (B13 backlog) |

## MCP tools round 3 (decision 29), PASS at ce42a3c (docs/reviews/backend-mcp-tools-r3.md, 18:08 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-MCPTOOLS-01 | MED | **CLOSED** | `lib/mcp/scope.ts` `explanationVisibilityRefusal`, applied at `explain-amount.ts` before the policy row is read. A customer key reads no figure at all, so the eleven keys the finding named are unreachable. |
| F-MCPTOOLS-02 | LOW | **OPEN** | `explain-figure.ts` lines 136 to 140 are unchanged. For the seven cancellation keys `amountCents` is still `explanationResultLine(explanation)?.cents`, i.e. the very line it is compared against (line 383); for the four ledger sums both sides are the same `accountSumCents` walk (line 297 against `explainAccountSum`); for `policy_fee` both sides are `terms.feeCents`. The comment still reads "THE CHECK THAT MAKES THE ANSWER WORTH TRUSTING, asked here for every figure and never assumed". No round was asked to fix it and no round did. See F-MCPTOOLS-09. |
| F-MCPTOOLS-03 | LOW | **OPEN** | The `check:mcp` fixture (`createPaidPolicy`) still creates a bound, paid policy with no endorsement and no cancellation, and `explain-figure.ts` still has no unit test. Ten of the fifteen keys are returned by no automated run. Confirmed in my own run: `cancellation_total_refund` answered "this policy has not been cancelled" and `endorsement_delta` answered "this policy carries no endorsement". |
| F-MCPTOOLS-04 | LOW | **CLOSED** | `lib/mcp/jsonrpc.ts`: `annotations: { ...(tool.annotations ?? {}), readOnlyHint, destructiveHint, openWorldHint }`. The derived hints now win. |
| F-MCPTOOLS-05 | LOW | **CLOSED** by the "reword and measure" option the finding allowed. The comment now separates the bounded answer from the unbounded work and carries the plans. I re-measured both, independently, and they reproduce (section 5). The index is deliberately not added; the reason (parallel migration numbering) is written down and is honest. |
| F-MCPTOOLS-06 | LOW | **PARTLY FIXED** | The payload half is now exact: the tool says one sanitised sentence per call is recorded in `message` and that this tool does not read that column. The other half survives: `whatThisMeans` still opens with "no amount, no argument and no request or response body is **recorded**", and `activity_log.message` does record amounts. See F-MCPTOOLS-10. |
| F-MCPTOOLS-07 | LOW | **CLOSED** | The `enum` is enforced in `argumentsSchemaRefusal` before `tool.run`, with two unit tests and a `check:mcp` assertion that proves no read happened (the same unknown key on a nonexistent policy gives the enum sentence, not the policy sentence). |
| F-MCPTOOLS-08 | LOW | **OPEN**, coordinator's | The README now describes seven tools, but `docs/COMPLIANCE-MATRIX.md` rows MCP-01 and BLD-09 still cite a five-tool surface, and neither new tool has ever been called on the deployed application. `docs/` is not this slice's to edit. |
| F-MCPTOOLS-01 | HIGH | **CLOSED** | The per-figure allowlist `FIGURE_KEYS_A_CUSTOMER_KEY_READS` and `figureVisibilityRefusal` are deleted from the branch. The replacement is a role refusal, so there is no "key a caller may name" to gate any more. |
| F-MCPTOOLS-02 | HIGH | **CLOSED** | A customer key naming `endorsement_delta` is refused before `endorsementFigure` runs, so `endorsementFormulaLines` and its `commission` line (and the 1500 bps rate inside the formula string) are unreachable. Asserted over HTTP in `check:mcp` on the exact key that leaked, and in `scope.test.ts`. The refusal sentence, the tool description and the README now all say the same thing as the code, which is what the finding asked for. |
| F-MCPTOOLS-03 | MED | **CLOSED** | Same mechanism: `premium_tax` and `policy_fee` are refused to a customer key, so `evidenceFromJournal` never runs for one and no `provenBy` entry, account name, entry type or cancellation figure comes back. |
| F-MCPTOOLS-04 | LOW | **CLOSED** | Same line as round 1's F-MCPTOOLS-04. |
| F-MCPTOOLS-05 | LOW | **CLOSED** | `explanationVisibilityRefusal` and `NO_EXPLANATION_FOR_THIS_KEY` are exported and covered by six tests in `lib/mcp/scope.test.ts`: a customer refused, the refusal naming no figure key, a broker allowed on its own policy and refused on another's, both staff roles allowed, the fifth role `agent` refused (fail closed), and a broker user with no broker attached passing the role gate and stopped by the policy gate. The rule is now one line, so the enumeration is genuinely exhaustive rather than a sample. |
| F-MCPTOOLS-06 | INFO | **CLOSED** | `lib/policy/explain-figure.ts` line 29 and `lib/mcp/never-delegated.test.ts` now say "slice B13-16 (decision 29)". `grep -rn B14 lib scripts README.md` returns nothing. |
| F-MCPTOOLS-01 (r1, r2) | HIGH | explain_amount let a customer key read the broker's commission, the ledger sums, the cancellation figures and journal entry ids | Role refusal in lib/mcp/scope.ts applied before the policy lookup; per-figure allowlist deleted | **CLOSED ce42a3c** |
| F-MCPTOOLS-02 (r2) | HIGH | endorsement_delta handed a customer key the commission line and the 1500 bps rate | Same refusal; asserted over HTTP on that exact key | **CLOSED ce42a3c** |
| F-MCPTOOLS-03 (r2) | MED | premium_tax and policy_fee handed a customer key journal entry ids and the cancellation's refunded tax | Same refusal | **CLOSED ce42a3c** |
| F-MCPTOOLS-04 (r1, r2) | LOW | tools/list spread a tool's own annotations after the derived hints | Annotations spread first, hints last | **CLOSED ce42a3c** |
| F-MCPTOOLS-05 (r2) | LOW | Nothing enumerated the visibility rule against the roles | Rule exported, six tests in lib/mcp/scope.test.ts including fail-closed | **CLOSED ce42a3c** |
| F-MCPTOOLS-05 (r1) | LOW | list_my_activity bounded its answer and not its work, and the comment claimed otherwise | Comment rewritten with measured plans; index deliberately deferred and the reason written down; re-measured by the reviewer | **CLOSED ce42a3c** |
| F-MCPTOOLS-06 (r2) | INFO | The code called this "slice B14" | Now "slice B13-16, decision 29" in both files | **CLOSED ce42a3c** |
| F-MCPTOOLS-07 (r1) | LOW | The advertised enum was enforced by no validator | Enforced in argumentsSchemaRefusal before the tool runs, two unit tests, one check assertion proving no read happened | **CLOSED ce42a3c** |
| F-MCPTOOLS-02 (r1) | LOW | explanationEndsOnTheFigure compares a number with itself for 12 of the 15 keys; the comment calls it the check made for every figure | Take the cancellation amount from the stored event figure, or say which keys the check can falsify | **OPEN** at ce42a3c |
| F-MCPTOOLS-03 (r1) | LOW | Ten of the fifteen figure keys are exercised by no test and no check; explain-figure.ts has no unit test | Endorse and cancel a fixture policy in check:mcp, or unit-test the module | **OPEN** at ce42a3c |
| F-MCPTOOLS-08 (r1) | LOW | COMPLIANCE-MATRIX MCP-01 and BLD-09 still describe a five-tool surface; neither new tool has deployed-app evidence | Coordinator updates the records; exercise both tools on the deployed URL before the AF-01 claim | **OPEN** at ce42a3c |
| F-MCPTOOLS-09 | LOW | The check assertion "a broker key still reads its own policy's explanation lines" passes through its "carries no endorsement" escape branch, so it cannot fail for the reason it was written | Endorse a fixture policy and assert the commission line for a broker and its absence for a customer, or drop the escape branch | OPEN (B13 backlog) |
| F-MCPTOOLS-10 | LOW | list_my_activity still says "no amount is recorded"; activity_log.message records refusal sentences carrying integer-cent amounts (lib/claims/limits.ts) | One word: "returned" instead of "recorded" | OPEN (B13 backlog) |
| F-MCPTOOLS-11 | INFO | rolesThatMayExplain is a hand-written second statement of the visibility rule, published in tools/list and compared to the rule by nothing | Derive it from the rule, or assert the two agree in lib/mcp/scope.test.ts | OPEN (B13 backlog) |
| F-MCPTOOLS-12 | LOW | The "no caller string reaches the call log" assertion still reads the last N rows of the shared mcp_calls globally, the contention the assertion beside it was just fixed for | Filter lastCallRows by this run's key ids, as the fixed assertion already does | OPEN (B13 backlog) |
| F-MCPTOOLS-13 | INFO | Two branches claim round 3, four of five commits were authored by another process, and the reviewed SHA is behind origin/main (clean merge, disjoint file sets, merged tree never built) | Coordinator picks the branch and runs typecheck and tests on the merged tree | OPEN (B13 backlog) |

## Production confirmation of the backend track (docs/reviews/backend-production-confirmation.md, FAIL at 100ef41 on one expected item, 18:45 local; MCP tools, monthly job, corrections, key creator, the 24 figures and the AF gate all PASS)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-BP-01 | MED | On production the breaks board still counts the probes (35 to act on, 31 of them $42.42) because `probe` is a stored classification and no run has happened since the deploy; the count is rising, not falling | Run the reconciliation once under the deployed code, then re-read the board and the MCP tool before claiming decision 28 is delivered | CLOSED by the production run of 18:38:24Z (19:20Z): a human staff_ops actor ran /api/jobs/reconcile from the browser, the stored run reads probe_count 32, provider_only 4, matched 7, and the board reads 4 breaks to act on (cycle-2 record, section 12); the click was Yoann's LIVE-0 with the co-pilot session, figures read by GET after it and committed in docs/handoffs/live-fire-day2.md (3c67d45) |
| F-BP-02 | MED | An explained break appears in no view of the deployed board: the count drops from 36 to 35 and the note, the author and the break itself are invisible | Land the interface port of the probe, explained and form panels; until then do not write break notes on production | PORT MERGED (ui-fixes at c408cb3, 20:04 local): the explained and probe headings and the explain form are on production; CONFIRMED PASS at c408cb3 (docs/reviews/ui-cycle-2.md, 20:38 local): the explained row visible under its heading with note, author and time, the probe heading present, the tiles counting the open set |
| F-BP-03 | LOW | The README reconciliation note describes three board headings that the deployed board does not have (raised from F-BREAKSBOARD-14 INFO now that the slice is deployed) | Close F-BP-01 and F-BP-02, or qualify the sentence | PORT MERGED (ui-fixes at c408cb3, 20:04 local): the explained and probe headings and the explain form are on production; CONFIRMED PASS at c408cb3 (docs/reviews/ui-cycle-2.md, 20:38 local): the explained row visible under its heading with note, author and time, the probe heading present, the tiles counting the open set |
| F-BP-04 | LOW | F-CORRECTIONSRULE24-02 reproduced on production: "$60.57 to collect ... carries $1,160.54 of additional premium" puts a tax-included amount beside a premium-only running total | Name the premium part ($59.18) in the same sentence, as that record already asks | OPEN (B13 backlog, same as F-CORRECTIONSRULE24-02: a tax-included amount printed beside a tax-excluded running total) |
| F-BP-05 | INFO | `list_my_activity` was proved on production to return the caller's own rows only; no second user has ever called the endpoint there, so cross-user exclusion is proved on the disposable database and not here | None required; do not cite production as evidence of tenant isolation for this tool | ACCEPTED as recorded |
| F-BP-06 | INFO | Every `reconciliation_runs` row carries `probe_count` NULL and the MCP answer reads it as 0, so a client cannot tell "no probe" from "predates the column" | None; it stops mattering after the first run under the deployed code | ACCEPTED as recorded |

## inspect_reference round 1 (decision 42), FAIL at 0b9202d (docs/reviews/backend-inspect-reference-r1.md, 20:35 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-INSPECT-01 | MED | A reference resolving to a broker (`acct_`, or a correlation id naming a broker) is answered with one arbitrary policy of that broker as the file's policy, with its number, status, term and terms in force, while the operations and entries beside it span the whole book | Return no single-policy header for a broker subject and say the lists span the book; assert both paths in check:mcp | see round 2 |
| F-INSPECT-02 | LOW | The narrowing filters a bounded page, so on a subject with more than 20 money operations the named operation is absent from its own file while the answer still names it | Read the named operation by id instead of filtering a page of 20 | see round 2 |
| F-INSPECT-03 | LOW | Four of the eight published reference shapes (`acct_`, uuid, claim number, correlation id) have no assertion anywhere; F-INSPECT-01 lives in that gap | One check:mcp assertion per published shape | see round 2 |
| F-INSPECT-04 | LOW | A broker key reads the activity rows of every actor on its own book, which no broker screen and no other tool shows, while webhook events and reconciliation are withheld on exactly that argument | Restrict to the key's own user like list_my_activity, or say so in the description and in the answer | see round 2 |
| F-INSPECT-05 | LOW | A broker key can tell a reference that exists but is not its own (refusal) from one that matches nothing (answer), so it can enumerate existence | Yoann decides: accept and disclose in the README, or answer "nothing matches" in both cases | DECISION PUT TO YOANN (20:35 local): one sentence for both cases (a reference that is not the broker's and one that matches nothing) so a broker key cannot probe existence; recommended |
| F-INSPECT-06 | INFO | `provider_record_only` also names a correlation id that matched no object, and an operation with neither policy nor claim answers "nothing matches" while the same case answers `provider_record_only` by another path | Name the states for what they are; no consequence observed | see round 2 |
| F-INSPECT-07 | INFO | The reviewer accidentally ran a second complete `check:mcp` on the shared `corgi_test`, adding one more set of fixtures and one more planted 777700-cent break with its note | None; another agent counting rows or breaks on `corgi_test` should know | DISCLOSED |

## inspect_reference round 2 (decision 42), PASS at 9b9902a (docs/reviews/backend-inspect-reference-r2.md, 20:35 local)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-INSPECT-02 | LOW | Still present. `readTheFile` reads 20 operations ordered by `created_at desc` and only then filters to `resolution.operationId`, so on a subject with more than 20 money operations the named operation can be absent from its own file while the answer still names it in `narrowedToOneMoneyOperation`. Not reachable on the current data (the richest fixture subject carries 4). |
| F-INSPECT-03 | LOW, partly closed | The two shapes that carried F-INSPECT-01 (`acct_`, correlation id) now have assertions. A claim number and an application uuid still have none in `check:mcp`; I exercised both by probe and they answer correctly (identical header for `CLM-02798` and for its uuid). |
| F-INSPECT-04 | LOW | Unchanged. A broker key still reads `activityOfSubject` rows written by staff actors on its own book, while the webhook and reconciliation panels are withheld on the argument that no broker screen shows them. Inside the tenant boundary, inconsistent with the tool's own stated principle. |
| F-INSPECT-05 | LOW, needs Yoann | DECISION PUT TO YOANN (20:35 local): one sentence for both cases (a reference that is not the broker's and one that matches nothing) so a broker key cannot probe existence; recommended |
| F-INSPECT-06 | INFO | Unchanged. `provider_record_only` still names a correlation id that matched no policy, claim or broker row, including one that named only a customer. |
| F-INSPECT-07 | INFO | Disclosed history of `corgi_test` contention. This round adds two more sets of fixtures: the builder's second run (their own disclosure) and my one authorised run below. |
| F-INSPECT-01 | MED | A reference resolving to a broker was answered with one arbitrary policy of that broker as the file's policy while the lists spanned the whole book | Broker header with every policy field null, the book flag, the policy count and a sentence that says it first, in the pure `lib/mcp/tools/broker-book.ts` | FIXED 9b9902a, verified by probe and by check:mcp |
| F-INSPECT-08 | LOW | A broker book file names no policy anywhere, so its money operations and journal entries cannot be attributed to a policy | Return `policyNumber` and `claimNumber` on each operation and `policyId` on each entry, at least on a book file | OPEN (B13 backlog) |
| F-INSPECT-09 | INFO | The second fixture policy planted for the `acct_` assertion is outside `ourPolicies`, the list the check's no-money-moved assertions watch | Add its id to `ourPolicies` | OPEN (B13 backlog) |
| F-INSPECT-02 | LOW | The narrowing filters a bounded page, so on a subject with more than 20 operations the named operation is absent from its own file | Read the named operation by id instead of filtering a page of 20 | OPEN (B13 backlog) |
| F-INSPECT-03 | LOW | Two published shapes (claim number, application uuid) still have no assertion in check:mcp; the two that carried F-INSPECT-01 now have three | One assertion per published shape | OPEN (B13 backlog) |
| F-INSPECT-04 | LOW | A broker key reads the activity rows of every actor on its own book, while webhook and reconciliation rows are withheld on exactly that argument | Restrict to the key's own user like list_my_activity, or say so in the description and the answer | OPEN (B13 backlog) |
| F-INSPECT-05 | LOW | A broker key can tell a reference that exists but is not its own (refusal) from one that matches nothing (answer) | Yoann decides: accept and disclose in the README, or answer "nothing matches" in both cases | DECISION PUT TO YOANN (20:35 local): one sentence for both cases (a reference that is not the broker's and one that matches nothing) so a broker key cannot probe existence; recommended |
| F-INSPECT-06 | INFO | `provider_record_only` also names a correlation id that matched no object, including one that named only a customer | Name the states for what they are | OPEN (B13 backlog) |
| F-INSPECT-07 | INFO | `corgi_test` carries several extra sets of this check's fixtures: the builder's second run and this reviewer's one authorised run | None; another agent counting rows or breaks on `corgi_test` should know | DISCLOSED |

## Interface cycle 2 review (docs/reviews/ui-cycle-2.md, PASS at c408cb3, 20:38 local; 94 POST instances in 49 contracts joined to 35 handlers, the mode line on 42 of 42 renders, 375 px on 8 of 8 screens, 24 of 24 figures)

| ID | Severity | Finding | Required correction | Status |
|---|---|---|---|---|
| F-UI2-01 | LOW | The probe half of decision 28 has a heading and no data: every production run reports `Probe 0`, the Probes tile reads 0 and the probe table is empty, while the 35 breaks to act on are mostly the `$42.42` payments the check script plants; the interface is honest, the classifier has not moved them | Close F-BP-01 (recognise or re-plant the existing PaymentIntents) and re-read the board; keep the README sentence qualified until then | RESOLVED at 92379e2 (19:20Z): a human staff_ops run at 18:38:24Z on production (activity_log actor_kind human, POST /api/jobs/reconcile) stored probe_count 32, provider_only 4, matched 7 over 42 Stripe records; the cycle-2 reviewer read `4 breaks to act on` and `32 probes from check runs` on the board and `32 probes, 4 breaks to act on` on the run row (docs/reviews/ui-cycle-2.md section 12, recon-runs.png) |
| F-UI2-02 | LOW | The explained-breaks table has no amount column, and the explained row's reference resolves through `resolveReference` to the webhook trail rather than to the row's own facts, so `$42.42` is on no path from that row | Add the provider amount column to `ExplainedTable`, or let `rowOfReference` win over `resolveReference` for a reference that is a row of one of the three lists (`app/ops/reconciliation/page.tsx:196`) | OPEN (B13 backlog) |
| F-UI2-03 | LOW | The breaks table dropped `Difference`, `First seen` and `What it means`, three of the nine columns UI-011 measured; `Open for` replaces `First seen`, the legend and the drawer carry the meanings, but provider minus ledger is no longer on the row | DISCLOSED, deliberate under decisions 2 and 9; recorded so UI-011 is not re-run against a column set that no longer exists | DISCLOSED |
| F-UI2-04 | LOW | The verbatim `Sandbox providers and test data. No real money.` on `/` and `/login` is the body of a closed `<details>` whose summary is the word `Sandbox`, so it is in the markup and not in the rendered text; both pages do carry a visible sandbox sentence, and `components/signed-out-frame.tsx` is not in this diff | Print it beside the summary, or record in the handoff that the pinned sentence is one click away | REOPENED as LOW, no action before the freeze (19:10Z): the coordinator's closure of 20:39 local was wrong, it read the HTML source, not the rendered text; the cycle-2 reviewer measured the DOM at 92379e2 and the coordinator confirmed in the markup at 19:05Z: the verbatim sentence is a paragraph inside a closed details on / and /login, so it is not in the rendered text; the visible sentence on both pages is `Work-trial build on sandbox providers and test data. No real money moves here.`, which satisfies AF-02 on its own; recorded in the handoff, wording only |
| F-UI2-05 | LOW | `/policies/<id>?asOf=<date>` with no `view` is silently ignored: `PolicyAsOf` renders only under `view === "timeline"` (`page.tsx:1210`), so links written before the rework no longer show the as-of panel, against decision 33 | Resolve the view to `timeline` when `asOf` is present and `view` is not, one line beside `pickView` | OPEN (B13 backlog) |
| F-UI2-06 | LOW | `app/ops/brokers/page.tsx:121` posts to `/api/brokers`, which has no route handler at `c408cb3` or `c408cb3^1`; the only action/handler mismatch in the tree, with the button `disabled` and `Route pending` printed on the card | Land the route (decision 21, coordinator) and remove the `disabled` and the sentence in the same commit | DISCLOSED |
| F-UI2-07 | LOW | The explanation's count-up paints intermediate money figures: 700 ms after opening, CGP-01707's sum row read `$2,212.95` where the settled figure is `$2,380.44`; designed, tested and disclosed (F-B12-13), but any screenshot taken inside ~1.2 s shows a figure the ledger does not hold | Note in the handoff that drawer evidence must be captured after the count settles | DISCLOSED |
| F-UI2-08 | MEDIUM | Evidence integrity: five of the 24 PNGs under docs/evidence/ui-cycle-2/ (approvals, claim-payments, endorse-preview, recon-ops-board, recon-runs) are byte-identical copies of the signed-out login page (md5 c5789dd085614cec9ed051b17793d746), while section 11 of the record says each was opened before being committed; found by the freeze-package session while indexing the pack, verified by the coordinator | Recapture the five signed in on production with a guard against a login landing, list the 24 md5 values in the record, date the correction; the DOM measurements of the record are not in question, the illustrations are | FIXED at c8d679d, merged b33edbc (19:00Z): six files recaptured with a guard (final URL not /login, AF-02 mode line present, per-screen strings present), the sixth being reference-drawer.png which was genuine but showed the refusal state of a hand-made reference; all 24 opened, 24 distinct md5 values listed in section 11, the false sentences corrected in place, defect and cause in section 12 of the record |

## Live-fire day 2, found during Yoann's own clicks (2026-09-09 evening)

| ID | Severity | Finding | Required action | Status |
|---|---|---|---|---|
| F-LIVE-01 | MED | The backdated correction (the panel's live-fire item 2) has no visible entry point on the policy: the band offers Endorse, Cancel and Open a claim, the sub-menu shows Overview, Endorsements, Claims, Money, Timeline, and the `Correct` view label exists only once the operator is already on `/policies/<id>/corrections/new` (`policyFormViews` is called with it in `corrections/new/page.tsx` alone); the only link is the one-line GET form under the Endorsements table (`correction-sections.tsx:189`), which reads as a filter. Yoann could not find the screen on his first LIVE-8 attempt at 92379e2 and was pointed at the direct URL by the co-pilot session | Interface batch: a `Correct a date` secondary action in the band for `staff_ops` when the policy carries at least one endorsement, and the `Correct` item in the sub-menu at all times for `staff_ops`, both linking to `/policies/<id>/corrections/new`; own commit and review note | OPEN, passed to the interface session (19:25Z); not blocking, the direct URL and the Endorsements line work |
