# UI-01 independent implementation review

Reviewer: independent `ui_review` agent. Initial review: 2026-09-08T14:19:00Z. Scope: UI-01 in PLAN.md, the presentation subset of B13, on `codex/corgi-interface`, base `59f2fcdf969ad92ad43b299dbac886593cb918bc` plus the working diff. Final frozen reference and verdict follow below. This is not an integrated trial sign-off.

## Startup receipt

Actually read: AGENTS.md; AUTOMATIC-FAILS.md in full (AF-01 through AF-06); READABLE-CODE.md; WORKFLOW-48H.md; REVIEWER.md; README.md; docs/BRIEF-REFERENCE.md (including general and Track 1); docs/PLAN.md including UI-01; docs/COMPLIANCE-MATRIX.md; latest UI entry in docs/STATUS.md and docs/DECISIONS.md; docs/reviews/FINDINGS.md; architecture sections on system shape, money, eligibility and provider modes; architecture re-review findings and verdict. No mandatory kit file missing. The brief is available as the repository's supplied extraction, not a separately retrieved original; separately supplied kickoff sandbox rules remain absent. Current local records are the working source for this isolated slice.

Additional source: installed web-design-guidelines SKILL.md and its [current upstream guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md), accessed 2026-09-08. These are UI engineering guidance, not a declaration of applicable law.

Next criterion: demonstrate that the visual refresh preserves authenticated server reads, financial form contracts, eligibility and approval explanations, while supporting responsive and keyboard use. Planned checks: source comparison, typecheck, frozen build/unit and local visual evidence. No database, provider, approval, deployment, push or reference-portal interactions are authorized or needed for this review. Approximately 39 hours remained before the recorded trial freeze at review startup.

## Scope and applicability

Examined all changed app page diffs, PortalShell, WorkspaceOverview, global CSS, dependency diff, raw approval/broker/new-policy source, and the base CSS. The changed pages remain server components; PortalShell receives the already-read display name and role and adds no database or provider access. No `lib`, `db`, `scripts` or `app/api` change exists against the base. Existing route authorization, money services, workers, webhook handlers, MCP work and known review findings are outside this visual change and are not newly certified.

The product is the existing synthetic-data Track 1 policy application. Stripe test payments and Connect verification remain distinct from the local simulated claim rail. This slice changes no custody, payment rail, jurisdiction, data collection or provider responsibility, so no additional legal applicability is inferred. Sources governing scope are the supplied brief general requirements 2/3/6 and Track 1, AGENTS financial/approval/operations safeguards, AF rules, and UI-01's explicit preservation contract, all read 2026-09-08. Hypothetical production duties remain outside scope.

## Initial findings

- **UI-R01 — MEDIUM, responsive regression:** `app/globals.css` `.login-grid` keeps two minimum 280px columns plus 24px gap at desktop widths just above 800px. At 820px, the 230px sidebar and 48px main padding leave 542px, less than the 584px grid minimum. Trigger: login at 820px. Consequence: horizontal overflow. Required correction: adapt that breakpoint or column minimum and verify the intermediate viewport.
- **UI-R02 — MEDIUM, responsive regression:** `app/globals.css` `.badge` newly applies `white-space: nowrap` to the full KYB warning paragraph in `app/broker/policies/new/page.tsx`. Trigger: a non-approved broker at mobile width. Consequence: warning overflows the page rather than wrapping. Required correction: allow sentence badges to wrap and verify the state at mobile width.
- **UI-R03 — LOW, diff hygiene:** `app/broker/page.tsx:31` trailing spaces and an extra blank line at EOF cause `git diff --check` to exit 2. Remove the whitespace.

Initial verdict: **FAIL** for these observed source-level responsive regressions. Final visual evidence and a frozen diff are also pending. No financial or form-contract regression identified.

## Checks actually executed

- `npm run typecheck`: PASS, exit 0.
- `git diff --check`: FAIL, exit 2, UI-R03 above.
- `git diff --name-only 59f2fcd -- lib db scripts app/api`: PASS for the preservation scope; no output, no backend changes.
- Read-only TypeScript AST comparison of `form`, `input`, `select`, `option`, `button`, `textarea` attributes between the base and each changed page (excluding only styling and email spellcheck): identical across all financial/authentication forms. The sole structural difference is the broker logout form moved to PortalShell with the same POST `/api/session/logout` and submit button.
- Manual page diff review: existing server authorization/queries/calculations, conditional eligibility and approval branches, canonical intent/hash display, hidden input values, money text, failure messages and provider labels preserved. Overview text explicitly labels claim payout and bank verification LOCAL SIMULATOR and calls the $1,000 threshold a trial assumption.
- Build/unit tests and rendered desktop/mobile/keyboard evidence: pending coordinator's frozen evidence; no database or provider tests executed, since they are outside the unchanged backend scope. No commit, staged secret scan, deployment, external browser test or candidate walkthrough performed by reviewer.

## Requirement and automatic-fail mapping

| Requirement | Control/evidence | Initial status |
|---|---|---|
| UI-01: presentational scope only | Full changed-file inventory, no backend-path diff, server/form comparisons above | PASS for source preservation |
| UI-01: responsive and accessible UI | Semantic links/buttons, decorative alt text, skip link, focus rule, focusable table scroll regions; viewport regressions UI-R01/02 | FAIL pending fixes and rendered evidence |
| AF-01: accessible deployment | No deployment in this isolated branch slice; deployment evidence belongs to final integrated gate | NOT RUN |
| AF-02: honest modes | Existing KYB/claim labels retained; overview local-simulator text; art never represented as provider evidence | PASS for UI labels only; live integration not re-tested |
| AF-03: immutable ledger | No backend, SQL, migration or monetary-calculation changes; preserved financial forms | PASS for unchanged source scope; database enforcement not re-tested |
| AF-04: sandbox/no real data | Review uses source and synthetic presentation evidence only; no database/provider operations or personal reference content added | PASS for reviewer actions; overall system not re-certified |
| AF-05: no committed secrets | No secret files read or copied; final staged/history scan remains coordinator's commit/push gate | NOT RUN until scan evidence |
| AF-06: candidate ownership | Short reading path: server page → PortalShell → CSS; WorkspaceOverview contains explicit presentational JSX | Explainability inspected; NOT REVIEWED WITH YOANN |

No PASS in this record certifies prior history or resolves the existing trial findings register. Technical evidence remains separate from Yoann's understanding.

## Re-review — 2026-09-08T14:31:20Z

Frozen reference: base `59f2fcdf969ad92ad43b299dbac886593cb918bc` plus all 19 source/asset/package files in [ui-01-source-manifest.json](../handoffs/ui-01-source-manifest.json). The manifest itself has SHA-256 `d257b20e24f785f414e75663c8509c02e405b1882c609b3e8ecfd7c46b0a813c`. Independently recomputed every listed hash: **19/19 match**, base HEAD matches. Shared handoff/status documentation is outside this source manifest; material source changes reopen this review.

Resolved findings:

- **UI-R01 resolved:** login uses `repeat(2, minmax(0, 1fr))` and `min-width: 0` on children. Coordinator's recorded CUA check of the actual login at 820px reports document width 820px, loaded images and fitting columns.
- **UI-R02 resolved:** global badge `nowrap` removed, `overflow-wrap: anywhere` added. Recorded CUA synthetic-state check at 390px reports a wrapped 338px warning and 390px document width.
- **UI-R03 resolved:** independently reran `git diff --check` and `git diff --cached --check`: both PASS.

Additional evidence inspected: [UI handoff](../handoffs/ui-01.md), raw `.local/ui-build.txt` (compiled, TypeScript complete, 19 pages generated), `.local/ui-typecheck.txt` (exit-success command output), and `.local/ui-unit-tests.txt` (220 total, 219 passed, zero failed, one explicitly skipped opt-in live Stripe test). Earlier independent typecheck remains valid for unchanged TypeScript except the simple reviewed staff Policies link restoration. No lint script exists. Both final WebP assets were independently opened and visually inspected; they contain decorative corgi scenes, no personal portal content.

Browser evidence is bounded: the coordinator recorded actual public home/login checks and synthetic shell/state fixtures at desktop, 820px, 390px and 320px, plus skip-link/table keyboard focus and horizontal scrolling. I independently opened only `http://localhost:4177/ops.html` through CUA and inspected its accessibility tree and desktop screenshot: clear synthetic-fixture notice, role explanation, simulator and threshold disclosures, correct navigation/cards and loaded artwork. I did not interact with the hiring reference, submit a form, use credentials, or claim that these fixtures exercise authenticated financial flows. Mobile/keyboard numerical observations are coordinator-run evidence, supported by the corrected CSS and explicitly distinguished from my independent desktop observation.

Independently ran `gitleaks protect --staged --redact --no-banner --no-color`: PASS, exit 0, approximately 45.85KB scanned, no leaks found. This covered the then-staged source/artwork/provenance/handoff files, before adding this review's final text. Coordinator must stage the final review and repeat the scan before committing. History/deployment checks remain NOT RUN in this local-only slice.

Final verdict: **PASS for UI-01's frozen presentation scope.** No unresolved material finding remains in this scope. Source contracts, financial/eligibility branches, exact approval intent, local-provider disclosures and bounded responsive/keyboard evidence satisfy the selected UI-01 criteria. AF-02/03/04 scoped assessments stand; AF-05 staged scan passes as described. AF-01 deployment and AF-06 candidate confirmation remain separate final-delivery gates, not implied passes. B13 and the integrated trial are incomplete; this review does not approve existing financial gaps, a merge, deployment or submission. Candidate walkthrough remains **NOT REVIEWED WITH YOANN**.
