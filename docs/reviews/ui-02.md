# UI-02 independent implementation review

Reviewer: independent `ui_review` agent. Startup: 2026-09-08T14:59:02Z. Branch `codex/corgi-interface`, base `c7784048a646c30c398d744eb451cd0e62ec128e`. Scope: replace UI-01's visual treatment with the client-portal visual language requested by Yoann, preserving existing financial and authorization behavior. Final source manifest, checks and verdict will be appended after the coordinator freezes the diff.

## Startup receipt

Actually reread AGENTS.md, AUTOMATIC-FAILS.md in full (all six bans), READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md, README.md, docs/BRIEF-REFERENCE.md including general/Track 1, current UI entries in STATUS/PLAN/DECISIONS, COMPLIANCE-MATRIX.md and the complete UI-01 review. The previously inspected unchanged architecture and financial findings remain relevant context; this visual slice does not re-certify them. Repository was clean when inspected, on the assigned branch and base; approximately 39 hours remained before the recorded trial freeze.

No mandatory kit file missing. The brief exists as its supplied repository extraction, not a separately retrieved original; separately supplied kickoff sandbox rules remain absent. Local documentation is the working record for this isolated slice. Only this review file is reviewer-owned. No implementation, provider/database action, approval, push or deployment is authorized in this review. Chrome is reserved for the coordinator until the local preview is handed over.

Next acceptance gate: inspect the frozen source delta for preserved server checks/queries, form actions/names/values, money and eligibility disclosures; inspect any new client boundary for presentation-only state; check responsive layout, focus and preview evidence. Initial status: **review pending**, not PASS.

## Applicability boundaries

The source requirement is Yoann's UI-only request and the UI-02 plan entry, supporting the presentation part of B13. The underlying product remains the synthetic Track 1 application with Stripe test-mode payments/Connect verification and an explicitly simulated claim rail. No new jurisdiction, custody, rail, provider duty or legal policy is introduced by the proposed skin. Trial general requirements 2/3/6 and AF safeguards apply to preservation and honest UI labels. Final deployment and candidate understanding are separate AF-01/AF-06 gates. Full B13 and the integrated trial remain incomplete.

## Frozen implementation review — 2026-09-08T15:14:09Z

Reference: base above plus the ten source/asset files in [ui-02-source-manifest.json](../handoffs/ui-02-source-manifest.json). Independently recomputed **10/10 hashes, all matching**. Manifest SHA-256: `7a2d956fb2366385ea4a3ac1f790272fa54f51aa572508ec93bcc32fd3ae6a7c`. Material changes to these files reopen this review. Documentation is outside the source manifest.

Actually inspected: the complete source delta for changed pages, PortalShell and WorkspaceOverview; new PortalFrame; all final CSS; final handoff and artwork provenance; raw typecheck/build/unit logs. The [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md) were fetched again on 2026-09-08 as engineering guidance. No additional legal/provider source research was needed for this unchanged funds flow.

The client boundary is limited appropriately: PortalShell has no `use client` directive and keeps role-dependent navigation and identity rendering on the server. PortalFrame accepts rendered sidebar/content slots and a section label, with one boolean for navigation visibility. It imports only React state and decorative icons; no session, user object, database client, financial function or provider adapter crosses as a new application-data prop. Existing server-page authorization, ownership and calculations remain untouched. The `hidden` attribute plus `[hidden] { display:none !important }` removes the collapsed sidebar from interaction; the toggle is a native `type="button"` with an accessible name, control target and expanded state.

### Checks and findings

- Independently ran `npm run typecheck`: PASS.
- Independently ran `git diff --check` and `git diff --cached --check`: PASS.
- Independently checked `git diff c778404 --name-only -- lib db scripts app/api`: no output. These paths are unchanged.
- Read-only TypeScript AST comparison across **all 13 app TSX files**, ignoring only `className`: form/input/select/option/button/textarea attributes identical to base. Manual diff review confirms financial expressions, conditional eligibility, exact approval intent and provider-mode disclosures unchanged. Logout POST action is preserved in the server-rendered sidebar.
- Inspected `.local/ui02-typecheck.txt`, `.local/ui02-build.txt` and `.local/ui02-unit-tests.txt`: build compiled and generated 19 pages; 220 tests total, 219 passed, zero failed, one explicitly opt-in live Stripe test skipped. No lint script exists. No database or provider test was run for this source-only financial preservation scope.
- Independently ran `gitleaks protect --staged --redact --no-banner --no-color`: PASS, exit 0, approximately 36.62KB scanned, no leaks. This precedes staging the final review text; the coordinator must repeat the scan after final staging. History scan is not claimed by this result.
- Independently viewed the actual local production login at `http://127.0.0.1:3108/login` via CUA, including its accessibility tree and desktop screenshot. Clear labels, artwork, gray surfaces, black action, visible focus ring and sandbox disclosure were observed. No credential was entered and no form was submitted.
- Independently activated the focused native navigation button with **Space**: expanded true → false, sidebar hidden; Space again restored expanded true and navigation. Native control semantics and visible focus satisfy the verified keyboard operation.
- **UI02-L01 — LOW, tool-limited verification:** locator Enter and CUA Return did not activate the toggle. The same locator Enter also failed to open the unchanged native `<summary>` control, while focus was confirmed on SUMMARY. This is evidence of an Enter-activation limitation in the test tool, not a demonstrated application-specific failure. Enter remains **NOT VERIFIED by CUA**; verify manually in a normal browser when convenient. Do not add a duplicate key handler to a native button on this evidence. Space is verified. This disclosed gap does not block the scoped keyboard-operability criterion.
- Independently viewed the desktop static operations fixture's accessibility tree and screenshot at `http://127.0.0.1:4177/ops.html`: synthetic/no-database notice, task navigation, role context, assumption-labeled threshold and LOCAL SIMULATOR disclosure visible. Static controls have no hydration and were not treated as functional evidence.
- Reviewed coordinator-run responsive observations in [UI-02 handoff](../handoffs/ui-02.md): 820px production login, 390px production error login, 320px operations fixture, 390px long-warning/table fixture, all with document width equal to viewport. Long text wraps; table overflow stays inside the scroll region. These measurements are coordinator-run evidence, not independently rerun mobile checks. Source preserves UI-01's wrapping, focus, skip-link and reduced-motion controls.

No material source, security, financial-contract or rendered-layout finding remains. The reviewed desktop preview tab was retained; reviewer did not alter a viewport or touch either external reference portal.

### Requirement/AF matrix

| Requirement | Control and evidence | Verdict |
|---|---|---|
| UI-02 presentation and useful navigation | Actual component/source and desktop observations; existing destinations preserved, unsupported customer actions absent | PASS |
| UI-02 financial and authorization preservation | No backend delta; all app form attributes identical; unchanged financial conditional branches | PASS for source scope |
| UI-02 responsive/client/focus behavior | Minimal boolean state, semantic button/hidden sidebar, Space test, responsive evidence above | PASS with disclosed Enter tool limitation |
| AF-01 accessible deployment | No deployment or external journey for this branch | NOT RUN, separate final-delivery gate |
| AF-02 honest integration claims | Simulator and Stripe-test labels retained; fixtures/artwork explicitly distinguished from integration evidence | PASS for UI labeling; integrations not rerun |
| AF-03 immutable ledger | No SQL/database/service/money-operation changes or reviewer database actions | PASS for unchanged source scope; runtime guards not retested |
| AF-04 sandbox and synthetic data | No credentials/provider calls; synthetic local previews; artwork prompt contains no account details | PASS for scoped work; no historical remediation certified |
| AF-05 secrets | Independent staged gitleaks PASS; reviewed provenance and handoff contain no secrets | PASS for scanned staged increment; rescan final review before commit |
| AF-06 ownership/readability | Reading path stays page → server shell → minimal frame → CSS | Explainability reviewed; NOT REVIEWED WITH YOANN |

Final verdict: **PASS for UI-02's frozen presentation scope**, with UI02-L01 disclosed. This is not a final trial, B13, deployment, merge or submission approval. Authenticated financial screens were source/build reviewed and were not each exercised with sandbox data. Existing financial findings and final integrated requirements remain in their own records. Candidate understanding remains **NOT REVIEWED WITH YOANN**.

## Scope reopened after font/button refinement request

The coordinator received a new user instruction while the preceding verdict was being recorded: investigate the reference fonts, use white text on orange actions, and reproduce the button relief. The prior PASS remains historical **only for manifest `7a2d956f…ae6a7c`** and does not approve these forthcoming font/CSS changes. Current UI-02 status is **REVIEW PENDING** until a new frozen manifest, font provenance/usage evidence and relevant visual/contrast checks are supplied. Chrome is again reserved for the coordinator. Prior evidence can be reused only for unchanged source.

## Final font/button re-review — 2026-09-08T15:34:04Z

This section supersedes the pending status above. Final reference: base `c7784048a646c30c398d744eb451cd0e62ec128e` plus the **16 files** in the current source manifest. Independently verified **16/16 SHA-256 values**. Manifest SHA-256: `c5f15f78645a7f3cf25fc13fcf8ccf3a360780be8dfa95546d8c7391cab9abcf`. All TSX files and artwork retain their previously reviewed hashes; the additional delta is CSS and six font/license files. Therefore the earlier source/form/client-boundary checks and unit evidence remain applicable without a new financial test run.

### Font provenance and checks

Read the final CSS font declarations/button rules, font provenance, both bundled licenses in full, the local subset helper, updated handoff/STATUS/DECISIONS/compliance entries and `.local/ui02-final-build.txt`. Also checked the official Google Fonts [Inter license](https://raw.githubusercontent.com/google/fonts/main/ofl/inter/OFL.txt) and [DM Sans license](https://raw.githubusercontent.com/google/fonts/main/ofl/dmsans/OFL.txt), accessed 2026-09-08. Both are OFL 1.1 (2007-02-26), permit bundled modifications subject to their conditions, and do not declare a Reserved Font Name after the copyright notice. Local license notices and font copyright metadata are retained. No F37 font is bundled. This is a scoped license-evidence assessment, not a claim about unrelated application licensing.

The system Python's fontTools inspection initially failed because Brotli was missing. Reran with the coordinator's existing `.local/font-tools-venv/bin/python`, without installing anything: all four WOFF2 files decoded. Inter normal/italic identify as Inter with weights 100–900; DM Sans normal/italic identify as DM Sans with weights 100–1000. All contain USD digits and punctuation and retain their copyright metadata. Provenance describes the subset ranges and WOFF2 conversion. CSS uses local URLs, real normal/italic faces, `font-display: swap`, and fallback families; it introduces no runtime third-party font request.

**UI02-F01 — explicit fidelity limitation:** headings use DM Sans as a free approximation. Exact F37 Bolton typography is **not achieved** and must not be described as an exact font match. The documented reference identification and WhatTheFont search do not grant a redistribution license. Excluding unlicensed F37 is consistent with the trial's $0 constraint; the approximation is clearly identified in the handoff and decision log.

### Final rendering and validation

Independently inspected the final production login via CUA at 1512px: document width 1512px, loaded artwork, visible focus and the final white/orange navigation. Computed heading was DM Sans, weight 500, 30px/36px; body and button Inter; button 16px/19.2px, radius 12px, lower shadow `rgb(98,98,98) 0px 4px 0px 0px`. `document.fonts.status` reported loaded and checks for DM Sans 500/30px and Inter 400/15px returned true. No browser error/warning log was returned. No credential was entered, no form submitted, and no reviewer viewport override was applied.

Inspected the final build log: compile and TypeScript successful, 19 pages generated. Coordinator's repeated final-font responsive evidence is recorded in the updated handoff: production login 820px and 390px, operations fixture 320px and 1512px, warning/table fixture 390px, with document width equal to viewport. Table overflow is contained; native pointer/Space navigation remains functional. The unchanged Enter test-tool limitation UI02-L01 remains disclosed. No claim that static fixtures prove authenticated actions is made.

Independently reran final-manifest verification, staged whitespace check and staged gitleaks: PASS; approximately 49.30KB scanned at that staging point, no leaks. The final review/status text must still be staged and scanned before commit. Different earlier scan byte counts refer to different staging points, not to a history scan.

### Contrast result — no accessibility-conformance PASS

**UI02-C01 — known contrast defects retained for the user's specified reference colors.** Independently calculated from final CSS tokens:

| Foreground/background | Ratio | WCAG 2.2 AA contrast benchmark |
|---|---:|---|
| White / `#ff5c00` selected navigation and orange action | 3.096:1 | Fails normal text 4.5:1 |
| `#ff5c00` heading / `#f6f6f6` workspace | 2.865:1 | Fails even large text 3:1 |
| White / orange hover `#ff7d33` | 2.551:1 | Fails normal text |
| White / orange pressed `#ff9d66` | 2.045:1 | Fails normal text |
| White / gray pressed action `#7b7b7b` | 4.233:1 | Fails normal text |

Benchmark source: [W3C SC 1.4.3 explanation](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html), accessed 2026-09-08. These are actual accessibility limitations, not passing contrast results. Matching the requested reference colors does not make them conformant. The brief did not establish a numeric WCAG acceptance requirement, and the user explicitly refined these colors; the implementation verdict below covers that visual request, functional preservation and bounded keyboard/responsive behavior, **excluding contrast/full accessibility conformance**. Improving these ratios requires relaxing the selected colors; do not imply that keyboard support compensates for them.

### Final scoped verdict

**PASS for the frozen UI-02 implementation, with UI02-F01 (font approximation), UI02-C01 (contrast defects) and UI02-L01 (Enter tool verification gap) explicitly disclosed.** No backend, monetary, authorization, form-contract or new client-boundary defect was identified. This PASS does not certify exact F37 reproduction or accessibility conformance.

AF-02/03/04 source/action assessments remain as in the earlier matrix; AF-05 passes for the staged content actually scanned and requires a final staging scan. AF-01 deployment remains NOT RUN for this revision. AF-06 remains **NOT REVIEWED WITH YOANN**. Full B13, financial integration, historical findings and the final trial delivery gate remain incomplete and outside this verdict. No merge, push, deployment, provider/database action or external-account interaction was performed by this reviewer.

## Local preview navigation correction — initial review, 2026-09-08T15:50:18Z

Scope: ignored visual-preview delivery only, at documentation HEAD `9acbb5b`; application implementation `8fdb957` was unchanged when checked. Reviewer `/root/ui_review` reread AGENTS.md, AUTOMATIC-FAILS.md (all six bans), READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md and the latest STATUS/handoff; no mandatory file missing. Prior brief, README and scope review reads remain applicable. Next criterion: local links resolve while synthetic fixtures cannot submit actions, including without JavaScript. No new provider/legal applicability is introduced.

Read `.local/build-navigable-preview.tsx` in full, SHA-256 `5112dcd052b3865e84c8b714601b4e0740379b1db0017175711f075eb49928e1`, and its direct presentation/money-formatter dependencies. Page service imports are explicitly replaced with synthetic adapters; unmapped imports and database calls throw. Rendering imports no actual financial service/database client. The helper's dynamic transpilation is confined to known local page sources; it is ignored inspection tooling, not a submitted application architecture.

Independent checks: parsed all 18 HTML files in `.local/ui-preview`; all 13 internal destinations linked by newly generated index pages returned HTTP 200. A direct empty POST to the local static server's `/api/session/logout` returned 501; this was not an authenticated or provider endpoint. CUA opened the previously broken Claims route, observed its synthetic notice and disabled sign-out control, and clicked the synthetic claim-detail link. No financial control was activated. `git diff --name-only 8fdb957 -- app components lib db scripts public package.json package-lock.json` was empty. Both helper and generated directory are ignored. No build rerun was warranted for unchanged application source.

Findings:

- **UI02-P01, material preview-safety/evidence gap:** the generator only replaces its current routes, leaving older publicly served `approver.html` (one form) and `states.html` (two forms) active in the output directory. Thus the claim of zero forms across the delivered preview is false, and action disabling is inconsistent. Static-server POST rejection limits the consequence to a failed request, not money movement. Regenerate those specific legacy files through the protected renderer or remove the known obsolete fixtures, then repeat the whole-directory HTML audit. `font-study.html` is also a legacy output without the new synthetic notice; classify or remove it explicitly.
- **UI02-P02, minor no-script inconsistency:** `/broker/kyb` retains the external `https://stripe.com/connect-account/legal` anchor and only blocks it with JavaScript. This is a public legal-document link, not a financial action, but the preview's promise of no external connection should be enforced in generated HTML if retained. Convert it to an inert explanatory element or qualify the promise.

**FAIL for the current local preview correction until UI02-P01 is fixed and independently rechecked.** This finding does not reopen the unchanged application source verdict above. Coordinator acknowledged both findings and is correcting the known artifacts. AF-02 is incomplete for this preview's blanket safety claims; synthetic-data labels on new routes are confirmed. AF-03/04: no database/provider/real-data operation occurred. AF-01 external deployment, AF-05 final staged scan and AF-06 candidate walkthrough are NOT RUN for this correction. The local harness is intentionally absent from a clean clone and does not replace deployed-app evidence; B13/final trial gates remain incomplete.

### Preview correction re-review — 2026-09-08T16:00:05Z

Reviewer `/root/ui_review` read the updated helper in full and independently verified SHA-256 `c34bc590f742a0dae88c250d81fc79c27011764969ecee40280090e61eba997d`. Known `approver.html` and `states.html` outputs now pass through the same protected renderer; HTTP/API anchors become inert spans before any JavaScript executes. Reparsed **all 18 served HTML files**: zero forms, zero HTTP/API anchors, all 29 fieldsets disabled, all 31 submit buttons disabled. All 13 linked internal destinations returned HTTP 200; empty POST to the local static server still returned 501. New-policy browser DOM independently confirmed inputs match `:disabled`. No financial action was attempted. HTML inventory SHA-256 (sorted relative-path/content-hash lines) is `e5b19f08a4a286694481e4bd9a584b6dad6bc237632411d8abc69554cf4ffd74`.

The separate `font-study.html` is explicitly classified in the current UI-03 handoff as an inert typography comparison, not an account/application fixture; it has no forms or external anchors. All application fixtures include the synthetic-record notice. The harness remains ignored local tooling with no clean-clone/deployment claim. Its explicit adapter/database boundaries are unchanged. **UI02-P01 and UI02-P02 resolved; PASS for the corrected local preview delivery at this helper/output revision.** Concurrent application navigation changes are separately reviewed in [UI-03](ui-03.md). The previous font, contrast and Enter limitations and incomplete final trial gates remain unchanged. Staged gitleaks independently passed for 7.82 KB before the final review documents are staged; coordinator must repeat the final scan.
