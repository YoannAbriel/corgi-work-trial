# UI-04: complete existing-screen UX review

## Initial design/source review — 2026-09-08

Reviewer: independent `/root/ui_review`; dedicated worktree `.worktrees/corgi-interface`, branch `codex/corgi-interface`, initial clean HEAD `20f3084`. Scope: every existing app TSX screen and shared presentation, plus the isolated preview delivery; proposed staff Policies list. No source implementation by this reviewer. Prior UI-01–03 scoped passes are not evidence of an exhaustive screen/state review.

Startup receipt: AGENTS.md, AUTOMATIC-FAILS.md (all six bans), READABLE-CODE.md, WORKFLOW-48H.md and REVIEWER.md actually read in this continuing session, as were the released general/Track 1 brief, README, current STATUS/PLAN/DECISIONS/COMPLIANCE and UI-01–03 reviews. Current source inventory and git state inspected; no mandatory kit file missing. Architecture financial/read boundaries revisited. Actually read every existing `app/**/*.tsx` screen, layout, PortalShell, PortalFrame, WorkspaceOverview, all globals.css, and the current local generator. Read `policiesOfBroker` and `brokersWithKybState` implementations for the new staff-list design. No browser interaction during this initial source audit; coordinator owns Chrome until capture handoff. Next criterion: preserve real form styling and role-appropriate navigation while documenting visual/state coverage for every screen.

Applied `web-design-guidelines` skill, read its SKILL.md and fetched the [current Vercel interface guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md) on 2026-09-08. Used as engineering review guidance, not an invented trial/legal requirement. Native links/buttons already supply keyboard activation; no redundant key handlers are required. Money formatting follows existing exact-cent requirements rather than changing arithmetic for cosmetic consistency.

### Design assessment

**DESIGN PASS** for the proposed read-only `/ops/policies` page: authenticate first, require existing staff_ops/staff_approver roles before any read, then gather broker IDs using `brokersWithKybState` and policy rows using `policiesOfBroker`. These inspected helpers read database rows without calling providers. Staff already reads all policy details; this adds discoverability, not new financial permission. Preserve helper amounts, include broker identity, link to existing details, and show empty/error states honestly. The per-broker query fan-out is a bounded trial tradeoff, not a scalable listing design; do not claim performance evidence.

**DESIGN PASS** for preserving native form DOM in the local demo while allowing input and GET-only preview navigation. Its isolated local server must refuse POST on every path, even without JavaScript, and feedback must clearly say no operation was executed. A discreet Sandbox demo disclosure/details can replace the rejected banner and role-link bar per the user's explicit refinement. Keep simulated/provider-mode facts accurate and qualify live-looking page prose in the disconnected demo. Do not fabricate a successful payment, approval, KYB call or ledger-backed cancellation computation. Only synthetic inputs/records are allowed. The prior zero-form invariant is superseded by this explicit interaction design; native form styling must now match the real application.

### Initial findings to resolve or account for

- **UI04-F01 — material preview fidelity defect:** `.local/build-navigable-preview.tsx:64` changes `form.card` into `section.card`, while `app/globals.css:609` and subsequent selectors style only `form.card`. Cancellation/new-policy/KYB/claim forms lose card, label and input styling in the delivered preview; blanket fieldset disabling also prevents GET cancellation navigation. Preserve real form markup and enforce local no-side-effect boundaries separately. The earlier HTML safety checks did not establish visual fidelity.
- **UI04-F02 — navigation defect:** `app/login/page.tsx:15` redirects every already-signed-in user to `/broker`; staff sees that page's wrong-role explanation. Use an appropriate staff/broker landing destination while preserving authorization checks.
- **UI04-F03 — decision hierarchy:** `app/ops/approvals/page.tsx:134` gives Approve and Reject the same black primary appearance. Differentiate the secondary rejection action without changing decision names/values, immutable intent display or eligibility conditions.
- **UI04-F04 — error/status feedback:** query-error paragraphs in new policy, KYB, brokers, policy detail, claim detail, approvals and cancellation lack the alert semantics already present on login; success/query feedback lacks status semantics. Add consistent accessible feedback where appropriate, preserving exact business outcomes.
- **UI04-F05 — repeated navigation names:** numerous table regions on policy/claim/cancellation pages all use `aria-label="Scrollable data table"`. Give each a contextual label or associate it with its heading so keyboard/screen-reader navigation distinguishes charge, coverage, refunds, reserves and journals.
- **UI04-F06 — form affordances:** KYB website is text rather than URL; most nonlogin forms omit autocomplete choices and explanatory-text associations. Review expected input types, autocomplete and `aria-describedby` for each field. Keep server validation, submitted names, hidden intent/version fields and money input semantics intact.
- **UI04-F07 — missing state evidence:** no dedicated loading/error-boundary screens exist at this baseline. Query-error paragraphs are not equivalent to a failed data load. Inventory these gaps and distinguish existing states actually rendered from new/unimplemented states; do not mark every screen/state tested from nominal fixtures alone.

Prior UI02-C01 contrast failures and UI02-F01 font approximation remain user-selected fidelity limitations, not accessibility/font passes. Layout risks such as long intent text, claim action buttons, nested approval tables/forms and mobile checkbox wrapping require actual rendered checks; source inspection alone does not confirm their appearance.

### Baseline screen/state inventory

All rows below are **SOURCE READ; VISUAL RE-REVIEW PENDING**. The coordinator must identify final route fixtures and viewport captures, including below-fold controls; final reviewer results will be appended.

| Existing screen | Meaningful states/sections to inspect |
|---|---|
| `/` | Public home, sign-in navigation, decorative image, sandbox disclosure |
| `/login` | Default, query error, native validation, already-signed-in landing, demo access details |
| `/broker` | Populated/empty policies; approved/ineligible KYB; nonbroker explanation |
| `/broker/kyb` | Unsubmitted, pending, approved, failed, submission/history tables, legal checkbox and field help, query feedback |
| `/broker/policies/new` | Full form, KYB warning, validation/query error, amount/date/select controls |
| `/ops` | Operations and approver role copy, links, preserved simulator disclosure |
| `/ops/brokers` | Populated/empty, pending/settling-window/failed/approved status, long reason, recheck action, query feedback |
| `/ops/claims` | Populated/empty, long rows, claim/policy links |
| `/ops/approvals` | Operations read-only, approver decision form, own-request block, decided request, empty queue, long canonical intent/hash and destination |
| `/policies/[policyId]` | Broker/staff versions; charge/coverage; payment eligibility; GET cancellation form; cancellation/refund failed/waiting/approved states; claims/intake; populated/empty journal; query feedback |
| `/policies/[policyId]/cancel` | Refusal and successful calculation layouts, no-refund/above-threshold/open-claim variants, long confirmation action and hidden version fields |
| `/ops/claims/[claimId]` | Open/closed; reserve form/history; bank verified/failed/missing; pending/ready/sent/settled/returned payments and action forms; close eligibility; journal/query feedback |
| Shared layout/components | Sidebar and collapse/restore, back arrow placement, breadcrumb hierarchy, focus/skip link, table scrolling, responsive typography, short-height/long-content behavior |
| Proposed `/ops/policies` | Staff-only access, populated/empty/error listing, broker context, links to details, mobile table |

Initial AF gate: AF-01 deployment NOT RUN; AF-02 demo labeling/control design pending implementation; AF-03 no reviewer money/database operation or source edit; AF-04 local synthetic source review only, no provider/real-data/spend; AF-05 final staged scan NOT RUN for the forthcoming revision; AF-06 NOT REVIEWED WITH YOANN. **Implementation verdict remains PENDING** until the frozen source, full route/state evidence and fixes are independently reviewed. B13 and final integrated trial gates remain incomplete.

## In-progress source re-review — 2026-09-08T16:30:31Z

Same continuing session and base `20f3084`; source is explicitly not frozen. Read current STATUS/PLAN, the evolving diff, new staff policy page, loading/error components, `.local/build-navigable-preview.tsx`, `.local/serve-ui-preview.tsx` and served `demo.js`. Mandatory reads remain as recorded above; no context reset or missing file. Browser remained reserved for the coordinator. No final source/visual PASS is issued here.

Independent source tests actually executed using TypeScript transpilation with explicitly supplied synthetic adapters (no actual auth/database/service import):

- Staff policy page: anonymous redirects to `/login`; broker and customer redirect to `/broker`; all three paths make **zero** broker/policy read calls.
- Both staff_ops and staff_approver render using the expected broker lookup and policy read; a synthetic read failure renders the safe alert without exposing the thrown cause.
- Already-signed-in staff_ops and staff_approver visiting login redirect to `/ops`.
- TypeScript AST comparison against base across 14 tracked TSX files found unchanged form/input/button/select/textarea/option contract attributes: method, action, name, value, defaultValue, required, disabled, min/max/length limits, multiple and checked state. Accessibility/autocomplete/URL-input refinements are intentional presentational changes and were read separately.
- No `lib`, `db`, `scripts` or `app/api` diff; `git diff --check` passed.
- Against the isolated server at 127.0.0.1:4177, all 16 combinations of POST/PUT/PATCH/DELETE with `/`, `/api/session/logout`, `/api/policies` and an unknown path returned **405**. Empty requests carried no account/financial data. Server rejects non-GET/HEAD before routing and contains no actual backend route handler. This proves local refusal only, not deployed authorization.

Source fixes underway address error/status semantics, contextual table labels, native forms, input affordances, staff Policies navigation and loading/error presentation. Loading/error variants still need rendered checks; the local error fixture passes an inert reset callback, so its visual rendering does not itself prove the real React reset action works.

Additional concrete fixture findings sent to the coordinator:

- **UI04-F08 — role continuity:** `renderPreview` currently applies selectedRole only to `/policies/*`. An approver navigating from `/approver` to `/ops/approvals` or `/ops/policies` silently renders the default staff_ops actor. Preserve the selected demo staff role across staff routes so normal navigation is coherent; this remains synthetic role selection, not authentication.
- **UI04-F09 — inconsistent approval evidence:** fixture `request.intentHash` hashes `demo-approval-intent`, while the rendered page says it is SHA-256 of the displayed canonicalIntent. Compute the hash from those exact canonical bytes. Synthetic status does not excuse internally contradictory displayed facts.

Both findings and full screen/state visual coverage remain open pending final freeze. No agents' approval, connected API, provider or database action was executed by this reviewer.

## Frozen-source and visual evidence re-review — 2026-09-08T16:46:54Z

After context recovery, actually reread AGENTS.md, AUTOMATIC-FAILS.md in full (AF-01–06), READABLE-CODE.md, WORKFLOW-48H.md, REVIEWER.md and current STATUS. Read the new UI-04 handoff and current PLAN/DECISIONS/COMPLIANCE entries. No mandatory file missing. Base remains `20f3084` on `codex/corgi-interface`. Next gate is final capture/build/staged-scan evidence; no code implementation or browser/provider operation by this reviewer.

Exact application scope: all **18/18** file hashes independently match `docs/handoffs/ui-04-source-manifest.json`, SHA-256 `0efe0160d6e87e5dee53c3a05038cb0d73ee9745356399136bbf8d393f41de7e`. It includes the four new files (staff policy index, loading, error and not-found), which ordinary unstaged `git diff --stat` does not yet include. Final source and all existing page/shared-component code were read; no `lib`, `db`, `scripts` or `app/api` changes.

Independent final-source checks:

- `npm run typecheck`: PASS. `git diff --check`: PASS.
- Repeated AST form-contract comparison against the base across **16 existing TSX files**: unchanged actions, methods, submitted names/values/defaults, hidden/version fields, required/disabled/checked attributes and numeric/length constraints. The first final comparison command had a script syntax error; corrected command passed. No application source changed to obtain the result.
- Earlier isolated staff-list guard tests remain valid: anonymous and nonstaff actors redirect before any read; both staff roles perform the expected read-only lookup; safe failure alert; staff login landing is `/ops`.
- F01–F07 source corrections independently confirmed: actual form elements preserved; secondary Reject appearance; contextual table labels; query alert/status semantics; URL/autocomplete/help affordances; styled boundary screens; mobile approval table/consent wrapping and independently scrollable sidebar. Cancellation confirmation copy uses the existing plan's zero-refund/approval decision without changing calculation or submission semantics.
- F08 resolved: HTTP requests carrying the synthetic approver role retain Demo Approver across `/ops`, staff policies, approvals and claim detail. F09 resolved: SHA-256 independently computed from the exact rendered canonical intent equals the displayed hash. Fixture `hasReserve:true` now agrees with its reserve/history.
- Local HTTP GET cancellation checks: valid date renders `$365.16`; term end renders no-refund copy; out-of-term date renders refusal. Native GET form remains present. All **16** independent POST/PUT/PATCH/DELETE combinations on four representative paths returned **405**, including unknown paths and session/financial endpoint names. No JavaScript is needed for that refusal. These empty requests hit only the isolated server, with no actual backend/provider handler.

Final local helper hashes at this check: generator `16903702bac54cc1448ad4bc6ec935866f43c4b416395121a9b91db7d96bfc41`; server `6213de2db3c6eff92b162a235d6076fde781db2a09dda637e0c329e0a657ecb2`; demo script `38ebc10c6a1a464e4385613c4b74ba319f0d9a05586f3d3ebd4050c33c57a829`. The full helper/server/script were read. Explicit synthetic adapter allowlist rejects unknown imports; database adapter throws; local server binds 127.0.0.1 and refuses non-read methods before routing. Native GET date navigation and local-only role cookie do not establish authentication. Discreet Sandbox details explicitly say no provider is connected; removed intrusive banner follows the user's instruction. Refund/return sample sections are visual state variants, not complete ledger histories or financial invariant evidence.

### Actual rendered inspection inventory

Independently viewed saved screenshots of **all 16 nominal screen variants at desktop and mobile widths**: home, login, operations, staff policies, brokers, claims, operations approvals, human-approver layout, broker policies, KYB, new policy, policy detail, cancellation, claim detail, loading and error. Also viewed all **18** initial mobile state captures: staff empty/error, broker empty, claims empty, brokers empty, approvals empty, KYB pending/failed/approved, new-policy warning, cancellation refusal/zero, own-request refusal, decided request, policy draft, claim closed, login error and not-found. Viewed seven desktop recovery variants: refund requested/failed/completed and payment ready/sent/settled/returned.

Independently inspected six additional mobile-bottom captures (approver, KYB, new policy, policy, cancellation, claim), plus `scroll.json`: all reach the footer after settled End scrolling, remaining distance 0±0.5 px at390×844. The apparent duplicate/cut-off sidebar in an earlier full-page approver capture is absent from its viewport-bottom capture; it is not evidence of an inaccessible form. Table content deliberately scrolls within its labeled region. Read `audit.json`, `states.json` and `http-checks.json`; their coordinator measurements support the corresponding saved captures, not an authenticated application run.

Some **initial captures predate final fixes**: desktop home/approvals/approver/KYB, narrow approval details and consent layout, approved-KYB heading, cancellation confirmation copy, draft/own-request fixture consistency and populated journal/bank examples. Their unchanged geometry can be reused, but they are not an exact final-source screenshot set. Newer bottom captures independently support the fixed approval/consent layout; HTTP/source checks support current copy and fixture facts. Coordinator has been asked to refresh the affected final views and identify them separately. At this entry the browser is blocked by a Chrome extension popup, and no independent UI-04 browser interaction has been performed.

**Source findings F01–F09 are resolved; final implementation verdict remains PENDING** for final affected-view evidence, actual current build evidence and staged secret scan. No new material source issue found. Existing font approximation and selected orange/white contrast limitations remain; no full WCAG, deployed/authenticated financial journey, B13 or integrated-trial PASS. AF-01 deployment NOT RUN; AF-02 PASS only for inspected honest local/source labels; AF-03 unchanged financial source/no mutation in this scope, runtime ledger checks NOT RUN; AF-04 PASS for synthetic/local-only review actions; AF-05 current staged scan pending; AF-06 NOT REVIEWED WITH YOANN.

## UI-04 checkpoint verdict — 2026-09-08T16:48Z

The refreshed `home-desktop.png` was independently inspected: current simplified copy, unobtrusive Sandbox details and final layout are correct. The local-harness manifest matches all three independently computed hashes above. Inspected the 25 explicitly staged paths; independently reran `git diff --cached --check` and `gitleaks protect --staged --redact --no-banner --no-color`: **PASS**, 31.98 KB, no leaks. This review file was not yet staged; repeat the scan after its addition and any subsequent changes.

Coordinator reports the final `npm run build` exited0 in tool session8809 (20/20 generated pages); the raw output has not yet been supplied for independent inspection. Coordinator also reports real local Next public home/login200, styled404 and11 protected-route streaming redirects to login, correctly distinguished from HTTP307. No authenticated session or financial action was exercised. Existing unit evidence is reused only for unchanged financial code (219passed,1optional live Stripe test skipped).

**BLOCKED — final UI-04 completion gate only.** Source review and the enumerated nominal/state/bottom-scroll evidence are favorable, with F01–F09 resolved. Chrome currently refuses automation because an extension UI is open; the remaining final affected-view refresh and interaction checks cannot be completed. This is a missing-evidence block, not a newly observed application failure. Do not mark the feature complete or treat this checkpoint as an exhaustive final-render PASS.

Resume after Chrome is available: refresh affected final approval/approver, KYB, cancellation and fixture-consistency views; verify normal GET-date/back navigation and short-height sidebar reachability; inspect the raw final build evidence; append the resulting independent verdict without deleting this history. Reuse unchanged screen and source evidence. No deployment/provider/full-financial/full-WCAG or candidate-understanding claim follows from this local UI review.

### Build evidence received — 2026-09-08T16:50Z

Actually read `.local/ui04-build.txt` after the coordinator supplied it: optimized compilation succeeded, TypeScript completed, all20/20 static pages generated and the final route inventory includes the new `/ops/policies` and not-found route. Coordinator's producing tool session14187 exited0. **Build evidence PASS**, replacing the pending raw-output item above. Only final affected-view refresh/reinspection and remaining Chrome navigation/scroll interactions block UI-04 completion. Report frozen at this checkpoint for explicit staging and repeated scan; no commit/completion authorization is implied.
