# UI-03 implementation review: breadcrumb hierarchy and parent navigation

Reviewer: independent sub-agent `/root/ui_review`. Reviewed 2026-09-08T16:00:05Z, branch `codex/corgi-interface`, base/HEAD `9acbb5bd8881ccdb72b5b89df619acbbf6fb2401`. Scope: PLAN UI-03, seven-file dirty source delta identified by `docs/handoffs/ui-03-source-manifest.json`, SHA-256 `d154b040bd0a4983d4d8a9ff33384a277a13b7074cf06a15195a445977201a7f`. All seven content hashes independently matched. This is a bounded B13 presentation review, not a complete B13 or trial gate.

## Startup receipt and applicability

Actually read in this continuing review session: AGENTS.md, AUTOMATIC-FAILS.md in full (all six bans), READABLE-CODE.md, WORKFLOW-48H.md and REVIEWER.md. Reread the general/Track 1 brief reference and README, current PLAN UI-03, STATUS, DECISIONS, COMPLIANCE-MATRIX and UI-03 handoff; prior architecture/UI-01/UI-02 reads remain applicable to unchanged financial architecture. No mandatory file missing; the UI-03 handoff was initially absent while being written and was read when available before verdict. Approximately 38 hours remain to the documented freeze. Next acceptance criterion: explicit current-page hierarchy and working parent navigation without changing authorization or financial behavior. Planned checks: exact diff/hash inspection, financial form contracts, local navigation, generated-preview negative checks, build/type evidence.

Sources: `docs/PLAN.md` UI-03 (user navigation requirement); `AGENTS.md` operations/security and feature-review sections (engineering safeguards); `AUTOMATIC-FAILS.md` AF-01–06 and `docs/BRIEF-REFERENCE.md` general/Track 1 (trial requirements), checked 2026-09-08 and effective for this trial. No new rail, provider operation, custody, jurisdiction, personal-data collection or financial control is introduced; no new legal applicability inferred.

## Code and checks

Read the full seven-file diff against base, PortalShell/PortalFrame and affected page entry points, including owning-broker/staff checks, policy/claim reads, cancellation planning/refusal and eligibility calculations. The only page deltas supply breadcrumb labels/links and correct a role-specific body backlink. All existing server authorization, queries, financial expressions, conditional action eligibility and provider labels remain unchanged. `lib`, `db`, `scripts` and `app/api` have no delta. Removing staff's sidebar `/broker` destination is consistent with that page's existing broker-only list: staff policy detail returns to `/ops`, while an owning broker returns to `/broker`.

PortalShell constructs role-appropriate root plus optional page trail on the server. PortalFrame receives only rendered content and string label/href arrays; no user/session/database object crosses the client boundary. Its only mutable state remains menu visibility. Native Next links use explicit parent URLs with prefetch disabled; the back arrow has an accessible name, and the last breadcrumb item is nonlinked with `aria-current=page`. There is no browser-history dependency or custom key handler. Existing visible focus styles remain; mobile CSS wraps the entire breadcrumb below the controls and supplies a 44 px parent target. The reading path is short and explicit.

| Requirement | Independent evidence | Result |
|---|---|---|
| Exact reviewed source | Recomputed all seven manifest hashes; inspected complete diff | PASS |
| Preserve financial form contracts | TypeScript AST compared every form/input/select/textarea/button attribute except className in four changed pages plus PortalShell against base; all equal | PASS |
| Working child/current/parent navigation | CUA local fixture: Policies → New policy → Back to Policies; Policies → Policy UI-PREVIEW-001 → breadcrumb Policies; cancellation refusal → Back to Policy. Current labels and URLs inspected | PASS |
| Semantic hierarchy and desktop rendering | CUA AX confirmed named Breadcrumb navigation, named parent link and current label; policy screenshot independently inspected, document/viewport both 1512 px | PASS |
| Responsive and real application evidence | Inspected coordinator's UI-03 handoff: claim/cancellation trails fit 320 px, return target 44 px; real built login parent link returns to `/` and root omits back arrow | PASS for recorded bounded evidence; these observations were not independently repeated |
| Build/types | Independently ran `npm run typecheck` (exit 0); read `.local/ui03-build.txt`, successful compilation/TypeScript and 19 generated pages | PASS |
| Safe synthetic preview | Full helper read and independent whole-directory HTML/HTTP audit, detailed in UI-02 correction re-review: 18 HTML, zero forms/API/external anchors, 13 linked routes HTTP 200, direct local POST 501 | PASS at recorded helper/output hashes |
| Staged hygiene | `git diff --cached --check` exit 0; independent `gitleaks protect --staged --redact --no-banner --no-color` exit 0, 7.82 KB, no leaks | PASS for inspected stage; repeat after final docs staging |

No unit suite rerun: this delta changes navigation markup and CSS, and prior financial unit evidence applies only to unchanged logic. No database, provider, authenticated money-flow, approval or deployment test was executed. The fixture's broker/approver selectors are synthetic navigation and do not prove authentication or role switching. Cancellation rendering tests the disclosed refusal fixture, not financial calculation success. Keyboard semantics/focus source and prior Space evidence were inspected; Enter-through-CUA remains the previously disclosed tool-verification gap, with no full keyboard-conformance claim.

## Automatic-fail coverage and verdict

| Gate | Scope result |
|---|---|
| AF-01 accessible deployed URL | NOT RUN for this branch/revision; local preview is not submission evidence |
| AF-02 honest integration modes | PASS for preserved application labels and explicit synthetic preview notices; live integration not rerun |
| AF-03 immutable money rows | PASS for no financial source/persistence delta; runtime ledger controls not rerun |
| AF-04 sandbox/data/spend | PASS for this review's local synthetic actions, no provider/database/account action or spend |
| AF-05 secrets | PASS only for scanned staged content; final stage/history/delivery scan not claimed |
| AF-06 candidate ownership | NOT REVIEWED WITH YOANN; technical readability does not certify understanding |

**PASS for the exact seven-file UI-03 implementation and bounded local navigation evidence. No unresolved material finding identified in this scope.** UI02-P01/P02 are closed in their preserved review history. Existing DM Sans approximation, reference-color contrast defects and Enter verification limitation remain disclosed; this is not exact-font or complete accessibility conformance. Full B13, integrated financial review, externally verified deployment and candidate walkthrough remain incomplete. No merge, push, deployment, external-account interaction or financial operation was performed by this reviewer.
