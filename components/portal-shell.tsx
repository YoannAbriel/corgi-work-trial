// The shell moved to components/shell/app-shell.tsx on 2026-09-09 (two-level navigation, page
// band). Every screen imports it from here, so the path they know keeps working.
export { PortalShell } from "@/components/shell/app-shell";
export type { Band, NavView, SectionId } from "@/components/shell/app-shell";
